# Alice work-session review packet

- Commit: `84e12b677a238bf076b7c7119af0d379d089c74f`
- Subject: feat: add transfer endpoint selector
- Authored: 2026-08-22T04:37:55+08:00
- Handover updated in this commit: `true`
- Active plan ID: `20260822-transfer-selector-rightclick-fix-v1`
- Active plan status at handoff: `APPROVED_FOR_IMPLEMENTATION`
- Push status: verify separately after `git push origin master`

## Changed files

```text
docs/HANDOVER.md
src/main/java/com/dddgn/alice/bot/BotSelftest.java
src/main/java/com/dddgn/alice/command/BotCommand.java
src/main/java/com/dddgn/alice/item/AliceItems.java
src/main/java/com/dddgn/alice/item/TransferEndpointSelector.java
src/main/java/com/dddgn/alice/item/TransferEndpointSelectorEvents.java
src/main/java/com/dddgn/alice/item/TransferEndpointSelectorEventsFixture.java
src/main/java/com/dddgn/alice/transfer/TransferCodes.java
src/main/java/com/dddgn/alice/transfer/TransferSelectionData.java
src/main/java/com/dddgn/alice/transfer/TransferSelectionFixture.java
src/main/java/com/dddgn/alice/transfer/TransferSelectionLifecycle.java
src/main/java/com/dddgn/alice/transfer/TransferSelectionSubmission.java
src/main/resources/assets/alice/lang/en_us.json
src/main/resources/assets/alice/lang/zh_cn.json
src/main/resources/assets/alice/models/item/transfer_endpoint_selector.json
```

## Commit summary

```text
 docs/HANDOVER.md                                   | 15 +++-
 src/main/java/com/dddgn/alice/bot/BotSelftest.java |  6 ++
 .../java/com/dddgn/alice/command/BotCommand.java   | 49 ++++++++++++
 src/main/java/com/dddgn/alice/item/AliceItems.java |  4 +
 .../dddgn/alice/item/TransferEndpointSelector.java | 16 ++++
 .../alice/item/TransferEndpointSelectorEvents.java | 66 ++++++++++++++++
 .../TransferEndpointSelectorEventsFixture.java     | 18 +++++
 .../com/dddgn/alice/transfer/TransferCodes.java    |  2 +
 .../alice/transfer/TransferSelectionData.java      | 46 +++++++++++
 .../alice/transfer/TransferSelectionFixture.java   | 88 ++++++++++++++++++++++
 .../alice/transfer/TransferSelectionLifecycle.java | 15 ++++
 .../transfer/TransferSelectionSubmission.java      | 72 ++++++++++++++++++
 src/main/resources/assets/alice/lang/en_us.json    |  3 +-
 src/main/resources/assets/alice/lang/zh_cn.json    |  3 +-
 .../models/item/transfer_endpoint_selector.json    |  6 ++
 15 files changed, 406 insertions(+), 3 deletions(-)
```

## Handover delta

```diff
diff --git a/docs/HANDOVER.md b/docs/HANDOVER.md
index 5df1ba0..8b3fe7f 100644
--- a/docs/HANDOVER.md
+++ b/docs/HANDOVER.md
@@ -59,9 +59,22 @@ GitHub：`github` 远端 `dddgn486/Alice-mcbot`（本会话未推 GitHub，push
 - Audit corrections retained: both legs take three-party pre/fresh/post observations and prove deltas; source preflight requires full bot and destination capacity before extraction; a proven source leg transitions ledger location to `IN_TRANSIT_BOT` before destination movement; destination no-write conflict suspends with bot location, changed/unexplained destination post state becomes `UNKNOWN_DISCREPANCY`; no automatic retry, resume, finish-insert or rollback claim. Restart lifecycle marks every unfinished ledger entry `SUSPENDED/manual_takeover_required`, and a persistent in-transit/suspended entry blocks unrelated task replacement for that bot.
 - Verification: `./tools/work-session-start.sh` passed with the approved plan; `./gradlew compileJava` and `git diff --check` passed. `./gradlew runServer -Dalice.selftest.auto=true` produced explicit `TRANSFER_FIXTURE_SUITE PASS` at 2026-08-22 00:13:18. Its request/state/code/location/source/bot/destination-delta assertions cover normal legs; capacity/source/predicate/endpoint rejection; simulation conflict; source/destination post mismatch; protected and ordinary ledger-only abort; duplicate/restart/replacement; suspension expiry; survival/removal; 201-tick phase and 2401-tick active timeouts including `BOT_INVENTORY`; and controlled `SEARCH_LIMIT`/`UNREACHABLE`/`FAILED` TransferTask mappings. The command subsequently entered the pre-existing broad selftest, which later reported a pre-existing mining selftest failure and was externally terminated at 240 seconds with exit `143`; broad selftest is not reported as PASS.
 - Known limits: fixture path outcomes use a transfer-only, inert-by-default `TransferTask` test seam and therefore do not claim real-world path search coverage. It does not cover actor disconnect, invoke Brigadier command parsing/permission, or drive manager-owned TransferTask through a complete live tick lifecycle. The fixture is server-side only and does not prove bot inventory packet visibility, actual Windows movement, GUI isolation, client rendering, or client-side command behavior.
-- Status: awaiting supervisor review. Do not mark C2 A1.1 `CLIENT_TEST_PENDING` until supervisor PASS. After PASS, Windows client matrix remains `CLIENT_TEST_PENDING` only: permission/command, normal end-to-end, visible in-transit inventory, source/bot/destination capacity rejection, destination external mutation, hazard interruption, restart/abort/manual takeover, and ordinary chest GUI isolation. User Windows evidence is the only acceptance evidence.
+- Status: `CLIENT_TEST_PENDING` after supervisor PASS recorded in `.alice-supervision/reviews/20260822-49be37e-a1-1-review.md`; this is not user acceptance. Run A1-A9 in one complete Windows client session. Fill the single-round form `D:\JAVA_projects\alice\.alice-supervision\client-tests\49be37e-a1-1\evidence\evidence-report.md` once (date, Windows SHA, environment, item/count, request UUIDs, results, user decision); overwrite `evidence/latest.log` and `evidence/debug.log` with the one-round complete logs; fill each A1-A9 scenario form (`evidence/A*-*/evidence-report.md`) with yes/no observations and required screenshots. The matrix covers permission/command, normal end-to-end, visible in-transit inventory, source/bot/destination capacity rejection, destination external mutation, hazard interruption, restart/abort/manual takeover, and ordinary chest GUI isolation. User Windows evidence is the only acceptance evidence.
 - Next safe step: supervisor review of the generated packet, followed by the Windows matrix if approved. Do not expand this package into A1.2 fulfilment, persistence recovery writes, generic container support, ACL, packets/UI, or ordinary task integrations.
 
+## Transfer Endpoint Selector (awaiting supervisor review)
+
+- Active plan: `20260822-transfer-endpoint-selector-v1`, baseline `49be37efb57375f4e4c82ee6d6f4fb18dfb98d85`. Added independent `alice:transfer_endpoint_selector` using the unshared `minecraft:item/stick` texture: server-side permission-2 selection only, right-click destination and Shift+right-click source. It validates the fixed original-single-chest endpoint, stores only a server-instance-scoped in-memory player draft for 2,400 ticks, clears it on server stopping, returns `PASS` so vanilla container interaction remains intact, and does not write NBT, inventory, ledger, or tasks on click.
+- `/alice transfer-selection status|clear|submit [<item>] [<count>]` delegates to the same existing `BotManager.assignTransfer` admission as the coordinate command. Submit defaults only on this route: the first slot-order `minecraft:` default/no-NBT source item and its total source count; explicit selection items are also restricted to `minecraft:`. No default item returns `default_item_unavailable`; successful submit clears the draft and failure preserves it. `/alice transfer-test` remains explicit and unchanged.
+- Verification: start gate and `./gradlew compileJava` passed; `git diff --check` passed. `./gradlew runServer -Dalice.selftest.auto=true` at 2026-08-22 03:26:33 logged `TRANSFER_SELECTION_FIXTURE_SUITE PASS`: actual source/destination count and serialized ledger-entry pre/post deltas remained zero for draft operations; draft overwrite/expiry/clear/server-clear, exact same-endpoint/cross-dimension codes, invalid endpoint, explicit/default resolver behavior, mod namespace rejection, `default_item_unavailable`, and captured submit request/admission delegate behavior including success-clear/failure-retain were asserted. The process then entered the existing broad selftest and was externally terminated at 240 seconds with exit `143`; broad selftest is not claimed as PASS.
+- Limits: headless fixture does not invoke Brigadier parsing/permission or a successful live `BotManager.assignTransfer` task admission because the shared selftest bot would legitimately enter protected transfer-task state; its narrow capture delegate verifies submit request construction and one admission call only. Windows E1-E10 must verify selector feedback, GUI PASS behavior, command parsing, live submission admission, and client-visible behavior. A1.1 remains `CLIENT_TEST_PENDING` and unchanged; this independent package is awaiting supervisor review, not client accepted.
+
+## Transfer Selector Right-Click Fix (awaiting supervisor review)
+
+- Active plan: `20260822-transfer-selector-rightclick-fix-v1`, baseline `a7e02fd63ced80cb7bdf6591aa6c30d418e4c657`. Added a server-side Forge `PlayerInteractEvent.RightClickBlock` handler matching the selector in the event hand. Permission-2 players record Shift/secondary-use as source and ordinary right-click as destination after the existing read-only `ChestEndpointRef.validate`; feedback and logging reuse selector stable codes. The handler deliberately never cancels the event or sets a result override, so ordinary chest right-click proceeds to the vanilla GUI. `useOn` is reduced to `PASS` only to prevent duplicate recording.
+- Verification: `./gradlew compileJava` and `git diff --check` passed. Focused server run at 2026-08-22 04:34:02 logged `TRANSFER_SELECTOR_EVENT_FIXTURE_SUITE PASS` for role mapping, permission/invalid endpoint rejection, single-record helper policy, and non-cancel policy; the existing `TRANSFER_SELECTION_FIXTURE_SUITE PASS` also remained green. The run then continued through the existing broad selftest and was externally terminated at 240 seconds with exit `143`; that timeout is not client acceptance.
+- Limits: the focused event fixture is a server-side helper/seam and cannot prove Forge event ordering or actual Windows GUI opening. Re-run client E2 (Shift source, GUI not required), E3 (ordinary destination plus GUI), E4 and status/clear checks after supervisor review. No A1.1 semantics or client acceptance status changed.
+
 ---
 
 ## 一、项目是什么
```

## Active Plan Snapshot

```markdown
# Alice Active Work Plan

- Plan ID: `20260822-transfer-selector-rightclick-fix-v1`
- Status: APPROVED_FOR_IMPLEMENTATION
- Baseline: `a7e02fd63ced80cb7bdf6591aa6c30d418e4c657`
- Client failure evidence: Windows `.alice-supervision/client-tests/a7e02fd-transfer-selector/evidence/`
- Failure review: `.alice-supervision/reviews/20260822-a7e02fd-client-failure-review.md`
- User decision: accept original interaction semantics — Shift+right-click records source without requiring GUI; normal right-click records destination and vanilla chest GUI must open.

## Objective

Fix only the transfer selector interaction entry. The current `Item.useOn` path records Shift/source but cannot observe normal chest right-click because the chest consumes the interaction first. Move endpoint recording to a server-side `PlayerInteractEvent.RightClickBlock` handler that runs before the block consumes the interaction, while never canceling or changing vanilla interaction.

## Allowed Scope

- Add one transfer-selector-specific Forge event handler, or extend `TransferEndpointSelector` with a subscribed static handler, for `PlayerInteractEvent.RightClickBlock` on `LogicalSide.SERVER`.
- Match only `alice:transfer_endpoint_selector` in the used hand; use the event hand stack, not a hard-coded main-hand stack.
- Permission level 2 gate; construct `ChestEndpointRef` from event level/dimension/position; reuse `ChestEndpointRef.validate`; Shift/secondary-use selects source, normal right-click selects destination; reuse `TransferSelectionData.select` and existing stable chat/log format.
- Do not cancel the event and do not set a cancellation/result override. Normal right-click must continue to vanilla chest GUI. Shift behavior remains vanilla secondary-use and is not required to open GUI.
- Remove, disable, or reduce `TransferEndpointSelector.useOn` so the same click cannot record twice. Non-container clicks must still produce one stable rejection and no draft mutation.
- Add focused testable helper/seam proving role mapping, single invocation, permission rejection, validation rejection, and non-cancel policy without changing production defaults.
- Update HANDOVER, compileJava, focused fixture, diff check, commit/push Windows origin, session-complete packet.

## Explicitly Forbidden

- No active `openMenu`, no packet/UI, no GUI interception, no `event.setCanceled(true)`, no change to vanilla crouch interaction.
- No modification to TransferRequest, ChestEndpointRef semantics, CapacityPreflight, primitive, ledger, TransferTask, BotManager.assignTransfer, HARD_PATH/SOFT_SURFACE or ordinary task chains.
- No new persistence/NBT/ACL/all-items behavior and no change to default item/count semantics.
- Do not mix governance files or GitHub README/history work into this fix.

## Verification

- Focused fixture must prove: normal click maps destination; Shift maps source; exactly one draft write per event; permission and invalid endpoint produce no draft; handler policy never cancels/overrides vanilla interaction.
- `./gradlew compileJava`; `git diff --check`.
- Client regression after supervisor PASS: rerun corrected E2 (Shift source, GUI not required), E3 (normal destination + GUI opens), E4, and status/clear/restart checks before continuing E5-E10.

## Stop Conditions

Stop if the fix requires actively opening a GUI, canceling the event, client packets, modifying transfer execution semantics, or changing ordinary tool behavior.
```

## Supervisor checklist

- Does this change preserve the architecture: goal-level decision, deterministic execution, server authority, semantic GUI interfaces?
- Does it preserve the separate boundaries among `HARD_PATH`, experimental `SOFT_SURFACE`, forced road construction, survival monitoring, and future tunnel planning?
- Does it contradict a numbered decision in `docs/HANDOVER.md` or the frozen plan in `docs/PATHING_REFACTOR.md`?
- Are verification evidence, client-test requirements, known limitations, and the next safe step recorded accurately?
- Was `./gradlew compileJava` run and was the committed revision pushed to `origin/master`?
