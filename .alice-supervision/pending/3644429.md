# Alice work-session review packet

- Commit: `3644429d2d88e1bb6fd1eb24654d828d1451d7f8`
- Subject: docs: record F1-F4 maintenance revision 10f4cd7
- Authored: 2026-08-21T17:43:33+08:00
- Handover updated in this commit: `true`
- Active plan ID: `20260821-interface-c1-maintenance-f1-f4-v1`
- Active plan status at handoff: `APPROVED_FOR_IMPLEMENTATION`
- Push status: verify separately after `git push origin master`

## Changed files

```text
docs/HANDOVER.md
docs/SUPERVISOR_HANDOFF.md
```

## Commit summary

```text
 docs/HANDOVER.md           | 3 ++-
 docs/SUPERVISOR_HANDOFF.md | 4 ++--
 2 files changed, 4 insertions(+), 3 deletions(-)
```

## Handover delta

```diff
diff --git a/docs/HANDOVER.md b/docs/HANDOVER.md
index 572b306..3581dd9 100644
--- a/docs/HANDOVER.md
+++ b/docs/HANDOVER.md
@@ -1,13 +1,14 @@
 # Alice 项目交接说明（给新会话的快速上手）
 
 > 用途：另一个对话/会话接手本项目时，先读本文件 + README + 设计文档，即可快速进入工作。
-> 更新：2026-08-21（本轮 C1 maintenance F1-F4 业务提交见 §〇；`interface-readonly-snapshot-v1` 的 S1-S4 已获用户验收，F1-F4 维护已收口）
+> 更新：2026-08-21（最新实现提交 `10f4cd7`：C1 maintenance F1-F4 收口；`interface-readonly-snapshot-v1` 的 S1-S4 已获用户验收）
 > 重要：本会话功能提交已同步到 Windows 端仓库（`origin` = `/mnt/d/JAVA_projects/alice`）。**每次代码修改完成后必须 `git push origin master`**，用户在 Windows 客户端实测。
 > 当前工作区的未提交改动均为监督流程：`.gitignore`、`AGENTS.md`、`docs/SUPERVISION_PROTOCOL.md`、`docs/supervision/`、`tools/`；它们不属于 Mod 功能实现，应作为独立治理提交核对，勿覆盖或混入功能提交。
 
 ## 〇、当前 Git 状态（重要）
 
 ```
+10f4cd7 feat: close C1 scanner maintenance F1-F4             ← 最新实现（C1 F1-F4 维护收口）
 ba63d15 docs: correct supervisor handoff revision          （治理：监督交接回填，位于 8bb2d7b 之后）
 276339f docs: define dsh bus supervision handoff           （治理：四角色 bus 交接）
 f9d89da docs: record blocked client checkpoint             （治理：客户端阻塞 checkpoint）
```

## Active Plan Snapshot

```markdown
# Alice Active Work Plan

- Plan ID: `20260821-interface-c1-maintenance-f1-f4-v1`
- Status: APPROVED_FOR_IMPLEMENTATION
- Created from: C1 `USER_ACCEPTED` evidence, `.alice-supervision/reviews/20260821-8bb2d7b.md`, `.alice-supervision/research/interface-c1-maintenance-f1-f4-shallow-20260821.md`, `.alice-supervision/research/interface-c1-maintenance-f1-f4-planner-20260821.md`, and `.alice-supervision/reviews/20260821-interface-c1-maintenance-route.md`.
- Approved by: user explicitly authorized F1-F4 maintenance scope in this session; supervisor adopted the planner's C1-only route review.
- Scope owner: Alice project supervisor

## Objective

Close the four outstanding C1 maintenance findings before any next functional package: prove formatter/snapshot non-mutation in the existing focused selftest, make unloaded snapshot capture conservative, remove unreachable generic slot-role heuristics, and align handover facts with the accepted C1 state.

## Why This Slice

The preceding `interface-readonly-snapshot-v1` is `USER_ACCEPTED` only for independent scanner S1-S4 interaction. F1-F4 remain correctness/documentation gaps that can be resolved using existing sources and the existing chest/dirt selftest fixture. This maintenance package is smaller and lower risk than opening C2+ capability semantics, transfer, adapters, or any task/movement expansion.

## Delivery Lane

- Lane: `maintenance`.
- Relationship to active blockers: isolates from the un-attributed historical old-save hang; it neither diagnoses nor repairs that observation.
- Throughput rationale: stabilizes the accepted C1 fact boundary and its evidence before the next functional package, without relying on unverified movement or machine-write capability.

## Allowed Scope

1. `src/main/java/com/dddgn/alice/capability/InterfaceScanner.java`
   - Call `level.hasChunkAt(pos)` before any target `getBlockState(pos)` or `getBlockEntity(pos)` access.
   - For `CHUNK_NOT_LOADED`, use the existing `blockId` string field with the conservative constant `unknown`; do not load/dereference target state to derive an id.
   - Delete only the unreachable generic `scanItems`, `scanEnergy`, `scanFluid`, and `slotHint` methods and their stale heuristic documentation.
   - State in the class documentation that generic C1 capture is unsided raw readonly fact capture; retain Mek output only as an explicitly non-generic legacy projection.
2. `src/main/java/com/dddgn/alice/bot/BotSelftest.java`
   - Modify only `runInterfaceSnapshotRegression`.
   - Use the existing chest/dirt fixture to prove `InterfaceScanner.format` does not mutate source/container state, the original snapshot stays stable after source chest mutation, and the existing non-empty item facts collection rejects modification.
   - Include every new assertion in the existing single `INTERFACE_SNAPSHOT_SELFTEST PASS|FAIL` decision and log enough deterministic detail to audit each assertion.
   - Do not create a machine, fluid, energy, mock-capability, tick, or chunk-loading fixture.
3. `docs/HANDOVER.md` and `docs/SUPERVISOR_HANDOFF.md`
   - Correct current HEAD/governance facts, C1 S1-S4 acceptance, scanner item identity, F1-F4 maintenance state, and retained limitations.
   - Preserve the historical old-save hang as un-attributed and not repaired; distinguish business implementation SHA `8bb2d7b` from governance checkpoints.

## Explicitly Out Of Scope

- C2/C3/C4 descriptors, machine/endpoint semantics, side traversal, capability simulation, insertion/extraction, transactions, persistence, network/JSON schema, GUI automation, adapters, LLM tools, or machine writes.
- Any change to `ScanWand.java`, including its post-capture block-name lookup. If full interaction-chain unloaded access becomes a concern, stop and replan.
- Any old-save hang diagnosis/fix, repeated old-save test, GitHub CI repair, or new client interaction.
- Any `MineTask`, `DropCollectionTask`, mining, collection, road, tunnel, FollowTask, survival, `HARD_PATH`, `SOFT_SURFACE`, fluid, escape, or movement change.

## Preconditions

- Prior C1 client evidence is `USER_ACCEPTED`, limited to S1-S4 in `.alice-supervision/client-tests/8bb2d7b.md` and Windows evidence report.
- Current implementation baseline is `ba63d15b7dec9bf4887f099bd45e407af5e99a19`; before edits, developer must verify it or stop for supervisor rebase/replan.
- Main work session must run `./tools/work-session-start.sh`, read this plan, `docs/HANDOVER.md`, `docs/SUPERVISION_PROTOCOL.md`, and the cited research/reviews before edits.

## Research Decision

- Supervisor shallow research: `.alice-supervision/research/interface-c1-maintenance-f1-f4-shallow-20260821.md`; local source evidence confines F1-F4 to C1 capture/selftest/docs and identifies no external-version-sensitive prerequisite.
- Planner review: `.alice-supervision/research/interface-c1-maintenance-f1-f4-planner-20260821.md`; adopted its F2 conservative-id clarification, existing-fixture-only F1 limit, F3 time-line discipline, and exclusion of `ScanWand.java`.
- Deep research: Not required
- Required report path(s): Not required
- Rejected/expired research conclusions: no C2+ or legacy Mek projection conclusion is adopted as generic C1 semantics; prior load-hang speculation is not adopted.
- Evidence confidence and version boundary: local current-source fact at Git `ba63d15`; Forge 1.20.1-47.4.10 / Java 17 existing checkout APIs only.

## Implementation Steps

1. Verify baseline and references; make F2/F4 changes in `InterfaceScanner.java` without altering capture schema, live raw formatter, or Mek legacy projection behavior.
2. Add F1 assertions only to the existing `runInterfaceSnapshotRegression`; then correct F3 documents only after actual code/verification facts are stable.
3. Run required checks, inspect diff/reference searches, update HANDOVER with actual evidence, commit, push `origin/master`, run `tools/session-complete.sh`, and stop for supervisor review. Any scope change or stop condition aborts implementation.

## Client Test Entry

No new client entry is authorized or required for this internal-only maintenance slice.

- Interaction contract: the existing independent `alice:interface_scanner` interaction must remain unchanged.
- Isolation: no task/movement/mining/collection/road/follow/soft-surface call chain may be touched.
- Stop condition: if item interaction, GUI cancellation, user-visible message/log contract, world state, or other client-visible behavior changes, stop implementation and return for a new `CLIENT_TEST_PENDING` matrix.
- Promotion boundary: S1-S4 acceptance remains narrow; any C2+ semantics or normal-task integration requires a separate user-approved plan and client evidence as applicable.

## Verification Evidence

- `./gradlew compileJava` must succeed.
- Run `./gradlew runServer -Dalice.selftest.auto=true`; record `INTERFACE_SNAPSHOT_SELFTEST PASS` with evidence of formatter purity, post-capture snapshot stability, and unmodifiable item list. If the existing broad run ends under external timeout/exit 143, do not call the suite fully passed; retain the focused observed evidence only.
- Source/diff review: `hasChunkAt` precedes target state/entity access; no live caller/reference remains for deleted heuristic methods; generic formatter remains raw indexed facts; no unauthorized files/behavior changed.
- Documentation review: compare all current-Git, C1 acceptance, tool-identity, F1-F4, and old-hang statements against Git and Windows evidence.
- No Windows test is required if interaction contract is unchanged. It is required anew only if the stop condition triggers.

## User Acceptance Gate

Not required for this internal-only slice if and only if the scanner interaction/GUI/message/log contract remains unchanged. The supervisor must review implementation evidence and preserve the prior S1-S4 acceptance boundary; no later C2+ or normal task integration is accepted by this plan.

## Handoff Rules

- Main work session must update `docs/HANDOVER.md`, commit, push `origin/master`, and run `./tools/session-complete.sh`.
- Any discovery that needs a new fixture, target-state access before `hasChunkAt`, a live caller of dead code, a contradictory Git/client fact, or any scope expansion stops work and returns to the supervisor for replanning.
```

## Supervisor checklist

- Does this change preserve the architecture: goal-level decision, deterministic execution, server authority, semantic GUI interfaces?
- Does it preserve the separate boundaries among `HARD_PATH`, experimental `SOFT_SURFACE`, forced road construction, survival monitoring, and future tunnel planning?
- Does it contradict a numbered decision in `docs/HANDOVER.md` or the frozen plan in `docs/PATHING_REFACTOR.md`?
- Are verification evidence, client-test requirements, known limitations, and the next safe step recorded accurately?
- Was `./gradlew compileJava` run and was the committed revision pushed to `origin/master`?
