# Alice work-session review packet

- Commit: `3823dd036843a34d6e264f339345dd956050b12e`
- Subject: docs: record selector E5-E8 client acceptance
- Authored: 2026-08-22T21:40:09+08:00
- Handover updated in this commit: `true`
- Active plan ID: `20220822-transfer-selector-item-argument-v1`
- Active plan status at handoff: `USER_ACCEPTED（E7 参数解析修复 + E8 两路径一致性，selector 场景全部关闭）`
- Push status: verify separately after `git push origin master`

## Changed files

```text
docs/HANDOVER.md
```

## Commit summary

```text
 docs/HANDOVER.md | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)
```

## Handover delta

```diff
diff --git a/docs/HANDOVER.md b/docs/HANDOVER.md
index 4c8f5ce..6301f6c 100644
--- a/docs/HANDOVER.md
+++ b/docs/HANDOVER.md
@@ -75,12 +75,13 @@ GitHub：`github` 远端 `dddgn486/Alice-mcbot`（本会话未推 GitHub，push
 - Verification: `./gradlew compileJava` and `git diff --check` passed. The focused event fixture now injects a recorder into the event dispatch seam and at 2026-08-22 04:34:02 logged `TRANSFER_SELECTOR_EVENT_FIXTURE_SUITE PASS`: ordinary maps destination and secondary-use maps source, each accepted event invokes the recorder once, non-selector hand invokes it zero times, rejection-code paths do not mutate the count, and the handler policy remains non-cancel/non-override. The existing `TRANSFER_SELECTION_FIXTURE_SUITE PASS` also remained green. The run then continued through the existing broad selftest and was externally terminated at 240 seconds with exit `143`; that timeout is not client acceptance.
 - Limits: the focused event fixture is a server-side helper/seam and cannot prove Forge event ordering or actual Windows GUI opening. Re-run client E2 (Shift source, GUI not required), E3 (ordinary destination plus GUI), E4 and status/clear checks after supervisor review. No A1.1 semantics or client acceptance status changed.
 
-## Transfer Selector Item Argument (awaiting supervisor review)
+## Transfer Selector Item Argument (USER_ACCEPTED)
 
 - Active plan: `20220822-transfer-selector-item-argument-v1`, baseline `5fa33ab36730eda59a95e9beda182842b0ff430d`. Selector submit now uses the fixed Minecraft `ResourceLocationArgument.id()` and `ResourceLocationArgument.getId(ctx, "item").toString()` for item-only and item+count branches. `/alice transfer-test` remains on its original `StringArgumentType.word()` and String getter.
 - Added a narrow parser fixture with no command execution, admission, ledger, task, bot, inventory, or world writes. It asserts `minecraft:iron_ingot` and `mekanism:ingot_steel` are fully consumed and parsed, and `bad:item:id` and whitespace-containing tokens are rejected. The `mekanism:` token's namespace is recorded as non-`minecraft`, so business rejection stays with the existing `TransferSelectionSubmission.resolve` (`invalid_item_id`) and is not asserted inside the parser fixture.
 - Verification: work-session gate, `./gradlew compileJava`, and `git diff --check` passed. Focused run at 2026-08-22 20:48:31 logged `TRANSFER_SELECTION_COMMAND_PARSE_FIXTURE_SUITE PASS` with details `vanilla=true modSyntax=true resolverPolicy=true badColon=true whitespace=true emptyPathByApi=minecraft: emptyNamespaceByApi=minecraft:iron_ingot`; the latter two record that fixed Forge 1.20.1 accepts and fully consumes `minecraft:` and normalizes `:iron_ingot` to `minecraft:iron_ingot`, so they are not asserted as parser rejections. The broad selftest then ran and was externally terminated at 240 seconds with exit `143`; it is not claimed as parser or client acceptance.
-- Limits: namespace business rejection for selector submit is guaranteed by the existing resolver policy, which the parser fixture does not invoke. E7 client regression (non-`minecraft:` token returns `invalid_item_id`, one valid `minecraft:` token confirms no regression) remains for the Windows session after supervisor acceptance.
+- Client acceptance: Windows `370ff343` overlay `f655be2`, 2026-08-22 21:03-21:36, evidence `.alice-supervision/client-tests/a7e02fd-transfer-selector/evidence/latest.log`. E7: non-`minecraft:` token returns `invalid_item_id` (line 153); valid `minecraft:` request `15cc8aae` completes (lines 159, 167). E8 two-path consistency: selection submit `324c537b` and coordinate `transfer-test` `190ff741` both `COMPLETED done` via the same `TransferTask` chain; `190ff741` additionally `VERIFIED transfer_verified source=0 bot=-3 destination=3` (lines 149, 154, 195, 202, 206). Mid-test coordinate-typo failures (`endpoint_not_single_chest`, `hard_path_unreachable`) were correct rejections confirmed by user chat logs (lines 171, 179). Selector E5-E8 matrix closed, R2/R3/R4/R9/R10 remain USER_ACCEPTED.
+- Limits: namespace business rejection for selector submit is guaranteed by the existing resolver policy, which the parser fixture does not invoke. Selector matrix is now fully client-accepted; A1.1 remains `CLIENT_TEST_PENDING` and unchanged.
 
 ---
 
```

## Active Plan Snapshot

```markdown
# Alice Active Work Plan

- Plan ID: `20220822-transfer-selector-item-argument-v1`
- Status: USER_ACCEPTED（E7 参数解析修复 + E8 两路径一致性，selector 场景全部关闭）
- Baseline: `5fa33ab36730eda59a95e9beda182842b0ff430d`
- User approval: explicit approval received; user owns client observation only, not code repair.
- Supervisor route decision: planner report `.alice-supervision/research/transfer-selector-item-argument-parse-plan-20220822.md` reviewed and adopted. This is a small, isolated, low-risk command parser repair; no further planning/research required.
- Supervisor review: `20260822-370ff343-e7-item-argument-review.md` PASS（服务端侧）+ E7 客户端回归 PASS（21:03-21:05）+ E8 客户端回归 PASS（21:24-21:36，`latest.log`）。实现提交 `370ff34323597f6c75f0532173300bc765357154`，Windows overlay `f655be2`。
- Parent accepted scope: transfer selector R2/R3/R4/R9/R10 USER_ACCEPTED; E5/E6 PASS; E7 PASS(已回归); E8 PASS(已回归)。Selector 场景全部关闭。

## Objective

Allow selector submit to parse complete ResourceLocation item IDs so valid syntax reaches existing resolver policy. `mekanism:ingot_steel` must reach selector resolver and return `invalid_item_id`; valid `minecraft:` items must retain current behavior.

## Allowed Scope

- `src/main/java/com/dddgn/alice/command/BotCommand.java` selector submit item argument only (current lines 118-123): use `ResourceLocationArgument.id()` and `ResourceLocationArgument.getId(ctx, "item").toString()`.
- Add a narrow command parse/resolve focused fixture or extend an existing command fixture, without invoking transfer admission, ledger, Task, BotManager, inventory, or world writes.
- Update `BotSelftest`/HANDOVER only as needed for accurate focused evidence.
- Run `compileJava`, focused fixture, `git diff --check`, session-complete; commit and push origin Windows after supervisor review.

## Explicitly Forbidden

- Do not change `/alice transfer-test` item argument or semantics in this package.
- Do not change `TransferSelectionSubmission.resolve`, TransferRequest, ChestEndpointRef, primitive, ledger, TransferTask, BotManager, inventory execution, selector item interaction, GUI, packet, NBT, permissions, or any other command.
- Do not weaken ResourceLocation syntax or convert valid `mekanism:` into parse failure.
- Do not request client tests before supervisor accepts the implementation.

## Verification Matrix

- Parser accepts `minecraft:iron_ingot` and selector resolver retains current valid path.
- Parser accepts syntactically valid `mekanism:ingot_steel`; resolver returns `invalid_item_id`.
- Parser rejects malformed `bad:item:id` and whitespace-containing token; fixed Forge API behavior for `minecraft:` and `:iron_ingot` is recorded rather than assumed (if normalized/accepted, resolver/registry policy remains authoritative).
- Syntactically parseable non-`minecraft:` item reaches existing resolver and returns `invalid_item_id`.
- No-argument selector submit remains default path.
- Existing `/alice transfer-test` command tree/behavior remains unchanged.
- After supervisor PASS and Windows sync, user performs only E7 regression: non-minecraft token returns `invalid_item_id`, plus one valid `minecraft:` token confirms no regression.

## Stop Conditions

Stop and return for replanning if implementation requires changing resolver/transfer layers, old transfer-test semantics, admission/ledger/Task/BotManager, permissions, GUI, or any client protocol; if fixed-version API does not compile; or if focused fixture and client behavior conflict.
```

## Supervisor checklist

- Does this change preserve the architecture: goal-level decision, deterministic execution, server authority, semantic GUI interfaces?
- Does it preserve the separate boundaries among `HARD_PATH`, experimental `SOFT_SURFACE`, forced road construction, survival monitoring, and future tunnel planning?
- Does it contradict a numbered decision in `docs/HANDOVER.md` or the frozen plan in `docs/PATHING_REFACTOR.md`?
- Are verification evidence, client-test requirements, known limitations, and the next safe step recorded accurately?
- Was `./gradlew compileJava` run and was the committed revision pushed to `origin/master`?
