# Alice work-session review packet

- Commit: `9534a5d812b4f33484770bcc6a86eab2c28419a0`
- Subject: feat: rewrite bot inventory GUI as custom packet + screen (direction B)
- Authored: 2026-08-23T04:10:04+08:00
- Handover updated in this commit: `true`
- Active plan ID: `20260824-bot-inventory-gui-custom-packet-v1`
- Active plan status at handoff: `APPROVED_FOR_IMPLEMENTATION`
- Push status: verify separately after `git push origin master`

## Changed files

```text
docs/HANDOVER.md
src/main/java/com/dddgn/alice/AliceMod.java
src/main/java/com/dddgn/alice/client/ClientBotInventoryState.java
src/main/java/com/dddgn/alice/client/gui/BotInventoryScreen.java
src/main/java/com/dddgn/alice/client/gui/ClientMenuScreens.java
src/main/java/com/dddgn/alice/command/BotCommand.java
src/main/java/com/dddgn/alice/gui/BotInventoryFixture.java
src/main/java/com/dddgn/alice/gui/BotInventoryService.java
src/main/java/com/dddgn/alice/gui/BotInventorySnapshot.java
src/main/java/com/dddgn/alice/network/AliceNetwork.java
src/main/java/com/dddgn/alice/network/BotInventoryActionPacket.java
src/main/java/com/dddgn/alice/network/BotInventoryPacket.java
```

## Commit summary

```text
 docs/HANDOVER.md                                   |   5 +-
 src/main/java/com/dddgn/alice/AliceMod.java        |   5 +-
 .../alice/client/ClientBotInventoryState.java      |  39 ++++
 .../dddgn/alice/client/gui/BotInventoryScreen.java | 164 ++++++++++++---
 .../dddgn/alice/client/gui/ClientMenuScreens.java  |  15 +-
 .../java/com/dddgn/alice/command/BotCommand.java   |  10 +-
 .../com/dddgn/alice/gui/BotInventoryFixture.java   | 154 ++++----------
 .../com/dddgn/alice/gui/BotInventoryService.java   | 233 +++++++++++++++++++++
 .../com/dddgn/alice/gui/BotInventorySnapshot.java  |  38 ++++
 .../java/com/dddgn/alice/network/AliceNetwork.java |   6 +
 .../alice/network/BotInventoryActionPacket.java    |  57 +++++
 .../dddgn/alice/network/BotInventoryPacket.java    |  51 +++++
 12 files changed, 602 insertions(+), 175 deletions(-)
```

## Handover delta

```diff
diff --git a/docs/HANDOVER.md b/docs/HANDOVER.md
index dc976aa..8349e33 100644
--- a/docs/HANDOVER.md
+++ b/docs/HANDOVER.md
@@ -69,8 +69,9 @@ GitHub：`github` 远端 `dddgn486/Alice-mcbot`（本会话未推 GitHub，push
 - Bot inventory GUI vanilla reuse (awaiting client verification): active plan `20260823-bot-inventory-gui-vanilla-reuse-v1`, baseline `25b1daa`. User re-test after 25b1daa → still failing: picked-up items vanish, placed items double, title truncated, label same color as panel. Source research `.alice-supervision/research/vanilla-inventory-menu-source-20260823.md` identified the root cause as Alice's custom scheme (plain Slot bound to armor/offhand, self-drawn panel, non-standard imageHeight) diverging from the vanilla InventoryMenu structure. Rewrote `BotInventoryMenu` to reuse the vanilla slot geometry and Slot types: bot armor/offhand now use dedicated `BotArmorSlot`/`BotOffhandSlot` subclasses mirroring `InventoryMenu$1/$2` (getMaxStackSize=1, mayPlace=canEquip, mayPickup honours binding curse, setByPlayer on-equip); `quickMoveStack` follows the vanilla partition semantics with `setByPlayer(EMPTY)` source sync and returns the pre-move copy (armor items route to the matching bot armor slot, offhand to the offhand slot); `BotInventoryScreen` draws a 0xFF-alpha panel with vanilla slot coordinates and default-contrast labels. Server verification: `BOT_INVENTORY_FIXTURE_SUITE PASS` (slotCount=77 noOverlap=true customArmor=true quickMoveOut=true) and all existing suites PASS at 03:23:53. Client G9 (pick-up no longer vanishes, place no double, Shift), background display, label readability, and G2-G6/G8/G10/G11 no-regression await Windows re-test; the exact vanish/double root cause needs client-side server/client slot-state confirmation.
 - Bot inventory GUI final fix (awaiting client verification): active plan `20260824-bot-inventory-gui-final-fix-v1`, baseline `2b995ba`. User re-test after 2b995ba → picked-up items still vanish / placed items still double; user chose to follow the source-research advice to use `inventory.png` + standard geometry. `BotInventoryScreen.renderBg` now blits the vanilla `inventory.png` (176x166) for the bot backpack section (which uses vanilla player-backpack slot geometry) and fills the region below y=166 with a plain 0xFF-alpha panel for the player grid. Added `[GUI_DEBUG]` diagnostic logging to `quickMoveStack` (before/after move: index, slotItem, stackCount, moved, stackNow, slotNow, returned original count) and to `clicked` (before/after/swallowed) so client testing can pinpoint the vanish/double root cause. Also verified armor on menu slot 0-3, offhand on menu slot 4, BOT_SLOT_CAP=41. Server verification: `BOT_INVENTORY_FIXTURE_SUITE PASS` (slotCount=77 noOverlap=true customArmor=true quickMoveOut=true) and all existing suites PASS at 03:33:29. Client re-test must capture and report the `[GUI_DEBUG]` logs to the supervisor to confirm/refute the vanish/double root cause.
 - Bot inventory GUI pickup sync fix (awaiting client verification): active plan `20260824-bot-inventory-gui-pickup-sync-fix-v1`, baseline `55e3d32`. User re-test after 55e3d32 → UI improved but picked-up items still vanish; user confirmed the operation is a normal left-click (PICKUP), not Shift. Source diagnosis `.alice-supervision/research/container-menu-clicked-pickup-source-20260823.md` (scheme A) confirmed the root cause: on PICKUP the server-side tryRemove correctly moves the item to the carried stack, but `Inventory.setItem`/`removeItem` does not call `setChanged()`, and the bot's own `FakeConnection.send()` is a no-op → the player's client bot-slot snapshot does not refresh, so the picked-up item appears to vanish. Fix: in `clicked()`, after a PICKUP that touches a bot slot (slotId < BOT_SLOT_CAP) and passes the read-only guard, force `setChanged()` on all bot slots to trigger `broadcastChanges()` and sync the updated bot inventory to the player's client. `quickMoveStack` was not changed (Shift is not the user's path). Server verification: `BOT_INVENTORY_FIXTURE_SUITE PASS` (slotCount=77 noOverlap=true customArmor=true quickMoveOut=true) and all existing suites PASS at 03:43:39. Client re-test must confirm normal left-click pick-up no longer vanishes.
-- Known limits: fixture path outcomes use transfer-only test seam, not real-world path coverage. Server-side only; does not cover actor disconnect or full manager-owned live tick. Source highlight after interrupt remains user misunderstanding (Alice has no highlight code). Bot death animation (red fall) persists on kill; short-term filter only prevents restoration after restart. Bot inventory GUI is implemented server-side (scheme A) but awaits Windows client verification; main-hand slot is not duplicated (the selected ordinary slot is the authority, matching the equipment renderer). The exact "vanish/double" root cause in the GUI is confirmed only by client-side server/client slot-state observation (noted in the source research); the `[GUI_DEBUG]` logs added in this package are to be collected from the next Windows client run.
-- Next safe step: A1.1 complete and accepted. Bot death animation/resurrection (long-term 方案 C) planned but not started — requires `BotPlayer.die()` override and resurrection logic. Bot inventory GUI vanilla-reuse fix awaits Windows client re-test (G9 pick-up/place/Shift, background display, label readability; G2-G6/G8/G10/G11 no-regression). The post-transfer main-hand residue defect requires a separate plan on `ChestBotTransferPrimitive` mainhand/backpack sync. Do not expand into A1.2 fulfilment, persistence recovery writes, generic container support, ACL, packets/UI, or ordinary task integrations without new approved plan.
+- Bot inventory GUI direction B (custom packet + Screen) rewrite (awaiting client verification): active plan `20260824-bot-inventory-gui-custom-packet-v1`, baseline `1621ad2`. After 6 failed rounds of `AbstractContainerMenu`, the user chose direction B: a custom S2C snapshot + C2S action packet + pure client `Screen`, bypassing the container-menu protocol entirely (root cause was the mixed index partition / setChanged chain). New server-authoritative layered: `gui/BotInventorySnapshot.java` (record botId/name/slots, 36 ordinary + 4 armor + 1 offhand, SLOT_COUNT=41), `gui/BotInventoryService.java` (read snapshot + `applyAction` as the ONLY write path — validates permission/bot exists/`isBusy` read-only/slot 0-40/canEquip/maxStack=1/binding-curse, then pushes the refreshed snapshot), `network/BotInventoryPacket.java` (S2C, via `PacketDistributor.PLAYER.with(viewer)`, opens the screen via `DistExecutor` client-only), `network/BotInventoryActionPacket.java` (C2S PICKUP/PLACE/QUICK_MOVE, handled on the server in `BotInventoryService`), `client/ClientBotInventoryState.java` (@OnlyIn volatile snapshot + `openScreen`), and a rewritten `client/gui/BotInventoryScreen.java` (pure `Screen`, renders from the snapshot, sends C2S intents, never mutates local state). `AliceNetwork` registers both packets; `BotCommand.bot-inventory` now pushes a snapshot instead of `openMenu`; `AliceMod` no longer registers `ModMenuTypes`; `ClientMenuScreens` is a no-op. Server verification: `BOT_INVENTORY_FIXTURE_SUITE PASS` (slotCount=41, snapshotPass=true, pickupPass=true, serverAuthority=true) and all existing suites PASS at 04:07:50. Client G1-G13 await Windows testing.
+- Known limits: fixture path outcomes use transfer-only test seam, not real-world path coverage. Server-side only; does not cover actor disconnect or full manager-owned live tick. Source highlight after interrupt remains user misunderstanding (Alice has no highlight code). Bot death animation (red fall) persists on kill; short-term filter only prevents restoration after restart. Bot inventory GUI direction B is implemented server-authoritatively (snapshot + applyAction + Pure Screen) but awaits Windows client verification; main-hand slot is not duplicated (the selected ordinary slot is the authority). The server-authoritative client never mutates local slot state, eliminating the vanish/double root cause at the protocol level; the retired `BotInventoryMenu`/`ModMenuTypes`/`ClientMenuScreens` are kept (unregistered) for rollback.
+- Next safe step: A1.1 complete and accepted. Bot death animation/resurrection (long-term 方案 C) planned but not started — requires `BotPlayer.die()` override and resurrection logic. Bot inventory GUI direction B awaits Windows client G1-G13 verification (opening, 36+4+1 display, pickup/place/Shift, armor wear, task read-only, server-authoritative consistency). The post-transfer main-hand residue defect requires a separate plan on `ChestBotTransferPrimitive` mainhand/backpack sync. Do not expand into A1.2 fulfilment, persistence recovery writes, generic container support, ACL, packets/UI, or ordinary task integrations without new approved plan.
 
 ## Transfer Endpoint Selector (awaiting supervisor review)
 
```

## Active Plan Snapshot

```markdown
# Alice Active Work Plan

- Plan ID: `20260824-bot-inventory-gui-custom-packet-v1`
- Status: APPROVED_FOR_IMPLEMENTATION
- Baseline: `1621ad2`（fix: sync bot slots to client on pickup click）
- User approval: explicit approval received (2026-08-24); user decides 背包交互重要、应彻底解决 → 方向 B（自定义 packet + Screen，彻底绕开容器菜单协议）
- User interaction decisions: 交互范围=点击拿取+放入+Shift（不做拖拽 cursor）；装备穿戴=允许（复用 BotArmorSlot 规则）；与旧容器菜单=保留但停用（保留旧代码备回滚，只新建方向 B 代码）
- Supervisor route decision: 规划员 `.alice-supervision/research/bot-inventory-gui-custom-packet-planner-20260824.md` + 调研员 `.alice-supervision/research/forge-custom-screen-c2s-packet-20260824.md` 均已验收采纳。方向 B 用自定义 S2C 快照 + C2S action + 客户端纯 Screen，根治 6 轮失败的容器菜单混合 index 分区/setChanged 链路。

## Objective

用自定义 packet + Screen 重写 Bot Inventory GUI（方向 B），彻底绕开 AbstractContainerMenu，解决拿取消失/物品翻倍/Shift 错乱。核心保证：**唯服务端权威**（客户端不发包不更新本地，收到服务端回推快照才刷新）。

## Allowed Scope

**新增**：
1. `src/main/java/com/dddgn/alice/gui/BotInventorySnapshot.java`（record：botId/name/slots 36普通+4armor+1offhand）
2. `src/main/java/com/dddgn/alice/gui/BotInventoryService.java`（服务端读写入口）
   - `snapshot(level, botId)`：读 bot.getInventory() 构造快照
   - `applyAction(...)`：**唯一写入口**，服务端权威校验后写 bot inventory
3. `src/main/java/com/dddgn/alice/network/BotInventoryPacket.java`（S2C 快照）
4. `src/main/java/com/dddgn/alice/network/BotInventoryActionPacket.java`（C2S action：PICKUP/PLACE/QUICK_MOVE/MOVE）
5. `src/main/java/com/dddgn/alice/client/ClientBotInventoryState.java`（@OnlyIn volatile 快照）
6. `src/main/java/com/dddgn/alice/client/gui/BotInventoryScreen.java`（重写为纯 Screen，自绘 36+4+1 槽）

**修改**：
7. `src/main/java/com/dddgn/alice/network/AliceNetwork.java`：注册 BotInventoryPacket（S2C）+ BotInventoryActionPacket（C2S）
8. `src/main/java/com/dddgn/alice/command/BotCommand.java`：`/alice bot-inventory` 改为发快照（不再 openMenu）
9. `src/main/java/com/dddgn/alice/gui/BotInventoryFixture.java`：改写为断言 snapshot + applyAction
10. `docs/HANDOVER.md`：记录

**保留但停用**：`BotInventoryMenu.java`/`ModMenuTypes.java`/`ClientMenuScreens.java`（保留旧代码备回滚，但**不再注册/不引用**；AliceMod 移除 ModMenuTypes.MENUS.register）

## 交互语义（用户确认）

- **第一版交互**：点击拿取（PICKUP）+ 放入（PLACE）+ Shift 快速移动（QUICK_MOVE/MOVE）
- **不做拖拽 cursor**（cursor 由服务端回推，防本地改数据导致拿取消失）
- **允许装备穿戴**：armor/offhand 允许放入可穿装备（canEquip、maxStack=1、绑定诅咒不可取），复用现有 BotArmorSlot/BotOffhandSlot 规则到服务端 handler

## 关键实现约束（唯服务端权威）

- 客户端 Screen **不发包不更新本地**；收到服务端回推快照才刷新
- 服务端 `applyAction` 首查：bot 存在 + `BotManager.isBusy`（taskActive 只读保护 + 槽位合法 0-40 + canEquip/绑定诅咒），写后回推快照
- packet 发送用 `PacketDistributor.PLAYER.with(viewer)`，绝不 `bot.connection.send`（FakeConnection no-op）

## Explicitly Forbidden

- 不改 `TransferTask`/`MineTask`/`ChestBotTransferPrimitive`/`TransferLedgerData`/`BotManager` 任务语义
- 不改 `ChestBotTransferPrimitive.syncBotInventorySlots`（主手残留 bug，另立计划）
- 不改 equipment 渲染
- 不实现拖拽 cursor（留作后续）
- 不扩展为 Bot 设置页面
- 不改旧 BotInventoryMenu 文件内容（保留），仅停用注册/引用

## Verification Matrix

**服务端验证**：
- `./gradlew compileJava` PASS
- `git diff --check` PASS
- 既有 suites（Transfer/Selection/SelectorEvent/CommandParse）保持 PASS
- 新 BotInventoryFixture：snapshot 读取 36+4+1 + applyAction（任务保护/堆叠/move/canEquip/绑定诅咒）断言

**客户端验收矩阵**（Windows `CLIENT_TEST_PENDING`，G1-G13 参考规划员报告）：
- G1 入口与授权（op 打开/非 op 拒绝）
- G2 快照显示 36 普通槽
- G3 4 装备槽 + 1 offhand
- G4 主手槽（selected）
- G5 拿取单格（不消失/不翻倍）← 核心
- G6 放入（堆叠正确）
- G7 Shift 快速移动（不重复/不残留）
- G8 装备穿戴/取下
- G9 任务保护（bot 执行任务时只读）
- G10 bot 不在场 → bot_unavailable
- G11 客户端/服务端无崩溃
- G12 服务端权威一致（三端守恒）
- G13 既有回归（A1-A9/equipment 渲染/死亡过滤）

## Stop Conditions

Stop and return for replanning if方向 B 仍需 AbstractContainerMenu 才能满足交互、需拖拽 cursor、或 existing fixtures regress.

## Implementation Time Estimate

- 实现：60-90 分钟（Snapshot/Service/2 packet/Screen/命令/清理）
- 客户端复测：30-40 分钟（G1-G13）
- 总计：1.5-2 小时

## Notes

- 核心正确性保证：唯服务端权威（客户端只发 intent 不改本地数据，收到服务端回推快照才刷新）——根治拿取消失/翻倍/Shift 错乱
- 已确认 FakeConnection no-op 不是障碍（玩家交互走玩家 connection）
- 旧容器菜单保留但停用（备回滚）
```

## Supervisor checklist

- Does this change preserve the architecture: goal-level decision, deterministic execution, server authority, semantic GUI interfaces?
- Does it preserve the separate boundaries among `HARD_PATH`, experimental `SOFT_SURFACE`, forced road construction, survival monitoring, and future tunnel planning?
- Does it contradict a numbered decision in `docs/HANDOVER.md` or the frozen plan in `docs/PATHING_REFACTOR.md`?
- Are verification evidence, client-test requirements, known limitations, and the next safe step recorded accurately?
- Was `./gradlew compileJava` run and was the committed revision pushed to `origin/master`?
