# Alice work-session review packet

- Commit: `a8b84b40e4e99b4c0be0421603887aae75f8bbcb`
- Subject: fix: sync bot main hand equipment for entity rendering
- Authored: 2026-08-23T00:17:44+08:00
- Handover updated in this commit: `true`
- Active plan ID: `20260822-bot-equipment-rendering-fix-v1`
- Active plan status at handoff: `APPROVED_FOR_IMPLEMENTATION`
- Push status: verify separately after `git push origin master`

## Changed files

```text
docs/HANDOVER.md
src/main/java/com/dddgn/alice/transfer/ChestBotTransferPrimitive.java
```

## Commit summary

```text
 docs/HANDOVER.md                                               |  3 ++-
 .../com/dddgn/alice/transfer/ChestBotTransferPrimitive.java    | 10 ++++++++++
 2 files changed, 12 insertions(+), 1 deletion(-)
```

## Handover delta

```diff
diff --git a/docs/HANDOVER.md b/docs/HANDOVER.md
index 37a3a76..823a81a 100644
--- a/docs/HANDOVER.md
+++ b/docs/HANDOVER.md
@@ -60,8 +60,9 @@ GitHub：`github` 远端 `dddgn486/Alice-mcbot`（本会话未推 GitHub，push
 - Server verification: Original `49be37e` focused fixture logged `TRANSFER_FIXTURE_SUITE PASS` at 2026-08-22 00:13:18 covering normal legs, capacity/rejection, conflict, mismatch, abort, restart, survival, timeouts, and path outcomes. Extended focused fixtures `924fa304` logged `TRANSFER_COMMAND_FIXTURE_SUITE PASS` and `TRANSFER_LIFECYCLE_EXTENDED_FIXTURE_SUITE PASS` at 22:58:10 covering A1 permission/parameter validation (valid/invalid item parsing, positive/zero count, same/cross dimension), A3 in-transit visibility (IN_TRANSIT_BOT + BOT_INVENTORY + bot NBT), A4 capacity rejection (capacity_rejected + leg2 failure + bot retains items), A8 abort (not-moved→ABORTED, in-transit→SUSPENDED + BOT_INVENTORY). All assertions PASS; no production transfer code changed.
 - Client acceptance: Windows `f655be2` (overlay of 370ff343 + 49be37e), 2026-08-22 23:13-23:16, evidence `.alice-supervision/client-tests/f655be2-a1-1-client/evidence/`. C1 external mutation (capacity_rejected when destination full, conservative stop, no retry), C2 survival hazard (hard_path_failed when lava blocks path, conservative stop observed), C3 restart (task suspended observed, manual takeover), C4 vanilla GUI isolation (no Alice interception). Core semantics verified: conservative stop, no automatic retry/resume, ledger conservation, GUI isolation. User decision: `USER_ACCEPTED`. Final review: `.alice-supervision/reviews/20260822-a1-1-final-acceptance.md`.
 - Bot inventory client sync fix (awaiting client verification): active plan `20260822-bot-inventory-sync-fix-v1`, baseline `0add921`. Added `syncBotInventorySlots()` in `ChestBotTransferPrimitive.insertBot()` to send `ClientboundContainerSetSlotPacket` for all 36 slots after bot inventory write, following `BotManager.syncMainHand()` pattern. Server verification: `TRANSFER_FIXTURE_SUITE` and `TRANSFER_LIFECYCLE_EXTENDED_FIXTURE_SUITE` remain PASS at 23:42:32. Client V1-V3 (basic function, multi-slot, selector path) await Windows testing; V4 performance observation optional. This isolated fix does not change other tasks (MineTask/PlaceTask inventory writes) or add highlight functionality.
+- Bot equipment rendering fix (awaiting client verification): active plan `20260822-bot-equipment-rendering-fix-v1`, baseline `bd9f0c3`. Extended `syncBotInventorySlots()` in `ChestBotTransferPrimitive` to add `ClientboundSetEquipmentPacket(bot.getId(), MAINHAND, mainHandStack)` for entity rendering after existing 36-slot inventory GUI sync. Server verification: `TRANSFER_FIXTURE_SUITE`, `TRANSFER_SELECTION_FIXTURE_SUITE`, `TRANSFER_SELECTOR_EVENT_FIXTURE_SUITE`, `TRANSFER_SELECTION_COMMAND_PARSE_FIXTURE_SUITE` all remain PASS at 00:15:17. Client V1-V3 (basic function with iron_ingot, different items diamond/stone/pickaxe, multi-slot consistency) await Windows testing; V4-V5 optional. This fix depends on `bd9f0c3` framework and only syncs MAINHAND (not offhand/armor). Does not implement slot priority logic (方案 A1).
 - Known limits: fixture path outcomes use transfer-only test seam, not real-world path coverage. Server-side only; does not cover actor disconnect or full manager-owned live tick. Client C2/C3 SUSPENDED/manual_takeover_required states proven by server fixture + client observation (conservative stop), but transfer-status query not captured in client logs. Source highlight after interrupt remains user misunderstanding (Alice has no highlight code).
-- Next safe step: A1.1 complete and accepted. Bot inventory sync fix awaits Windows client V1-V3 verification. Do not expand into A1.2 fulfilment, persistence recovery writes, generic container support, ACL, packets/UI, or ordinary task integrations without new approved plan.
+- Next safe step: A1.1 complete and accepted. Bot inventory sync fix and equipment rendering fix await Windows client V1-V3 verification. Do not expand into A1.2 fulfilment, persistence recovery writes, generic container support, ACL, packets/UI, or ordinary task integrations without new approved plan.
 
 ## Transfer Endpoint Selector (awaiting supervisor review)
 
```

## Active Plan Snapshot

```markdown
# Alice Active Work Plan

- Plan ID: `20260822-bot-equipment-rendering-fix-v1`
- Status: APPROVED_FOR_IMPLEMENTATION
- Baseline: `bd9f0c3`（fix: sync bot inventory to client after transfer insert）
- User approval: explicit approval received (2026-08-22); user observes "Bot 手持物品途中不可见" in client test
- Supervisor route decision: 调研报告 `.alice-supervision/research/bot-held-item-rendering-20260822.md` 已审核采纳。根因是 bd9f0c3 只同步 inventory GUI 数据（ClientboundContainerSetSlotPacket），未同步 equipment 渲染（ClientboundSetEquipmentPacket）。方案 A（在 syncBotInventorySlots() 内部扩展）最优。

## Objective

修复 Bot 手持物品客户端渲染问题：Bot 搬运物品途中，客户端实体模型上能看到手持物品（不只是 GUI）。

## Allowed Scope

**只改一个文件**：`src/main/java/com/dddgn/alice/transfer/ChestBotTransferPrimitive.java`

**具体改动**（基于调研报告方案 A）：
1. 在 `syncBotInventorySlots()` 方法（行 228-234）内部扩展：
   - 保留现有的 36 个 `ClientboundContainerSetSlotPacket`（inventory GUI 数据同步）
   - 新增 `ClientboundSetEquipmentPacket`（equipment 实体渲染同步）
2. 伪代码：
   ```java
   private static void syncBotInventorySlots(Inventory inventory) {
       if (!(inventory.player instanceof ServerPlayer bot)) return;
       
       // 现有：同步所有 inventory 槽位（GUI 数据）
       for (int i = 0; i < inventory.getContainerSize(); i++) {
           bot.connection.send(new ClientboundContainerSetSlotPacket(-2, 0, i, inventory.getItem(i)));
       }
       
       // 新增：同步主手 equipment（实体渲染）
       ItemStack mainHandStack = inventory.getItem(inventory.selected);
       bot.connection.send(new ClientboundSetEquipmentPacket(
           bot.getId(),
           java.util.List.of(
               com.mojang.datafixers.util.Pair.of(
                   net.minecraft.world.entity.EquipmentSlot.MAINHAND,
                   mainHandStack
               )
           )
       ));
   }
   ```
3. 必要时新增 import：`ClientboundSetEquipmentPacket`、`EquipmentSlot`、`com.mojang.datafixers.util.Pair`

**必要时仅准确更新**：`docs/HANDOVER.md` 记录修复（在 bd9f0c3 章节补充此修复）

## Explicitly Forbidden

- 不改 `TransferTask`、`BotManager`、其他 transfer 文件
- 不实施方案 A1（优先主手槽位）——留作独立子任务，需客户端验证后决定
- 不实施方案 B（Task 层同步）或方案 C（扩展 syncMainHand()）
- 不改其他任务（`MineTask`、`PlaceTask`）的 inventory 同步
- 不同步副手或其他装备槽位（只同步 MAINHAND）
- 不实现 Bot inventory GUI（那是另一个计划）

## Verification Matrix

**服务端验证**：
- `./gradlew compileJava` PASS
- `git diff --check` PASS
- 既有 `TRANSFER_FIXTURE_SUITE` / `TRANSFER_LIFECYCLE_EXTENDED_FIXTURE_SUITE` 保持 PASS（不应受影响）

**客户端验证矩阵**（V1-V3 必需，V4-V5 可选）：
- **V1（基本功能）**：Transfer 途中观察 Bot 手持物品模型 → 预期显示铁锭模型
- **V2（不同物品）**：测试 diamond/stone/pickaxe → 预期所有类型正确渲染
- **V3（多槽位）**：Transfer iron_ingot×64 → 预期手持物品与主手槽位一致
- **V4（与 bd9f0c3 回归，可选）**：打开 bot inventory GUI → 预期 GUI 和手持模型都正确
- **V5（性能，可选）**：F3 监控 packet 频率 → 预期无卡顿

**最少客户端证据**：
- V1/V2/V3 各一次实测截图（Bot 手持物品模型）
- 共用日志（latest.log/debug.log）
- 总测试时间约 15-20 分钟

## Stop Conditions

Stop and return for replanning if implementation requires changing TransferTask/BotManager/other tasks, implementing slot priority logic (A1), synchronizing offhand/armor slots, or if existing fixtures regress.

## Implementation Time Estimate

- 实现：10-15 分钟
- 客户端验证：15-20 分钟
- 总计：25-35 分钟

## Notes

- 此修复**依赖** `bd9f0c3` 的 `syncBotInventorySlots()` 框架，只需在其基础上扩展
- 如客户端测试发现"手持物品与 GUI 不一致"（风险 3），再实施方案 A1（优先主手槽位）
- `bd9f0c3` 解决了 "GUI 看不到物品"，本修复解决 "实体模型上看不到手持物品"
```

## Supervisor checklist

- Does this change preserve the architecture: goal-level decision, deterministic execution, server authority, semantic GUI interfaces?
- Does it preserve the separate boundaries among `HARD_PATH`, experimental `SOFT_SURFACE`, forced road construction, survival monitoring, and future tunnel planning?
- Does it contradict a numbered decision in `docs/HANDOVER.md` or the frozen plan in `docs/PATHING_REFACTOR.md`?
- Are verification evidence, client-test requirements, known limitations, and the next safe step recorded accurately?
- Was `./gradlew compileJava` run and was the committed revision pushed to `origin/master`?
