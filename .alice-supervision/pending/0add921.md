# Alice work-session review packet

- Commit: `0add92185c21386e75a23c5a916815a87dd2ea57`
- Subject: docs: A1.1 USER_ACCEPTED (server fixture + client C1-C4)
- Authored: 2026-08-22T23:21:31+08:00
- Handover updated in this commit: `true`
- Active plan ID: `20260822-a1-1-focused-fixture-expansion-v1`
- Active plan status at handoff: `APPROVED_FOR_IMPLEMENTATION`
- Push status: verify separately after `git push origin master`

## Changed files

```text
docs/HANDOVER.md
```

## Commit summary

```text
 docs/HANDOVER.md | 13 ++++++-------
 1 file changed, 6 insertions(+), 7 deletions(-)
```

## Handover delta

```diff
diff --git a/docs/HANDOVER.md b/docs/HANDOVER.md
index 6958285..60b1c2b 100644
--- a/docs/HANDOVER.md
+++ b/docs/HANDOVER.md
@@ -52,16 +52,15 @@ GitHub：`github` 远端 `dddgn486/Alice-mcbot`（本会话未推 GitHub，push
 下一步状态：**C1 S1-S4 + F1-F4 已完成并经监督二审 PASS**。C1 S1-S4 客户端矩阵为 `USER_ACCEPTED`（窄范围：独立扫描器身份、只读箱子扫描、无 block entity 扫描、原版钻石铲隔离）；F1-F4 维护提交 `10f4cd7` 与治理回填 `3644429` 已推送 `origin/master`，二审见 `.alice-supervision/reviews/20260821-interface-c1-maintenance-f1-f4-review.md`。历史旧存档卡死保持未归因、未修复；若恢复诊断，先建旧/新世界对照并采集完整日志与客户端/服务端线程转储，在证据能区分 registry remap、假人 SavedData 恢复、连接注册、客户端同步或其他模组初始化前，不修改业务代码。下一功能包尚未选择；C2+、AttackTask、库存转移、LLM 和所有 SOFT_SURFACE 扩展继续禁止，直至新的用户批准计划。
 
 
-## A1.1 Original Chest -> Bot -> Chest Transfer (implementation awaiting supervisor review)
+## A1.1 Original Chest -> Bot -> Chest Transfer (USER_ACCEPTED)
 
-- Active plan: `20260821-original-chest-bot-transfer-a1-1-v1`, baseline `3644429d2d88e1bb6fd1eb24654d828d1451d7f8`. This package is narrow C2 A1.1 only: explicit permission-level-2 command moves one default no-NBT `namespace:item` positive count between two loaded same-dimension original single chests through the bot's ordinary 36 inventory slots.
+- Active plan: `20260821-original-chest-bot-transfer-a1-1-v1` (original implementation) + `20260822-a1-1-focused-fixture-expansion-v1` (server-side test expansion), baseline `3644429d2d88e1bb6fd1eb24654d828d1451d7f8` (original) → `924fa3044ce5aa68bb8f4b44e1de7a076b50b1d0` (focused fixtures). This package is narrow C2 A1.1 only: explicit permission-level-2 command moves one default no-NBT `namespace:item` positive count between two loaded same-dimension original single chests through the bot's ordinary 36 inventory slots.
 - Implemented package: `transfer/` immutable request/endpoint/observation/preflight/primitive/independent SavedData ledger and focused fixture; `TransferTask`; restricted `BotManager` admission/replacement/lifecycle guards; `/alice transfer-test`, read-only `/alice transfer-status`, and ledger-only `/alice transfer-abort`. There are no bindings, UI/packets, generic capabilities, double chests, multi-batch, tags/components, mod containers, direct endpoint writes, SOFT_SURFACE or changes to mining/collection/roads/tunnels/fluids/escape.
 - Audit corrections retained: both legs take three-party pre/fresh/post observations and prove deltas; source preflight requires full bot and destination capacity before extraction; a proven source leg transitions ledger location to `IN_TRANSIT_BOT` before destination movement; destination no-write conflict suspends with bot location, changed/unexplained destination post state becomes `UNKNOWN_DISCREPANCY`; no automatic retry, resume, finish-insert or rollback claim. Restart lifecycle marks every unfinished ledger entry `SUSPENDED/manual_takeover_required`, and a persistent in-transit/suspended entry blocks unrelated task replacement for that bot.
-- Verification: `./tools/work-session-start.sh` passed with the approved plan; `./gradlew compileJava` and `git diff --check` passed. `./gradlew runServer -Dalice.selftest.auto=true` produced explicit `TRANSFER_FIXTURE_SUITE PASS` at 2026-08-22 00:13:18. Its request/state/code/location/source/bot/destination-delta assertions cover normal legs; capacity/source/predicate/endpoint rejection; simulation conflict; source/destination post mismatch; protected and ordinary ledger-only abort; duplicate/restart/replacement; suspension expiry; survival/removal; 201-tick phase and 2401-tick active timeouts including `BOT_INVENTORY`; and controlled `SEARCH_LIMIT`/`UNREACHABLE`/`FAILED` TransferTask mappings. The command subsequently entered the pre-existing broad selftest, which later reported a pre-existing mining selftest failure and was externally terminated at 240 seconds with exit `143`; broad selftest is not reported as PASS.
-- Known limits: fixture path outcomes use a transfer-only, inert-by-default `TransferTask` test seam and therefore do not claim real-world path search coverage. It does not cover actor disconnect, invoke Brigadier command parsing/permission, or drive manager-owned TransferTask through a complete live tick lifecycle. The fixture is server-side only and does not prove bot inventory packet visibility, actual Windows movement, GUI isolation, client rendering, or client-side command behavior.
-- Status: `CLIENT_TEST_PENDING` after supervisor PASS recorded in `.alice-supervision/reviews/20260822-49be37e-a1-1-review.md`; this is not user acceptance. Run A1-A9 in one complete Windows client session. Fill the single-round form `D:\JAVA_projects\alice\.alice-supervision\client-tests\49be37e-a1-1\evidence\evidence-report.md` once (date, Windows SHA, environment, item/count, request UUIDs, results, user decision); overwrite `evidence/latest.log` and `evidence/debug.log` with the one-round complete logs; fill each A1-A9 scenario form (`evidence/A*-*/evidence-report.md`) with yes/no observations and required screenshots. The matrix covers permission/command, normal end-to-end, visible in-transit inventory, source/bot/destination capacity rejection, destination external mutation, hazard interruption, restart/abort/manual takeover, and ordinary chest GUI isolation. User Windows evidence is the only acceptance evidence.
-- Extended focused fixtures (awaiting supervisor review): active plan `20260822-a1-1-focused-fixture-expansion-v1`, baseline `f655be2`. Added `TransferCommandFixture` (A1 parameter validation: valid/invalid item parsing, positive/zero count, same/cross dimension) and `TransferLifecycleFixture` (A3 in-transit visibility with ledger state + bot inventory NBT, A4 destination capacity rejection with leg2 failure + items remain in bot, A8 abort for not-moved→ABORTED and in-transit→SUSPENDED). Verification: `./gradlew compileJava` and `git diff --check` passed. Focused run at 2026-08-22 22:58:10 logged `TRANSFER_COMMAND_FIXTURE_SUITE PASS` and `TRANSFER_LIFECYCLE_EXTENDED_FIXTURE_SUITE PASS`; all A1/A3/A4/A8 assertions PASS. These are server-side focused checks only; C1-C4 (external mutation, survival danger, restart, GUI isolation) remain for Windows client after supervisor approval.
-- Next safe step: supervisor review of the generated packet, followed by the Windows matrix if approved. Do not expand this package into A1.2 fulfilment, persistence recovery writes, generic container support, ACL, packets/UI, or ordinary task integrations.
+- Server verification: Original `49be37e` focused fixture logged `TRANSFER_FIXTURE_SUITE PASS` at 2026-08-22 00:13:18 covering normal legs, capacity/rejection, conflict, mismatch, abort, restart, survival, timeouts, and path outcomes. Extended focused fixtures `924fa304` logged `TRANSFER_COMMAND_FIXTURE_SUITE PASS` and `TRANSFER_LIFECYCLE_EXTENDED_FIXTURE_SUITE PASS` at 22:58:10 covering A1 permission/parameter validation (valid/invalid item parsing, positive/zero count, same/cross dimension), A3 in-transit visibility (IN_TRANSIT_BOT + BOT_INVENTORY + bot NBT), A4 capacity rejection (capacity_rejected + leg2 failure + bot retains items), A8 abort (not-moved→ABORTED, in-transit→SUSPENDED + BOT_INVENTORY). All assertions PASS; no production transfer code changed.
+- Client acceptance: Windows `f655be2` (overlay of 370ff343 + 49be37e), 2026-08-22 23:13-23:16, evidence `.alice-supervision/client-tests/f655be2-a1-1-client/evidence/`. C1 external mutation (capacity_rejected when destination full, conservative stop, no retry), C2 survival hazard (hard_path_failed when lava blocks path, conservative stop observed), C3 restart (task suspended observed, manual takeover), C4 vanilla GUI isolation (no Alice interception). Core semantics verified: conservative stop, no automatic retry/resume, ledger conservation, GUI isolation. User decision: `USER_ACCEPTED`. Final review: `.alice-supervision/reviews/20260822-a1-1-final-acceptance.md`.
+- Known limits: fixture path outcomes use transfer-only test seam, not real-world path coverage. Server-side only; does not cover actor disconnect or full manager-owned live tick. Client C2/C3 SUSPENDED/manual_takeover_required states proven by server fixture + client observation (conservative stop), but transfer-status query not captured in client logs. Bot inventory client sync issue noted (items visible only after restart); source highlight after interrupt noted; both non-blocking for A1.1 acceptance.
+- Next safe step: A1.1 complete and accepted. Do not expand into A1.2 fulfilment, persistence recovery writes, generic container support, ACL, packets/UI, or ordinary task integrations without new approved plan.
 
 ## Transfer Endpoint Selector (awaiting supervisor review)
 
```

## Active Plan Snapshot

```markdown
# Alice Active Work Plan

- Plan ID: `20260822-a1-1-focused-fixture-expansion-v1`
- Status: APPROVED_FOR_IMPLEMENTATION
- Baseline: `f655be2`（Windows overlay，含 370ff343 + 49be37e A1.1 实现）
- User approval: explicit approval received (2026-08-22); user requests server-side focused fixture for A1/A2/A3/A4/A8, reserving only C1-C4 for Windows client testing.
- Supervisor route decision: A1.1 (`49be37e`) server implementation PASS, CLIENT_TEST_PENDING. User申请将简单状态输出确认改为服务端自测，只保留主观性/不可控因素场景（C1外部变更、C2生存危险、C3重启、C4 GUI隔离）在客户端。这是低风险测试代码扩展，无需规划员报告。
- Previous completed plan: `20220822-transfer-selector-item-argument-v1` USER_ACCEPTED（selector E7/E8 全部关闭）。

## Objective

扩展现有 `TransferFixture.java`（`49be37e` 已有 `TRANSFER_FIXTURE_SUITE PASS`），用服务端 focused fixture 覆盖 A1/A2/A3/A4/A8 的简单状态输出确认，减少客户端测试负担。客户端只保留 C1-C4（外部变更、生存危险、重启、GUI）。

## Allowed Scope

- 只在 `src/main/java/com/dddgn/alice/transfer/TransferFixture.java` 扩展测试方法，或新建 `TransferCommandFixture.java` / `TransferLifecycleFixture.java`。
- 可 mock command context、permission level、bot inventory、chest handler capacity。
- 可断言 ledger state、inventory NBT、delta、error code、request UUID、状态转换。
- 覆盖点：
  1. **A1 权限与命令**：permission gate (level 0/1 拒绝, level 2 通过)、参数解析（合法 item+count、非法 item/count=0/不同维度）、admission 返回 request UUID
  2. **A2 正常转移**：已有（确认 source→bot→dest + delta）
  3. **A3 在途可见性**：mock 两段 leg，断言中间状态 `IN_TRANSIT_BOT` / `BOT_INVENTORY` + Bot inventory NBT
  4. **A4 容量不足**：mock 目标满容量、断言 `capacity_rejected` + source delta=0
  5. **A8 Abort**：断言未抽取→ABORTED、在途→SUSPENDED + BOT_INVENTORY + 零写入
- 必要时仅准确更新 BotSelftest 调用与 HANDOVER 记录。
- 运行 `compileJava`、focused fixture（新 suite 名称分别 PASS）、`git diff --check`、session-complete；生成 pending，暂不 push。

## Explicitly Forbidden

- 不改生产代码（`TransferTask`、`BotManager`、`TransferLedgerData`、`ChestBotTransferPrimitive`、ledger、admission）。
- 不触及 GUI/packet/client protocol/NBT serialization。
- 不改既有 `TRANSFER_FIXTURE_SUITE` 已覆盖的 A2 测试。
- 不引入外部测试框架或非 vanilla/Forge mock。
- 不扩展到 C1-C4（外部变更、生存危险、重启、GUI），这些保留客户端。

## Verification Matrix

- `./gradlew compileJava` PASS
- `git diff --check` PASS
- Focused run 输出新 suite 名称并 PASS（如 `TRANSFER_COMMAND_FIXTURE_SUITE` + `TRANSFER_LIFECYCLE_EXTENDED_FIXTURE_SUITE`），日志显示 permission/parse/in-transit/capacity/abort 各断言结果
- Broad selftest 可外部终止，不作为验收
- HANDOVER 更新：记录新 fixture 覆盖范围、focused 时间、A1/A2/A3/A4/A8 服务端 PASS、C1-C4 仍需客户端

## Stop Conditions

Stop and return for replanning if implementation requires changing production transfer code, existing `TRANSFER_FIXTURE_SUITE` behavior, BotManager/ledger/task lifecycle; if focused fixture cannot cleanly mock permission/capacity/in-transit/abort states; or if any fixture method fails to compile or assert.
```

## Supervisor checklist

- Does this change preserve the architecture: goal-level decision, deterministic execution, server authority, semantic GUI interfaces?
- Does it preserve the separate boundaries among `HARD_PATH`, experimental `SOFT_SURFACE`, forced road construction, survival monitoring, and future tunnel planning?
- Does it contradict a numbered decision in `docs/HANDOVER.md` or the frozen plan in `docs/PATHING_REFACTOR.md`?
- Are verification evidence, client-test requirements, known limitations, and the next safe step recorded accurately?
- Was `./gradlew compileJava` run and was the committed revision pushed to `origin/master`?
