# MineTask 场景 A 客户端验收证据

日期：2026-09-05

测试环境：PCL 标准 Forge 1.20.1 / Forge 47.4.10 / Alice / WorldEdit 7.2.15

工件：`alice-1.0.0-1.20.1.jar`

## 操作

使用 `/function alice_test:scene_a` 重置场景并领取启动器，手持 `alice:mining_scene_tester` 普通右键启动。连续完成三轮测试，中间重复执行场景 A 重置。

## 用户观察

用户确认修复版本测试正常。复用 Bot 的第二轮及后续测试不再出现整体位置偏移，也不再在完成瞬间瞬移。Bot 的可见移动过程与实际站位一致，场景 A 可以稳定重复完成。

## 服务端关键事实

三轮测试均记录：

- 起点：`(0,64,0)`
- 目标：`(4,64,4)`
- 挖掘站位：`(4,65,4)`
- 终态：`COMPLETED`
- 结果码：`done`
- 持续时间：49 ticks
- 任务级恢复次数：0

关键日志保存在同目录的 `scene-a-key-lines.log`，完整运行日志保存在 `latest.log`。

## 结论

场景 A 已达到 `USER_ACCEPTED`。已验证正常路径规划、目标挖掘、拾取收尾、Bot 复用，以及复用 Bot 后客户端位置同步。

## 边界

本证据只覆盖场景 A，不代表场景 B、C 或 D 已通过。场景 B 需要单独验证固定视线遮挡和有限目标访问清障。
