# A1.1 原版单箱 -> Bot -> 单箱监督二审

- Review date: 2026-08-22
- Plan: `20260821-original-chest-bot-transfer-a1-1-v1`
- Baseline: `3644429d2d88e1bb6fd1eb24654d828d1451d7f8`
- Reviewed commit: `49be37efb57375f4e4c82ee6d6f4fb18dfb98d85`
- Verdict: `PASS -> CLIENT_TEST_PENDING`

## Authority and verification

- `HEAD`, local `origin/master`, and remote `master` all point to `49be37e`.
- Developer review packet: `.alice-supervision/pending/49be37e.md`.
- `./gradlew compileJava`: PASS.
- `git diff --check`: PASS.
- Focused server evidence recorded in the packet/HANDOVER: `TRANSFER_FIXTURE_SUITE PASS` at 2026-08-22 00:13:18, with request/state/code/location/source-bot-destination deltas.
- Broad selftest later reported a pre-existing mining selftest failure and was externally terminated with exit 143; it is not accepted as a full-suite pass.

## Architecture review

PASS. The transfer remains an explicit, deterministic, server-authoritative two-leg operation. The source leg persists `IN_TRANSIT_BOT` before movement to the destination. Destination write is independently simulated/freshness-checked and preserves remainder or enters unknown discrepancy. No cross-endpoint atomicity, rollback, automatic retry, resume, finish-insert, GUI binding, packet/UI, generic capability, mod container, multi-batch, or owner/ACL behavior was added.

`TransferTask` consumes existing `SurfacePathfinder`/`PathExecutor`; controlled fixture movement outcomes are inert by default and are confined to the transfer fixture seam. No C1, MineTask, DropCollectionTask, roads, tunnels, fluids, escape, FollowTask, AttackTask, or SOFT_SURFACE behavior was expanded.

## Lifecycle and safety review

- In-transit or bot-held abort remains `SUSPENDED`, `BOT_INVENTORY`, `manual_takeover_required`, and `blocksBot`; it performs no inventory write. Only an untouched pre-extraction request becomes ordinary `ABORTED`.
- `assignFollow` now respects a rejected replacement and cannot overwrite a protected transfer.
- 200-tick phase timeout, 2400-tick active timeout, and 12000-tick suspension expiry are represented in the task/ledger and focused fixture. Proven bot location is retained on post-extraction timeout.
- Restart, survival interruption, removal discrepancy, conflict, post mismatch, and controlled path-code mappings are reported with stable evidence.

## Accepted limits

The following remain explicitly unverified and are not represented as PASS: real-world HARD_PATH search behavior (fixture seam is not path-search proof), actor disconnect, Brigadier parsing/permission execution, complete manager-owned live-tick orchestration, bot inventory client synchronization, Windows movement/visibility, GUI isolation, and rendering. These are client/review follow-ups, not a reason to expand A1.1 into generic test infrastructure.

## Required user Windows matrix

Status is `CLIENT_TEST_PENDING`, not user accepted. Run A1-A9 in one complete Windows client session. Fill the single-round form `D:\JAVA_projects\alice\.alice-supervision\client-tests\49be37e-a1-1\evidence\evidence-report.md` once; overwrite `evidence/latest.log` and `evidence/debug.log` with the one-round complete logs; fill each A1-A9 scenario form with yes/no observations and required screenshots. The user must test permission/command behavior; normal source->bot->destination transfer; visible in-transit inventory; source/bot/destination capacity rejection with source delta zero; destination external mutation/conflict; hazard interruption; restart/manual takeover/abort; and ordinary chest GUI isolation. Any unexplained inventory delta, automatic retry/resume, task replacement while protected, or GUI interception returns the package to BLOCK.
