# 监督员授权：DSH P0 受控部署 3083

- 日期：2026-08-24
- 授权方：用户（明确批准）
- 监督员二审：已通过，P0 三项闭合完成
- 3082 验证：bus state 200；持久化测试 16/16；真实 agent E2E MAINTAINER_VERIFIED
- 关联产物：3082 已验证的同一 P0 `lib/` 构建产物
- 关联报告：`.alice-supervision/dsh-p0-implementation-result-20260824.md`
- 二审闭合：`.alice-supervision/dsh-p0-second-review-closed-20260824.md`

## 授权范围

仅授权把**已在 3082 lab 验证通过的同一构建产物**受控部署到 3083。不得重新构建、混入未验证改动、修改 Alice 业务代码或扩大 P0 范围。

## 受控步骤

1. 记录 3083 当前 profile、bus 版本、进程状态和工作树状态。
2. 备份 3083 当前 `dsh-agent-bus/lib/` 到带时间戳的回滚目录。
3. 计算并记录备份 `lib/` 全量逐文件 SHA-256。
4. 确认待部署产物与 3082 已验证产物逐文件 SHA-256 完全一致。
5. 部署同一 `lib/` 到 3083 profile；不得编辑生产 profile 的源码，不得手工混拷其他文件。
6. 重启 3083，记录启动时间、进程、端口和日志。
7. 验证 bus state、无 schema/加载错误、旧任务读取、新工具可见性和基本调用路径。
8. 保存部署后产物 SHA-256、日志、验证结果和回滚命令。

## 硬性边界

- 不部署任何未在 3082 验证的文件或构建结果。
- 不修改 3082、3081、Alice 业务代码、P1、FakeConnection、HARD_PATH、GUI 产品逻辑。
- 不做 `contract_failures`、自动失败升级或其他 P1/P2 功能。
- 若备份哈希不完整、产物哈希不一致、启动失败、schema 错误或 GUI 异常，立即停止，恢复备份并报告，不自行扩大修复范围。

## 交付报告

请更新 `.alice-supervision/dsh-p0-implementation-result-20260824.md` 或另写部署报告，包含：

- 用户批准时间与本授权文档路径；
- 3083 备份路径及备份全量 SHA-256；
- 部署产物全量 SHA-256 与 3082 产物一致性证据；
- 重启/进程/端口/状态检查；
- schema/GUI/旧任务/新工具验证结果；
- 回滚路径和实际是否触发回滚。

本授权只允许本次受控部署，不代表后续功能扩展授权。
