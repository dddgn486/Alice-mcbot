# 审查询查执行任务：批 1 / bot 模块（最高优先级：bot 物理地基）

- 派发：Alice 架构监督员
- 执行：Alice 深度调查员（session-f26bd205）
- 日期：2026-08-25
- Alice 基线：`6c2b461`（HEAD，审查基线）
- 依据规划：`.alice-supervision/research/full-review-plan-20260825.md`（已验收）§1.2 批 1、§2 审查模板、§5 执行路径/验收/停止条件
- 任务书：本文件

## 审查目标（bot 模块，批 1 第一批）

按规划 §1.2：**bot 物理模块为最高优先级**（用户决策者指定：bot 物理是重要基础，直接影响软路径实现与后期任务实现）。审查必须聚焦两条未修复问题线：

1. **玩家跳跃异常**（`ab510fd` 引入，能稳定跳 1.5 格）
2. **bot 物理失效**（自 `2298dd3`（P1 物理观测实施）起存在：无法推挤、无击退）

**实态基线**：P1 客户端同步修复 CLIENT_TEST_FAILED（ab510fd 引入问题、bot 物理失效未修复、复测记录从未创建）。审查**只提交证据，不修**（§5.4：修改权归修复主线，审查不得派发修复）。

## 审查对象

`src/main/java/com/dddgn/alice/bot/`（6 文件）：
- `BotPlayer.java`（核心：玩家化生命周期/travel/碰撞/击退/推挤）
- `FakeConnection.java`（核心：S2C 同步语义，ab510fd 改动点）
- `BotManager.java`（核心：BotSession 排他/spawn/同步）
- `BotSelftest.java` / `BotWorldData.java` / `TaskExecutionRecord.java`（辅助）

## 审查方法（严格按规划 §2 模板产出）

报告必须覆盖模板全部 8 个字段（§2.1-§2.8）：
1. **定位与文件清单**：每文件一句职责 + 3 字角色（状态机/门面/原语/夹具）
2. **边界归属**：bot 物理在地基线（供 HARD_PATH/SOFT_SURFACE 所有线依赖）；对照 R30（矿链 HARD_PATH 固定）/R31（软移动借鉴成熟实现）/R33（NATIVE_TRAVEL 已验证平地+单格高差）/R35（落地需真实支撑顶面）/R36（FollowTask 独立）
3. **功能正确性核查点**：宣称行为 vs 代码实现差异清单；每点标证据类型（静态核对/服务端可证/需客户端实测）
4. **已知坑与 R# 对照**：命中 R# 清单 + 当前代码是否仍遵守；边界漂移待监督员判定项
5. **Dual-View**：服务端可证（fixture/日志）vs 需客户端实测（渲染/同步时序/物理可见性）——**必须写明"服务端 PASS ≠ 客户端正确"（引用 ab510fd 教训 + EVIDENCE_CONFLICT 规则）**
6. **证据等级**：现有验证证据分级（USER_ACCEPTED/CLIENT_TEST_PENDING/服务端 PASS/无证据）+ 缺口
7. **风险等级**：A 补测试 / B 修复 / C 替换候选 / D 维持（按 §2.7 触发条件）
8. **替换判据**：按 §4.1 五判据（M1-M5）填"已知证据/缺证据"，输出"替换决策点所需证据清单"——**只列方向不选型**

**特别核查项（必须回答）**：
- `FakeConnection.send()` 当前实现（ab510fd 广播逻辑）：广播范围（是否含自己）、包类型过滤、对玩家客户端物理的干扰路径（玩家跳跃异常可能根因）
- `BotPlayer` 的 `isPushable()`/`Entity.push()`/`travel()` 调用链：bot 无法推挤、无击退的代码层面证据
- 回退测试结论对照（`65f4863` 跳跃正常 + bot 仍无法推挤 → 两条独立问题线）是否与代码事实一致

## 禁止事项（硬性）

- **不修改任何文件**（业务代码/HANDOVER/active plan/skill/客户端记录）
- 发现疑似 bug **只记录证据，不修**（规划 §5.1/§5.3）
- 不预定替换结论（候选只列方向）
- 审查结论不得要求改 FakeConnection 同步方案后自行实施（§5.4：修改权归修复主线）
- 与 active plan p1-client-sync-fix-v1 冲突时停止该模块并注明实态 CLIENT_TEST_FAILED（§5.3）
- **工具可用性**：如你无法使用 read/bash（历史出现过），先列出你当前可用工具清单并复测 read 具体报错，区分真缺失/瞬态；确认真缺失再向监督员 request_input 说明（不要直接放弃）

## 产出

报告写到：`.alice-supervision/research/review-batch1-bot-20260825.md`
- 覆盖 §2 全部 8 字段
- 明确区分"已确认事实 / 架构推论 / 待调查"（证据分级）
- 批汇总待监督员验收后由监督员提交（本任务只产出模块独立报告）

完成后 report_task 附报告路径。

## 验收标准（规划 §5.2）

- 报告覆盖 §2 全部字段
- 玩家跳跃异常 + bot 物理失效两条问题线各有代码层证据（文件+行号）
- 明确区分已确认/推论/待调查
- 无文件被修改（git status 与审查前一致，仅新增 research/ 报告）
- 无预定替换结论