# 审查执行任务：批 3 / item 模块（测试工具/选择器入口语义）——批 3 第一个模块

- 派发：Alice 架构监督员
- 执行：Alice 深度调查员（session-f26bd205）
- 日期：2026-08-25
- Alice 基线：`6c2b461`（HEAD，审查基线）
- 依据规划：`.alice-supervision/research/full-review-plan-20260825.md`（已验收）§1.2 批 3、§2 审查模板、§5 执行/验收/停止
- 前置证据：批 1（bot/pathing/task）+ 批 2（transfer/gui/client/network）已验收报告

## 审查目标（item 模块，批 3 第一个）

item 是**测试工具与选择器入口**：右键/Shift 语义、support→foot 映射、目标选择。审查必须聚焦：

1. **选择器入口语义**：`TargetSelector`/`SoftMoveSelector`/`SoftPathProbeSelector`/`TransferEndpointSelector` 的右键/Shift+右键语义（对应不同测试工具入口）
2. **support→foot 映射（R32）**：软移动探针的 support 顶面 → foot 脚位映射是否一致
3. **物品注册面**：`AliceItems` 注册了哪些物品？（测试工具物品清单）
4. **事件语义**：`TransferEndpointSelectorEvents`（transfer 已验收："deliberately not cancel"）与此处交叉核对
5. **边界**：测试工具是否只挂实验线（soft 探针/selector），不触碰矿链/道路

## 审查对象

`src/main/java/com/dddgn/alice/item/`（8 文件）：
- `AliceItems.java`（物品注册面）
- `RoadPlannerItem.java`（道路计划物品——road 批核查，item 只核对注册/入口）
- `SoftMoveSelector.java`（软移动选择器）
- `SoftPathProbeSelector.java`（软路径探针选择器）
- `TargetSelector.java`（目标选择器）
- `TransferEndpointSelector.java`（传输端点选择器）
- `TransferEndpointSelectorEvents.java`（选择器事件——transfer 批已交叉）
- `TransferEndpointSelectorEventsFixture.java`（夹具）

## 审查方法（严格按规划 §2 模板，8 字段全覆）

1. **定位与文件清单**：每文件职责 + 3 字角色（注册/选择器×5/事件/夹具）
2. **边界归属**：测试工具线（selector 入口）vs 矿链/道路/传输（各自入口）；对照 R32（support→foot）、A1.1（transfer 选择器验收）
3. **正确性核查点**：宣称行为 vs 实现（右键/Shift 语义、映射）；证据类型（静态/服务端/客户端）
4. **R#/决策对照**：R32（support→foot 映射）、transfer 选择器相关（A1.1 记录）、soft 探针相关（R33/R35——批 1 已核查，此处核对选择器入口映射）
5. **Dual-View**：服务端可证（语义/映射一致性）vs 需客户端实测（右键交互手感/高亮）
6. **证据等级**：各选择器的客户端验证记录（A1.1 transfer 选择器有记录；soft 探针 T1-T2 USER_ACCEPTED）
7. **风险等级**：A/B/C/D
8. **替换判据**：按 §4.1 M1-M5（选择器入口：自研事件 vs 成熟方案——只列方向不选型）

**特别核查项（必须回答）**：
- **入口语义表**：四个选择器（Target/SoftMove/SoftPathProbe/TransferEndpoint）的触发方式（右键/Shift+右键）+ 对应动作（任务派发/菜单/探针）+ 事件是否 cancel 的语义标注
- **support→foot 映射（R32）**：SoftMoveSelector/SoftPathProbeSelector 的 support 顶面 → foot 脚位计算（与批 1 的 MovementHelper.supportTopY 交叉核对）
- **物品白名单**：AliceItems 注册物品清单（是否有遗留/无关物品）

## 禁止事项（硬性）

- 不修改任何文件（业务代码/HANDOVER/active plan/skill/客户端记录）
- 疑似 bug 只记录不修；不预定替换结论
- **GUI FROZEN 边界不可触碰**（选择器交互若触 GUI 侧只记录）
- 与 p1-client-sync-fix-v1 交叉时只提交证据（修改权归修复主线）
- 工具不可用时：先列可用工具清单复测 read 报错，确认真缺失再 request_input

## 产出

报告写到：`.alice-supervision/research/review-batch3-item-20260825.md`
- 覆盖 §2 全部 8 字段
- 含特别核查项结论（入口语义表/support→foot 映射/物品白名单）
- 明确区分已确认/推论/待调查

完成后 report_task 附报告路径。

## 验收标准（规划 §5.2）

- 报告覆盖 §2 全部字段
- 特别核查项全部回答（入口语义表 / R32 support→foot 映射核对 / 物品白名单）
- 与批 1/2 交叉核对一致（transfer 选择器/soft 探针）
- 无文件被修改；无预定替换结论；区分已确认/推论/待调查