# 审查执行任务：批 2 / client 模块（仅客户端状态与渲染，双端证据缺口）

- 派发：Alice 架构监督员
- 执行：Alice 深度调查员（session-f26bd205）
- 日期：2026-08-25
- Alice 基线：`6c2b461`（HEAD，审查基线）
- 依据规划：`.alice-supervision/research/full-review-plan-20260825.md`（已验收）§1.2 批 2、§2 审查模板、§5 执行/验收/停止
- 前置证据：`.alice-supervision/research/review-batch2-gui-20260825.md`（gui 已验收：方向 B 完全接管，ClientBotInventoryState 属 client 模块——gui 审查已交叉核对）、`.alice-supervision/research/review-batch2-transfer-20260825.md`（transfer 已验收）

## 审查目标（client 模块，批 2 第三个）

client 是**仅客户端层**（状态存储/渲染/屏幕）：服务端权威的客户端镜像。审查必须聚焦：

1. **Client*State 职责**：`ClientBotInventoryState`/`ClientRoadState`/`ClientTargetState`——纯客户端只读状态？（是否只由 S2C 包更新、不改本地决策）
2. **渲染器**：`TargetOutlineRenderer`（目标高亮渲染）——客户端渲染边界、是否触碰服务端权威
3. **双端证据缺口**：这些客户端状态的更新是否都有对应 S2C 包（服务端推送）？哪些状态**无服务端来源**（客户端自造 = 盲区）？
4. **与 gui 接缝**（gui 已验收）：`BotInventoryScreen`/`ClientMenuScreens`/`ClientBotInventoryState` 与方向 B 的一致性（gui 审查已确认——client 侧反向核对）

## 审查对象

`src/main/java/com/dddgn/alice/client/`（6 文件）：
- `ClientBotInventoryState.java`（bot 库存客户端只读状态——gui 方向 B 的客户端存储）
- `ClientRoadState.java`（道路客户端状态）
- `ClientTargetState.java`（目标客户端状态）
- `gui/BotInventoryScreen.java`（方向 B 屏幕，纯客户端渲染）
- `gui/ClientMenuScreens.java`（方向 A 遗留 no-op）
- `render/TargetOutlineRenderer.java`（目标高亮渲染器）

## 审查方法（严格按规划 §2 模板，8 字段全覆）

1. **定位与文件清单**：每文件职责 + 3 字角色（状态/屏幕/渲染）
2. **边界归属**：客户端层（仅客户端）vs 服务端权威（S2C 推送）；对照 FROZEN（gui 相关，19af345）、R#（如目标高亮/道路渲染相关决策）
3. **正确性核查点**：宣称行为 vs 实现；证据类型（静态/服务端/客户端）
4. **R#/决策对照**：ClientRoadState/ClientTargetState 的 S2C 来源（RoadPlanPacket/TargetPacket 属 network 批后审——client 先核对接收侧）；高亮渲染决策（A1.1 相关）
5. **Dual-View**：客户端状态的服务端来源核查（每个 Client*State 的 setter/更新入口 + 对应 S2C 包）；需客户端实测项（渲染/同步时序）
6. **证据等级**：各 Client*State 的客户端验证记录（有/无）
7. **风险等级**：A/B/C/D
8. **替换判据**：按 §4.1 M1-M5（客户端渲染方向：自研渲染 vs vanilla/成熟方案——只列方向不选型）

**特别核查项（必须回答）**：
- **S2C 来源覆盖**：ClientRoadState/ClientTargetState/ClientBotInventoryState 的每个更新入口是否都有服务端 S2C 包来源？列出 客户端状态字段 ↔ 对应 packet 的映射表；**标出无服务端来源的状态（= 客户端自造 = 双端证据盲区）**
- **渲染器边界**：TargetOutlineRenderer 是否只读渲染（不写回/不改决策）
- **@OnlyIn 标注**：全员是否 Dist.CLIENT 隔离（不会在服务端误触发）

## 禁止事项（硬性）

- 不修改任何文件（业务代码/HANDOVER/active plan/skill/客户端记录）
- 疑似 bug 只记录不修；不预定替换结论
- **GUI FROZEN 边界不可触碰**（BotInventoryScreen 属冻结线——不得建议修改）
- 与 p1-client-sync-fix-v1 交叉时只提交证据（修改权归修复主线）
- 工具不可用时：先列可用工具清单复测 read 报错，确认真缺失再 request_input

## 产出

报告写到：`.alice-supervision/research/review-batch2-client-20260825.md`
- 覆盖 §2 全部 8 字段
- 含特别核查项结论（S2C 来源映射表/渲染器边界/@OnlyIn 隔离）
- 明确区分已确认/推论/待调查

完成后 report_task 附报告路径。

## 验收标准（规划 §5.2）

- 报告覆盖 §2 全部字段
- 特别核查项全部回答（状态↔packet 映射表 / 无服务端来源状态标盲区 / 渲染器只读 / @OnlyIn 全员）
- 无文件被修改；无预定替换结论；区分已确认/推论/待调查