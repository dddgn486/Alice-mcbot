# 审查执行任务：批 2 / transfer 模块（传输线：ledger/request/selector 与 inventory 耦合）

- 派发：Alice 架构监督员
- 执行：Alice 深度调查员（session-f26bd205）
- 日期：2026-08-25
- Alice 基线：`6c2b461`（HEAD，审查基线）
- 依据规划：`.alice-supervision/research/full-review-plan-20260825.md`（已验收）§1.2 批 2、§2 审查模板、§5 执行/验收/停止
- 前置证据：
  - `.alice-supervision/research/review-batch1-task-20260825.md`（批 1 task 已验收——T10/T11 确认 TransferTask D 维持：ledger 状态机 + SEARCH_LIMIT 独立码 `HARD_PATH_SEARCH_LIMIT` + TransferFixture 8 场景 USER_ACCEPTED）
  - 批 1 bot/pathing 报告（transfer 用的 PathExecutor 属 HARD_PATH——pathing 已确认 R30）

## 审查目标（transfer 模块，批 2 第一个）

transfer 是**传输业务主线**：ledger/request/selector 与 inventory 的耦合、A1.1 已验收范围核验、与 GUI/network 的接缝。审查必须聚焦：

1. **A1.1 已验收范围核验**（USER_ACCEPTED 不可翻案）：原版箱子转移 A1.1-A9 已用户验收——当前代码是否仍在该验收语义内（ledger 状态机、capacity_rejected、hard_path_failed、保守停止、无自动 retry）
2. **ledger/request/selector 与 inventory 耦合**：TransferSelectionData/TransferSelectionLifecycle/TransferSelectionSubmission 与 BotInventorySnapshot/InventoryObservation 的读写面（谁可写、谁只读——审查根因分析维度 1 的采集点）
3. **GUI FROZEN 边界**（19af345 不可翻案）：transfer 的 GUI 交互是否触碰 FROZEN 线（BotInventoryMenu 属 gui 模块，批 2 后审——transfer 只核对"是否调用了 GUI 相关类来改变 FROZEN 语义"）
4. **与 network 的接缝**（批 2 后审 network——transfer 只核对 ledger 状态与请求是否在服务端权威内，不依赖客户端回传）

## 审查对象

`src/main/java/com/dddgn/alice/transfer/`（13 文件）：
- 核心：`ChestBotTransferPrimitive.java`（转移原语，bot 审查 A1.1 报告提到 syncBotInventorySlots）、`TransferLedgerData.java`（ledger 状态机）、`TransferRequest.java`（请求）、`TransferCodes.java`（结果码）
- 选择器：`TransferSelectionData.java` / `TransferSelectionLifecycle.java` / `TransferSelectionSubmission.java` / `TransferSelectionFixture.java`
- 容量/观测：`CapacityPreflight.java` / `InventoryObservation.java` / `ChestEndpointRef.java`
- 测试/钩子：`TransferFixture.java`（8 场景，USER_ACCEPTED）/ `TransferTestHooks.java`

## 审查方法（严格按规划 §2 模板，8 字段全覆）

1. **定位与文件清单**：每文件职责 + 3 字角色（原语/状态机/请求/选择器/容量/夹具）
2. **边界归属**：传输线（ledger 服务端权威）vs 矿链（HARD_PATH）vs GUI（FROZEN）vs network（接缝）；对照 R#（transfer 相关）+ A1.1 验收边界
3. **正确性核查点**：宣称行为 vs 实现（A1.1 验收语义逐一核对）；证据类型（静态/服务端/客户端）
4. **R# 对照**：transfer 命中的 R# + 当前遵守性（重点：R26 SEARCH_LIMIT 独立码——task 报告 T11 已确认，本模块交叉核实；A1.1 验收记录 2ef35e5.md/f0ef6fc.md 中的决策）
5. **Dual-View**：服务端可证（ledger 状态迁移/拒绝码/fixture 场景）vs 需客户端实测（inventory 同步可见性/选择器交互）——引用 ab510fd 教训
6. **证据等级**：A1.1 USER_ACCEPTED（客户端记录存在）+ TransferFixture PASS + 缺口
7. **风险等级**：A/B/C/D 判定（task 报告已定 TransferTask D 维持——本模块核验 transfer 目录其他文件是否有隐蔽问题）
8. **替换判据**：按 §4.1 M1-M5 填已知/缺证据（方向：无现成成熟方案对照时填"无明确成熟候选"或"vanilla Container 机制借鉴"——只列方向不选型）

**特别核查项（必须回答）**：
- **A1.1 验收语义保持**：capacity_rejected（源满/目标满）、hard_path_failed（路径中断保守停止）、无自动 retry/resume、ledger 守恒——当前代码逐一核对（服务端语义）
- **inventory 同步缺口**：`syncBotInventorySlots()`（ChestBotTransferPrimitive.insertBot 后发送 SetSlot 包）——这是 bot 审查 A1.1 记录过的修复点；核对当前实现与客户端验证状态
- **选择器与 GUI 接缝**：TransferSelection* 是否依赖客户端回传（服务端权威要求：不依赖客户端回传才符合服务端权威）——若依赖，标"需用户决策/待 network 批核对"
- **forcePickup 反射兜底**：DropCollectionTask 提到 forcePickup（task 报告 T13 附近）——transfer 目录是否有同类反射兜底，标证据等级

## 禁止事项（硬性）

- 不修改任何文件（业务代码/HANDOVER/active plan/skill/客户端记录）
- 疑似 bug 只记录不修；不预定替换结论（候选只列方向）
- **GUI FROZEN 边界（19af345）不可触碰**：不得把"建议修改 GUI"当作审查结论，只记录耦合事实
- 与 p1-client-sync-fix-v1 交叉时只提交证据（修改权归修复主线）
- 工具不可用时：先列可用工具清单复测 read 报错（区分真缺失/瞬态），确认真缺失再 request_input

## 产出

报告写到：`.alice-supervision/research/review-batch2-transfer-20260825.md`
- 覆盖 §2 全部 8 字段
- 含特别核查项结论（A1.1 语义保持 / inventory 同步状态 / 选择器服务端权威 / forcePickup 证据等级）
- 明确区分已确认/推论/待调查

完成后 report_task 附报告路径。

## 验收标准（规划 §5.2）

- 报告覆盖 §2 全部字段
- 特别核查项全部回答（A1.1 逐语义核对 / inventory 同步验证状态 / 选择器是否依赖客户端回传 / forcePickup 证据等级）
- R# 对照完整 + A1.1 不可翻案边界确认
- 无文件被修改；无预定替换结论；区分已确认/推论/待调查