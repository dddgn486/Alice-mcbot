# 佐证任务：定位"bot 实体 tick 链无碰撞反应"的精确机制（修复注射点前置）

- 派发：Alice 架构监督员
- 执行：Alice 主开发员（session-30b693e7）
- 日期：2026-08-25
- Alice 基线：`e9af1b3`（HEAD；NPE 佐证已验收）
- 性质：**只读证据采集**——不改任何业务实现与 fixture；不判定根因（定案由监督员执行）；不实现修复
- 依据：定案方向已收敛（监督员定案记录 `research/f1-f6-assertion-result-20260825.md` 尾部 + NPE 佐证 `research/npe-evidence-result-20260825.md`）：B3 NPE 吞错**已排除**（stderr 0 字节+透传佐证）；定案方向 = **实体 tick 链（aiStep/pushEntities 碰撞段）未被有效驱动**；任务层直接 travel 不经 `bot.tick()` 实体链（已确认事实）

## 背景（已确认事实链）

1. F1-F6 断言结果（`f1-f6-assertion-result-20260825.md`）：
   - F1 hurt false / delta 恒 0；F3b 实体推挤链无位移；tickChain 碰撞 flags 无变化（horizontalCollision=false->false）
   - F4 PASS（travel 摩擦衰减执行）；F5/F2 PASS（时序/保留）
2. NPE 佐证（`npe-evidence-result-20260825.md`）：
   - stderr 0 字节 + stdout 236KB 透传佐证 → **无 NPE** → B3 排除
   - 驱动链三线区分：实体级 ServerLevel entityTickList / 任务层 BotManager.onServerTick（只调 session.tick 不调 bot.tick）/ fixture 手动 bot.tick()
   - 关键事实：任务层直接 travel 不经 bot.tick() 实体链 → F4 PASS 但 F3b/tickChain FAIL 并存

## 待定位的精确机制（本任务唯一目标）

**为什么 `bot.tick()` 实体链（含 aiStep/pushEntities 碰撞段）在驱动下不产生碰撞反应？** 需要给出**代码/字节码级证据**回答以下候选（只列证据，不预定哪个成立，由监督员定案）：

1. **aiStep() 提前返回路径**：javap 反编译 `LivingEntity.aiStep()`（及 `ServerPlayer` 若无 override）——列出所有"提前 return / 跳过 pushEntities / 跳过碰撞处理"的分支及触发条件（如 `isEffectiveAi`/`isAlive`/`noPhysics`/`isSpectator` 检查顺序）
2. **BotPlayer 的实体 tick 驱动状态**：ServerPlayer 在 ServerLevel 中由谁驱动 tick（PlayerList.tick? entityTickList? 服务器主循环?）——`placeNewPlayer` 注册后 bot 是否进入有效 tick 列表；**代码证据**（谁调用 bot.tick()，调用频率，与任务层 travel 的关系）
3. **F3b/tickChain 最小复现定位**：fixture 手动 `bot.tick()` 驱动时——aiStep 是否进入（是否有条件阻止其进入 pushEntities 段）；该段在什么条件下才会设置 horizontalCollision/verticalCollisionBelow flags（阻塞物/墙面场景下）
4. **碰撞 flags 的设置来源**：horizontalCollision/verticalCollisionBelow 是在 aiStep 内哪个点设置的（javap 定位 setter 调用点 + 触发条件）——用于说明"为何墙撞无变化"

## 证据要求

- 每条证据给出：文件/字节码位置 + 行号/字节码偏移 + 触发条件
- javap 反编译对象：`LivingEntity.aiStep` / `ServerPlayer.tick` / `LivingEntity.pushEntities`（1.20.1-47.4.10 mapped jar，之前审查已验证可用路径）
- 源码证据：BotManager.onServerTick / BotManager.spawn / BotManager.BotSession.tick（驱动链）
- **区分"事实"与"推断"**：标注每条证据分级

## 禁止事项（硬性）

- **不改任何业务实现**：BotPlayer/FakeConnection/SoftMovementPrimitive/BotManager/BotSelftest/BotPhysicsAssertionFixture 零改动
- **不判定根因**：本任务只采证据，定案由监督员按区分表执行——不写"根因是 X"
- **不实现修复**：不修 tick 驱动、不修 aiStep、不修 pushEntities
- 如需临时日志/实验手段超出只读范围 → request_input 报监督员批准，不得自行加
- 不触碰跳跃异常线/tunnel/transfer/road 等其他线

## 产出

报告写到：`.alice-supervision/research/physics-chain-mechanism-evidence-20260825.md`
- aiStep 提前返回分支清单（含条件/触发）
- bot.tick() 实际驱动链（代码证据：谁调用/频率/与任务层关系）
- F3b/tickChain 复现定位（aiStep 是否进入 pushEntities 段 + 为何 flags 无变化）
- 碰撞 flags 设置点定位（javap）
- 证据分级（已确认事实/推断/待调查）

完成后 report_task 附报告路径。

## 验收标准（规划 §5.2 + 任务书）

- 报告落盘 `research/physics-chain-mechanism-evidence-20260825.md`
- ① aiStep 提前返回分支清单（每条含位置+条件）② bot.tick() 驱动链代码证据（谁调用+频率）③ F3b/tickChain 复现定位 ④ 碰撞 flags 设置点 javap 定位——四项齐全
- 证据分级明确（事实/推断/待调查）
- 未修改任何业务实现与 fixture；未判定根因；未实现修复