# DSH P0 3082 E2E Tool Test Record

- Status: MAINTAINER_VERIFIED
- Verification tier: MAINTAINER_VERIFIED only; this does not approve or authorize 3083 deployment.
- Lab URL: `http://127.0.0.1:3082/`
- Workspace: `/home/fb486/projects/dsh-agent-bus-lab`
- Agent session: `session-214482f9-a9f9-48e2-8556-0afb778fc1b6`
- Task target: `c84ba17a-bc8b-48af-aa19-547362c3e63a`

## Preconditions

- [x] Session belongs to the 3082 lab workspace and was live: workspace `68952679-072f-4adc-a8b8-3fc8e725d062`.
- [x] Target task was readable in the 3082 lab ledger.
- [x] Before calls: 5 tasks; stats `completed=3, failed=1, canceled=1`; target `failed/settled/timeout`.
- [x] No pre-existing test emergency record.

## Required Calls

1. `check_required_skills(task_id=<target>)`
2. `check_evidence_state(task_id=<target>, from_state="SERVER_VERIFIED", to_state="CLIENT_TEST_PENDING")`
3. `check_evidence_state(task_id=<target>, from_state="SERVER_VERIFIED", to_state="USER_ACCEPTED")`
4. `git_sync_check()`
5. `record_emergency_sync(source_commit="p0-gui-e2e", target_path="/tmp/p0-gui-e2e", sha256="e2e-test", reason="P0 GUI endpoint verification", recovery_plan="remove or archive the test record after verification")`

Only these P0 tools may be called. Do not create, edit, report, settle, cancel, or call another tool.

## Evidence Captured

### Initial four-tool point test

- `check_required_skills(task_id="c84ba17a-bc8b-48af-aa19-547362c3e63a")` → actual agent response: `satisfied=[]`, `unsatisfied=[]`, no parameter/empty-value error. The legacy task has no required skill set.
- `check_evidence_state(..., SERVER_VERIFIED -> CLIENT_TEST_PENDING)` → actual response: allowed; read-only, no state write.
- `check_evidence_state(..., SERVER_VERIFIED -> USER_ACCEPTED)` → actual response: `VIOLATION — transition ... is not allowed by state-machine.yml`; read-only, no state write.
- `git_sync_check()` → first response exposed a bug: `dirty:true` plus `fatal: not a git repository`.
- `record_emergency_sync(...)` → actual response succeeded, returned the record path, file contents were complete, and the test record was removed afterward.

### Corrective retest

- `get_task(task_id="c84ba17a-bc8b-48af-aa19-547362c3e63a")` → returned full record including `title`; previous `value.title is not a declared property` schema error is gone.
- `git_sync_check()` → returned `repository:false`, `remotes:(not a git repository)`, `dirty:false`, and an explicit error; no false dirty result.
- No lifecycle-changing tool was called during the retest.

### Independent before/after evidence

- After calls: 5 tasks; stats remain `completed=3, failed=1, canceled=1`; target remains `failed/settled/reason=timeout`.
- Lab worktree remains clean; no emergency records remain after cleanup.
- 3082 bus state remains HTTP 200; logs contain no schema/error entries.
- The four initial tool calls and corrective retest were performed by the real agent session in the 3082 GUI. This record is MAINTAINER_VERIFIED, not user acceptance for 3083.

### Corrective retest raw evidence

- `get_task` raw response returned the complete failed task record, including `title: 延迟十分钟读取 README`; no `value.title is not a declared property` error.
- `git_sync_check` raw response returned `repository: false`, `remotes: (not a git repository)`, `dirty: false`, `emergency: 0`, plus the explicit non-Git error. The previous dirty contradiction is fixed.
- Independent bus-state check: task count `5`; stats `completed=3, failed=1, canceled=1`; target task remains `failed`, `settled=true`, `reason=timeout`.
- Lab worktree status before/after remains clean; test emergency record was already removed.

## Result

V1-V11 plus the real-agent endpoint check are MAINTAINER_VERIFIED. 3083 remains undeployed and untouched.
