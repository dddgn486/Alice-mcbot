# M5 跳跃异常与 M3 击退失效联合证据调查

## 性质
只读深度调查，不修改业务代码、active plan 或客户端验收记录；不构成修复授权。

## 已知客户端事实
- Windows 视频：`videos/2026-08-26 00-04-09.mp4`
- Windows `run/logs/latest.log` 时间范围约 00:00:10–00:04:40。
- bot 多次生成/清除：生成约 00:00:49、00:01:46、00:04:12；清除约 00:01:05、00:03:33、00:04:33。
- bot 存在期间用户观察玩家跳跃约 1.5 格；bot 清除后跳跃恢复正常。
- M2 推挤可见已通过；M3 击退仍无可见位移。
- F1 修复后服务端 `hurtOk=true`、health 下降，但 delta 仍为 0；`BOT_PHYSICS_C1_CONSUME` 只证明 C-1 残余消费。

## 调查问题
1. `ab510fd` 引入的 `FakeConnection` 三类包广播，是否能解释“bot 存在时玩家跳 1.5 格、bot 清除后恢复”？
2. M3 击退无效是否是同一广播/实体同步问题，还是服务端 knockback 根本未产生？
3. 是否存在 bot 被加入 PlayerList 后，广播给真实玩家自身/错误 entityId/重复运动包/错误 recipient 的路径？
4. 在不改业务代码的前提下，给出最小对照实验：已知 good=`65f4863`、bad=`ab510fd`、当前=`f4f6ca6`，明确需要记录的 packet 类型、entityId、recipient、tick、玩家位置/速度和 bot delta。

## 优先证据
1. Alice 当前源码：`FakeConnection.java`、`BotManager.spawn`、`BotPlayer.java`。
2. Git 对照：`65f4863` → `ab510fd` → `f4f6ca6`，重点审查广播逻辑和调用时序。
3. Windows `latest.log`、`debug.log`、视频元信息/可抽帧证据；无法直接解析视频时必须如实说明。
4. 固定 Forge 1.20.1-47.4.10 mapped bytecode，仅在需要确认 packet/ServerPlayer 语义时使用。

## 必须回答
- 事实、推断、未证实假设分开写。
- 用时间线把 bot 生成/清除与跳跃异常关联，但不得把时间相关性写成因果定案。
- 比较 M3 服务端：`hurtOk=true`、health 变化、delta=0 与客户端可见失败，判断是否 `EVIDENCE_CONFLICT`。
- 评估 `FakeConnection.broadcastToRealPlayers` 是否把 bot movement/motion/teleport 包送给真实玩家；指出缺少哪些可观测日志。
- 至少给出两个可证伪假设及定向验证；不得直接提出实现授权。
- 明确下一最小调查/验证包与停止条件。

## 禁止
- 不改 `FakeConnection`、`BotPlayer`、`BotManager`、fixture、FakeConnection 广播策略或任何业务代码。
- 不修改 active plan，不标记 USER_ACCEPTED，不把 M3/M5 当作已通过。
- 不把视频无法抽帧伪报为已观看；视频只可作为文件存在/元数据或静态帧证据。
- 不把 `BOT_PHYSICS_C1_CONSUME` 当作击退证据。

## 方法与 skill 记录
本任务要求使用项目本地 skill 方法：
- `debugging-root-cause-analysis`：假设驱动 + 对照验证 + 反模式自检
- `test-coverage-matrix`：服务端/客户端/双端同步矩阵
- `evidence-collection-standard`：时间戳、行号、视频/日志、EVIDENCE_CONFLICT
运行时 skill catalog 当前拒绝这些 manifest ID；如无法正式加载，报告必须如实记录，不得伪造 skill_usage。

## 输出
`.alice-supervision/research/m5-jump-and-m3-knockback-investigation-20260826.md`
