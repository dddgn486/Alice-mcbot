# M3/M5 Packet 诊断观测实施任务

- Work package ID：`20260826-m3-m5-packet-observer-v1`
- 用户批准：2026-08-26，批准 D0-D4 诊断观测
- 性质：只采证、可禁用、可回滚；**不构成 M3/M5 修复授权**

## 必读
- `.alice-supervision/active-plan.md` Approved Diagnostic Observation Work Package
- `.alice-supervision/research/m3-m5-packet-diagnostic-plan-20260826.md`
- `.alice-supervision/research/m5-jump-and-m3-knockback-investigation-20260826.md`
- `docs/HANDOVER.md`
- `FakeConnection.java` / `BotManager.java` / 既有 fixture

## Allowed Scope

只允许增加独立诊断输出或既有诊断 seam，记录：
- `corr`、serverTick/clientTick（可得时）
- source bot UUID/name/entityId
- packet concrete class、packet entityId
- `hasPosition`/`hasRotation`、relative `xa/ya/za`、yaw/pitch/onGround（可得时）
- motion `id/xa/ya/za`
- teleport `id/x/y/z/yaw/pitch/onGround`
- recipient UUID/name/entityId/dimension/distance（可得时）
- manual vs tracking 来源、同 tick/entityId/recipient 重复计数
- M3 hurtOk/source/health/delta/position/onGround/collision/C1 displacement（不得改变状态）
- M5 bot spawn/clear tick、玩家 position/velocity/onGround（已有 seam 可得时）

## Hard Boundaries

- 不改变 packet 类型过滤、recipient 集合、发送顺序、packet 对象、callback、tracking、hurt、C-1 或客户端语义。
- 不修改 `FakeConnection` 的业务行为；若必须触及该文件，只能添加诊断日志，改动最小且逐行可审。
- 不修改 fixture 断言、active plan、客户端验收记录。
- 不添加自动回退、修复逻辑、position 校正、entityId 改写、去重发送或 API 行为变化。
- 不触碰 MineTask/DropCollectionTask/HARD_PATH、5 个 SOFT travel 点、P0 门禁。

## Required Validation

- 运行 `./tools/work-session-start.sh` 并读取其输出的 active plan/HANDOVER/相关报告。
- `./gradlew compileJava`。
- 运行最小 headless 既有套件，确认 F1/F2/F4/F5/R2/R3/C1 不回归。
- `git diff --check`。
- 记录诊断输出示例与不可获得字段。
- 只提交诊断文件/最小日志 seam；commit/push；更新 HANDOVER 与证据报告；执行 `./tools/session-complete.sh`。

## Stop Conditions

- 无法保证只记录不改变语义：停止并 request_input。
- compileJava 或既有套件失败：停止，不修复顺带问题。
- 发现 packet entityId/recipient 错误、重复无法解释、观察器改变时序：停止，保留证据，不修改业务路径。
- 诊断导致 crash、世界变化、bot 悬挂/重复生成或客户端异常扩大：立即回滚诊断提交。

## Deliverables

- 最小诊断观测改动（可禁用、可回滚）
- `.alice-supervision/research/m3-m5-packet-observer-result-20260826.md`
- HANDOVER 诊断段（实际 commit、字段、限制、下一步）
- push 后状态与 Windows 诊断矩阵说明
- report_task 附任务书路径

## 不构成授权

本任务完成后只交付诊断证据；M3/M5 修复必须由监督员审核证据并取得用户另行批准。
