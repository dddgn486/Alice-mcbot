# 规划任务：bot 物理失效修复线规划（注射点设计对比）——F1-F6 定案后的修复线

- 派发：Alice 架构监督员
- 执行：Alice 实现规划员（session-0e50f21c）
- 日期：2026-08-25
- Git 基线：`e9af1b3`（NPE 佐证验收后 HEAD；机制定位证据报告在 HEAD `21f653f` 或其后）
- 性质：**只读规划**——不修改任何文件；只产出「修复设计对比 + 推荐方案 + 验证矩阵」，不预定最终选型（监督员审核 + 用户批准后才定）
- 用户批准：✅（2026-08-25 批准「补佐证 → 规划修复线」两步走；本任务是第二步的规划环节）

## 背景（定案已闭环——监督员定案裁决已落盘）

监督员定案（`.alice-supervision/research/f1f6-bot-physics-verdict-20260825.md`）：

**主根因 = 实体 tick 链（aiStep→pushEntities→move 碰撞段）的推挤/碰撞响应未被 bot 有效消费**：
1. 任务层链正常（`BotManager.onServerTick` 直接 `travel`，不经 `bot.tick()`）——F4/F5/F2 PASS
2. 实体链（碰撞/推挤承载段）失效——push 写入 delta=0.5 但 tick 后位移 0、碰撞 flags 恒 false（F3b/tickChain FAIL）
3. B2（时序覆盖）排除（F5 两时序均有位移）；B3（NPE 吞错）排除（stderr 0 字节 + 完整透传佐证）
4. 碰撞 flags 仅由 `Entity.move` 碰撞段设置（javap 335/381）——位移 0 → 无碰撞 → flags false，与"push delta 未被实体 move 消费"吻合
5. ⚠️ 未定案项（修复规划必须覆盖或标注）：
   - 精确注射点三候选（机制报告 ③:55-57）：a. 实体链内 push delta 在 move 前被清零（任务层 travel 与实体链 delta 处置差异）；b. `pushEntities` 内部"实体-实体"推挤条件未满足（Zombie 放置高度/碰撞箱不重叠）；c. 墙距 3 格外单 tick 位移不足（数值维度，需复测墙置相邻格）
   - F1 hurt false 单独路径：是伴随还是第二根因——**修复后复测区分**（标记 ⚠️）

## 必须产出（报告写到 .alice-supervision/research/physics-fix-plan-20260825.md）

### 1. 修复方案设计对比（核心，至少 3 候选详列）

针对定案主根因（实体链推挤/碰撞响应未消费），给出**至少 3 个修复设计方向**，每个包含：
- 方案概述（一句话 + 改动文件/位置）
- 注射点（具体到类/方法/行号候选）
- 机制原理（如何让 pushEntities 的推挤/碰撞在 bot 上生效）
- 覆盖/排除的未定案项（三候选 a/b/c 各被哪些方案覆盖；F1 hurt false 复测如何安排）
- 风险（对任务层 travel 链正常行为的回归风险——F4/F5/F2 不得回归）
- 改动量级（文件数/行数/工程日估计）

三候选方向（供展开，不限定）：
- **方案 A（实体链驱动修复）**：确保 `bot.tick()` 实体链完整驱动且不被任务层绕过（如 BotManager 补 bot.tick() 调用或修正任务层与实体链的驱动关系）
- **方案 B（实体链 delta 保真修复）**：修复 pushEntities/move 段对 push delta 的消费（如修正清零路径、确保 push delta 进入 move）
- **方案 C（任务层物理等价补链）**：任务层 travel 之外补"推挤/碰撞等价消费"（如任务层显式调用 pushEntities/move 等价段）

### 2. 推荐方案与理由（对比后给出推荐，但不代替用户批准）

- 推荐 1 个方案（或组合），理由：覆盖未定案项最多/回归风险最小/改动最小/与现有架构（任务层 travel 主导）最一致
- 明确"为什么不用其他方案"

### 3. 回归与验证矩阵

- 修复后必须复测：F1-F6 全绿（服务端）+ 客户端 M2/M3（Windows 实测，用户执行）
- 回归保护：任务层 travel 链（F4/F5/F2 不得回归）、矿链 HARD_PATH（R30 不触碰）、SOFT 实验线边界（R33/R35/R36）
- 与跳跃异常线区分（修复不触碰 FakeConnection 广播）

### 4. 实施授权边界

- 规划不构成实施授权；实施方案需监督员审核 + 用户批准后写入 active plan
- 实施范围：只改定案主根因相关实现（BotPlayer/BotManager/travel 相关），具体由方案确定
- 修复后复测项（F1 hurt 第二根因区分）必须包含在实施验收标准中

## 禁止事项

- 不改任何文件（业务代码/测试/skill/HANDOVER/active plan）
- 不评估具体代码对错（那是修复实施阶段）
- **不预定最终选型**（只给对比与推荐，选型由监督员审核 + 用户批准）
- **不实现修复**（规划不是实施）
- 不触碰跳跃异常线/tunnel/transfer/road 等其他线
- 不触碰 P0 门禁（SoftPhysicsObservationTest/skills-manifest/state-machine）

## 验收标准

- 报告写到 .alice-supervision/research/physics-fix-plan-20260825.md
- 至少 3 个修复方案详列（概述/注射点/原理/覆盖项/风险/量级）
- 推荐方案 + 理由（覆盖最多/风险最小/改动最小）
- 回归验证矩阵完整（F1-F6 全绿 + 客户端 M2/M3 + travel 链不回归 + 矿链/实验线边界）
- F1 hurt false 复测安排明确
- 未改任何文件；不预定最终选型；不实现修复