你是 Alice 项目的深度调查员。不要修改 Alice 业务代码，不要修改 `.alice-supervision/active-plan.md`，不要提交功能代码。

调研主题：original-chest-bot-transfer
派发原因：跨层高风险行为 + Forge 1.20.1 原版容器/ServerPlayer 写入与持久恢复语义是 A1 C2 实现关键前置。
任务来源：用户已选真实 bot 背包运输；`.alice-supervision/research/inventory-transfer-a1-user-policy-20260821.md`；A0 review `20260821-inventory-transfer-transaction-a0-review.md`。

目标：在固定 Minecraft/Forge `1.20.1-47.4.10` 下，确定原版 chest <-> `ServerPlayer`/BotPlayer 背包的最小、可验证访问和写入模型，特别是容器能力/菜单交互、背包容量预检、原子批次、写入后同步、重启/中断时在途物品事实，以及是否可由本地固定源码证明；不能实现或试运行写入。

Alice 当前边界与用户决策：
- C1 S1-S4 已 USER_ACCEPTED，F1-F4 已 PASS/COMPLETED；C1 raw facts 不构成 C2 endpoint/write/side 权限。
- 用户选择真实 bot 背包搬运；首个端点只限原版箱子；首个方向是端到端 source chest -> bot inventory -> destination chest；容量不足在抽取前失败；生存中断/重启时在途事务挂起并人工接管。
- 精确物品 ID 和数量未来将是可选参数；未填写的“全量”不得被假设为单次原子事务。调查必须评估把全量拆成有界原子批次的必要性。
- 不实现物品标记区、权限系统、模组容器、液体、端点直转、LLM 自动恢复、AttackTask、路径/软移动改动。
- 现有 `SurvivalSystem` 只发硬中断，不含自动恢复；现有 BotWorldData 只保存主手，未证明完整背包/事务恢复。

优先来源：
1. 本地 Alice 源码与当前 Git `3644429d2d88e1bb6fd1eb24654d828d1451d7f8`。
2. 本地固定 Forge 1.20.1-47.4.10 source/universal/userdev jars，以及固定 Mojang/Parchment 源码。如用外部源码，必须固定 Forge/Minecraft tag/commit。
3. 原版容器/ServerPlayer 库存的官方映射源码或稳定 Forge 源码；外部项目只能补充机制，不能覆盖本地事实。

必须回答：
1. 原版 chest 和 ServerPlayer inventory 在固定版本中有哪些可用、可审计的访问/写入路径？哪些路径会触发原版菜单/事件/同步，哪些只改数据？首个实验应选哪个路径，为什么？
2. 如何在实际抽取前证明 bot 背包可容纳精确 item/count，且“容量不足不允许部分抽取”？需要检查哪些 slots、stack/component 比较和 max stack rules？
3. `simulate=true` 和 actual write 的竞态如何检测？最小 pre/simulate/actual/post 证据和源/背包/目标 delta 是什么？是否存在真正原子跨端点操作？
4. `source -> bot -> destination` 端到端请求应如何拆为原子批次；未填写 item/count 的“全量”为什么必须在首版拆批而非单笔事务？
5. 生存中断、玩家/bot disconnect、server restart、任务替换、external chest mutation、destination full、重复命令时，每种情况下可否证明 in_transit item location？现有 BotWorldData 保存范围是否足够？若不够，A1.1 最小保守策略是什么？
6. Shift 右键绑定 source、右键绑定 destination、未绑定回落 bot inventory 的客户端/服务端事件隔离应如何设计为独立测试工具，且不劫持原版箱子 GUI？此问题只给设计建议和客户端矩阵，不实现。
7. 建议的最小 A1.1 与后续 A1.2 的文件/模块边界、停止条件、headless fixtures、Windows 客户端矩阵，以及哪些结论必须由用户再确认。

证据规则：
- 每项事实记录来源路径/URL、版本/commit、类/方法和行范围；明确 A/B/C 证据等级。
- 区分已确认事实、架构推论、不可迁移风险、未确认内容。
- 不把 `isItemValid` 当容量证明，不把 simulate 当 reservation，不把 C1 slot index 当角色/side 语义，不把编译/headless 当客户端接受。
- 说明哪些结论仍需 Windows 客户端验收。

禁止事项：
- 不调用或试运行 `insertItem`/`extractItem`、Container/Inventory 写入、菜单 click；不改代码、资源、active plan、HANDOVER、审核/客户端记录。
- 不扩大到模组容器、Mek/AE、流体、side traversal、端点直转、权限实现、存储标记区、LLM 恢复、AttackTask、`HARD_PATH`/`SOFT_SURFACE`、挖矿/拾取/道路/隧道。

输出：`.alice-supervision/research/original-chest-bot-transfer-research-report-20260821.md`

调研止损：
- 找到足以确定保守 A1.1 路径、必须用户决策或本地证据不足需要保守降级时停止。
- 不泛搜自动化模组；不为全量、多容器、模组/液体兼容寻找“通用方案”。
- 报告本身不构成实现授权，只有监督员可采纳结论并提出新的用户批准计划。
