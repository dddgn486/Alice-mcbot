# 实施任务：F1-F6 服务端物理断言（bot 物理失效定案前置）

- 派发：Alice 架构监督员
- 执行：Alice 主开发员（session-30b693e7）
- 日期：2026-08-25
- Alice 基线：`6c2b461`（HEAD）
- 用户批准：✅（2026-08-25 批准 active plan `20260825-f1f6-bot-physics-assertions-v1`）
- 依据：`.alice-supervision/active-plan.md`（APPROVED）+ `.alice-supervision/research/f1-f6-implementation-plan-20260825.md`（规划，已验收）

## 范围（严格遵守 active plan Allowed Scope）

**只加服务端断言/测试代码，不改任何业务实现；不预定根因结论；不预定修复方案。**

### 必须产出

1. **新建** `src/main/java/com/dddgn/alice/task/BotPhysicsAssertionFixture.java`（独立 fixture，仅服务端断言）：
   - **F1**：`bot.hurt(damageSource, 1.0f)` 后**同一 tick** 读 `getDeltaMovement()`——hurt/knockback 链是否触发（delta 非零 = 触发；hurt 返回 false = isPushable/hurt 链未触发）
   - **F3**：3a `bot.push(0.5,0,0)` 后读 delta+位移（push 写入路径）；3b 在 bot 碰撞箱内放置另一实体（原版 Zombie 等，headless 可构造）观察推挤链（aiStep/pushEntities 运行佐证）
   - **F4**：travel 前后 delta 摩擦衰减（约 0.91 缩放 = travel/aiStep 链完整执行；delta 原样 = 衰减未执行 B3 候选；delta 归零 = 异常清零路径）+ 伴随 tick 链完整性断言（horizontalCollision/verticalCollisionBelow 变化）
   - **F5**：同 tick（hurt 后立刻 travel）vs 延迟 1 tick（hurt 后等 1 tick 再 travel）时序对照
   - **F2**：F5 延迟对照基础上，断言「击退后延迟 travel」的位移**包含击退分量**
   - fixture 内**自清理**：hurt 后恢复 health（setHealth 基线值）、push 后 teleportTo 归位、**不写档**（不触碰 BotWorldData/saveToWorld）
2. **修改** `src/main/java/com/dddgn/alice/bot/BotSelftest.java`：`setup()` 中 `bot = BotManager.spawn(...)` 之后、`softPhysicsPass = SoftPhysicsObservationTest.run(level)`（:352）之前或紧邻，加**一行调用** `boolean botPhysicsAssertionPass = BotPhysicsAssertionFixture.runF1F6(level, bot);` + 日志 `BOT_PHYSICS_ASSERTION_SUITE PASS|FAIL f1= f3= f4= f5= f2= tickChain=`（每 F 项独立布尔）
3. 日志字段按 evidence-collection-standard：`corr=<fixture-run-uuid>` / `tick` / `delta_x/delta_y/delta_z`（三位小数）/ `health` / `horizontalCollision` / `verticalCollisionBelow`

### 执行方式

- 服务端 headless：`-Dalice.selftest.auto=true` 启动服务器一次全覆（或 `/alice selftest` full 模式）
- 主挂钩点：`SoftPathProbeTask` 的 travel 调用（SoftPathProbeTask.java:141-144）——fixture 中创建任务驱动，在其 travel 前后读 delta（覆盖 F2/F4/F5）；其余 4 个 travel 调用点（SoftPathMineTask:167-170 / FollowTask:106-107 / SoftMoveProbeTask:119-120 / settle:82-86）为**可选复验**（第二轮），首次实施只做主挂钩点

## 禁止事项（硬性，违反即失败）

- **不改业务实现**：BotPlayer / FakeConnection / SoftMovementPrimitive / travel 驱动 / 5 个 task 语义 零改动
- **不改 P0 门禁**：SoftPhysicsObservationTest 现有断言零改动（新 fixture 独立文件）；skills-manifest / state-machine 零改动
- **不预定根因结论**：实施只收集证据（F 项结果 + 日志），**根因判定由监督员按区分表执行**——你不判定、不写"根因是 X"
- **不预定修复方案**：不实现任何修复（不碰 hurt 链 / tick 链 / FakeConnection 同步）
- 不触碰玩家跳跃异常线（独立）；不触碰 tunnel/transfer/road 等其他线
- 不得以"编译通过/服务端 PASS"自称"客户端验证通过"

## 验证与产出

- `./gradlew compileJava` 通过
- headless 运行产出 `BOT_PHYSICS_ASSERTION_SUITE` 日志（含每 F 项结果 + corr + 字段）
- 证据报告写到 `.alice-supervision/research/f1-f6-assertion-result-20260825.md`：
  - 各 F 项实际结果（PASS/FAIL + 具体数值/日志行号）
  - 佐证日志（latest.log 行号范围）
  - 实施后 git status 确认（仅新增 1 文件 + 1 处修改）
  - **不写根因结论**（监督员按区分表定案）
- 完成后 report_task 附报告路径

## 验收标准（规划 §5.2 + active plan）

- BotPhysicsAssertionFixture 覆盖 F1/F3/F4/F5/F2 全部断言 + 自清理（health 恢复/push 归位/不写档）
- BotSelftest.setup() 一行调用 + `BOT_PHYSICS_ASSERTION_SUITE` 日志存在
- headless 运行日志含每 F 项结果 + corr + delta/health/collision 字段
- 证据报告落盘（各 F 项数值 + latest.log 行号 + git status 确认）
- **未修改任何业务实现**（git diff 仅新增 fixture + BotSelftest 一行）；未判定根因；未实现修复