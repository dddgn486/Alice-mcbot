# 审查执行任务：批 1 / pathing 模块（HARD_PATH/SOFT_SURFACE 边界）

- 派发：Alice 架构监督员
- 执行：Alice 深度调查员（session-f26bd205）
- 日期：2026-08-25
- Alice 基线：`6c2b461`（HEAD，审查基线）
- 依据规划：`.alice-supervision/research/full-review-plan-20260825.md`（已验收）§1.2 批 1、§2 审查模板、§5 执行/验收/停止
- 前置证据：`.alice-supervision/research/review-batch1-bot-20260825.md`（bot 模块已验收——bot 物理失效根因候选 B2：任务层 travel 覆盖击退 delta，与你模块的 SoftMovementPrimitive/SoftPathProbeTask 直接相关）

## 审查目标（pathing 模块）

pathing 是 **HARD_PATH 与 SOFT_SURFACE 边界的核心线**（规划 §4.2 候选方向：寻路算法/软移动执行）。审查必须聚焦：

1. **HARD_PATH 边界**：`PathExecutor` 固定 HARD_PATH（R30：矿链不可切 SOFT）——当前代码是否仍遵守
2. **SOFT_SURFACE 隔离**：`SoftMovementPrimitive`/`SoftPathProbeTask`/`FollowTask` 独立实验线（R33/R35/R36）——是否未接入矿链/拾取
3. **SEARCH_LIMIT 语义**（R26）：预算耗尽 ≠ UNREACHABLE ≠ 通道授权
4. **与 bot 物理失效的关联**（前置 bot 审查 B2：`SoftMovementPrimitive.java:48-53` 每 tick travel 覆盖击退 delta——你模块是根因候选的直接持有者）

## 审查对象

`src/main/java/com/dddgn/alice/pathing/`（14 文件）：
- 核心：`PathExecutor.java`（HARD_PATH 执行）、`SurfacePathfinder.java`（曲面寻路）、`AStarPathfinder.java`（A* 核心）、`SoftMovementPrimitive.java`（软移动原语，B2 根因候选）、`MovementHelper.java`（辅助）
- 边界：`Goal.java`/`PathNode.java`/`OpenSet.java`（算法数据结构）、`MovementMode.java`/`MovementType.java`（模式枚举）
- 隧道：`TunnelPlan.java`/`TunnelPlanner.java`/`TunnelObstaclePolicy.java`（未来隧道线，未实现？）
- 回归：`PathingRegression.java`（回归套件）

## 审查方法（严格按规划 §2 模板，8 字段全覆）

1. **定位与文件清单**：每文件职责 + 3 字角色
2. **边界归属**：HARD_PATH（矿链固定）/ SOFT_SURFACE（软路径独立实验）/ 隧道（未来线）；对照 R26/R30/R31/R33/R35/R36
3. **正确性核查点**：宣称 vs 实现；证据类型（静态/服务端/客户端）
4. **R# 对照**：R26 SEARCH_LIMIT≠UNREACHABLE、R30 PathExecutor 固定 HARD_PATH、R31 软移动借成熟实现、R33 NATIVE_TRAVEL 验证边界、R35 落地支撑、R36 FollowTask 独立——每条的当前代码遵守性
5. **Dual-View**：服务端可证（A* 成本/寻路可达性）vs 需客户端实测（软移动物理可见性、实体交互）——引用 bot 审查 B2：**任务层 travel 覆盖击退 delta 需要服务端断言 fixture 验证**
6. **证据等级**：现有证据分级 + 缺口
7. **风险等级**：A/B/C/D 判定
8. **替换判据**：按 §4.1 M1-M5 填已知/缺证据（寻路：自研 A* vs 成熟库；软移动：自研 travel 注入 vs 成熟 input-override——**只列方向不选型**）

**特别核查项（必须回答）**：
- **B2 交叉验证**：`SoftMovementPrimitive.java:48-53`（xxa=0/zza=1/travel(0,0,1)）每 tick 重算 deltaMovement —— 是否/如何覆盖外部写入的击退/推挤 delta？给出代码行证据与建议的服务端断言方案（怎么 fixture 证明）
- PathExecutor 是否可被任何任务切换到非 HARD_PATH 模式（R30 遵守性）
- SEARCH_LIMIT 耗尽时的行为：是否仅"停止并报告"（R26 要求）还是存在通道授权路径
- TunnelPlanner/TunnelPlan：实现状态（未实现/部分/已实现）——若未接入矿链则标注"未来线待评估"

## 禁止事项（硬性）

- 不修改任何文件（业务代码/HANDOVER/active plan/skill/客户端记录）
- 疑似 bug 只记录不修；不预定替换结论（候选只列方向）
- 不与 p1-client-sync-fix-v1 冲突（若 SoftMovementPrimitive/travel 驱动与修复线交叉，只提交证据，修改权归修复主线）
- 工具不可用时：先列可用清单复测 read 报错（区分真缺失/瞬态），确认真缺失再 request_input

## 产出

报告写到：`.alice-supervision/research/review-batch1-pathing-20260825.md`
- 覆盖 §2 全部 8 字段
- 含 B2 交叉验证结论（bot 审查根因候选在你模块的代码事实）
- 明确区分已确认/推论/待调查

完成后 report_task 附报告路径。

## 验收标准（规划 §5.2）

- 报告覆盖 §2 全部字段
- B2（任务层 travel 覆盖击退 delta）有代码行证据 + 服务端断言方案
- R26/R30/R31/R33/R35/R36 遵守性逐条核查
- 隧道线实现状态明确
- 无文件被修改；无预定替换结论；区分已确认/推论/待调查