# Bot 死亡机制深度调研任务

## 调研目标

调查 Bot 死亡的根因、Minecraft/Forge 死亡机制、其他 mod 的成熟实现方案，设计 Alice Bot 的完整死亡与复活机制。

## 问题描述

**观察**（2026-08-23 00:21-00:35 客户端）：
- Bot 倒在地上呈侧倒红色死亡动画，但未被清除
- Tab 列表仍有 tango，名字标签可见
- `/kill tango` 显示"被杀死"但 Bot 无反应
- 用剑攻击 Bot 有粒子效果但无反应
- Alice 所有事件系统失效（`onServerTick`、`onLivingDeath`、`selftest` 全部卡住）
- 删除 `alice_bot.dat` 后恢复正常

**当前实现**（`BotManager.onLivingDeath`）：
```java
@SubscribeEvent
public static void onLivingDeath(LivingDeathEvent event) {
    if (event.getEntity().level().isClientSide) return;
    if (!(event.getEntity() instanceof BotPlayer bot)) return;
    String reason = event.getSource().getLocalizedDeathMessage(bot).getString();
    BotLog.info("假人死亡: {} → 直接清除", reason);
    Component msg = Component.literal("[alice] 假人 " + bot.getName().getString()
            + " 死亡: " + reason + " → 已清除");
    bot.getServer().getPlayerList().broadcastSystemMessage(msg, false);
    event.setCanceled(true); // 阻止原版死亡流程(掉落物/死亡动画),直接消失
    remove(bot);
}
```

**缺陷**：
1. `event.setCanceled(true)` 未能阻止死亡动画（Bot 仍倒地红色）
2. 死亡 Bot 被写入 `alice_bot.dat`，下次加载时恢复死亡状态导致系统失效
3. 用户期望：Bot 能像正常生物死亡（掉落物品、完整动画、可复活），而非硬清除

## 调研范围

**Allowed（必须联网）**：
1. **内部分析**：
   - 只读分析 `BotPlayer.java`、`BotManager.java`、`BotWorldData.java`
   - 搜索项目中所有与 death/die/remove 相关的代码
   - 查阅 `restoreFromWorld()` 恢复逻辑

2. **Minecraft/Forge 原版机制**（联网查阅）：
   - Minecraft 1.20.1 原版 `ServerPlayer.die()` 完整流程
   - `LivingDeathEvent` 的触发时机、优先级、cancel 语义
   - `event.setCanceled(true)` 为何未能阻止死亡动画
   - 原版玩家复活机制（`respawn()`、重生点、死亡屏幕）
   - Forge GitHub issues：fake player death、`LivingDeathEvent` cancel 问题

3. **其他 mod 的成熟实现**（联网查阅源码）：
   - **Baritone**：其 fake player 如何处理死亡（是否有死亡动画、是否复活）
   - **Carpet mod**：fake player 死亡与复活机制
   - **ComputerCraft**：turtle 死亡（机器人实体）如何处理
   - **Create mod**：机械臂等实体的"损坏"与修复机制
   - 其他知名 mod 的 fake player 实现模式

4. **技术决策点**：
   - Bot 死亡时是否应该掉落物品？（普通 inventory + 装备？）
   - Bot 死亡后是否应该有复活机制？（传送回家？原地重生？需要玩家手动复活？）
   - Bot 死亡动画如何处理？（完整播放？立即消失？）
   - Bot 死亡后任务如何处理？（中断？保留？自动恢复？）
   - 多人服场景：Bot 死亡是否广播给所有玩家？

**Forbidden**：
- 不修改任何代码
- 不运行测试或客户端
- 不扩展到其他 Bot 能力（Attack/Follow）

## 交付要求

报告路径：`.alice-supervision/research/bot-death-resurrection-mechanism-20260823.md`

报告必须包含：

### 1. 根因分析（代码路径 + 行号）
- 为何 `event.setCanceled(true)` 未能阻止死亡动画？
- 为何死亡 Bot 加载后导致系统失效？（`restoreFromWorld()` 逻辑缺陷？Bot tick 抛异常？）
- `BotManager.onLivingDeath` 为何未触发？（事件优先级？时机问题？）
- `remove(bot)` 为何未执行或执行失败？

### 2. Minecraft/Forge 原版死亡机制（官方文档 + 源码）
- `ServerPlayer.die()` 完整流程（伪代码或关键步骤）
- `LivingDeathEvent` 触发时机与 cancel 语义
- 原版复活机制（`respawn()` 方法、重生点逻辑）
- Forge 1.20.1 的 fake player 死亡已知问题（GitHub issues）

### 3. 外部 mod 参考实现（URL + 关键代码片段）
- Baritone fake player 死亡处理（GitHub URL + 具体文件）
- Carpet mod fake player 复活机制（GitHub URL + 具体文件）
- ComputerCraft turtle 死亡/损坏机制（如适用）
- Create mod 机械实体"损坏"处理（如适用）
- 总结外部 mod 的通用模式（cancel event？自定义复活？强制 remove？）

### 4. 修复方案（3-4 个备选）
- **方案 A（最小修复）**：加载时清除死亡 Bot
  - 在 `restoreFromWorld()` 检查 `bot.isAlive()` 或 `bot.getHealth() <= 0`
  - 如果死亡，调用 `remove(bot)` 不加载到游戏
  - 优点/缺点/风险
  
- **方案 B（阻止死亡）**：强化事件处理
  - 提升 `onLivingDeath` 事件优先级（`EventPriority.HIGHEST`）
  - 立即恢复满血 `bot.setHealth(bot.getMaxHealth())`
  - 取消事件并强制 `remove(bot)` 或传送到安全位置
  - 优点/缺点/风险
  
- **方案 C（自然死亡 + 复活）**：实现完整死亡与复活机制
  - 允许 Bot 自然死亡（掉落物品、播放动画）
  - 3 秒后自动复活（传送回家/重生点）
  - 任务中断并标记 `FAILED: bot_died`
  - 优点/缺点/风险（实现复杂度、任务恢复策略）
  
- **方案 D（参考外部 mod）**：采用 Baritone/Carpet 的成熟模式
  - 具体描述其实现方式
  - 适配 Alice 的改动点
  - 优点/缺点/风险

### 5. 推荐方案与实施路线
- 推荐优先级：方案 X（短期） + 方案 Y（长期）
- 实施步骤（最小可行 → 完整功能）
- 每步的验证矩阵（服务端测试 + 客户端实测）
- 停止条件

### 6. 风险评估
- 修复后是否影响其他模块（mining/transfer）？
- Bot 复活后任务如何恢复？（需要新的任务状态？）
- 多人服场景：Bot 死亡/复活的权限控制
- 性能影响（复活逻辑的 tick 开销）

## 证据标准

- 代码路径引用格式：`文件名:行号`
- Minecraft/Forge API 引用需注明版本（Forge 1.20.1-47.4.10）
- 外部 mod 引用需提供 GitHub URL + 具体文件路径
- 修复方案需给出伪代码或精确方法调用
- 不得猜测，所有结论必须基于代码证据、官方文档或外部成熟实现

## 项目上下文

- 项目路径：`/home/fb486/projects/alice`
- 当前 HEAD：`a8b84b4`（fix: sync bot main hand equipment for entity rendering）
- 相关模块：`src/main/java/com/dddgn/alice/bot/`
- 问题复现：Bot 死亡后存档，下次加载时系统失效
- 用户期望：Bot 像正常生物死亡（掉落物品、完整动画、可复活）

## 外部搜索建议

**必须联网搜索**：
1. `"Forge 1.20.1" + "LivingDeathEvent" + "setCanceled" + "not working"`
2. `"Baritone" + "fake player" + "death" + "respawn"`
3. `"Carpet mod" + "fake player" + "death" site:github.com`
4. `"Minecraft ServerPlayer" + "die" + "respawn" + "mechanism"`
5. `"Forge fake player" + "death animation" + "cancel event"`

**推荐查阅的 GitHub 仓库**：
- Baritone: `cabaletta/baritone`
- Carpet: `gnembon/fabric-carpet`（注意：Fabric 而非 Forge，架构可能不同）
- ComputerCraft: `cc-tweaked/CC-Tweaked`
- Create: `Creators-of-Create/Create`
