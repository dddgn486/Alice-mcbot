# Alice Supervisor Review

- Review ID: `20260821-8bb2d7b`
- Reviewed commit: `8bb2d7b1641c0b6e6063d4c78ae0357896fd0c90`
- Plan ID: `20260821-interface-readonly-snapshot-v1`
- Verdict: CONDITIONAL_PASS
- Stage status: CLIENT_TEST_PENDING

## Findings

### F1 - Medium - Required immutability/read-only assertions are not executed

Plan verification required proof that formatting does not mutate the world/container and that a captured snapshot remains unchanged after later container mutation. `runInterfaceSnapshotRegression` only captures a chest and dirt, then checks status, slot count and the initial diamond count. It never calls `InterfaceScanner.format`, mutates the chest after capture, or tries to modify the snapshot collections.

Evidence: `src/main/java/com/dddgn/alice/bot/BotSelftest.java:349-371`; active-plan verification requirements.

Impact: the implementation is structurally immutable by code review (`record`, copied strings, `List.copyOf`, copied stack/fluid/tag), but the recorded `INTERFACE_SNAPSHOT_SELFTEST PASS` proves less than the approved matrix. Do not claim runtime proof of formatter purity or post-capture immutability.

Required before the next functional package: add focused assertions for formatter non-mutation and snapshot stability after source-container mutation; failure must be visible as `INTERFACE_SNAPSHOT_SELFTEST FAIL`.

### F2 - Medium - `CHUNK_NOT_LOADED` is checked after world-state access

`InterfaceScanner.capture` calls `level.getBlockState(pos)` before `level.hasChunkAt(pos)`. The approved contract requires an unloaded position to be distinguished without relying on access that may synchronously resolve/load the chunk or return a fallback state.

Evidence: `src/main/java/com/dddgn/alice/capability/InterfaceScanner.java:41-47`.

Impact: `CHUNK_NOT_LOADED` exists in the schema but its conservative no-load semantics are not demonstrated. This does not affect the known loaded chest/dirt scan, but it matters before C1 snapshots become a shared planning fact source.

Required before the next functional package: check `hasChunkAt` before reading block state/block entity and use an explicit unknown/unavailable block id for the unloaded result; add focused coverage if practical without loading the target.

### F3 - Low - Handover primary facts remain stale

The implementation updated verification paragraphs but left the top current-HEAD block at `ae32c4a` and the command/tool list at `钻石铲右键`. Actual HEAD/origin is `8bb2d7b`, and scanning moved to `alice:interface_scanner`.

Evidence: `docs/HANDOVER.md:4-17`, `docs/HANDOVER.md:216-239`; Git HEAD/origin check.

Impact: new sessions can read the wrong revision and test the wrong item despite the later pending-client paragraph being accurate.

Required before the next functional package: update the top revision facts and tool list; preserve the explicit CLIENT_TEST_PENDING statement.

### F4 - Low - Source comments and dead legacy generic scanners still describe heuristic slot semantics

The generic formatter correctly emits slot indices without roles, and the snapshot contains no slot hints. However, the class Javadoc and now-unused `scanItems/scanEnergy/scanFluid/slotHint` methods still describe and implement the old heuristic path.

Evidence: `src/main/java/com/dddgn/alice/capability/InterfaceScanner.java:18-29`, `155-192`, `317-337`.

Impact: no runtime architecture violation in this commit, but later maintainers may mistakenly revive heuristic input/output facts.

Required before the next functional package: remove the dead generic scan methods and update Javadoc to state C1 raw facts; retain Mek legacy projection as explicitly non-generic.

## Scope and Architecture Review

- PASS: changes are limited to the approved capability records/scanner, focused selftest, Alice item registration, event hook and resources.
- PASS: no insertion, extraction, transfer, side-specific endpoint semantics, persistence, network/JSON, LLM tool or machine write was introduced.
- PASS: snapshot copies item/fluid/tag values and stores immutable collections; no `LazyOptional`, capability handler, BlockEntity, ServerLevel, ItemStack or FluidStack reference is retained.
- PASS: generic snapshot records slot/tank indices and raw contents without input/output role inference.
- PASS: Mek-specific projection is labeled `legacy ... 非 C1 通用事实` and remains outside the structured generic fields.
- PASS: `ScanWand` only matches `AliceItems.INTERFACE_SCANNER`; source inspection shows no remaining vanilla `Items.DIAMOND_SHOVEL` hook.
- PASS: no task, movement, HARD_PATH/SOFT_SURFACE, mining, collection, road, FollowTask or survival file changed.

## Verification Review

- Supervisor reran `./gradlew compileJava`: BUILD SUCCESSFUL.
- Supervisor ran resource/JAR packaging: `interface_scanner.json`, `en_us.json`, and `zh_cn.json` are present in the built JAR.
- `run/logs/latest.log` contains `INTERFACE_SNAPSHOT_SELFTEST PASS chest=true status=OK slots=27 first=minecraft:diamondx3 dirt=true status=NO_BLOCK_ENTITY`.
- Energy/fluid paths have compile/code review only; no runtime fixture evidence, accurately recorded by HANDOVER.
- Existing full smoke ended under external timeout/exit 143; it is not treated as a complete regression PASS.
- Registry remap warning for `alice:interface_scanner` is consistent with the reused development world assigning a new registry ID; no missing-model/translation warning was found. Client appearance still requires user evidence.

## Verdict

`CONDITIONAL_PASS`: retain `8bb2d7b` and proceed only to the scanner-item Windows test matrix. The C1 snapshot is accepted as `HEADLESS_PARTIALLY_VERIFIED`, not as full verification of every planned invariant. No C2/C3/C4 capability, transfer work, endpoint semantics or next functional package is authorized.

After user client evidence, perform a narrow maintenance correction for F1-F4 under supervisor authorization before opening the next product package.
