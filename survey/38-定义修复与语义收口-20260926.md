# 38 · 定义修复与语义收口（**交付主工作流马上做**）（2026-09-26）

> **基线 commit**：`a5b0c88`（= `survey/37` 的补遗 2）。⚠️ 读前先 `git log -1` 对一下。
> `docs/AI_DECISIONS.md` 最后一条 = **`D-447`** ⇒ 本报告建议的新条目为 **`D-448`**。
>
> ## ⚠️ 性质声明（**与 `survey/35`/`36`/`37` 不同**）
>
> **本文不是讨论记录，是待执行的定义修复清单** —— 用户 2026-09-26 原话：
> 「你说的**定义修复和语义收口可以写成报告，让主工作流马上做**」
> ⇒ 本文**给出具体改法、位置、判据**，供主工作流**直接落地**。
> ⚠️ **勘测侧不自己改**（`docs/` 一行不碰，勘测侧只增不改 `survey/`）；文中"建议文本"是**提案**，不是已生效的决策。
>
> **事实分级**：✅ 已核实（当场读代码/文档/外部源码，给 `file:line`）· 📋 用户原话（逐字）·
> ⚠️ 推断（未实测）· ⏳ 待裁。

---

## §0 结论：要主工作流做的**三件事**

| # | 事项 | 落点 | 成本 |
|---|---|---|---|
| **一** | ⭐ **`TaskZoneRegistry:24` 的措辞修掉**（它与 `D-338` ①「任务区是子类」直接冲突） | `src/main/java/com/dddgn/alice/protection/TaskZoneRegistry.java:24` | 一行注释 |
| **二** | ⭐⭐ **落一条 `D-448`：锚点任务的定义与定位**（"简易档 / 备用方案"，**不是第二套实现**） | `docs/AI_DECISIONS.md` | 0.1 轮（纯文档） |
| **三** | ⭐ **`survey/26 §2.3` 的一条结论被实测推翻**（「固定锚点 ❌ 没有」）⇒ 登记更正 | `docs/AI_DECISIONS.md`（随 `D-448` 附注即可） | 随二 |

⚠️ **三件的性质不同**：**一**是**代码注释**与决策原文的**直接冲突**（属缺陷级，因为它会误导后续读者）·
**二**是**缺失的定义**（锚点任务全仓无定义）· **三**是**勘测侧的旧错误**（`survey/26` 是 2026-09-21 的勘查，结论有误）。

---

## §1 【第一件】定义修复：`TaskZoneRegistry:24` 的措辞冲突

### 1.1 冲突原文（✅ 双方均为逐字）

| 出处 | 原文 |
|---|---|
| `src/main/java/com/dddgn/alice/protection/TaskZoneRegistry.java:24` | 「**任务区**（`D-338` ⑦ + 附注二 + 附注四）—— 保护区**父类**之外的**另一种**区块级授权封套。」 |
| `docs/AI_DECISIONS.md:13340`（`D-338` 标题） | 「保护区是**具体父类** + 子类（**安全区 / 任务区**）+ 账本与恢复口径 + 返程目标优先级」 |
| `docs/AI_DECISIONS.md:13344`（`D-338` ①） | 「**子类区域继承 ⒜⒝**（账本 + 权限要求）。已知子类：— **安全区**…— **任务区**…」 |
| 📋 `L6`（用户 2026-09-26 逐字） | 「**任务区还是保护区的子区域啊**，只是说**保护区是父类，任务区继承保护区的部分定义和功能**」 |

### 1.2 ⚠️ 为什么这条必须修（不是措辞洁癖）

- **`L6` 的纠正对象就是这句话**：勘测侧正是因为读到 `:24` 的「父类**之外**」，才在 `survey/37 §1.3.4` 里
  连着错两次（E1→E2→E3）。
- ⇒ ⭐ **这条注释会持续误导下一个读者**（勘测侧就是活例）。
- ✅ **可两全**：`survey/37 §1.3.4` 末已给出读法 —— **`:24` 说的是「载体」（独立注册表），不是「类型层次」**。
  ⇒ 修法不是删掉"之外"，是**把两件事分开写**。

### 1.3 ⭐ 建议改法（提案，供主工作流采用/改写）

> **原文（`:24`）**：「任务区（`D-338` ⑦ + 附注二 + 附注四）—— 保护区**父类**之外的**另一种**区块级授权封套。」
>
> **建议改为**：
> 「任务区（`D-338` ⑦ + 附注二 + 附注四）—— ⚠️ **它是保护区的子类**（`D-338` ①：子类继承账本 + 权限要求），
> **不是"父类之外的另一种东西"**。本类之所以独立，是因为**载体**不同：任务区由**任务自己声明**
> （随 `scopeId` 生灭、零新存储），而保护区父类实例存在 `SafeZoneData`（认领集）。
> **类型层次见 `D-338` ①，载体见本类。**」

- ⚠️ 若主工作流认为不必这么长，**至少要把"父类之外"这个易误读的表述去掉**。
- ⚠️ **建议同时检查 `:42-43`**（「任务区**可以覆盖保护区父类**」）—— 该句**语义正确**（讲覆盖规则），
  但连着 `:24` 读会强化"它是另一种东西"的错觉 ⇒ 可加一句"（因为它是子类，覆盖父类实例是正常情形）"。

---

## §2 【第二件】语义收口：落一条 `D-448`

### 2.1 为什么需要（✅ 实查）

- ⭐ **"锚点任务"全仓无定义**：`grep` 只命中 `survey/26`（2026-09-21 的勘查）与 `survey/29/37`;
  `docs/` 里**没有**它的定义条目。
- ⭐ **"备用方案 / 简易模式"这个提法全仓 0 命中**（`survey/37 §1.4b` 已记）⇒ **是用户新定的口径**。
- ⇒ **不落成 `D-338` 那样的条目，它下次还会被理解为"另一块地"**（勘测侧已经这样理解过一轮）。

### 2.2 📋 用户三次澄清（**逐字**，`D-448` 的依据）

| 轮 | 原话 |
|---|---|
| `L4` | 「锚点任务**还没做过**，锚点任务是一种**类**区域任务，但不同的是，**区域任务必须划任务区（保护区的子区域）按区块划分**，要做保护区权限管理，而**锚点任务一般在保护区外，区域由锚点和任务范围决定**，**权限管理还没讨论**」 |
| `L5` | 「**不是保护区也可以建立任务区**，而**锚点任务跟区块无关，只由锚点和任务决定的半径决定**」 |
| `L7` | 「**锚点任务，的意义是，临时部署或者快速部署，不需要圈地，随时可以设置锚点开始任务**，你可以理解成**车万女仆的那种工作模式**，只是有 **home 模式**和**跟随模式**，也就是**固定锚点或者跟随玩家**」 |
| `L8` | 「这个锚点任务**不是用来补区域任务缺口的**，因为他**已经被其他项目做过**，他的**身份应该是一种简易区域模式，更简单**，或者说**对区域任务模式做一个备用方案**」 |

### 2.3 ⭐⭐ 语义收口的核心（**这条是本报告最要紧的一句**）

> ⚠️ **"备用方案"容易被读成"第二套实现"**（两套代码、两套判据、同步维护）。
> ⭐ **正确读法 = "备用"指「部署方式的简繁」，不是「两套实现」** ——
> 依据 ✅ `survey/26 §4.1` 逐字：**「锚点 = 同一套任务的取值，不是两个任务」**
> （该表还给了代价对照：「写成两个任务 = 两份代码、两套判据、同步维护」vs
> 「**一套任务 + 锚点参数 = 一套判据；换锚点 = 换参数**」）。

⇒ **建议的口径（提案）**：

```
一套任务（不定工作区 + 范围内自动搜索目标）
  ├─ 部署档 A（完整）= 区域任务：划任务区（保护区子类，按区块）→ 权限/账本/恢复全套
  └─ 部署档 B（简易）= 锚点任务：设锚点（锚点 + 任务决定的半径）→ 干一段 → 走人
       ⚠️ 差别只在"范围从哪来"与随之而来的开销；**不是两套实现**
```

### 2.4 `D-448` 建议条目文本（**提案，供主工作流采用/改写**）

> ### `D-448`：**锚点任务的定义与定位** —— 区域任务的**简易档**（部署方式），**不是第二套实现**
>
> **依据**：用户 2026-09-26 四次澄清（`L4`/`L5`/`L7`/`L8`，勘测侧记录 = `survey/37 §1.3`/`§1.4`/`§1.4b`）
> + `survey/26`（2026-09-21 车万女仆勘查）。
>
> **一、定义**
> - **锚点任务** = 一种**类区域任务**：**范围 = 锚点 + 任务决定的半径**（⭐ **与区块无关**）；
> - ⭐ **价值 = 部署成本低**：**临时/快速部署，不需要圈地，随时可设锚点开工**；
> - **现状 = 还没做过**（`L4` 逐字）。
>
> **二、与区域任务的关系（⭐ 语义收口）**
> - 两者是**同一套任务的两个"部署档"**，**不是两条产品线**，**更不是两套实现**；
>   依据 `survey/26 §4.1`：「**锚点 = 同一套任务的取值，不是两个任务**」；
> - **完整档 = 区域任务**：划**任务区**（保护区**子类**，`D-338` ①）按区块 → 权限 · 账本 · 恢复 · 目标内/外全套；
> - **简易档 = 锚点任务**：只设锚点与半径，**原则上不声明区域** ⇒ 权限/账本开销归零（区外本就 `D-398` R4 无限制修改）；
> - ⭐ **共用的部分**（范围内目标识别 + 判据）**才是"一套任务 + 锚点取值"真正能省的地方**。
>
> **三、明确不做**
> - ⛔ **不做成第二套实现**（否则 = "两份代码、两套判据、同步维护"，`survey/26 §4.1` 已列为反面）；
> - ⛔ **不是为了补区域任务的缺口**（`L8` 逐字）—— 区域任务**已有资产**（`REGION_LUMBER`/`LumberRegionState`/`TaskZoneRegistry`）。
>
> **四、⏳ 未定（本条目不裁）**
> - ⚠️ **锚点落在保护区内 / 跨保护区域边界**时怎么办（`L4` 说"权限管理还没讨论"的边界情形）；
> - ⚠️ **"半径"这种范围怎么接进"区块级派生 + 忽略 Y"的现有授权机制**
>   （`TaskZoneRegistry:74`「Y 不参与派生」；`survey/34 §6` 已核 `ZoneAuthority` 无 Y）；
> - ⚠️ 是否需要 `ZoneAuthority` 之外的**半径判据**（`D-356` 的 `MineIntent` 是方形作业区，
>   而锚点要半径 —— ⚠️ **形状不同，未验证能否复用**）。
>
> **五、既有警告一并承继（`survey/26 §5`）**
> - ⚠️ **无区域边界 ⇒ 归位点从"安全网"升为"运行时必需件"**（锚点任务正因为不圈地，才更需要"能回来"）；
> - ⚠️ **`D-333` 守卫线冻结**：无区域时按什么冻结？（`survey/26 §5` 已登记"待定"）。

---

## §3 【第三件】`survey/26` 的一条结论被实测推翻（含一条**新发现**）

### 3.1 ⚠️ 被推翻的结论

- ✅ `survey/26 §2.3` 结论表逐字：
  | | 车万女仆 |
  |---|---|
  | **锚点** | **玩家**（`maid.getOwner()`） |
  | **半径** | **10 格** |
  | **固定锚点** | ❌ **没有**（它的前提是主人一定在场） |
- ✅ 并说：「**所以作者设想的"两种锚点"中，第二种是现成且验证过的，第一种（固定锚点）是车万女仆没有的原创补充。**」
- 📋 用户 2026-09-26 纠正：「**车万女仆确实有 home 模式**，可能你没查到」
- ⛔ ✅ **用户是对的**（见 `§3.2`）⇒ **`survey/26 §2.3` 的"固定锚点 ❌ 没有"是错的**。

### 3.2 ⭐ 本轮实测（✅ 从 `TartaricAcid/TouhouLittleMaid` 分支 `1.20` 拉到源码）

⚠️ `survey/26` 查的是 `api/task/IMaidTask.java`（**任务**抽象）；本轮查的是**新出现的
`ai/agent/tool/implement/`**（**LLM 工具层**）—— 这是两份报告口径不同的原因。

| 工具类 | `TOOL_ID` | 作用（✅ 源码逐字） |
|---|---|---|
| `SwitchSitTool.java` | `switch_sit` | 「Use this when the user wants the maid to **sit or stand**. Set `sit=true` to sit. Set `sit=false` to stand. ⚠️ **Do not use this to control follow mode.**」 |
| `SwitchFollowStateTool.java` | `switch_follow_state` | 「Use this when the user wants to **start you following or stop here**. Set `follow=true` to following the user, set `follow=false` to stop following. ⚠️ **Do not use this to make the maid sit down.**」 |
| `SwitchScheduleTool.java` | `switch_schedule` | 「Use this when user wants to change the **schedule**.」参数 = `MaidSchedule` 枚举：**`DAY`**（06:00–18:00 Work / 18:00–22:00 Leisure / 22:00–06:00 Rest）· **`NIGHT`**（18:00–06:00 Work / 06:00–14:00 Rest / 14:00–18:00 Leisure）· **`ALL`**（00:00–24:00 Work） |
| `SwitchWorkTaskTool.java` | （另） | 切换**干什么活** |

- ⭐ **两工具的描述互相点名**（"别用这个控制 follow mode" / "别用这个让女仆坐下"）
  ⇒ **`sit` 与 `follow` 是两个互斥的独立状态** —— **正是用户说的 `home` 模式与跟随模式**。
- ✅ 另有：`entity/task/TaskIdle.java`（待机任务）· `ai/service/Site.java` / `AvailableSites.java`（站点）
  · `patchouli` 手册条目 `maid_home.json` + 配图 `textures/book/home.png`。
- ⛔ **勘测侧未能读到 `TaskIdle.java` 正文**（`raw.githubusercontent.com` 对该仓超时；
  ⚠️ **`site`/`sit` 与"home 点"的具体实现仍未核**）⇒ **本报告只断言"home/跟随两态确实存在"，不断言其内部实现。**

### 3.3 ⭐⭐ 由这次实测得到的一条**新发现**（对 Alice 直接有用）

> **车万女仆把"在哪"与"干什么"分成了两组正交工具**：
> - **在哪**：`switch_sit`（待机/在家）· `switch_follow_state`（跟随玩家）
> - **干什么**：`switch_work_task`（工作任务）· `switch_schedule`（**什么时候**干）
>
> ⇒ ⭐ **三根正交的轴**：**锚点（在哪） × 任务（干什么） × 日程（什么时候）**。
> ⇒ 这与 `survey/26 §4.1` 的结论**一致且更完整**：**锚点 = 作业区的来源，不是作业区本身。**
> ⇒ ⚠️ **Alice 今天只有前两根轴的雏形**（`D-356` 作业区 + `JobRequest.Kind`）；
> **第三根轴（日程：什么时候干）在 Alice 侧 0 命中**，且**用户的 `L7` 也没提到它**。
> ⇒ ⭐ **勘测侧建议：这条作为"已知可选维度"登记，但**不进第一层**（`survey/37 §6.4` 的口径）。**

### 3.4 建议的登记方式

- ⚠️ `survey/` 是"**只增不改**"目录 ⇒ **不要改 `survey/26` 正文**；
  建议**随 `D-448` 加一条附注**（或独立附注）写明：「`survey/26 §2.3` 的'固定锚点 ❌ 没有'**已被实测推翻**，
  正确结论 = **车万女仆确有 `sit`/`follow` 两态**（`SwitchSitTool` / `SwitchFollowStateTool`），
  差异来源 = `survey/26` 查的是 `api/task/`，本轮查的是 `ai/agent/tool/`」。
- ⭐ **并建议勘测侧在自己目录追加一条补遗**（`survey/37 §3.5`，**只增不改**）—— **本报告已同时附上**。

---

## §4 判据（**这三件事怎么算做完**）

| # | 事项 | 判据（可核） |
|---|---|---|
| **一** | `TaskZoneRegistry:24` 措辞 | ⭐ `grep -n '父类.*之外的另一种' src/main/java/com/dddgn/alice/protection/TaskZoneRegistry.java` ⇒ **0 命中**；且改后注释里**同时**出现"子类"与"载体"两词 |
| **二** | `D-448` | ⭐ `grep -n 'D-448' docs/AI_DECISIONS.md` ⇒ **命中**；且条目里**必须**含"**不是两套实现**"与"**简易档**"两句（防下次再被读成第二条产品线） |
| **三** | `survey/26` 更正登记 | ⭐ `grep -n 'SwitchSitTool\|switch_follow_state' docs/AI_DECISIONS.md` ⇒ **命中**（说明更正已随 `D-448` 落盘） |

- ⚠️ **门禁建议（可选）**：若主工作流希望防漂，可加一条**只读检查**——
  「`TaskZoneRegistry` 的类注释**不得**出现"父类之外"这一短语」。
  ⚠️ 但**勘测侧不建议**为一句注释专门加门禁（成本 / 收益不成比例），**改为在 `D-448` 里写死口径即可**。

---

## §5 本报告**没有**做的事

- ❌ **没有改任何 `docs/` 或 `src/`**（勘测侧本分；`survey/` 只增不改）；
- ❌ 未跑任何电池 / CORE / 客户端；
- ⚠️ **车万女仆的 `TaskIdle.java` 正文未读到**（`raw.githubusercontent.com` 对该仓超时）
  ⇒ **"home 模式的内部实现"未核**，只核到「`sit`/`follow` 两态存在且互斥」（工具描述逐字）；
- ⚠️ `MaidSchedule` 的**完整语义**只从 `SwitchScheduleTool` 的 `SCHEDULE_PARAM_DESC` 读到（三条枚举值），
  **未核其执行侧**；
- ⚠️ 本报告**不裁定**「锚点落在保护区内 / 跨边界」与「半径怎么接授权机制」两问（`§2.4` 四）。

---

## §6 声明

- **`§1`、`§2.1`、`§3.2` 的全部 `file:line` 与源码片段**：勘测侧**当场读**得到（仓库内文件 + GitHub API），
  **可复算**；
- **`§2.2` 的四段用户引用**：**逐字**；
- **`§2.4` 的 `D-448` 条目文本**：⭐ **勘测侧提案，不是已生效的决策**；主工作流**可改写**，
  但**建议保留 `§4` 判据里点名的两句**（"不是两套实现" / "简易档"）；
- **`§3.3` 的"三根正交轴"**：由工具清单**读出的推断**，⚠️ **未核其实施效果**；
- 本文**只增不改**，不修改 `survey/01`–`37` 与本轮涉及的任何既有文档的结论
  （对 `survey/26` 的更正**以"附注/补遗"形式登记，不改其正文**）。
