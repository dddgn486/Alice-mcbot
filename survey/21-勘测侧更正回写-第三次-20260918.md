# 勘测侧更正回写（第三次）—— `survey/20 §2.3` 实质性遗漏 + 两处行号

- 日期：2026-09-18
- 角色：**手机端勘测员**。本报告**不是实现授权**，是**勘测侧的错误更正**。
- 基线：`0ef677a`
- 触发：主线 `0d04ec7`（D-306 / D-307）的复算 + `OPEN_ITEMS_LEDGER.md` 的「勘测侧两处待回写」登记。
- 纪律：**只增不改** —— 本报告**不修改** `survey/03` 与 `survey/20` 的原文，只在此处列出正确值。
  （原文的错误保留，是"过去说过的话"；本报告是它的更正层。）

---

## §0 一句话

**`survey/20 §2.3` 断言「项目今天没有『保护区』这个统一概念」是实质性遗漏** ——
**机制上早已存在**：`protection/SafeZoneData` + `BlockBreakSafety.hasBlockEntity` + `WritePolicyMatrix.Zone` 两层。
**这不是行号错，是漏了一整个包 + 两条已拍板的设计。**

---

## §1 ⭐ 更正 A：`survey/20 §2.3` 的实质性遗漏（**最重要**）

### 1.1 我原来写了什么

> **⇒ 勘测侧核实结论**：项目今天**没有**「保护区」这个统一概念。有的是三个互不相干的东西：
> `CollectGrants`（掉落物授权）/ `ScopeBuffer`（记账作用域）/ `WriteEnvelopes`（任务写权）

### 1.2 实际是什么（主线复算，逐条可核）

| 已有的东西 | 内容 | 位置（本次已复核存在） |
|---|---|---|
| ⭐ **`protection/SafeZoneData`** | `SavedData`（key `alice_safe_zones`）：**区域**（按维度隔离 · 水平圆形半径 · **覆盖该维度所有高度**）+ ⭐ **方块 ID 黑名单** + ⭐ **标签黑名单**（全世界通用） | `protection/SafeZoneData.java` |
| **保护区声明入口** | `/alice` 命令：加区域 / 移除 / **增删方块·标签黑名单**（`changeBlockRule`）/ 汇报 | `command/BotCommand.java`（`:537` / `:544` / `:572` / `:585`） |
| ⭐ **已接在破坏闸门上（非装饰）** | `BlockInteraction`（读 `protectionReason`）· `protection/BlockBreakSafety` · `pathing/core/CapabilityGate`（② 保护区分支 → `ZONE_*` 拒绝码）· `pathing/core/MovementCapabilities`（`requiresZoneAuthorization=true`，注释写明"此前 false ⇒ 分支**永不触发**、字段退化成装饰"） | 多处 |
| **已接在候选源上** | `job/mine/MineCandidateSource:139` · `job/lumber/LumberCandidateSource:68` · `road/RoadObstaclePolicy:42` · `pathing/core/session/PathSession:746` | 四处 |
| ⭐ **模组机器已有专门保护** | `BlockBreakSafety.clearingRefusal`：`state.hasBlockEntity() → "block_entity"`，注释（**D-095**）原文：「含方块实体的方块（箱子/熔炉/漏斗/告示牌/刷怪笼/**模组机器**）不得作为清障对象…**模组机器可能内容物直接蒸发**」 | `protection/BlockBreakSafety.java` |
| **另加高代价方块** | `isExpensiveToClear` = 黑曜石 / 哭泣黑曜石 / 强化深板岩 | 同上 |
| ⭐ **区域两层早已拍板** | `WritePolicyMatrix.Zone`：`Zone#EXTERNAL`（默认，不是 Alice 的地）/ `Zone#WORKSPACE`（Alice 的地）。**原文**：「**保护区不是第三层**：它是**优先级更高的独立闸门**（`SafeZoneData` 命中即禁止破坏），与"归属"**正交**，两个概念不该挤在同一列」 | `action/WritePolicyMatrix.java:30-35`（**D-207 ①**，2026-09-14 拍） |

### 1.3 ⚠️ 为什么我漏了（给未来的自己）

**我犯了 `survey/18 §1` 那个病的第 5 次，但这次是新的子型：**

| 子型 | 本次 |
|---|---|
| ① 把"字段存在"当"值正确" | — |
| ② 把"方案存在"当"已落地" | — |
| ③ 把"我的环境"当"项目环境" | — |
| ⭐ **④ 把"我没搜到的包"当"项目没有"**（**本次新增**） | 我在 `decision/` 和 `ledger/` 两个目录里找，**没进 `protection/`** —— 而 `protection/` 正是那个包的名字 |

**⇒ 教训具体化为一条可执行纪律：**

> **断言"项目没有 X"之前，必须先 `grep` 全仓的 X 关键词，再看目录清单。**
> **`ls src/main/java/com/dddgn/alice/` 是断言"有没有"的最低成本前置。**

**⇒ 而这条纪律今天就能验证**：`protection/` 一直在那里，我在报告 `20` 里写"没有保护区概念"时**没执行过 `ls`**。

### 1.4 连带更正：`survey/20 §4 B-1` 与 `D-207 ①` 的张力

- 我在 B-1 里提出「区域 = 上限，任务 = 需求」，隐含"把区域收成一个父类"
- ⚠️ **`D-207 ①` 明确说「保护区不是第三层」、两个概念**不该挤在同一列**
- **⇒ 主线读法（我接受）**：交集是**必需且正确**的；"不取交集"指的是**别把区域当笼子**（作者原意）
- **⇒ 且用户的最终裁定（D-305 ③）是「两个区域 + 换档」** —— 出区 = **上限换档**（既不归零、也不用申请）

---

## §2 更正 B：行号（`survey/20 §2.1`）

| 位置 | 我写的 | ✅ 实际 | 备注 |
|---|---|---|---|
| `ESCAPE_MAX_NODES` / `ESCAPE_MAX_MILLIS` | `PathRequest.java:34-35` | **`:38-39`** | 常量定义确实在该文件，只是行号偏了 |
| `survivalEscape` 动作集 | `:66-72` | **`:66-74`**（动作集在 `:69-71`） | 主线已用 `:69-71` 指 `BREAK_*`，✅ 与实测一致 |

**⇒ ⚠️ 这类"行号在范围内但内容错位"是结构性盲区**（主线 R-P1 门禁只抓**越界**，抓不到"指向了别的行"）。
**⇒ 影响面**：`survey/19` / `survey/18` 的同一常量行号是对的 ⇒ **说明是我在报告 `20` 里手抄时改错了**，不是复制源的问题。

---

## §3 更正 C：`survey/03` 的一处越界

| 位置 | 内容 | ✅ 实际 |
|---|---|---|
| `survey/03-ATM9-勘测报告.md:231` | 引用 `InterfaceScanner.java:167-172` | ⚠️ **该文件仅 162 行** ⇒ `:167-172` **越界** |

**⇒ 说明**：这条引用是**勘测早期**（`03`，基线 `f099ef6`）写的，当时 `InterfaceScanner` 可能有 170+ 行，后来主线重写变短了。
**⇒ 但按"只增不改"纪律，我不改原文** —— 正确值应由读者以本报告为准。

**⇒ 主线为何不在 `survey/` 上跑 R-P1 门禁（原文）**：「R-P1 **刻意不扫 `survey/`**（只增不改 ⇒ 红它无法修复）」—— **这个判断我认同**。

---

## §4 ⭐ 本条最该被主工作流记住的一件（因为它会重复发生）

**⇒ 勘测报告里所有的「项目今天没有 X」都应当被当作**假设**，而不是**事实**。**

| 我的历史断言 | 实际 |
|---|---|
| 「今天没有统一区域概念」（`20 §2.3`） | ❌ **有**（`SafeZoneData` + 两层 `Zone` + 黑名单） |
| 「`CollectDropsTask` 第 4 参可能是物品过滤」 | ❌ **不是**（UUID 白名单） |
| 「留掉落物会被算成可回收性失败」 | ⚠️ **说重了**（只告警不判失败） |
| 「逃生预算可疑」 | ⚠️ **收回**（是性能护栏） |

**⇒ 建议（供主线参考，非要求）**：勘测报告里**建议性的断言**加一个显式标记
（例如 `⚠️未核` / `✅已核`），让读者一眼分出"这是我查过的"和"这是我推的"。
**⇒ 报告 `20` 已部分做到（§2 标了"全部为已核实事实"），但 §2.3 那条正好落在标记之外。**

---

## §5 收档：主线对报告 20 的采纳情况（供未来查证，非勘测结论）

| 报告 20 的条目 | 主线处置 |
|---|---|
| §0 分层表 A 层（`RegionCap` 查询 / 越界降级 / 区外通道） | ⚠️ **主线提异议**：三条的前提都是"保护区/安全区"定义，而那是 D-2 待拍板内容 ⇒ **无定义时 `RegionCap` 是无调用者的死代码** ⇒ **正确顺序是 D-2 定义 → 才谈 A 层**。**⇒ 我接受这个异议** |
| §4 B-1 三处自相矛盾 | ⚠️ 主线标"待澄清" ⇒ **本报告 §1.4 即澄清**：按 D-305 ③「两个区域 + 换档」+ D-207 ① |
| §8 七项待拍板 | ✅ **2026-09-18 逐条走完**（`D-305`） |
| §1.3「记账只能管放」 | ✅ **承重事实成立**（`WorldModLedger:20-26` 原文） |
| §5 C-1 Baritone 三事实 | ✅ 被 `D-329/D-330` 纳入登记 |
| §7 三条自我更正 | ✅ 全部成立 |

**⇒ ⚠️ 特别注意**：主线的异议（A 层顺序）与报告的 B-1 矛盾**都不是勘测员被判错**，而是**报告没读到 `WritePolicyMatrix`**（§1.3）造成的。
**⇒ 这就是"漏一个包"的代价：不是一行错，是让读者按错误顺序开工。**

---

## §6 未来纪律（本条写入勘测员自己的操作规范）

1. ⭐ **断言"项目没有 X"前，先 `ls` 顶层包 + `grep` 全仓关键词**（本次教训：`protection/` 一直存在）
2. 报告里的每条断言标 **✅已核 / ⚠️未核 / 🔵勘测侧推断**，并把三种标记的含义写在报告头部
3. `survey/` 内的**行号引用**在写入前逐条 `sed -n '<行>p'` 复核（不靠记忆、不靠复制粘贴）
4. ⭐ **先录节点，再写报告**（作者 2026-09-17 规定；`survey/.notes/`）
