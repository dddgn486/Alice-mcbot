# 勘测报告（供主工作流借鉴）

> **本目录不是实现授权。** 这里的每一份都是**建议 / 勘测 / 参考方案**，
> 主工作流**可以采纳、可以部分采纳、也可以不采纳**。
>
> **与主线的边界约定**（作者 2026-09-10 指定）：
> 本目录由**手机端智能体**维护，角色是**勘测员**，不是工程师。
> 类比：主工作流是「在固定位置设计修建桥梁的工程师」；勘测员只负责
> 「**寻找哪里适合修桥、可以学习谁的设计、估计要修多大、什么材料合适**」。
>
> 因此本目录**不干预正在进行的开发主线**，除非发现主线严重偏离原定方向。

## 为什么单独一个顶层目录

主工作流正在高频编辑 `docs/`（`docs/README.md`、`AI_DECISIONS.md`、`AI_PROJECT_STATE.md` 等）。
把勘测报告放在 `docs/` 下会造成**合并冲突**且需要改主线的索引文件。
放在顶层 `survey/` 下：**只新增文件，零改动既有文件**，主线想引用时再自己加一行索引即可。

## 与其他文档的关系

| 文档 | 角色 |
|---|---|
| `docs/AI_DECISIONS.md` | **决策的权威来源**。本目录的结论若与它冲突，以它为准 |
| `docs/AI_PROJECT_STATE.md` | 当前进度（活化文档，主线维护） |
| `docs/archive/legacy-2026-08/PRODUCT_ARCHITECTURE_ROADMAP.md` | **蓝图来源**（已归档，但是长期路线的权威描述） |
| `docs/archive/legacy-2026-08/AI_PLAYER_DESIGN.md` | 架构总纲与三条产品主线 |
| `docs/RISK_SYSTEM_DESIGN_DRAFT.md` / `RISK_SYSTEM_ISSUE_LIST.md` / `RISK_SYSTEM_REVIEW_20260910.md` | 已由主线入库的风险系统草案 + 复核记录（本目录不重复） |
| `docs/JOB_LAYER_DESIGN.md` | L3 Job 层设计（主线） |

## 索引

| 文件 | 内容 | 基线 |
|---|---|---|
| [`01-蓝图勘测报告.md`](01-蓝图勘测报告.md) | 从仓库证据重建**项目整体目标**：三条产品线、七个阶段、四条差异化支柱、C0–C5 兼容分级；勘测结论（关键路径、桥位、可学方案、规模估计、蓝图缺口） | `c6ec86a` |
| [`02-职责归属勘测.md`](02-职责归属勘测.md) | 主线的**结构性病灶**（同一职责无单一归属 → 重复 / 死抽象）；**尚未归属的 7 项职责**表；按"什么时候真的会疼"排序；不建议动的清单 | `c6ec86a` |
| [`03-ATM9-勘测报告.md`](03-ATM9-勘测报告.md) | **第一个整合包材料**：All the Mods 9（ATM9）1.1.1 实测环境勘测。<br>权威硬事实（Forge 47.4.0 / 435 模组）→ C0–C5 适配地图 → **4 处会推翻现有设计的意外** → Alice 现有资产存活率审计 → 待实测清单。<br>附录含 **435 模组版本锁定全清单**；文末含「补遗」（独占矿石分档 + 一条被否假设留档） | `f099ef6`<br>补遗 `90b8390` |
| [`04-Policy-Gate-勘测.md`](04-Policy-Gate-勘测.md) | 蓝图 §2.2「需补出的层」的**实际形状勘测**。<br>修正蓝图写法：不是「一个层」，是**三件成熟度不同的事**（判定已做对 / 词表已存在但**结构倒置** / 身份字段**永远为空**）。<br>含 `instanceof TransferTask` 中断缺陷实测（5 处）、预算三层互不相干、检查点实为三个；**成本窗口表**（现在改 3 处 vs 之后改 30 处） | `90b8390` |
| [`05-任务发布与交互勘测.md`](05-任务发布与交互勘测.md) | 用户提出的**任务发布交互设计**（对话 + 显式表单 + 前置条件 + 默认任务 + 提权）的勘测。<br>核心：误判成本不对称（LLM 只建议、表单才确认）；**最高权限壁垒 = D-024 不变式**（判据已现成）；**回退默认任务有三个触发器**（完成/失败/阻塞等待），统一了「离线队列」与「默认任务」；结果词表 4→6 并给出**准入判据**。<br>含前置资源**四态**、GUI 管道现状、表单参考建议（非交付物）。<br>**文末「补遗」= 完整决策记录**：8 项拍板 + 信封结构（Grant 分层）+ **责任转移机制**（确认即转移）+ 提权生命周期 + 挂起策略（主判据 = 世界前提） | `9114a73`<br>补遗 `55273c6` |
| [`06-额度继承草案.md`](06-额度继承草案.md) | 父子任务**额度传递**的草案（`04` §5.2 与 `05` §4.4 标记的未解决项，本报告即其答案）。<br>核心命题：**「继承」是歧义词** —— 时间类只能**共享**（父扣子也扣），配额类只能**分配**（父给子上限）。<br>六条规则：scope 一致性 / 两类传递 / 预留 / 级联取消 / 挂起冻结 / 可归因。<br>★ 含**实测缺陷**：`LumberJob.clearedBlocks` 用 job 级计数器比 tree 级限额（`MAX_CLEAR_PER_TREE`）→ 累计 8 格后所有后续树以 `:clear_budget` 失败 | `d6676ae` |
| [`07-方向审核-20260914.md`](07-方向审核-20260914.md) | **项目方向审核**（跨 2026-09-10→09-14 的 **319 个提交**）。<br>确认用户已定位的根因（验证成本 →「轮次」成为规划单位 → 台账自我生成）；<br>⚠️ **同一模式已搬进代码**：夹具与生产同包且全进发布 jar，`task/` 占代码库 **39.8%**；<br>⚠️ ★ **模组适配没有终点线**（分母 = 435 模组）—— 建议改「覆盖率驱动」为「**需求驱动**」（§3.5，**在 Create/EC 之前**采纳才有效）；<br>给出 5 条有序建议（T3 收尾 → 停止规则 → 夹具分离 → 转决策层 → 预算护栏） | `1978091` |
| [`08-挖矿范例与风险系统形态-20260915.md`](08-挖矿范例与风险系统形态-20260915.md) | **挖矿范例的框架承重 + 风险系统形态勘测**（跨 12 个提交）。<br>★ **对齐**：挖矿**不是转向** —— 它是蓝图**阶段 B**的一项（八项里仅 3 项有实现），与主工作流的"砍树"**同阶段**；用户澄清的"需求化模组适配"= 蓝图 §1.2 = **阶段 D**，其**全部关键类型源码 0 命中**（前置阶段 C 同样为 0）。<br>★ **短线→长线的唯一本质差别：把「到达」从 5 格放大到 60 格**（`floor(reach+1.54) ≈ 6`；无"下到某 Y 层"概念）。远距离寻路**不需要分层，需要"粗目标"**（Baritone 用滚动重规划；Alice 的骨架已在跑，只缺 `GoalXZ` 式粗目标）。<br>★ **通道挖掘挡着两堵墙**：① 授权层（D-076 + `miningApproach` 禁三件；机制 `withWorldModification` **已存在且已有生产用户** = `CollectDropsTask`）② **可回收性要求层**（放开授权后立刻撞上 —— `PILLAR`/`BREAK_*` 在评估器里被判 `LOCAL_STEP`，而门槛表全都是 `LOCAL_STEP` ⇒ **能通过不是因为安全，是没人要求更多**）。<br>★ **风险系统形态 = 一把已有的尺子 + 一个空着的插槽 + 一个缺失的搜索方向**：尺子（四级 + 门槛表 + 逐边评估器）**有真读者**（35/41 处），插槽 = `SAFE_EXIT_REQUIRED`/`EMERGENCY_EXIT_REQUIRED`（**无产出者**），缺失方向 = **反向可达性（逃生集）搜索**（全仓 0 命中，即草案 §3.2）。<br>含 **S1–S6 六片实际进度**（1 完成 / 1 接线 / 1 部分 / 3 未动）、**M1–M6 的提交顺序建议**（含对审计的**唯一实质修改**：**M4 先于 M3**）、**停止条件反推的必需清单**（用户已拍板判据）、4 项待实测、4 项显式留白。<br>⚠️ **并更正勘测员自己的两处过度解读**（把 `RoadBuildTask` 当"样板" —— 用户裁定其不可复用） | `ccf7bb7` |
| [`09-逃生与液体与粗目标复核-20260915.md`](09-逃生与液体与粗目标复核-20260915.md) | **逃生形态 + 液体处理 + 粗目标代价复核**（跨 37 个提交）。<br>★ **粗目标代价复核（主工作流 `review §5` 点名）**：结论 = **"比 M1–M6 都小"不成立** —— `GoalSpec` 接口虽只有一个实现，但 `AStarMovementSearch:52` 的 `chunkLoaded` **硬前置**挡在搜索之前（S-2/P1-A 安全规则），且 `MAX_PARTIAL_HOPS=4`/`maxReplans=2` 是**故意**设的空转上限 ⇒ 需**换一个停止规则**。★★ **且它解决"远"不解决"深"** ⇒ 对挖矿竖井**无帮助**；但有**真实消费者** = `FollowTask.MAX_TARGET_DISTANCE=24`。<br>★ **两条线撞同一堵墙**：挖矿「通道挖掘」与溺水「自救」卡在**同一对**前置（放宽 D-076 / 内核垂直移动），而文档里是**两份独立的待拍板**、**无人发现是一个决定**。<br>★ **逃生形态 = 「退开一步」不是「活着离开」**：半径 8 格 + 可规划（D-238），行动 = 一次普通走路或纯输入上浮。**真正的杀手是三个条件叠加**（半径 8 / 必须可规划 / `PathRequest.of` 无 `PILLAR`）⇒ **自挖竖井里 bot 无解**。<br>★ **液体：不要模拟流向**（否定性建议，四重理由 + 岩浆不追你/水追得上但不致死）；**邻液体硬禁是错的层级** —— 分界是**拓扑**不是液体种类 ⇒ 属可回收性不属危险系统；**"另找一条路"不是通用方法**（1 格宽隧道出口数=1，封住=0）。<br>★ **权利的正确轴 = 「这个任务改不改世界」**（已是字段 `changesWorld()`）⇒ 逃生写权应是**事前留在信封里的逃生准备金**；⭐ **最便宜的是「原路返回」**（全仓无轨迹记忆，让问题不发生）。<br>⚠️ 登记勘测员自己的 **M5 错误**（照抄审计"0 调用者"未自验）。<br>**补遗 A（追加）**：★ **D-238 把一次「无界规划」塞进了判决路径** —— 用户问"维生逃跑路线规划是否 1 tick 内完成"：答**不是 1 tick，是跨 2 tick**，但**比 1 tick 更糟**：`PathRequest.of` 用 `SearchBudget.UNLIMITED`（**无节点+无时间上限**）；且 `decide()` **每 tick** 调 `plannableRefuge` 而缓存键是**精确脚位** ⇒ `HOLD_NO_EXIT` 语义下 bot 在移动 ⇒ **缓存每 tick 失效** ⇒ 每 tick 一次 17³ 扫描 + 可能一次规划。**用户担心的前提正是今天刚被打破的**（D-238 之前无规划、前提成立） |
| [`10-多bot与tick预算-20260915.md`](10-多bot与tick预算-20260915.md) | **多 bot 并行、tick 预算、异步搜索（架构层勘测）**。触发 = 用户问「Baritone 单开线程短期有效，但**多 bot 并行怎么办**？修复也不能一 bot 一线程吧」。<br>★ **Baritone 的单开线程不能照抄**：它是**客户端 mod**（读 world 副本，读脏了无所谓）；**Alice 读权威服务端世界** ⇒ 异步要额外付代价（撕裂读 / **触发区块加载** / chunk 系统非线程安全）。<br>★★ **多 bot 的灾难不是「线程互相堵」，是「N 个 bot 分一个 50ms tick」** —— **今天就成立**（`BotManager:964` 调度**串行**），不需要等线程；**一个 bot 的重活把其他所有人的生存时钟一起拖慢**；**"漏一 tick"不是"变慢"是「这个 tick 什么都没做」** ⇒ 危险中的 bot = **少逃一次 = 死** ⇒ 级联。<br>★ **"一 bot 一线程"错在**：真正稀缺的是**主线程的 50ms**，且**结果的"应用"永远必须在主线程**。<br>★★ **项目已有这个形状的完整模板**：`LlmClient.askAsync` + `GoalDirector.pending`（不在主线程等 / **single-flight** / 节流 / 看门狗 / 主线程只收结果）⇒ **"异步搜索"是搬运不是新建**。<br>★ **根解 = 给 tick 做预算**（全仓 **0 命中**）—— 与 `WriteBudget`(64/32/32) **同一思想**；`survey/05` 的「**授权信封 per-bot**」是它在**权限**上的版本，**缺的是「算力」上的版本**。<br>含**三层形状**（先别做规划 → 有界池+可取消+优先级 → 主线程只收结果）、**建议顺序**（先 B1 原路返回/B2 tick 预算，**C1 异步排最后** —— 在"逃生不需要搜索"之前做异步，是给本可消失的问题建基础设施）、⚠️ **已登记的债**（`PathingStats.snapshotAndReset` 清空式 ⇒ 异步会丢数据）、5 项显式留白（**全部成本数字均为量级推算、未经实测**） | `4fb0956` |
| [`11-资源与性能量级勘测-20260915.md`](11-资源与性能量级勘测-20260915.md) | **资源占用 / TPS / FPS 量级勘测**。触发 = 用户问「Alice 到底会占用多少游戏资源？TPS 和 FPS 会有多大变化？估算一下现在和未来」。<br>★ **先把"性能"拆成三类否则一定算错**：① **存在成本**（区块票 ⇒ 保持加载，≈ 一个 AFK 真玩家）② **固定成本**（每 tick ≈ 8 项小检查 + 一套玩家物理，很小）③ **突发成本**（一次吃掉几十~数千 ms）⇒ **只有第 ③ 类伤 TPS**。<br>★ **现在 = 稳态 ≈ 一个玩家，问题全在"某一次调用"**。**三个悬崖**：`DEFAULT_MAX_MILLIS=3000`（最坏 **60 tick 停顿**）／行走类生产路径全用 `SearchBudget.UNLIMITED`（**连 3 秒上限都没有**，`WalkToTask`/`FollowTask`/`PlaceTask`/`TransferTask`/`CollectDropsTask`/`MineBlockRunner`/`SurvivalExitTask` 全部）／软危险持续期**每 tick** 17³ 扫描 + 可能一次规划（`survey/09` 补遗 A）。<br>★★ **FPS 的答案取决于怎么跑**：**专用服务器 + 独立客户端 ⇒ FPS ≈ 0 影响**（客户端有自己的渲染预算）；**单机（集成服务端）⇒ 服务端每个停顿直接变成掉帧** ⇒ **"你感觉到的卡"多半不是 FPS 而是 TPS**。<br>★ **一次菜单构建 ≈ 29 万次方块读**（树 24 格 ≈12 万 + 矿物 11 目标 × 25³ ≈17 万）——⚠️ **主线已知**（`MineMenuCheckTask:176` 自述"白烧百万次读"）。本报告补的是另两面：(a) **它冗余**：11 个目标各扫一遍同一个立方体（`MineCandidateSource.candidates` 裸三重 for、无缓存）⇒ **1 次扫描 + 11 路分类可省 91%，纯重构**；(b) **频率上限 = 6 次/分**（`minIntervalTicks=100` + `maxRequestsPerMinute=6`）⇒ 是**尖峰**不是**稳态**。<br>★ **未来（N bot）量变来自 bot 数**：`BotManager:964` 调度**串行** ⇒ **N 个 bot 分一个 50ms tick**；固定成本 ×N、区块面积 ×N、尖峰 ×N 且互相阻塞；⚠️ **最担心的是一条待确认** —— bot 包里 `mobcap` **0 命中** + 创建时显式 `SURVIVAL` ⇒ **假人可能扩 mobcap ⇒ 非线性开销**。<br>★ **判断：现在不值得为性能做任何事**（稳态无可优化空间）。只推荐两件：① 菜单 11 次冗余扫描 → 1 次（省 91%）② 行走类请求补有界预算（**一行**）。<br>⭐ **实测不需要新基建**：`PathPlan` 已带 `nodesExpanded`/`movementsConsidered`/`elapsedMillis`，`PathRetryRunner:62` 是行走类规划必经之处 ⇒ **加一行日志即可**。含 5 项待实测、5 项待确认、5 项显式留白（**不主张异步、不改常量、不给"能带几个 bot"的数字**） | `4fb0956` |
| [`12-系统全貌图与图的维护-20260916.md`](12-系统全貌图与图的维护-20260916.md) | **系统全貌图 + "图该怎么活"（维护判据）**。⭐ **不是新勘测，是拼图**：把散在 5 份文档里的风险系统拼成**一张四层全貌图**（用户明确要求："看不到风险管理的全貌"）。<br>⚠️ **含勘测员对自己 11 的一次口径更正**：曾说"`mermaid` 0 命中 ⇒ 一张渲染图都没画"——**错**，`docs/authz/flow.svg` **早就自动生成**（只是不用 mermaid，用纯手写 SVG）。**教训：只扫技术名词就下"有没有 X"的结论会误判，要扫行为面。**<br>★ **项目已有"关系图"的正确形态，而且是三份**：`authz-map.py` → `OVERVIEW.md` + **`flow.svg`** + `index.html`；`policy-map.py` → `POLICY_MATRIX.csv`；`machine-map.py` → `MACHINE_MAP.csv`。**三者都接了门禁**（`check-all.sh:127-129`）⇒ **图过期 = 构建失败**。抽出**四条纪律**：单一出处 / 产出勿手改 / **双向防漂移** / ⭐ **解析不到就响亮失败（否则门禁变"永远 PASS 的橡皮图章"）**。<br>⚠️ **`authz-map.py` 的结构性弱点**：单一出处是**人维护的 CSV**（另两个是**代码**）⇒ 它会漂；排序结论：**能选代码当单一出处，就别选人维护的表**。<br>★★ **风险系统全貌 = 四层 + 一个横切**：第 0 层**永不开的底线**（✅ 已在代码里真的在拦）/ 第 1 层三档 H·G·S（❌ 仅文档，`Mode` 枚举 0 命中）/ 第 2 层 10 个开关（⚠️ 仅清单，**只落地 1 个且默认 false**）/ 第 3 层分层评估器（❌ 零实现）／横切：**可回收性尺子**（✅ 真的在拒东西）。<br>⭐ **反直觉发现**：**已实现的两件恰好是"最不该关"的**（底线 + 尺子），而**玩家能控的 1 个开关默认就是关的** ⇒ **今天玩家的开关"几乎什么都控制不到"** —— **这才是"风险系统让人感觉绕"的关键：你看到"到处都是的避险措施"全是不可协商的底线，本就不该出现在开关面板上**。<br>★ **玩家的开关本质 = `RiskProfile` 的人可读投影**；**消费者只有两个**（规划层候选过滤 + Movement 层守卫开关，A.4 原文）；⭐ **A.1 第 5 条已撤回"替代策略"** —— 低风险模式**只需禁用**高风险 Movement，规划器自己拼楼梯（2026-09-09 实测 4 格落差用 `PLACE_STEP×4` 下去）。<br>★ **"每个高级系统都做关系图"的答案：分三类，只做第一类** ① 能从代码/CSV 推出来的 ⇒ **生成器 + 门禁**（已有三个先例）② 需要人判断的"全貌/为什么" ⇒ **只写指针不画框**（会过期且不会让任何东西失败，等于散文规则）③ 一次性理解辅助 ⇒ **放 `survey/`**（"只增不改"天然解决"过期怎么办"）。 | `59e46d6` |
| [`14-产品定位重构-AE外挂-20260916.md`](14-产品定位重构-AE外挂-20260916.md) | **产品定位重构**（用户连续四轮自我怀疑与目标澄清的落档）。<br>⚠️ **勘测员更正**：上一轮把用户澄清过的「工作站」**错误地向上抽象**成"从 JEI 惯例推导的能力概念"，并把「AI 自动设计产线」**错记成用户提的第 3 档**。**两处都错** —— 工作站是**玩家亲手逐格划分的**；自动设计产线**从来不是用户提的**（原话："可以拿去做一个更牛逼的项目"）。**教训（本目录第三次同类错误）：转述用户设想时禁止替用户向上抽象。**<br>★★ **核心结论：定位从"替代 AE"改为"AE 的外挂"** —— AE 管「管道+库存+自动合成」，**Alice 管「管道接不上的那一层」** ⇒ 互补而非替代，**产品价值随玩家进度递增（替代定位是递减）**。<br>⛔ **明确否定："AI 自己设计产线"不是本项目目标**（用户定性"另一个项目的野心"），**不得塞进蓝图阶段 C/D**。⭐ 这条直接解释了用户的自我怀疑："原来我无意间扩充了产品目标"。<br>⭐⭐ **"工作站" = 玩家亲手逐格划分**（占位/输入口/输出口/主交互面）+ **能力声明**（"这个工作站等价于 mek 磨粉机" ⇒ **自动继承该类全部配方**）⇒ **工作站 ≠ AE 样板：样板是实例，工作站是类型声明**。<br>⭐ **语义准确口径**：默认**只看外部（与 AE 相同）**；**"读本体配置"是预设授予的能力** ⇒ **与信封思想同构（默认最小权限）**。⚠️ **硬边界**：bot **只对单方块机器调模式，不动复杂流水线的机器配置**。<br>⭐⭐ **最意外的一条**：用户想用"玩家点击标注"解决的**槽位语义，项目里已经解决了，方法更硬** —— `FurnaceStation.java:29`：「输入/燃料/输出**不采信自述顺序**，一律用 **`mayPlace` 行为判定**」+ `GridDiscovery.scan(menu)`（610 行）⇒ **点击标注不是必需品，是可选加速**（但它是**社区通路的第一块砖**，见下）。<br>⚠️ **硬前提**：「社区接适配」今天接不上 —— `MachineMap` 的单一出处是**代码**⇒ **社区加一个模组必须改 Java + 重编译 + 跑电池** ⇒ **适配必须数据化，"社区接"才成立**。<br>★ **工作站预设两层**：通用结构预设（单方块机器+独立输出口）/ **模组专属预设**（法阵：怎么启动、怎么摆祭品、魔力从哪牵）⇒ **逐模组适配三档**：只看外部 / 通用预设 / **延伸到 JEI 管不到的地方**（档 3 是加分项，不是承诺）。<br>★ **AE 时代功能 7 条**（写样板/管理样板/路线顾问/路线选择交玩家/样板编辑预设/批量倍增）+ 边界「太特殊太细节只能玩家自己改」。⚠️ **技术坏消息**：AE 样板**不是配方是编码**（不含配方 ID/可替换语义）⇒ Alice 必须自己做「配方→样板」映射（✅ 好消息：**纯数据变换，不碰世界**）。<br>✅ **已拍板五条**：① 无形条件 **C**（预留字段，无期限延期）② 路线报告 **B**（确定性算 + LLM 说人话）③ 点击槽位**不确定，先做辅助工具** ④ 写样板 **C**（草稿 + 写入两段）⑤ AE 功能 **C**（阶段 D 通用适配 + **新阶段 AE 深度集成**）。<br>⭐ **勘测观察**：「路线顾问」是性价比最高的一档（只读配方图、零世界修改、AE 完全不做、玩家上线即感知）；⚠️ 但 `MachineMap` 今天 **59 行只有 1 行 `EXECUTABLE`** ⇒ **"第一个真能操作的工作站"是这条路线的关键验证点**。 | `f224d52` |
| [`15-路线修正企划-20260916.md`](15-路线修正企划-20260916.md) | ⭐ **路线修正企划（本目录当前最新、也是最重要的判断汇总）**。**⚠️ 性质：不是待办、不是排期、不是提案** —— 用户 2026-09-16 原话「今天讨论的事情可以说**目前项目基本不会马上做什么**，所以写清楚情况，这是**未来路线的修正和企划**」⇒ 用途是 ①记录"想清楚了什么" ②**划边界（防它们变成默认前提）** ③**标证伪点**。<br>★★ **七项修正**：① 定位 **"替代 AE"→"AE 的外挂"**（价值随玩家进度递增而非递减）② ⛔ **"AI 自动设计产线"划出项目范围**（永不在本项目）③ **工作站 = 玩家亲手划分 + 能力声明**（样板是实例，工作站是类型声明）④ **可回收性从主卖点降为底座/许可证**⑤ ⚠️ **三处结构性膨胀点体检**（`task/` 41% / `item/` 78 文件 / `docs/` 33,476 行，唯一一条"迟早要还"）⑥ **LLM 职责边界收敛 + 三条未落地点** ⑦ **两处真正的未知**（队列调整 L3 / 适配数据化）。<br>⭐⭐ **对用户自我怀疑的正面回答**：「可回收性中后期不是卖点」观察准确，但结论要反过来 —— **自由度的对立面不是"保守"，是"不受控"**；**一个会乱用机器的 bot，只有同时保证"任何失误都有退路"，玩家才敢给自由度** ⇒ **可回收性 = 敢放开权限的前提**。<br>★ **膨胀归因（勘测判断）**：**不在决策-执行框架**（`decision/` 18 文件、`pathing/core/` 39+19、`action/` 11 都结构清晰），**而在 `task/`+`item/`+`docs/`**；"修补补"感的真实来源 = **一次改动要同步 6 处描述**（代码→注册表→生成视图→门禁→决策号→台账），且当天 38 个提交的 fix **全是"修锚点"不是"修逻辑"**。<br>⚠️ **"越来越保守"的机制已登记**：加守卫 → 守卫互相打架（D-248 是实例）→ 加固/放宽反复 → 为不误杀而设保守 ⇒ 覆盖面变小。**建议盯一个数**："诚实失败"比例（`UNREACHABLE`/`NO_REACHABLE_CANDIDATE`/`HOLD_NO_EXIT` 占终态百分比）。<br>★ **队列调整五层（L1–L5，标注为勘测归纳）**：L1 参数 / L2 顺序 ✅ 可做；**L3 重新规划 ⚠️ 卡在已登记的"长作业中段静音（无周期复评）"**；L4 目标改写 / L5 发现缺失能力 ❌（**建议目标定在 L1–L3，L5 交给"问玩家"**）。<br>★ **两处未知**：① 「社区接适配」**今天接不上**（`MachineMap` 单一出处是**代码** ⇒ 必须数据化才成立）+ 格雷是**唯一被点名要单列层次**的模组；② 槽位语义**项目里已解决且更硬**（`mayPlace` 行为判定 + `GridContainerIdentity`）⇒ 点击标注降为**可选的社区通路第一块砖**。<br>★ **显式留白 8 条**（含"不主张给陪伴功能排优先级——用户本轮未给出可分档的需求，勘测不替用户发明"）；**含 6 项待确认 + 每条修正都带「证伪点」**（什么观察会推翻它）。<br>⭐ **补遗（2026-09-16 深夜追加，只增不改）**：① **工作站不只是「中间节点」** —— 能力声明是**并集**：自定义配方（≈AE 样板，只覆盖这一份）**∨** **类别声明**（「等价于 mek 磨粉机」⇒ 继承该类全部配方）；② **预设允许「部分读内部」**（磨粉机+漏斗+箱子 = **单方块机器+独立输出口预设** ⇒ 可读机器本体配置），**但必须「可控」** ⇒ 与 §3.3 同构：读内部 = 预设授予的能力，默认不读；③ ⭐ **新增登记：社区通路有 3 道闸** —— 闸1 适配**数据化**（改 JSON 不改 Java）❌ ／ 闸2 适配**可视化产出**（游戏内标注⇒自动生成数据）⚠️ ／ 闸3 适配**可验证**（一条命令自证）⚠️ ⇒ **「吸引模组作者主动适配」要成立，三道闸都得过；没过之前不是没兴趣，是没入口**；④ **预设 = 容纳档 3 的容器**（含魔法仪式：怎么启动、怎么摆祭品）⇒ **可缺席**：无预设的模组退回档 1/2，**项目依然可用**；⑤ 本轮最终裁定：**问题4 = C**（写样板，草稿+写入两段）、**问题5 = C**（阶段 D 通用适配 + **新阶段 AE 深度集成**）。 | `4adab1f`<br>补遗 见文末 |
| [`16-陪伴性与外部驱动者-20260916.md`](16-陪伴性与外部驱动者-20260916.md) | ⭐ **陪伴性 · 外部驱动者 · 事件上报分级**（本报告是 `15` 的延续：`15` 说"做什么"，`16` 说"什么时候开工"）。**⚠️ 性质：按开工时机分档的方案集，不是待办、不是审查任务、不含待修清单。**用户要求分五档：**【要做】/【要清】/【要标】/【留口】/【不虑】**。<br>★ **【要做】只有两条，且都是"把已有事实接出来"**：① ⭐ **进度叙述原料已就绪且已在快照里** —— `Job.progressSummary()` 是**接口契约**（`LumberJob` = `"trees 3/8 logs 12/40 cleared=5"`、`MineJob` = `"mined 2/8 failed=3"`），且经 `DecisionSnapshot.java:139` → `tree[0].progress` **已送进决策层** ⇒ **缺的不是数据，是"有人定期看一眼"**；② ⚠️ **失败的逐次事实传不到决策层** —— `MineJob.finishedMinerNode` **只赋值、从不重置**（`grep "finishedMinerNode = null"` **零命中**），而 `subTasks()` 在新目标上场时走 `miner != null` 分支 ⇒ **失败事实被盖住**，`attemptFailures` 只活在 `progressSummary()` 那一行文本里。<br>★ **【要清】五条（现在不做但必须先想清楚）**：③ **「驱动者」概念**（用户当场修正："不一定是完全替代 LLM，可能是**其他兼容层**" ⇒ 这是**多驱动者 + 身份分层**，不是换模型）；④ ⚠️ **桌面 AI 不继承玩家权限，只继承信封**（"和玩家交互" ≠ "以玩家身份行事"，混起来会让 `PermissionGate` 的"自己问自己"失效 —— **唯一一条想错了会伤到根的东西**）；⑤ **陪伴性 = 触发源 + 提议动作，不是权限**（用户举的三件事里，**只有"恶作剧"真撞红线**；"主动跟随"`FollowTask` 早就有、"主动保护"只是缺触发源）；⑥ **对话线 vs 决策线必须分开**（依据 `05` 的"NL 只生成草稿、表单是唯一真值"）。<br>⭐⭐ **本轮最有复用价值的一条判断**：**"恶作剧"应从「危险动作」改归为「一种请示的措辞」** —— 不要问"bot 能不能有恶意"，要问"bot 能不能**提出**一个玩家可能不喜欢的提案"。恶作剧的陪伴价值**几乎全在"提议"那一刻**，而**拒绝权**正是让它变好玩的关键；`PermissionGate` 已支持**任意 capability + 任意 options** ⇒ 加 `capability="prank"` 不需要改架构。（⚠️ 但 `GoalAction` 动作集里**没有 `propose`** ⇒ "提议"今天**无处落脚**。）<br>★ **【要标】`NO_PROGRESS` 判据语义**：`window <= 0` 时**短路**（`progressFingerprint` 根本不执行 ⇒ 零成本）；指纹是三信号 `|` 串联（任务 + `progressSummary` + 脚位 + 背包哈希）⇒ **任何一项变就算有进度**，项目注释写明这是**刻意**的（"走路中的任务不会误报"）。⚠️ **代价**：**一个"一直在走、但挖不动"的 bot 永不触发 `NO_PROGRESS`**，而这是 `MineJob` 主循环的形状⇒ **它检测的是「站着不动」，不是「白忙一场」**。**只标记三条（检测语义 / `attempted` 集合会自然收敛 / `maxTicks=3600` 前无人知晓），不判断不建建议 —— 审查留给主工作流。**<br>★ **【留口】三条（现在不动功能，只保证接得上）**：⑦ **决策缝的替换点只有一行** —— `LlmClient.askAsync(system, prompt) → CompletableFuture<Reply>`；下游（`GoalAction.parse` 白名单 / `BotManager.assignJob` 唯一入口 / `PermissionGate`）**绝不能换**；协议层是 **OpenAI 兼容**（`POST {url}` + `{messages}` → `choices[0].message.content`）⇒ **任何能说 OpenAI 协议的东西都能当驱动者**；⚠️ `LlmConfig` **不只被 `GoalDirector` 读**（`EventThresholds` 也读）；⑧ **触发集合可扩**（加一个 `onXxx` + 前缀即可，自动继承节流+单飞+看门狗），⚠️ **但配额全局共享**（`maxRequestsPerMinute=6`）⇒ 判据不是"有没有用"而是"值不值得花 1/6 分钟配额"；⑨ **AI 独立于决策的说话出口**（今天只有 `report_status`（要等决策触发）/ `PermissionGate` 卡片 / `tell()`）⇒ 将来必须**与决策节流解耦**，⚠️ 代价是可能动 `PROTOCOL_VERSION`。<br>★ **【不虑】四条**：桌面 AI 的延迟预算（零实测）/ 多驱动者并发（**已 single-flight**）/ "AI 自己设计产线"（`15` 已划出）/ ⚠️ **"同一个声音、两个身份"的边界**（自由来自"没有身体"、约束来自"有身体" ⇒ **不是"机甲 vs 束缚"，是"有身体就必须有可控性"**）；⚠️ 桌面 AI 的**请示通道可见性**列为【留口】+【要清】，是**整套愿景的硬闸**（不是"请求发不出去"，是"答复收不到/看不到"）。<br>⚠️ **含勘测员自我更正**：同一轮对话里曾说"把那个指纹改成节流上报，进度感知就白送了" —— **错**；核实后是"半白送"（引擎短路），且**不该复用它**（它是为卡住检测设计的），该用的是 `job.progressSummary()`。**教训（本目录第四次）："某段代码已经写好了 ⇒ 改一行就能用"是必须核实到执行路径的断言。** | `632b65a` |
| [`17-以手段换约束-20260917.md`](17-以手段换约束-20260917.md) | ⭐ **以手段换约束**（主线 = **不靠收紧策略，而是靠新增手段提高可用性**）。三条同主线设想：**给 bot 多一只眼睛 / 那只眼睛装在哪 / 给 bot 多一条命（但要付代价）**。**⚠️ 性质：可能实现的功能登记，不是排期、不是承诺。**<br>⭐ **§1 视觉作为「只读语义标注器」**：用户设想 —— 用 API 原生识图通道，在需要时做**一次**视觉识别（看玩家修的建筑给评价 / 认野外遗迹并报告）。⚠️ **先切开：这不是 C 路线**（`docs/INTERACTION_LAYERS_COMPARISON.md` 的三条路线是**交互方式**）—— 差别是结构性的：视觉不是唯一输入、不是主循环、按需低频、失败可退回原能力 ⇒ 准确定名 **「带外视觉 / 按需观察」**。★ **项目感知边界（已核实）**：今天全是**「按目标查表」**（`InterfaceScanner` 到容器层 / `TreeScanner` 只认注册方块 / `MachineMap` 59 行只认登记过的）⇒ **不是「理解场景」** ⇒ 用户三个例子全是 **「语义缺失」不是「数据缺失」**。⭐ **视觉的价值 = 给一堆方块坐标加一层意义**（语义标注器，不是导航仪）。⛔ **硬边界：只读，不写**（理由三条：像素不可验证 / "没看到"≠"不存在" / 看错后果不对称）⇒ 因此它**天然落在 `survey/16` §6 的「对话线」**（无需授权），且与 §5.3 恶作剧那条**同构**（只提议/只报告，不直接行动）。<br>⭐ **§2 bot 视角渲染（照相客户端）**：⚠️ **含勘测员自我更正** —— 首次答歪（答成了"渲染 bot 实体"，那确实零成本），用户要的是**从 bot 的眼睛渲一次世界**。★ **架构障碍（四条，已核实）**：需要 GL 上下文（**专服没有**）/ 需要 `ClientLevel` / ⭐ **`Camera` 是客户端单例**（⇒"按次数渲染"本质是"把这一帧借给 bot"）/ 需要渲染线程。⭐ **用户已选定形态：借用玩家客户端**（无需新进程/新账号，且**正好覆盖"陪玩家看建筑"**；⚠️ 代价：玩家必须在场 / 相机共享（不做离屏 framebuffer 则玩家画面会闪）/ 需新 C2S 包 ⇒ 动 `PROTOCOL_VERSION`）。⚠️ **必须一起钉死：一双眼睛还是 N 双眼睛** ⇒ 共享是 O(1)，每 bot 一副**立刻撞 `survey/10` 的多 bot 灾难**。<br>⭐ **§3 带成本的回收性道具（用户称之为"外挂"）**：用户原话「**不可能继续收紧风险管理策略**，会让 bot 越来越束手束脚」⇒ 与 `survey/15` §4.4 那条机制对得上（加守卫 → 互相打架 → 为不误杀而设保守 ⇒ 覆盖面变小）。★ **本质是换维度**：收紧策略 = **减少 bot 能做的事**（免费 ⇒ 会被滥用）；加外挂 = **增加 bot 能承受的事**（⭐ **有成本 ⇒ 会自我约束**）⇒ **「要花成本」本身就是正确性保证**：把"放宽策略"从"我们拍脑袋"变成"**玩家用资源投票**"（= `survey/16` §4 的正确延伸：不是"我们给更多权限"，是"**玩家决定给多少装备**"）。⚠️ **用户举的例子形态要改一处**：「**死亡前**传送回安全地点」——**"死亡前"这个瞬间可能不存在**（岩浆/虚空/摔落/贴身爆炸几乎无窗口）⇒ ⭐ **建议改成「死亡后回来」**（**死亡事件一定存在**）。★ 而项目**已预留过这条路**：`BotManager.onLivingDeath` 注释「暂时策略，**后续可改为重生**」。⇒ 用户真正要解决的是「**死了之后进度归零**」。<br>★ **五条建议（按性价比，勘测建议非裁定）**：① ⭐ **复活锚点/绑定护符**（只改变**失败代价**，不改变行为策略 ⇒ 最干净）② ⭐ **召回符**（**玩家**主动召回，不增加 bot 自主权，天然是"陪伴行为"）③ **一次性保命**（⚠️ 诚实边界：救不了虚空/秒杀，只能宣传成"多一次机会"）④ **任务保险**（失败消耗资源自动重跑 ⇒ 与 `survey/16` §2 那个断点正好对上）⑤ **临时权限凭证**（把"放宽 D-076"从"全局改规则"变成"玩家按次购买"）。⚠️ **一条设计原则（本节最重要）**：**外挂只能降低「失败的代价」，不能降低「失败的概率」**（能让 bot 更"能扛"⇒做；能让它更"不会错"⇒危险，与 `survey/15` §2"自动设计产线"是同一种病）。<br>⭐ **§4 外部参照：一个 141 小时实测**（用途 = **用户两条设想的动机证据**，不是"别人做了什么"）：**Vals AI 用 GPT-6 Astra 跑 141 小时 Minecraft**。⚠️ **含两处中文转载更正**（时长 141h 非 21h；游戏是 Minecraft 非《我的世界：传奇》）。(a) ⭐ **它自己总结出「可回收性」原则**（"永远随身携带关键物品；永远不要再把它们存放在没有防备的箱子里"）—— 区别只在**它靠经验学到、我们靠代码保证**；(b) 它自责"**15 秒的思考害死了我们**"⇒ 命中"决策延迟=死亡"，而 Alice 的维生**独立于决策层、每 tick 跑、不等 LLM**；(c) ⭐ **"忙碌 ≠ 进展"**（被炸后它"几小时只种土豆"）⇒ **给 `survey/16` §7 标记的那条提供外部样本**，并给出更准的判据：**`failed` 在涨、`mined` 不动 ⇒ 这就是"它在种土豆"，纯靠两个计数器的差，不需要任何智能**。 | `f6a16f0` |
| [`18-勘测侧更正回写-20260917.md`](18-勘测侧更正回写-20260917.md) | ⚠️ **勘测侧更正回写**（对主线 `docs/reviews/2026-09-17-勘测分诊与任务队列.md` 第三节 12 处「勘测侧更正」的回应）。**⚠️ 用途：本报告 = `01`–`17` 的「更正索引」—— 凡被列出的条目，以本报告为准。**<br>★ **复核结果：12 处逐条独立复算，全部成立，无一驳回**（含逃生请求**其实有界** `PathRequest.java:38-39 ESCAPE_MAX_NODES=4_000/ESCAPE_MAX_MILLIS=100`、`STATION_BY_TYPE` 实为 **11 条**（路径 `decision/RecipeDump.java:58`）、`tools/recipe-graph.py:30` 是**第二份副本（9 条）且无门禁**、`PARAM_SET`/`QUEUE_REORDER` 在 `src/` **0 命中**、`DECLARE_DONE`/`QUEUE_COMMIT`/`RESOURCE_SELF_FETCH` 在 `AI_DECISIONS.md` **0 命中**、`FurnaceStation` 相关行是 `:29`/`:463` 非 `:128`、可回收性读取点已漂移为 **44/36**、`recipe-graph.py --selftest` **实跑失败**且门禁 grep = 0、`docs/authz/flow.svg` **早已存在**且三份图**已接门禁**）。<br>⭐⭐ **本报告最重要的不是"错了 12 处"，是"错在哪一类"** —— **12 处里没有一条是推理错，全是同一类：把「我看到的」当成「项目的事实」**，三种子型：① **把"字段存在"当"值正确"**（照抄一个**没数过**的条数 18）② **把"方案存在"当"已落地"**（提案表里的 `PARAM_SET` 当成"今天可做"，**没去 `src/` 确认**）③ **把"我的环境"当"项目环境"**（附录写 `cd /workspace/Alice-mcbot`，那是 Coomi 沙盒路径）。**⇒ 缺的纪律只有一条：任何"数字"或"状态"都必须能指向一次当场执行的复算，不能指向"我记得"。**⚠️ **这是 `survey/12` 那条教训（"只扫技术名词就下结论是错的"）的第二次发作，只换了对象：上次是"有没有"，这次是"有多少/行不行"。**<br>★ **一条必须点明的口径差异**：`10/11` 说"无界"时我算的是**所有 `PathRequest` 工厂方法**；事实反转 —— ⭐ **逃生恰恰是唯一有界的**（`survivalEscape` 刻意收紧），⇒ 准确表述是「**不是逃生无界，是逃生是唯一有界的，其余全是无界的**」。`PathRequest.java:38-39` 的注释本身就是那条建议的**先例背书**。<br>★ **给主工作流的实用产出**：12 处里 **5 处**（#4/#5/#7/#10/#11）根因同一 —— **同一语义有多份副本，而勘测只看到一份**。⚠️ 补一个分诊文档没写全的点：`recipe-graph.py` 不只是"没挂门禁"，**它连自检都跑不起来**（缺原版数据目录）⇒ **"挂门禁"之前先得让它能跑，否则会做出一个永远 PASS 的橡皮图章门禁** —— **那正是 `survey/12 §3` 第四条纪律要防的东西**。<br>★ **对分诊文档 §4「6 个裁定」只回应能答的 2 条**（⑤ `NO_PROGRESS` 语义 / ④ 预算与并行），**其余 4 条（①②③⑥ 产品口径）不表态** —— 属于用户的地盘。<br>⚠️ **保留错误不改写**：按「只增不改」，`01`–`17` 一行都不动 —— **错误比正确更有信息量**（改写会让错误的推理链消失，下次还会犯）。<br>⚠️ **`survey/17` 未被分诊**（它在分诊之后推送），本报告只登记这一事实；并按 §1 教训**主动标注它最可能有两处同类风险**。 | `50cd547` |
| [`19-勘测侧复核回写-第二次-20260917.md`](19-勘测侧复核回写-第二次-20260917.md) | **勘测侧复核回写（第二次）** —— 对主线 `survey/17` **完整分诊（37 条）**及 **6 条「架构冲突」**的回应。**⚠️ 与 `18` 独立**（`18` 对第一次分诊的 12 处更正，本报告对第二次分诊）。<br>★ **6 条架构冲突逐条复算：5 条成立、1 条提出异议**。① `GoalDirector.tell` 确为 `private`（`:495`）且外部调用 **0** ✓；② **prompt 是唯一通路**（`GoalDirector:294-300`）✓；③ ⚠️ **比所述更强**：`GoalAction.parse` 只有 `(String reply, BotPlayer, CandidateMenu)`，**无任何来源标记** ⇒ "只读"确实无法构建成红 ✓；④ ⚠️ **比所述更强**：`PermissionGate.answer` 已改**包内可见**（`PermissionService.java:21` 自述"跨包直调**编译不过**"）⇒ ⭐ **这是编译期保证，不是约定** ✓；⑥ 视觉与桌面 AI 确实都缺"谁驱动的"这一维 ✓。<br>⚠️ **第 ⑤ 条我提出异议**：分诊说"加包**不必跳** `PROTOCOL_VERSION`"——**机制上对**（`SimpleChannel` 版本匹配是**字符串相等**，不扫包清单），**但"勘测高估成本"这个定性我不接受**：`17 §2.3` 说的是 **C2S 包**，而 C2S 的真相是「**旧客户端根本发不出来 ⇒ 该特性对它们静默不可用**」（不是"必须跳版本"）⇒ **这是需要显式登记的兼容性代价，不是可以无视的**。<br>⭐ **分诊点名要勘测员回答的一条**（裁决项 d「**一双眼睛还是 N 双眼睛**」）—— 本报告交付完整依据：**这不是取舍，是成本量级**（依据 `survey/10` 的串行调度结论）：一双 = **O(1)**，每 bot 一副 = **O(N)**；⚠️ 且后者**不只是贵，是结构上做不到** —— **`Camera` 是客户端单例** ⇒ "每 bot 一副"= **需要 N 个客户端进程**。**⇒ 建议一双**（且"借玩家的眼睛"本身就是陪伴行为）。<br>★ **对分诊队列第 8 条（`NO_PROGRESS` 指纹修正）补三条**：**a** 剔除失败计数后 `progressSummary()` 单独不变就会报警 ⇒ **阈值要重定，这属于"会改行为"**；**b** ⭐ **`failed` 涨是有价值信号，只是属"负面进展"——不要丢弃，拆成正向/负向两个信号**，否则"它在连败"又变成不可见；**c** 给了可红判据与反向对照。<br>⭐⭐ **本报告新增第 13 处自我更正**：`18` 里我把 `RecipeDump` ↔ `recipe-graph.py` 说成「**同一份数据的第二副本 / 已经漂移**」—— **不准确**。分诊队列第 1 条的结论更准：**两者服务不同输入域**（Java=运行时**类型 id**，Python=数据包**序列化器 id**），关系是**子集**；`crafting`/`smithing` 只在 Java 侧出现是**预期** ⇒ **`11` vs `9` 不是 bug，是设计**。✅ 已独立复跑 `python3 tools/station-mapping.py --check` = **PASS**（Java=11/Python=9），门禁 `check-station-mapping` 已挂 `check-all.sh:130`。⚠️ **这一处又落回 `18 §1` 的同一子型（把"我看到的"当成"项目的事实"）：****看到两个相似的 Map 就假设是同一份数据，而没确认它们的输入域。** | `e449d99` |
| [`20-保护区与紧急提权-20260917.md`](20-保护区与紧急提权-20260917.md) | **保护区（父）→ 安全区（子）** + **紧急提权** + **世界修改记账的边界**。<br>★ 核心：`WorldModLedger` 原文自证「只记放置，挖无法配对恢复」⇒ 「提权只放搭路」是**技术上正确**而非保守。<br>★ 核实：`survivalEscape` 今天**已含** `BREAK_*` ⇒ 该提案是**收紧**；「免批准但显式」= 现成的 `Policy.NOTIFY`。<br>★ Baritone 源码实测（`cabaletta/baritone` 1.20.1）：**没有分层寻路** —— 用「7 系数竞速 + 早退」+ `Goal` 层次的粗目标；且其 yolo 策略**不可照搬**（它不还债，Alice 要还）。<br>★ 逃生三形态（局部 / 放弃任务 / 就地固守）+ 8 格预算的正确性说明。<br>⚠️ 含 §0 **优先级分层表**（A 可立刻做 / B 待拍板 / C 仅登记 / D 设计冲突）—— 审计前先读 §0。<br>❌ 含 3 条勘测侧自我更正（`drops_left` 不判失败、`List.of()` 是 UUID 白名单非物品过滤、逃生预算收回质疑） | `cddc90c` |
| [`21-勘测侧更正回写-第三次-20260918.md`](21-勘测侧更正回写-第三次-20260918.md) | **勘测侧更正（第三次）** —— ⭐ **`survey/20 §2.3` 是实质性遗漏**：我断言「项目今天没有统一区域概念」，**实际早已存在**（`protection/SafeZoneData` 区域+方块/标签黑名单 · `BlockBreakSafety.hasBlockEntity` 不可清障=专治模组机器 · `WritePolicyMatrix.Zone` 两层 `EXTERNAL/WORKSPACE` · `CapabilityGate.ZONE_*` · 4 处候选源过滤）。<br>★ **新增第 4 类子型**：**「把『我没搜到的包』当『项目没有』」** —— 我只在 `decision/`+`ledger/` 里找，**没进 `protection/`**（那正是它的包名）。<br>★ 连带更正 B-1：`D-207 ①` 早已拍「**保护区不是第三层**、两概念不该挤同一列」⇒ 主线读法（交集必需）正确，本报告 §1.4 澄清。<br>★ 行号更正：`PathRequest.java:34-35` → **`:38-39`**；`survey/03` 的 `InterfaceScanner.java:167-172` **越界**（该文件 162 行）。<br>⭐ **纪律**：断言「项目没有 X」前，先 `ls` 顶层包 + `grep` 全仓关键词。<br>★ 含主线对 §0 分层表的异议（**A 层顺序错**：无 D-2 定义时 `RegionCap` 是死代码）⇒ **勘测侧接受** | `0ef677a` |
| [`22-挖矿形态与风险守卫与垂直移动-20260918.md`](22-挖矿形态与风险守卫与垂直移动-20260918.md) | **补 `20` 漏掉的两个板块**（挖矿形态 / 通道挖掘）+ **风险模式与守卫** + **垂直移动 Baritone 对照**。<br>★ 挖矿 vs 低级挖掘差三件：**目标来源**（自己扫 `SCAN_RADIUS=24` 只找已暴露 vs 调用方给）/ **作业形态** / **判据**（quota+产物入包）。<br>★ ⚠️ 收集**已经是「挖完一起捡」**（`minedCount>=quota` 才 `startCollect`）⇒ 要拆的是**归属耦合**（`origin=firstMined`）；`List.of()` 是 **UUID 白名单**，非物品过滤。<br>★ ⭐ `WorldModLedger` **原文自证**「只记放置，破坏型修改无法配对恢复」⇒ **「挖」没有账本兜底**（⚠️ 但**推不出"不给挖"**，见 §9）。<br>★ ⭐⭐ **`§9` 更正（重点）**：主线已裁定 **提权「搭和挖」两种权限一起给**（`D-305` ④「挖保留 + 小预算 + 套既有闸门」）——**不靠"禁用挖"控风险，靠「挖掘黑名单 + 闸门」控风险**（黑名单**已有**：`SafeZoneData` 方块/标签黑名单 + `hasBlockEntity` 禁清障模组机器 + 高代价方块）。⚠️ 勘测侧原写"只放搭路、不放挖掘"是**错的推论**，且一度误记为"搭和**建**"。<br>★ ⭐ **作者裁定：守卫线冻结**（保持现状；必须开启的 vs 可开关的以后区分；**实测**影响效率/安全时才做可调试接口）。<br>★ ⚠️ 核实：**高风险模式 = 守卫全关 + 不记危险成本 = 今天的默认** ⇒ 提不了效率；`HAZARD_ADJACENCY_PENALTY=20.0` 是**固定值不是倍率** ⇒ 长距离被稀释。<br>★ ⚠️ **Baritone 不是「挖靠独立边挖边走」** —— 它**每个 Movement 自带**挖（`positionsToBreak`）+ 放（`costOfPlacingAt`）；**Alice 反而更对称**，理由是 `changesWorld()` 要当信封分档的**唯一静态口径**。<br>★ 真缺口 = **斜向上升那一格**（`PLACE_STEP` 的 `dy∈{0,-1}`，`PILLAR` 只有垂直）⇒ ⚠️ **别动 `ASCEND`**（会打穿信封）。<br>⚠️ 含 **7 条勘测侧自我更正** + 「能保证到达+返回吗」的**三处空档**（安全终点缺坐标 / 扫描未验 / 无可测量判据） | `0ef677a` |
| [`23-决策层提案评价与方法论答复-20260920.md`](23-%E5%86%B3%E7%AD%96%E5%B1%82%E6%8F%90%E6%A1%88%E8%AF%84%E4%BB%B7%E4%B8%8E%E6%96%B9%E6%B3%95%E8%AE%BA%E7%AD%94%E5%A4%8D-20260920.md) | 对 LLM 决策层任务队列提案的评价（6 条做好 / 3 处隐藏坑：刹车≠成功、终态级陈旧无判据、升级链无上限）+ 对「经验能否加速高级任务」的答复（判据层能带走 / 领域层带不走） | `a31ecbc` |
| [`24-搜索预算与水平探索设想-20260921.md`](24-%E6%90%9C%E7%B4%A2%E9%A2%84%E7%AE%97%E4%B8%8E%E6%B0%B4%E5%B9%B3%E6%8E%A2%E7%B4%A2%E8%AE%BE%E6%83%B3-20260921.md) | 搜索超预算定性（共享资源记账缺失，非"搜索慢"）+ 两条建议（每 tick 总预算 / 动作与守卫必须同时落地）+ Baritone 对照（场景不同，无现成答案）+ 三个离线可行性问题 + 水平探索＝目标预筛（与预算耦合不可控、三条限制）+ 执行顺序应先拆返回码 | `630ed6e` |
| [`25-挖矿三路线-跟随探洞鱼骨-20260921.md`](25-%E6%8C%96%E7%9F%BF%E4%B8%89%E8%B7%AF%E7%BA%BF-%E8%B7%9F%E9%9A%8F%E6%8E%A2%E6%B4%9E%E9%B1%BC%E9%AA%A8-20260921.md) | 三条备选挖矿路线定性（跟随＝缩小问题 / 探洞＝换找法 / 鱼骨＝取消搜索）+ 鱼骨是唯一直接击中卡顿根因者 + 四个待拍板项（玩家可达性判据 / 追玩家的度 / 起点合法性 / 推进顺序）+ ⚠️ 一处口径更正（世界修改记账只服务于保护区及其子类） | `67ba3ab` |
| [`26-车万女仆任务形态勘查与锚点模式-20260921.md`](26-%E8%BD%A6%E4%B8%87%E5%A5%B3%E4%BB%86%E4%BB%BB%E5%8A%A1%E5%BD%A2%E6%80%81%E5%8B%98%E6%9F%A5%E4%B8%8E%E9%94%9A%E7%82%B9%E6%A8%A1%E5%BC%8F-20260921.md) | 车万女仆外部源码勘查（任务抽象形状 / 玩家为锚+半径10 现成 / VERTICAL_SEARCH_RANGE=4 / MaidMineTask 是空壳）+ 挖矿缺席的三条前提不成立（勘测侧推断）+ 锚点＝一套任务的取值 + 凝矿兰可能是第一个真正落点 + 三处待查 | `6d63722` |
| [`27-当前核心问题与依赖关系图-20260921.md`](27-%E5%BD%93%E5%89%8D%E6%A0%B8%E5%BF%83%E9%97%AE%E9%A2%98%E4%B8%8E%E4%BE%9D%E8%B5%96%E5%85%B3%E7%B3%BB%E5%9B%BE-20260921.md) | 当前状态收束：三条线（寻路内核未收口／维生待定方向／三挖矿路线未开工）+ 核心问题清单（按依赖排序）+ B 实验把"深矿能不能到"变成事实（能到，不是没路）+ 88 段绕远差最后一步验证 + 5 条建议（全部无裁决） | `018fa55` |
| [`28-内核准入标准与规划师提案-20260922.md`](28-%E5%86%85%E6%A0%B8%E5%87%86%E5%85%A5%E6%A0%87%E5%87%86%E4%B8%8E%E8%A7%84%E5%88%92%E5%B8%88%E6%8F%90%E6%A1%88-20260922.md) | ⭐ **内核准入标准**（`D-394` 的根因不是「漏了一个检查」，是**没有一个地方规定检查该放哪**）+ ⭐ **`K4-P1` 门禁结构上抓不到谓词漂移** —— `tools/kernel-predicates.py` 的 `rule_k4()` 只是**字符串包含检查**（`if f「MovementHelper.{predicate}(」 not in body`）⇒ 只看**提没提到**、**不看两侧是否等价** ⇒ `D-394` **静默 6 天**；⚠️ 且方向正是它没设防的那个：注释自称防「执行比内核宽松」，而 `D-394` 是**规划比执行宽松** ⇒ **确定性死循环**）+ **架构三问答复**（更严格对齐 Baritone？抽内核检查？执行层缺统一标准？⇒ ⭐ **Baritone 的准入就在 `static cost()` 里、返回 `COST_INF`** ⇒ **能不能做**与**值多少钱**是**同一个函数的返回值** ⇒ 结构上不可能漂移 ⇒ **要对齐的是结构不是检查条数**；应抽的是「①能力类条件」进**边的生成条件**、「②时序类条件」留执行侧**但必须可识别**）+ ⭐⭐ **节点 E：不建议增加「规划师」角色** —— 四项职责里**框架／需求整理两项已被现有机制覆盖**（`minimal-implementation-planning` skill + `OPEN_ITEMS_LEDGER` 2071 行 + 40+ 设计文档 + 21 个 skill），**性能保证／复杂度上限两项是真空缺**，但**需要的是尺子不是人**；⭐ **全仓库唯一一处机器化性能保证** = `tools/kernel-predicates.py:26 SEARCH_BUDGET_CEILING_MILLIS=60`（成立三条件：**有一个数**／**门禁读源码**／**有反向对照**）⇒ 建议扩成**两把尺子**（**tick 负载预算尺**／**准入来源单一尺**）+ **逐条回答昨日遗留的 6 个待答问题**（含与已废弃「规划员」的本质区别判据：**产不出门禁 ⇒ 就是旧规划员改名**）。✅ 含一条**勘测侧自我更正**（第一版把「11 个执行工厂 vs 10 个 `MovementType`」写成可能有缺口 —— **实为 10 对 10 一一对应**，第 11 个是接口）。⚠️ **性质：勘测侧建议，无裁决**；§8 列出 5 条**待查**（含 3 处降级逻辑是否也静默）。 | `4f1d840` |
| [`29-讨论记录-20260923.md`](29-讨论记录-20260923.md) | ⭐ **讨论记录（2026-09-23）**:**性质 = 不是勘测报告，是讨论记录**（含用户裁定 / 勘测侧观点 / 已核实事实 / 待实测项 / 待裁定项；**不是实现授权，不构成待办**）。<br>⭐⭐ **本轮最重要的发现是一条待实测的推断**：**击杀产物的归属缺口** —— `ScopeBuffer.pendingEntry` 的两条配对路径（`matchBreakSource` / `matchIndirectOrigin`）**都只遍历 `recentBreaks`**（只认 `BlockEvent.BreakEvent`）⇒ 击杀不产生破坏事件 ⇒ **不登记** ⇒ `DropPolicy.effectiveProvenance` 回落 `FOREIGN` ⇒ 默认策略 `ASK` ⇒ `mayPickUpPassively` **直接拦下** ⇒ **杀了拿不到、且静默**（与 `ScopeBuffer:98` 自述的那条失效同源；接缝 `registerAsOurs` 现成）。<br>其余：**怪物层全仓空白**（`Monster/hostile/threat` 仅 1 处 `hostile` 局部变量；`attack` 唯一真实命中是**破坏方块**；`BotPlayer:272` 自述不用 Mob AI）· **现有风险盘点**（`RecoverabilityPolicy` 今天**只有 `FALL` 一条真能拒**；`RiskSwitches` 3 开关；三档在代码里不存在）· **三风险重分类**（液体=危险 / ⭐**矿洞=承诺违约**（可判定、复用可回收性） / 怪物=全域）· **反射层 vs 规划层**（⭐ Mob AI 快的原因是**它不需要保证** ⇒ 建议 L1 反射 / L2 规划两层逃生，保险物品=**买回"允许失败"**）· **猎杀封闭集 = 显式枚举「掉肉 + 无害」**（+ 双向防漂移门禁 + 反向对照；⚠️ 项目里**食物/饥饿零命中**）· **限制 LLM**（⭐ 队列长度不是好办法、该用预算制；循环检测须**结构化参数**；⭐ **最强的限制是"它压根看不见"**；**能力清单从代码生成**须先于记忆库）· **层1 外部调研**（Mineflayer 7.5k / Voyager 7.2k（⚠️ **创造+和平** ⇒ 指标不可比）/ Mindcraft 5.8k / Cradle / Sid 1.4k / GPT-6 Astra 141h + 车万女仆（**挖矿结构性缺席**）/ MineColonies / 凝矿兰）· **优先序按"离代码的距离"排（P0–P4）** + **§4 待实测 8 条** + **§5 待裁定 Q-1…Q-22**。 | `98bd354` |
| [`30-诊断与下一步表-20260923.md`](30-%E8%AF%8A%E6%96%AD%E4%B8%8E%E4%B8%8B%E4%B8%80%E6%AD%A5%E8%A1%A8-20260923.md) | ⭐ **诊断与「下一步表」**（与 `29` 配套：`29` 是讨论记录，本文件是结论与建议）。**性质：诊断 + 方法建议 + 候选清单，不是待办、不是排期、不是实现授权。**<br>⭐⭐ **核心结论：卡住的原因不是"能力不够"或"方法不对"，是「方法太贵」** ⇒ 要的是**把已有方法的单位成本降一个数量级，不是加新方法**；**本文没有一条建议要求新能力**。<br>**量化诊断（可复算）**：`task/` **51,556 行 = 49.8%**（`survey/15` 记 41% ⇒ 涨了），其中**夹具 84/181 文件 = 46%**；而"内核" `pathing/` 仅 **8.1%** ⇒ **大部分工程投在"证明内核对了"，不是"让内核更对"**；`docs/` **46,357 行**（09-16 是 33,476 ⇒ **6 天 +38%**，≈2,147 行/天）= "台账自我生成"的量化证据。<br>⭐ **办法：把「优先级队列」换成「下一步表」** —— **Makefile，不是 TODO list**：顺序来自**依赖求值**而非判断 ⇒ **不能重排**（因为无可重排之物）；每行五列（`goal`/`precondition`/`scope`/`exit`/`unblocks`）**全部是事实**；⭐ **范围限制的具象化 = `D-332` 的档位语言推广到"改动"上**（允许改的集合 / 预算 / 红线）⇒ **冲突 ⟺ scope 相交**，可计算；⭐ 好处：**优先级排序永远选不出"无前置、高杠杆"的小活**（正是 `lumber_job`），求值能。<br>**三条杠杆**：① **CORE 反馈时间**（A 修 `lumber_job`：⭐ 已定位 `idempotent=false` 的高置信根因 = **`LumberJob` 缺 `MineTask` 那个终态闩锁**（`D-175`）；B **逐步缓存**：收益更大且**不需等 A**，但**必须先验步间独立性**，否则假绿）② **修假绿夹具**（**假绿比红贵**；建议把"能红"做成断言）③ **武器化已有结论**（底线不许进配置面 / 必选·可选分界；模板 = 已落地的 `JobKindContract` 与 `check-far-goal-usage.sh`）。<br>**§5 主工作流可马上做**（A1–A4 / B1–B3，各带判据·成本·风险）+ **§5.1 明确不建议现在做的**（猎杀功能本体须等归属、怪物线须等保险定位、队列须先有表、记忆库须排在能力清单之后）。 | `d6db852` |
| [`31-云端迁移方案-20260923.md`](31-%E4%BA%91%E7%AB%AF%E8%BF%81%E7%A7%BB%E6%96%B9%E6%A1%88-20260923.md) | ⭐ **云端迁移方案 + 部署手册**（基础设施；**不是待办、不是实现授权、不改任何契约**）。**一句话：控制面（DSH agent + 编译）上云，测试面（无头电池 + 真人客户端）留本地** ⇒ 云端**从 16 G 降到 8 G**，且**测试面一行都不用改**。<br>**分期**：零期 Codespaces 免费验证（✅ 官方文档核过：最小 **2 核 8 G 32 G** 盘，免费 **120 core-hours/月** = 2 核 60 h，**只在实际运行时计费**，⭐ **端口转发天然解决多设备**；⚠️ 但它是"开发工作台不是常驻服务器"，32 G 盘对 gradle 偏紧）→ 一期 DSH 上云 → 二期测试面（**先别做**）→ 三期（⭐ **二期做完三期可能就不需要**）。<br>**一期部署手册**（§4，**每步带判据**）：非 root + **Node ≥22.15**（硬要求，`dsh-session-log.mjs` 用 `zlib.zstdDecompressSync`）+ `npm i -g @deepseek-ai/dsh` + ⭐ **迁 `~/.dsh/`（`settings.yaml` profile + `sessions/`，最容易漏）** + systemd 保活 + **Caddy 反代加鉴权**（⚠️ 别裸暴露 3081）+ 仓库/Deploy Key + 编译验证。<br>⭐ **工具链已核**：DSH = `@deepseek-ai/dsh`（npm 公开包，`bin: dsh → lib/bin.js`，latest `0.1.5-rc.2`）；`dsh-web-app` 自述含 **frontend dist serving** ⇒ **`dsh web` = 服务端跑 agent、浏览器只是壳** ⇒ 多设备无缝切换**结构上成立**（⚠️ 但**会话是单写者** ⇒ 纪律：**一次只有一个设备写入**，并给出并发验证法）。<br>⭐ **推送三选一**（✅ 本机实测背书）：**SSH 443**（`ssh.github.com` 握手成功）· HTTPS+PAT+显式代理 · Gitee 中转 —— ⚠️ **镜像只解决"读"不解决"写"**。<br>⚠️ **二期唯一真障碍 = 世界母本必须唯一**（脚本自述 5 个 CORE 步依赖该世界绝对坐标地形，换世界 ⇒ 假红）；**云⇄本地用 git tag 当通道**（本地不用公网 IP、不用穿透）；`dsh-webhook` 是**反方向**，用不了。<br>**§8 安全清单**（含 ⚠️ **别在 Codespaces 装 GitHub 定制智能体** —— `AGENTS.md` 所有 harness 都认，会与主工作流撞车）· **§10 八条验收判据** · **§11 八条待定/未核实**（含 `dsh web` bind 地址/`engines` 为空/并发语义/首次 decompile 内存峰值）。⚠️ **§4 全部步骤未在真机跑过**。 | `2f10f66` |
| [`32-内核关门后路线勘测-20260924.md`](32-%E5%86%85%E6%A0%B8%E5%85%B3%E9%97%A8%E5%90%8E%E8%B7%AF%E7%BA%BF%E5%8B%98%E6%B5%8B-20260924.md) | ⭐ **内核关门后的路线勘测**（`D-430` 用户「同意关门」⇒「**去找勘测员聊计划，根据勘测报告再决定排期**」⇒ **本文就是那份排期输入**）。**性质：建议，不是排期、不是待办、不是实现授权。** 基线 `3093e31`。<br>**§0 结论**：**`J-1`（鱼骨切片 1）开工前提全部成立、可立刻开工**；但 **`J-0.2` 的 `P4′` 必须先裁**，而**读数指向的答案是「选项 1（`P4″`）」而不是收紧毫秒轴**。<br>⭐⭐ **四条 `§11-J` 未覆盖的发现**：<br>**① `P4′` 是假杠杆 / `P4″` 是真杠杆（两条独立读数互证）** —— 台架（`D-429`）说生产形态最坏单 tick **402 ms**，**够不上原版 2000 ms 阈值** ⇒ 原判据"收到 ≤60 ⇒ 告警消失"**算术上不成立**；真机根因分析说告警来自**mine 循环 ≈198 ms/轮**、⭐ 且 `visited=1` ⇒ **耗时不在分片，在"每轮附带的那次规划/搜索"**。⇒ 毫秒轴管"一 tick 塞几次搜索"，`P4″` 管"单次搜索多长" ⇒ **只有后者直击**（`D-429 §三.4` 自己已写这句）。<br>**② ⭐ 鱼骨顺带治"4 倍预算"这个真病灶（⚠️ 推断）** —— ⚠️ 先修正一个误读：鱼骨**不是零搜索**，是「**每格 1 次、距离恒 1 格（节点数 O(1)）**」（`§5` 方案 A）；判据 **C3** 正是"搜索规模恒定 + 零 `SEARCH_LIMIT`" ⇒ **它顺带验证卡顿根因分析的预测②**（但**不修** `MineJob` 现有的 198 ms/轮，是"新路径没这个病"）。⚠️ `§5` 自己把"极廉价"标为**风险**⇒ 建议切片 1 **加每轮规划耗时读数**（`PathingStats` 现成，零成本）。<br>**③ ⚠️「搬运」这条线不在 `§11` 任何一节里**（`grep` 复算：命中全是 `R-2` 模块搬家与 `TransferTask` 历史登记）；而 `survey/03` 已核实**整合包里矿是机器产的** ⇒ 层1 真缺口可能是"把机器产出的东西搬回来"，而 A1.1 边界很窄（单箱/同维度/已加载/无 NBT）⇒ **建议登记为 `J` 候补项，不立刻做**。<br>**④ ⭐ 客户端轮次必须合并观测** —— `J-1.3`（鱼骨验收）与 `P4′` 的决定性实验（GC 日志 + 空闲基线）都要客户端轮 ⇒ **同一次会话可同时满足**；且理由不只是省时间：**同会话内对照比跨会话可信**（同 `D-429` 选"同轮跑三配置"之理）。<br>**§3 逐项核对**：`J-0.1 P2c` = `D-430` 已按"先不改"归档（**无需再裁**）· `J-1.1/1.2` 前提成立 ⚠️ 但**落物坑会咬 C2**（`PL-1` 实测"每格收集 ≈130 tick，落物掉到 y=67 白等"）· `J-1.4` 两处前提更正（**跟随判据主语错**：玩家可达 ≠ bot 可达；**探洞"四判据全可离线"存疑**）· `J-2.1` = `D-419` 自认"未达到"的那半句 · `J-3.1` 大半已完成（用户已全搬）。<br>**§4.2 ⚠️ 成本账：`§11-J` 漏了云端额度** —— Codespaces 官方机制：**终端有输出也会重置空闲超时**⇒「让 agent 一直做」与「按运行时长计费」**直接冲突**；建议编译/门禁挪 CI（Actions 3K 分钟，不占额度）+ 超时改 5 分钟。<br>**§5 待裁定七条**（A `P4′`→选 `P4″` / B 鱼骨开工 / C 落物设计 / D 合并采集 / E 搬运登记 / F `Q-1` 在 `J-2` 前裁 / G 云端成本）· **§6 诚实边界**（4 条推断全部点名含验法；明确不主张新能力、不改内核）。 | `3093e31` |
| [`33-回迁需求与时间安排-20260924.md`](33-%E5%9B%9E%E8%BF%81%E9%9C%80%E6%B1%82%E4%B8%8E%E6%97%B6%E9%97%B4%E5%AE%89%E6%8E%92-20260924.md) | ⭐ **回迁需求与时间安排**（**转达用户的安排与要求给主工作流**；不是排期、不是待办、不是授权）。**来历**：用户 2026-09-24「你只负责做一件件事，**让主工作流知道我的安排，让他做回迁的准备，最好带着会话上下文回迁**」。<br>**§1 安排**（用户口述）：**10/2 回家、约 10/7 回校**；家里有笔记本 ⇒ 可做本地客户端测试；⭐ **本地管家 `tools/client-agent/` 就是为此写的**；这次上云是**提前适应**那趟 ⇒ 云端的真实用途不是"永久搬走"。<br>**§2 要求**：① 做回迁准备 ② ⭐ **带着会话上下文回迁**。<br>⭐⭐ **§3 核心：把"带会话上下文"拆成两个层次**（技术难度差一个数量级）—— **(a) 本机 DSH 能继续同一条会话**：需**版本相同 + slug 相同**，而现状**两者都不满足**（云端 `rc.3` / 本机 `rc.1`；slug 也不同）⇒ `§13` 已判"不建议直接采纳"；成本≈1 轮，价值=锦上添花。**(b) 下一个主工作流"知道发生过什么"**：只需**可读文本** ⇒ ✅ **工具已在仓库里**（`tools/dsh-session-log.mjs`）⇒ **在云端用"生成它的那一代"工具解码成 JSONL/文本带回**（纯文本**不依赖版本**）。⚠️ 该工具要 **Node ≥22.15**，且**解码器有版本世代，升代后会响亮失效** ⇒ 必须用对版本解。⚠️ **与 `§13` 不冲突**：`§13` 讲的是 (a)（结论正确），本文补的是 (b) —— **归档不是白归档，当"上下文交接材料"就对了**。<br>**§4 时序**（⚠️ 全部要赶在限额耗尽前）：`cloud-rollback.sh` **要 SSH 上去才能跑** ⇒ 耗尽后可能就跑不了；⚠️ **计费周期重置日需用户自己看** `github/settings/billing`（若正好 10/1 ⇒ 这几天别烧额度）。<br>**§5 ⭐ 更正 `survey/32` 一条**：`P4′` 的"需要客户端数据采集"**已被主工作流否决** ⇒ `32 §2 发现四` 的那半作废，**客户端轮只需做鱼骨验收**；⚠️ 但三条不受影响（同会话对照更可信 / **落物坑会咬鱼骨 C2** / 客户端轮永远是稀缺资源）。<br>**§6 登记**：`tools/client-agent/` **未进 `§11` 队列**（建议补 `J-1.5`，免得再被误读成"自己找活"）；信箱在云端 ⇒ 实例停则"云端指挥"断，⚠️ **但管家支持脱机形态**（`client-agent.cmd "任务文本"`）。 | `a2e5b05` |
| [`34-通道施工器草案评审-20260925.md`](34-%E9%80%9A%E9%81%93%E6%96%BD%E5%B7%A5%E5%99%A8%E8%8D%89%E6%A1%88%E8%AF%84%E5%AE%A1-20260925.md) | ⭐ **通道施工器草案评审**（回答草案 `§9` 的六个问题 + 独立评审；**不是授权、不是排期、不是待办**）。基线 `edeeab9`（草案 = `docs/plans/2026-09-25-通道施工器草案.md`）。<br>**§0 结论**：**诊断对（四个代码锚点逐字复核成立）、分层方向对，但把三件独立的事捆成了一次大重构** —— 其中两件是**便宜修复**且正好对应你的两个可观察目标。⭐ **建议顺序 `P0 → P0.5 → P1 → L2 →（等第二个消费者）L3`**。<br>**§1 复核的四个锚点**（全部成立）：模式 A 接近用 `PathRequest.of`（`MiningPlanner:367` 逐字）· 模式 B 用 `miningApproach`（`:303/:391`）⇒ **同一类里两套能力** · 跳过判据 = `bodyPassable`（`FishboneJob:527`）· 「任何理由都放弃支巷」是已知简化且**预登记条件已被真机触发**（`:540-543`）。<br>⭐ **§1.1 草案没引但更关键的事实**：`FishboneJob:647-649` 注释自述「**第一版用 `bodyPassable` 实测当场红**」（顶棚矿毒死头位格）⇒ **同一个文件里对"这格算不算通"有两个判据，而 A1 是第三次撞** ⇒ **A1 的真根因不是"顺序错"，是"判据有两个来源"**。<br>⭐ **§2.1 A2 的说服力被低估**：同一位置同一 tick，鱼骨 `SPUR_RETURN status=REACHED` 而挖掘器 `no_reachable_standing_point` ⇒ **同一 bot 同一瞬间两个组件给出相反答案** = **分层缺失的铁证**（能力集不同，而"能不能到"本应同一个问题）。<br>**§3 建议顺序**：**P0** `§1.3` 失败理由分瞬时/永久（**标准动作，单独就能救回大部分被弃单元**）· **P0.5** A2 改成「**能力随操作走**」（调用方注入 capability，默认仍 `of`）——⚠️ **不同意草案 S0 的"临时放宽"**（全局改默认、非终态）· **P1** A1 判据唯一化（一个方法即可，不需要 L2/L3）· **P2** `L2` **现在提**（几乎免费）/ `L3` **等第二个消费者**（草案 §7 自述 S4 才有）。<br>**§4 逐条答六问**：Q1 一半一半（B 能吃 A2 吃不了 A1；且形态要改成"注入"）· Q2 规模偏乐观（≈1–1.5 轮，**吃时间的是不变式与夹具**）· Q3 分层常见坑**三条**（含 ⭐ **"完成后没"两个判据 = 三项目共同教训，本项目已踩两次**）· Q4 **长期保留枚举**（加目标类型是内核改动，撞 `D-430` 门线）· Q5 **不借 `road/RoadPlan.CellKind`，新起词表**（+ 建议登记 `road/` 退役候选）· ⭐ Q6 **parkour-place 建议不做**（三条理由：鱼骨不需要 / 产品答案已在 §10.3 / 撞 `D-024`），⚠️ 且指出草案的统计口径是**伪判据**（"跨>1格占多少"不可测）⇒ **改成"放弃理由里横向缺格有几次"**（与 S3 失败升档天然合并）。<br>**§5 外部先例**（Baritone 补一条 `Settings.java:78/362/369` 能力挂 process；Litematica/MineColonies ⚠️**凭印象未核实**）· **§6 ⭐ `I5` 不用新造"保护集合"** —— 元凶是 Alice **自己的组件**（追簇 + `collect-drops` 的 `PATH_ACCESS`）⇒ 建议做成 `ZoneAuthority` 的**又一个输入源**（已核实其**五处消费点**）⚠️ 但粒度不同（区域级 vs 格集合）需一次设计 · **§7 红线**：草案"L3 四条禁令"是**散文**，必须做成**能红的门禁**（否则 `D-036` 的"不许造第二内核"没有牙齿）· **§8 诚实边界**（5 条推断全部点名含验法）· **§9 待你拍四条**（顺序 / L3 时机 / parkour / `road/` 退役）。 | `edeeab9` |
| [`35-寻路挖掘架构专项与站位选优审计-20260926.md`](35-%E5%AF%BB%E8%B7%AF%E6%8C%96%E6%8E%98%E6%9E%B6%E6%9E%84%E4%B8%93%E9%A1%B9%E4%B8%8E%E7%AB%99%E4%BD%8D%E9%80%89%E4%BC%98%E5%AE%A1%E8%AE%A1-20260926.md) | ⭐⭐ **「寻路—挖掘」历史遗留专项**：站位选优审计 · 模式 A/B 定位 · **工作排期表整理建议**。基线 `e43ac1f`（**不是授权、不是排期、不是待办**；⚠️ 用户说的"鱼骨新方案"**不在本文范围**）。<br>**§0 结论**：**架构路线没错（`D-066` 的 LOS 硬前提 + `D-076` 授权面是对的，`A*` 结构上给不了），错的是粒度** —— 三种性质不同的问题被塞进一个 `MiningPlanner`，其中最贵的模式 B **本质是寻路问题、被搬到挖掘层重实现了一遍**。⭐ **模式 B 架构上确实冗余（用户判对）；A 应留下并变便宜，B 应交出去。** ⚠️ **但对"正在做的鱼骨"，这次改动只能带走 1.5 条** —— 因为**鱼骨根本不走模式 B**（`FishboneJob:231` ⇒ `standableOnly=true` ⇒ `MiningPlanner:186` A 一失败就 `return`）。<br>**§2 「额度」= 三样东西**（23 个 `budget` 标识符）：① 主线程性能闸（不能放）② 世界写入/红线（不能让）③ **完成度上限（策略，可以放）** ⇒ 用户要放的是 ③，且**授权面与额度面在代码里本就是两个东西**（不必动红线）。<br>**§3 沙砾：用户判对** —— Baritone `coalesce()` 第一行 `assumeVerticalShaftMine` = **改站位形态、不是禁挖**；全部沾重力方块处仅 4 处，唯一不可替代 = `Movement.java:157-160`（在飞 `FallingBlockEntity` ⇒ 暂停挖掘）。Alice `pathing/` 零命中；`PathSession.currentTargetStillValid()` 只管"段的目标"**不管"正在挖的格"** ⇒ 与 `D-447`(F1) 同族：**F1 是空间维度判据错位，沙砾是时间维度判据错位**。<br>**§4 站位选优审计（核心，全实查）**：① 鱼骨**已经在用固定流程**（`FishboneJob:231`，但只是参数装的，`CURRENT` 失败仍走 `selectBest`）· ② **模式 B 只有一个生产消费者**（`MineJob:555`；`BotManager:2157` 自述是开发/测试入口）· ③ 成本结构（`MiningPlanner` 631 行：A 全枚举 **507 格** + `exactTopK` 4–10 次全预算 A\*；B 固定 13 格 + `MAX_APPROACH_PLANS=3`）· ④ **Baritone 的全部是 `GoalComposite` + 一句 `PathingCommand(goal,…)`**（本轮实拉 1.20.1 源码核过；`GoalThreeBlocks` 还是它的私有内部类）· ⑤ **诊断：模式划分对、粒度错**（三种问题塞进一类：(a) 就地能不能挖 (b) 附近有没有站位 ⭐(c) **怎么挖过去 = 寻路问题**；(c) 搬家才是"噼里啪啦一大堆"的源头）· ⑥ **追簇爆炸根因**：追簇时站位连续变化，而站位选优是"为单个静态目标设计的重武器"。<br>**§5 归因码：`做不了` 与 `不让做` 同形**（用户原话点到的要害）。⭐⭐ **本轮钉死那一行**：`MiningPlanner:182-190` 在 `standableOnly=true` 时把**除 `search_incomplete` 外的一切理由改写成** `no_reachable_standing_point` ⇒ **吞掉 `no_valid_standing_point`（`:255`，= 缺地板/被包住）**。完整因果链 = `D-066` 把 LOS 改成硬前提（它自己登记的后果见 `AI_DECISIONS.md:1093`）⇒ `no_valid_standing_point` 变垃圾桶 ⇒ `:190` 再改写 ⇒ **归因粒度两级丢失，根在 `D-066`（而 `D-066` 是正确的裁定，只是代价没被对冲）**。<br>**§6 「啥任务有优势」判定表**：三条判据（①"能挖到"过滤非平凡 ②接近路径要写世界 ③≥2 个方案且成本差异大）⇒ ⭐ **只有"目标被方块包住、需判断从哪个方向进去最便宜"这一种任务真的需要它**；其余消费面只要它顺带提供的一两个廉价子答案，**却全都付了 507 格的钱**。<br>**§7 「失败/超预算后是否退化成 Baritone 模式」——设计文档 `Q5′` 至今标着"待评估"**。⭐ **`退化` 底下是三件不同的事**：模式 B（另一套候选）/ 兜底（1 次搜索）/ **真·Baritone（Goal 化，Alice 不存在，`K2`）** ⇒ **现在的「A 无解→B」，B 不是 Baritone 模式，是同一件重武器的第二套弹药**。且真机实测 13/13 全 `!reached()` ⇒ **同一目标不同站位可达性高度相关** ⇒ "换一批候选再算"是**成本放大器**。建议顺序：**① CURRENT → ② 目标级一次搜索 → ③ 站位枚举**（单向不可逆）。<br>**§8 对用户判断的核对**：✅ **模式 B 架构冗余判对**；⚠️ **修正一：「成本特别大」已过期**（2.4 s 是 B **截断前**，截断后 B 只跑 3 次而 A 跑 4–10 次）；⭐⭐ **修正二（本轮新发现）：模式 B 没有 LOS 门**（`selectBestApproach:455-460` **只存不算**，无 `if (!los.isClear()) continue;`）⇒ **"挖过去 → 到了才发现摸不到"**（运行期 `LINE_OF_SIGHT_BLOCKED`）⇒ 清障 ⇒ 重规划 ⇒ **先付挖路钱、再付清障钱、最后付重算钱**；且 `MiningPlan.isExecutable()` **全仓只被日志用**（`MineTask:930`）⇒ 不构成门。⭐ **A 的贵在"枚举"（可优化），B 的贵在"事后"（设计漏的）**；✅ **模式 A 的优势判对但需精修**（不是"不考虑可达性"，是**顺序**：先可触及性筛、再可达性排）；✅ **基地任务判对**，且 ⭐ `LineOfSightChecker.samples` 的 **6 个面心已含槽位交互要的原语**（`successfulSample` 编码了"摸到哪个面"，只是没暴露成 `Direction`）；⚠️ **边界：鱼骨不走 B**。<br>**§9 三桶（回答"一口气能解决多少"）**：**能解决 5 类**（归因两级丢失⭐ / B 无门 / B 无结论可信度 / 消费面浪费 / 无短路）· **不能解决 8 条**（通道成品规格 / `I5` 破坏面 / `D-362` / `abandonSpur` / 沙砾 / `isInWall` / 落差梯子 / 追簇 4.5 格上界）· ⭐⭐ **已有现成解法 11 条**（最省力的是把 `MineJob:160/167` 那套 `SEARCH_LIMIT` 40-tick 冷却 + 连 8 次口径**从采矿区提升为通用口径** —— 「给他时间一点点完成」项目里**已经有了**，只是只在一个地方）。⚠️ **诚实边界：这次改动对鱼骨完成度无可测提升**。<br>**§10 ⭐ 工作排期表整理建议**：现状诊断为**两张表都自称"唯一排期来源"**（`OPEN_ITEMS_LEDGER:2075 §11` vs `:2232 §J`）· ⭐ **`1.4x` 序号不表达顺序**（实查顺序为 `…1.4j→1.4t→1.4u→1.4v→1.4k→1.4l…`，读顺序要靠读内容）· 成本估计与实际偏离（`§J-1` 定档 6–8 轮，实际三天 22 行）· 状态口径两套混用 · **同一事项 3–5 处副本**（`F2`/`I5` 是活例）· `§J-4`(明确不做) 无编号排最末。七条建议：**只留一处**（`§J` 为唯一排期、`§11` 降为历史归档）· **序号与执行顺序解耦**（加"当前执行顺序"一行，**不重排行**）· ⭐ **成本拆三段（实现/验证/客户端）并声明"真机轮 = 新发现的来源，不计入实现轮"** · 状态口径统一到表头那套 · ⭐ **裁定/状态/断点三样东西三个文件各自一个出处**（`AI_DECISIONS`=裁定 · `§J`=状态 · `HANDOVER`=只写断点）· `§J-4` 提前并编号（防同一问题被问第三遍）· ⭐⭐ **给 `§J-1` 加退出条件**（现在的形态是"发现日志"不是排期表 —— 这正与 `N10` 的"防止画大饼"同病）。<br>**§11 口径不一致**：`F2` 三处对不上（`HANDOVER:1468` 说"已裁定 A" · `AI_DECISIONS.md` **无 F2 条目** · `1.4t` 仍写"待裁定"）⇒ 缺一个 D 编号；⭐ **「通道成品规格」是同一不变式的三半**（`I3`✅/`I5`放置面✅/`I5`破坏面⏳`1.4s`/`I2`(F2) 无编号）。 | `e43ac1f` |
| [`36-鱼骨施工与挖掘状态解耦方案评审-20260926.md`](36-%E9%B1%BC%E9%AA%A8%E6%96%BD%E5%B7%A5%E4%B8%8E%E6%8C%96%E6%8E%98%E7%8A%B6%E6%80%81%E8%A7%A3%E8%80%A6%E6%96%B9%E6%A1%88%E8%AF%84%E5%AE%A1-20260926.md) | ⭐⭐ **鱼骨第 4 次架构提案评审**：「施工状态 / 目标挖掘状态」彻底解耦 + 风险回顾 + 通道补路语义。基线 `1e29ecd`（**不是授权、不是排期、不是待办**）。<br>**§0 结论**：⭐ **方向对，而且是四次提案里第一次动「职责边界」而不是「档位」** —— 用户自己点出的「**一个是单位化确定性施工，一个是不稳定的不确定范围工作**」正是 `FishboneJob` 现在最别扭的地方（`EXCAVATE` 与 `IN_PLACE` 互相穿插、靠 `pendingSpurReturn`/`spurReturnTarget`/`chaseApproachUnit` 三个字段双向耦合）。⚠️ **但它仍在"加概念"**（2 状态机 + 转换协议 + 记录图 + 起点规则 + 修复顺序 = **5 个**）⇒ 准确表述是「**把复杂度从运行期分支搬到设计期状态机**」，**不是无条件简化**。<br>**§2 方向对的三处支撑**：① `FishboneTemplate` 是纯几何 record（"单位化"已有载体）· ② **两件性质不同的事确实在互相穿插**（相位枚举逐字）· ⭐ ③ **"区外 = 所有权限"已是既定政策，不需新裁定**（`D-398` R4「保护区外无限制修改」+ `Z3`/`D-415` 区外取消格数额度 + `WriteGrant` 逐字"不含预算"）。⭐ 提案**顺带杀掉三样**：施工↔矿双向耦合 · 每条支巷各自的退回状态机 · `dugCells` 反推暴露面的绕。<br>**§3 ⭐ 通道补路：三层已齐、只缺判据**（用户问"现在做吗"）。**能力** ✅（`PLACE_STEP_AND_TRAVERSE` 已在 `withPlacement:133`/`miningApproach:159`）· **权限** ✅（⭐ `D-446` 注释逐字：「支撑格**刻意不保护** —— 它是"**补地板**"的合法目标（`I2`）」）· ⭐ **留下** ✅ **反直觉：区外放的块本来就会留下**（`WorldModLedger:41` "区外条目一律不认" + `:163-165` "区外垫脚方块不会在任务收尾被拆回…是 `D-398` R2 的推论不是遗漏" + `1.4p` 真机 `[Ledger] skip … 累计 15 次`）⇒ **"补路要留下"在区外是默认行为**。缺的**只有** `I1`/`I2`/`I3` 三条判据 = `1.4t` 收成 D 编号（**0.1 轮纯文档**）⇒ ⭐ **补路比记录图更靠前**（记录图的三态判据就是补路的判据，同一件事）。⚠️ **边界**：保护区内语义相反（记账 ⇒ 会被 `RestoreScopeTask` 收走）⇒ **必须显式声明"鱼骨是区外任务"**。<br>**§4 `D-446` 与记录图是同一不变式的两种实现**（`D-446`=禁止/预防 · 记录图=记录+清理/治疗；用户的更通用）⇒ 建议登记为"记录图落地后第一个该重新评估的候选"。⭐⭐ **更进一步：`1.4s`（`I5` 破坏面）可能可以不做了** —— 它是预防式且自带**难裁的例外**（须豁免鱼骨自己挖穿通道）；治疗式只需**事后一次比对**（服务端事实 vs 规格）。**为什么治疗更简单**：预防要可信的"施工必需格集合"，而 `D-446` 已证明**这集合难划**（通道层格好划、**支撑格难划而偏偏最容易被挖掉**）。⚠️ 但 `1.4s` 有独立证据（真机第四轮 A1）⇒ 建议**挂起不删**。<br>**§5 记录图不需要落盘**（`M3` 的后续）：`FishboneJob` 全仓 **0 处** `SavedData`/`CompoundTag`/`serializeNBT`；`BotManager:2760-2764` 的 `lastExecutionRecord` 是**终态摘要、不持有 Job 引用** ⇒ ⭐ **用户"绝对不要保留记录"在架构上已自然满足**；`survey/35 §4.2` 的"第四张真相图"担忧**降级**。⚠️ 但两条硬要求随之而来：**`SUMMARY` 必须报残留**（返程可能没修完 + **失败终态时记录也丢**）+ 读数不许误导（`1.4u`/`F3` 教训）。<br>**§6 ⭐ 服务端方块状态即时同步（`M4` 的担心不成立）**：`FishboneJob` 直读 `level.getBlockState`（5 处），全仓**无** `blockStateCache`/`cachedState`/`lastKnownState` ⇒ **无延迟窗口** ⇒ **"更新一次"这个动作根本不需要**。⚠️ **但 `M4` 担心的方向恰好命中另一处真问题：在飞重力方块**（`FallingBlockEntity` 是**实体**，`pathing/` 零命中）⇒ **一句话：方块状态不用管，实体状态要补**。⚠️ 另核出两条：**`RETURN` 是自由寻路不是沿单元走**（`FishboneJob:1242-1246`）⇒ "沿途恢复"= **本次唯一必须新增的机制**；**真缝隙**：已完工子巷的破损**没有任何一次行程会经过它**（用户已定"接受+如实计数"）。⭐ "起点固定"的解法本鱼认同，**理由更强**：起点必须"足够靠前"正方向才覆盖全部，允许新起点会导致"越修越往前"或"起点落在破损后面 ⇒ 那段永远修不到"。<br>**§7 ⭐⭐⭐ 风险回顾 R1–R10**（用户点名要的）。⚠️ 总判断：这次减的是**运行期分支**（模式 B/兜底），加的是**设计期状态机** ⇒ **复杂度不是消失，是搬家**；搬家不一定划算（`R6` 给了判据）。**最要紧三条**：**R1 行为扩张**（现状 `FishboneJob:1069-1071` 逐字「⚠️ **不许走去挖**」+ `opportunisticTarget:817-829` 三条件第③条是 `inPlaceReachable`（不移动）；新设计"立刻脱离"= **允许移动** ⇒ 失败面从"丢一格"变"打断主线"）· **R3 `MAX_TICKS` 公式失配**（`maxTicks` 由模板推导、真机实测 47 tick/单元，而**追簇不计入 `advanceCells()`**（`FishboneTemplate` 注释逐字），超时判据 `FishboneJob:355` ⇒ **簇规模一涨就在挖矿中途撞 `goal_timeout`**，且拆掉 `oreBudgetPerUnit=32` 后**没有上界**）· **R4 "修复"是新的写入授权方向**（事实只能告诉你"这格不是我以为的样子"，**不能告诉你"是谁改的"** ⇒ 可能拆玩家的火把/箱子/模组机器；现有 `I5` 从不主动拆非自己放的 ⇒ **反向新授权**）。其余：**R2** 挖矿失败会传染给施工（`cluster_abort_stuck`，回程失败真机发生过）· **R5** 沿单元走 vs 自由寻路是**设计内部矛盾**（逐格走 ⇒ 失败面从 1 条腿变 N 条腿；反向那趟=多一趟全程 ≈16 分钟）· **R6** 概念数量（转换条件模糊 ⇒ 状态机更贵；先给"脱离判据"定唯一出处）· **R7** 资产作废面可数（10 个字段/相位；夹具 `slice1` 42 条 + `slice2` 88 条）· **R8 ⭐ 最要紧的实操风险：真机轮稀缺 + `D-446` 还没测** ⇒ 建议**先把当前 jar 测掉再做改造**（本报告唯一一条顺序性建议）· **R9** 三道保险覆盖同一不变式（成本双算）· **R10** 无长期记忆 ⇒ 同一失败重复付费。<br>⭐⭐ **§7.1 降险建议：把方案切两刀** —— **第一步只做「记录图 + 沿单元返程修复」，不做「脱离施工去挖」**（新概念 5 → 2，**`R1`/`R2`/`R3` 三条全部不触发**）；**第二步**才做"脱离 → 挖簇 → 回归"（那时三条必须逐条有判据与红臂）。<br>**§8 依赖顺序**：**0 先测当前 jar** → **1 `1.4t` 收成 D 编号**（0.1 轮）→ **2 补路动作** → **3 记录图** → **4 沿单元返程** → 5 才谈 `D-446` 回退/`1.4s` 挂起 → 6 才谈第二刀。⭐ **注意 1/2 在 3 之前**（记录图的三态判据就是补路的判据）。⚠️ 另一条分割线：**"簇内用 Bariton 高效模式"要 `K2`，而记录图不需要 `K2`**。⚠️ 另有便宜替代：后续簇内格走 **`CURRENT` 快路径**（`MiningPlanner:221-241` 一行 LOS，矿脉连续 ⇒ 大概率已触及）⇒ **只是换 `MiningProfile` 挡位，不是新架构**。<br>**§9 待拍 8 条**（`J` `1.4s` 挂起 · `K` `D-446` 回退 · ⭐`M` `bridge_budget_exhausted` 改**几何事实族**（用户说"鱼骨对**过多的悬空单元**要放弃"= 产品裁定，但该码读起来像"我额度用完了"）· ⭐`N` "修复"的**写入授权范围** · ⭐`O` **"脱离判据"的唯一出处** · `P` `SUMMARY` 残留字段名 · ⭐`Q` 是否采纳"切两刀" · `L` `bridgeBlockBudget` 改窗口）**+ 已关闭 4 条**（`G` 子巷破损=接受+计数 · `H` "最靠近主巷"=施工顺序最早 · `I` 关闭 · 记录图不落盘）。 | `1e29ecd` |
| [`37-讨论记录-探索与地图的可行范围与第一层目标-20260926.md`](37-%E8%AE%A8%E8%AE%BA%E8%AE%B0%E5%BD%95-%E6%8E%A2%E7%B4%A2%E4%B8%8E%E5%9C%B0%E5%9B%BE%E7%9A%84%E5%8F%AF%E8%A1%8C%E8%8C%83%E5%9B%B4%E4%B8%8E%E7%AC%AC%E4%B8%80%E5%B1%82%E7%9B%AE%E6%A0%87-20260926.md) | ⭐⭐ **讨论记录**：探索与地图的可行范围 + **第一层目标限定**。基线 `b53fa29`。⚠️ **与当前进度（`J-1` 鱼骨线）无关，不进排期、不产生待办** —— 用途 = **把已查的证据存下来**（下次不必重查）。<br>**§1 用户原话逐字**（`L1` 缩小探索范围 / `L2` 文 vs 图 / `L3` 第一层目标限定）。<br>**§2 探索先例**：⭐ **Baritone `ExploreProcess`（完整源码逐行读过）** = 标准 frontier exploration，跑在持久区块访问图上；六条可抄：**① 三态 `{EXPLORED, NOT_EXPLORED, UNKNOWN}`（`UNKNOWN` = "region 还在加载，先别说没探过" ⇒ 防假否定）② 菱形/螺旋按曼哈顿距离外扫 ③ ⭐ 目标点故意偏向区块远端（`offset = renderDistance << 4`，站区块中心浪费一半覆盖）④ `GoalXZ` 二维 + 可选 `exploreMaintainY` 保 Y ⑤ 外部 JSON 过滤器 ⑥ 阻塞时 `REQUEST_PAUSE` 而非失败**；⚠️ **前提是 `CachedWorld`，去掉它 `ExploreProcess` 就不成立**。🌐 **MineExplorer**（`arXiv:2605.30931`，EMNLP 2026）摘要逐字：「**remains challenging**…strong models can handle many single-hop tasks but **degrade sharply when hidden prerequisites must be coordinated over longer trajectories**」+「larger models or thinking modes **do not consistently** translate into better performance」⇒ ⭐ **支持 `L1` 的降级判断：这一层公认没解，且不是换模型能救**。<br>**§3 环境识别三条路线**：**视觉**（VPT `2206.11795` · MineDojo/MineCLIP `2206.08853` · JARVIS-1 `2311.05997` —— 全要 GPU + 海量视频）· ⭐ **文本**（**GITM `2305.17144`**：纯文本观测、**不用 GPU**、首个拿遍主世界科技树）· **符号**（Baritone + Alice：直读方块状态）。⭐ 所有 LLM agent 论文**都在补同一块：记忆**（Voyager 技能库 · JARVIS-1 多模态记忆 · Optimus-1 知识图+经验池 · Odyssey 40 原语+183 组合 · MINDSTORES `(state,task,plan,outcome)` 嵌入库 · PEAM 参数化记忆）⇒ **没有一篇靠"更聪明的单步决策"解决探索**。<br>**§4 ⭐ 文 vs 图（回答 `L2`）**：**SpatialEval**（`2406.14852`，NeurIPS 2024）摘要四条 ——「(1) 空间推理难到**掉到随机猜测以下**」「(2) ⭐ **Despite additional visual input, VLMs often under-perform compared to their LLM counterparts**」「(3) ⭐ **when sufficient textual clues are provided, multimodal models become less reliant on visual information**」「(4) 图文**冗余**能提升」。**AECV-Bench**（`2601.04819`，2026）摘要逐字：图上**数门窗 0.40–0.55 accuracy**「remains unsolved」。**项目侧** ✅ 全仓 `data:image|ImageIO|BufferedImage|multimodal|image_url` **0 命中**；唯二 PNG 是 GUI 贴图；⚠️ `SpeechChannel.KIND_VISION_REPORT` 只是消息种类字符串、**不传图**。⭐⭐ **第三条路（本文建议）**：**给 LLM 查询接口，不给画面** —— 它不需要**知道**地图，只需要**能问**地图（同时不违反 `D-135` 边界：`DecisionSnapshot` 注释逐字「**不放方块级世界细节**…塞进去只会让 LLM 开始管执行细节」）。⚠️ 诚实边界：**证据全部是摘要、未读全文**；**未找到 MC 场景下的"文 vs 图"受控对照** ⇒ 不能替用户断言"MC 里图像更差"。<br>**§5 项目现状核对（实查）**：**① 树识别器已存在且已是可泛化形状**（`TreeScanner`：硬编码 8 种方块 → **`BlockTags.LOGS`**；不要求有树叶；`MIN_LOGS=3`/`MAX_LOGS=64`）· ⚠️ **② 但 `CandidateMenu:129-132` 注释记着一个真机教训**：「**必须复用 Job 自己的候选源**（`LumberCandidateSource`），**而不是裸 `TreeScanner`**」—— 2026-09-12 实测把 Job 注定拒绝的树递给 LLM（菜单给 `tree@22,64,218`，Job 报 `no_reachable_candidate`）⇒ ⭐ **"识别器"与"能不能做"是两件事，开放识别器必须连可行性判据一起给** · **③** `ScopeBuffer` 已订阅 `EntityJoinLevelEvent`/`LivingDeathEvent`/`BlockEvent.BreakEvent`，但**以任务为中心注册** ⇒ 跨任务要耐久记忆；⭐ **但"树被砍了"根本不需要事件 —— 惰性重校验就够**（读取时读那一格，O(1)、零订阅、零持久化）· ⚠️ **④ 用户举的两例不是一类**（树=静态 O(1) / 猪=会动的实体，需决定"跟踪"还是"只记上次见到在哪"）· **⑤ 群系 0 命中**（但直接调就能用；⚠️ 判据宜落**方块/标签**而非群系名，整合包群系多） · ⭐⭐ **⑥ 地形难度：算法已有、只缺出口** —— `StandingCostField`/`StandingCostEstimator` = **纯通行 Dijkstra 成本场**（注释逐字「S2 Dijkstra 成本场（默认）…一次纯通行 Dijkstra」），但**只在规划时算一次、用完即丢，从未暴露** ⇒ **"让 LLM 知道这地方很陡"只需一个出口**（降采样成一个数；⚠️ 未实测） · ⚠️ **⑦ 感知层扩不到区块**：`PerceptionSnapshot` 是**半径立方体暴力扫描**（radius=5 ⇒ 1331 次；区块尺度 ⇒ 98304 次 ⇒ 不可行）⇒ **要的不是调大半径，是换形状（识别器 + 标记列表）——正是 `L1` 的方向**；且 `/alice observe` 硬编码 `PerceptionProfile.MINING` · ⭐ **⑧ 更便宜的替代：环形记录**（"最近去过的 N 个区块 + 结果"，不需地图、不需持久化）。<br>**§1.3 ⭐⭐⭐ 锚点任务与任务区（`L4`→`L5`→`L6` 三次澄清；**本节含勘测侧连错两次的记录，保留不删**）**：用户原话 —— `L4`「**锚点任务还没做过**…区域任务必须划任务区（**保护区的子区域**）**按区块划分**，要做保护区权限管理；**锚点任务一般在保护区外，区域由锚点和任务范围决定**，**权限管理还没讨论**」·`L5`「**不是保护区也可以建立任务区**，而**锚点任务跟区块无关，只由锚点和任务决定的半径决定**」·`L6`「**任务区还是保护区的子区域啊**，只是说**保护区是父类，任务区继承保护区的部分定义和功能**」。⭐ **勘测侧两次错的记录（E1–E3）**：E1 把"子区域"读成**领地**；E2 据此反向推翻成"任务区不是子类"并引 `TaskZoneRegistry` 当证据；E3 又外推"锚点任务可能不必新造机制、该机制正是它要的形状"。⭐ **错因**：① 术语两种读法（"子区域"= 领地 ⊂ ？还是 子类 extends ？）**没回 `D-338` 原文定**，反而**拿实现注释推翻用户定义**；② **拿碎片外推**（把"野外独立划分"直接当锚点任务的解，没先核"锚点任务的范围是什么形状"）。✅ **权威原文（本轮读到）**：`D-338` 标题「保护区是**具体父类** + 子类（安全区 / 任务区）」· `D-338` ①「**子类区域继承 ⒜⒝（账本 + 权限要求）**…**任务区**：按任务授予不同等级的修改权限与预算」· `D-338` ⑦「**任务区由任务自己声明**：区域 = **目标包围盒 + 余量**…**随 `scopeId` 生灭**」· `TaskZoneRegistry:42-43`「任务区**可以覆盖保护区父类**（**也可以在没有保护区的野外独立划分**）；⛔ 不得覆盖任何子类声明（今天=安全区）」· `:74`「**Y 不参与派生**…忽略 Y、覆盖全高度」。⇒ ⭐ **三条同时成立**：任务区**是**保护区的子类（继承账本+权限）· **可在非保护区野外划** · 锚点任务**跟区块无关**（半径）。⭐ **1 与 2 不矛盾**：正因为任务区**自己就是一种（子类）保护区**，声明它**不要求先有保护区认领** —— 它**自带**继承来的账本与权限要求。⚠️ **留下的真落差**：现有授权机制是「**区块级派生 + 忽略 Y**」，而锚点任务要「**半径**」⇒ 这很可能就是 `L4` 说"权限管理还没讨论"所指（勘测侧推断，未裁定）。⚠️ **另一条措辞张力（未裁定，属文档债）**：`TaskZoneRegistry:24`「保护区父类**之外**的另一种封套」vs `D-338` ①「任务区是**子类**」——勘测侧读法 = **载体**（独立注册表）vs **类型层次**（继承）。**§6 ⭐ 第一层目标限定（`L3`）**：用户口径「**不能现在做完**，要做**最基础的部分**，**验证 Alice 能做一个通用助手**，特别是**区域任务和锚点任务**，都**更基础更好实现**，然后跳到第二层」。**为什么更基础**（勘测侧核对）：**范围有界**（vs 开放世界）· **范围内扫 ⇒ 复用现有候选源** · ⭐ **不需要跨会话记忆** · ⭐ **失败判据可判定**（"这一圈扫完了"，vs 探索的"真没有"与"走错方向"不可区分）· **预算可由范围推导** · ✅ **已有资产**（`REGION_LUMBER` / `LumberRegionState` / 常驻巡查状态机）。⭐⭐ **`survey/26 §4.1` 正好支持** —— 用户 2026-09-21 原话「两种锚点模式是**为一套任务准备的**」，该报告结论逐字：**「锚点 = 同一套任务的取**值**，不是两个任务」** ⇒ **"一套任务 + 锚点取值"天然就是"有界范围 + 目标搜索"，正是第一层最基础部分的现成骨架**。⚠️ 同报告记了否定证据（车万女仆三条前提「`VERTICAL_SEARCH_RANGE = 4`、主人必须在线、目标必须可走」**在挖矿上全不成立**，附属 `MaidMineTask` 是空壳）⇒ **可抄抽象形状，不是它的实现**。**§6.4 不由第一层承担的七项**（探索 / 区块地图 / 群系 / 地形难度出口 / 视觉通路 / 持久化标记 / 多模态）——⚠️ **勘测侧推断，非用户裁定**。**§6.5 形状**：**区域任务（✅有资产：`REGION_LUMBER`/`LumberRegionState`/`TaskZoneRegistry`，可直接开工） + 锚点任务（⚠️还没做过）· 范围内目标识别（复用候选源 + **带可行性判据**）· **判据 = 范围扫完 ⇒ 如实收工**；⚠️ **两者共用的部分（范围内识别 + 判据）才是『一套任务 + 锚点取值』真正能省的地方**。<br>⚠️ **本文与当前进度无关、不进排期、不产生待办**；`§7` 七条待验证仅备查。⭐ **最值得记住的一句**：**"给 LLM 地图"被证据否掉，而更好的第三选项是"给它查询接口"** —— 但**第一层目标的限定不依赖本文任何一条**，因为区域/锚点任务**不需要探索、不需要地图、不需要跨会话记忆**。 | `b53fa29` |




## 使用方法（给主工作流）

1. **只读**：结论与证据都带文件 / 行号 / 数据来源引用，可直接核对。
2. **采纳时**：请把结论落到 `AI_DECISIONS.md`（成为真正的决策）——
   本目录里的东西**没有决策效力**，只是候选。
3. **不同意时**：直接在主线文档里写明反对理由即可，不必删本目录（保留"为什么当时这么想"有参考价值）。
4. **证据分级**：本目录所有结论均为**静态阅读代码 + 全仓符号扫描 + 反编译/文档交叉核对**；
   凡需运行才能确认的，均已显式标注「**待确认**」。
   **未运行代码、未做客户端实测。**

## 整合包勘测的复现方法（本目录通用）

勘测一个整合包**不需要下载整包，也不需要运行游戏**。ATM9 的两步取证：

1. **拿权威清单（约 4 KB）**：CurseForge 的整合包 zip 里 `manifest.json` 只有几十 KB 且位于 zip 首位。
   用 **HTTP Range 请求**取尾部中央目录 + 该文件数据段，即可拿到加载器版本、模组数、每个模组的
   `projectID` / `fileID`。**不要用 `HEAD` 判断可用性**——它可能返回 `200` 但 `size_download=0`。
2. **`projectID` → 模组名 + 精确版本**：`api.cfwidget.com/<projectID>`（CurseForge 官方数据镜像），
   用 `manifest` 里的 `fileID` 在其 `files` 数组里精确匹配，得到**整合包锁定的 jar 名**。

> ⚠️ **不要相信二手整合包模组清单站**（如 mcmod.cn）。ATM9 实测中该站版本数据大面积滞后 / 错误，
> 甚至把加载器判成 NeoForge。**一律以 `manifest.json` 为准。**

## 维护约定

- 每份报告头部标注 **基线 commit**；基线过期时结论可能失效（尤其是"当前状态"类描述）。
- 报告**只增不改历史结论**；有新结论追加新文件或在文末加「补遗」，避免悄悄改写过去说过的话。
- ⭐ **先录节点，再写报告**（作者 2026-09-17 规定）：每次撰写报告前，**先在 `survey/.notes/` 记录当前讨论的节点**
  并核对一遍，确认无遗漏后再动笔。**一次报告对应一个记事文件。**
  - 为什么：此前的报告内容不全 —— 往往**只讨论了最后说的几个论点**，对话中段的论点被盖掉。
  - `.notes/` **不进 git**（已在 `.gitignore`），它是勘测员自己的**防丢笔记**，不是交付物。
  - 节点记录要求：**按对话顺序、不按重要性**；**节点（用户说了什么）与结论（勘测员怎么看）分开记**。
