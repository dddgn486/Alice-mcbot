# K-1 前缀搜索自检场景（一键；孤立长方体开阔场景，2026-09-16）
# 目的：让"预算耗尽 ⇒ 交出 best-so-far 前缀"这条判据**位置确定**（此前该夹具从 bot 当前所在处规划
# ⇒ 单跑 `single:partial_search` 时地形不对就整组红；违反"夹具自己传送 bot 到场景起点"的纪律）。
# 布局：一条**向东 ≥20 格、完全平坦开阔**的走廊 ⇒ 距离 4/8/12/16 的目标都可达（校准必然成立）。
# ⚠️ 实测坑（2026-09-16）：走廊横跨 3 个区块，**最东那块没加载时整条 `/fill` 会失败**
#（`setblock` 生效、标记柱生效，但大 `fill` 改 0 格 ⇒ 规划期只看到"没有地板"）。
# 所以固定起点取**走廊中段** (16,64,245)，让加载半径覆盖整个场景；夹具侧另有区块加载等待 + 地板断言。
# 地板 y=63（脚位 y=64），走廊 x 0..40 / z 240..250。
function alice_test:partial_search_reset

fill 0 63 240 40 63 250 minecraft:stone
fill 16 63 239 16 66 239 minecraft:stone
