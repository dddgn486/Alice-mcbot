# `survey/28` 逐条核对 + 评估 + 计划（2026-09-22）

- 被核对对象：`survey/28-内核准入标准与规划师提案-20260922.md`（356 行，基线 `4f1d840`）
- 本文角色：**主工作流（工程师侧）**；`survey/28` 自述"无裁决"，本文是**核对 + 裁决范围**。
- 纪律：只核对**承重事实**；每条给出可复现命令或 `文件:行`；报告未查证的项不替它认定。

## 一、承重事实核对（4 项，全部落地）

| # | 报告主张 | 核对结果 | 证据 |
|---|---|---|---|
| 1 | `kernel-predicates.py` 的 `rule_k4` 只是**字符串包含**，抓不到谓词漂移 | ✅ **成立** | `tools/kernel-predicates.py`：`if f"MovementHelper.{predicate}(" not in body:` |
| 2 | 10 个执行工厂 ↔ 10 个 `MovementType`，无缺口 | ✅ **成立** | `*ExecutionFactory.java` 11 个（含接口 `MovementExecutionFactory`）；`MovementType` 枚举 **10** 个常量 |
| 3 | 「性能保证」全仓库只有**一处** | ⚠️ **成立但低估** | 只有 `SEARCH_BUDGET_CEILING_MILLIS=60` 被门禁**读**；源码里另有 **3 个未受门禁保护的 ms 常量**：`SearchTickBudget.DEFAULT_MAX_MILLIS_PER_TICK=400`（**=8 个 tick！**）、`PathRequest.ESCAPE_MAX_MILLIS=100`、`SurvivalSystem.PRECHECK_MAX_MILLIS=20` |
| 4 | `OPEN_ITEMS_LEDGER` = 2 071 行；`minimal-implementation-planning` 已覆盖"整理需求" | ✅ **成立** | `wc -l docs/OPEN_ITEMS_LEDGER.md` = 2071 |

### 我替报告**结了它自己的两条待查**（`survey/28 §8`）

- **§8 #3**（11 vs 10）：**已收口** ⇒ 10 对 10（第 11 个是接口）✓ 报告自己的更正正确。
- **§8 #5**（`kernel-predicates.py` 里除 `rule_k4` 外还有几道是"字符串包含"型）：**已量化** ⇒
  `not in (body|src|text|source)` 命中 **39 处**，散在 **50 条** `rule_*` 里（未逐条分类）
  ⇒ ⭐ **§4.1 的盲区是系统性的，不是 `rule_k4` 一处的偶发**。

## 二、我补的第三条证据（加固报告 §4.2 的结论）

执行侧**独有**的"能力类"拒绝码（= 只存在于执行侧、规划侧不查的判据 ⇒ `D-394` 那一类）：

| 工厂 | 独有能力类码（节选） | 个数 |
|---|---|---|
| `Fall` | `FALL_EDGE_BLOCKED` / `FALL_LANDING_INVALID` / `FALL_LANDING_BOTTOM_SLAB` / `FALL_LANDING_FLUID` / `FALL_COLUMN_BLOCKED` / `FALL_NOT_RECOVERABLE_NO_BLOCKS|NO_FACE|HEADROOM` | ~8 |
| `Pillar` | `PILLAR_HEAD_BLOCKED` / `PILLAR_PLACE_OCCUPIED` / `PLACE_NO_VALID_FACE` / `PLACE_RESOURCE_UNAVAILABLE` | ~4 |
| `Ascend` | `ASCEND_FALLING_BLOCK_ABOVE` / `ASCEND_FROM_CLIMBABLE` | 2 |
| `Descend` | `DESCEND_REJECTED_LANDING_HAZARD` / `DESCEND_REJECTED_OVERSHOOT_CLIFF` | 2 |
| `Downward` | `DOWNWARD_BLOCK_NOT_BREAKABLE` | 1 |
| `Traverse` | —（只有 `STALE_START` / `INVALID_PRECONDITION` / `MISSING_CONTEXT` / `UNSUPPORTED_SPEC`） | **0** |

⇒ ⭐ **这就是"准入来源单一尺"要数的可数项**（≈17 个候选点），**不是"复杂度"这种形容词** ——
报告 §6.6 #5 自己说"必须换成可数项"，本文把它数出来了。

## 三、评估（同意 / 修正 / 不同意）

| 报告的判断 | 我的评估 |
|---|---|
| `D-394` 不是"漏了一个检查"，而是"**没有地方规定检查该放哪**" | ✅ **同意**，且与我在 `D-394` 现场看到的一致（`canAscend` 布尔 + 工厂另一份 = 两份必然漂移） |
| 不加「规划师」角色，改成两把机器尺子 | ✅ **同意**。判据是硬的：`K4-P1` 是机器、每轮跑，它**仍然**没拦住 `D-394` ⇒ 再加一个"产出散文"的角色面临同一困境；`AGENTS.md` 自己的唯一指标是"规则被违反时 = 构建红" |
| 「对齐 Baritone 要对齐**结构**（准入与成本同函数），不是抄条数」 | ✅ **同意方向**。⚠️ 但我要加一条代价判断：把 10 个 `canX` 布尔全改成"成本即准入"要动 `CostModel` + 生成器 + 工厂三处，**不是本轮该开的**；**尺子 2（准入来源单一）是它的低成本近似**，可以立刻挂判据 |
| 今天六个提交里只有 `D-389`/`D-390` 是能力新增，其余是还债 | ✅ **同意**（且这条正是"别让加尺子变成新一轮还债"的警告） |
| `R-1`/`R-2` 优先（有现成正样本、零新架构） | ⚠️ **部分同意**：`R-2` 应当**立刻做**（它正是你"审查并加入内核"那条指令的**收敛工具**，没它审查就退化成 10 处一次性补丁）；`R-1` 排在其后（更偏还债） |

## 四、计划（按"能力 vs 还债"标注 + 我的推荐次序）

| 序 | 事项 | 性质 | 依赖 / 挂载点 |
|---|---|---|---|
| **P1** | ⭐ **尺子 2「准入来源单一」**：`kernel-predicates.py` 新增规则 —— 执行工厂里出现**未标注的能力类**拒绝码 ⇒ 红；并给出**标注机制**（`// K4-CAPABILITY` / `// K4-TIMING` 之一，二者必居其一） | 尺子（但是**你那条指令的收敛工具**） | 现成锚点 = 上面 17 个候选点 + 10 个工厂 ↔ 10 个 `MovementType` 的对称性 |
| **P2** | **Movement 审查切片**：`Ascend`（已完成 `D-394`）→ `Descend`/`Downward`（你的「挖脚下不掉下去」，**需真机复现点**）→ `Diagonal`（26→6 面刚改）→ `Traverse` → `Pillar` → `Fall` | 能力 + 还债 | 每片：Baritone `文件:行` 对照 + 差异表 + 红旗/绿旗夹具 + 电池步 |
| **P3** | **掉落物「本作业范围内」收集授权**（你 2026-09-22 已裁定，**未实现**） | 能力 | `provenance=GRANTED_AREA`；夹具要保「范围外/玩家丢的仍不碰」 |
| **P4** | ⭐ **尺子 1「tick 负载预算」**：每个"跨 tick 摊销 / 有界搜索 / 批量扫描"循环**必须声明 ms 或节点上限**且 < 1 tick；把**未受保护的 3 个 ms 常量**（尤其 `DEFAULT_MAX_MILLIS_PER_TICK=400`）纳入 | 尺子 + 还债 | `kernel-predicates.py`（不新建文件） |
| **P5** | ⭐ **`D-391` 收口**（"被同族包住的目标"给不出站位/进入）+ **死候选剔除**（`candidates` 单调涨到 1037 ⇒ 每次选择全量复检） | 能力（你要的"矿簇挖完"）+ 性能 | 已排除 A2 截断 / 搜索预算；需新探针先补读数 |
| **P6** | **搜索线程化**（`D-369 §四`）—— `D-388 §五` 自证"不等于根治"，且是**多 bot 并行**的前提 | 能力（架构级） | 需线程安全世界视图 |
| **P7** | `lumber_job` 在 CORE 报告里**单列一行**（防"唯一失败仍是既有 X"盖掉新失败） | 尺子（廉价） | `RegressionBatteryTask` 汇报行 |

> ⚠️ **反漂移自检（`method-vs-goal-drift`）**：P1/P4/P7 都是**尺子**，本身**不算能力进展**；
> 它们值得做的理由只有一个 —— **被违反时构建会红**。若 P1 落地后不能给出"注入即变红"的证明，它就该被撤掉。
> **本轮的产出目标仍是 P2/P3/P5**（能力），尺子只做"顺手挂上"。
