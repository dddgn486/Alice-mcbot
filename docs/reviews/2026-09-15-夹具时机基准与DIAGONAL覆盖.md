# 2026-09-15 评审：夹具时机基准失效（两个自愈场景从未测过"中途"）+ DIAGONAL 覆盖是偶发凑出来的

**范围**：`task/PathingRegressionTask.java`（寻路串联回归）—— 只碰夹具层，**未改内核**。
**验证等级**：`SERVER_TESTED`（无头生产服务端；`single:pathing` + `core`）。**不是** `WINDOWS_CLIENT`。

---

## 0. 一句话结论

1. **三处缺陷**，都是"**声明/环境与事实不符**"，且**后两处是修前一处时暴露的**：
   - ① `wallTick`/`disturbTick` 的 `30` **从来没生效过** —— 比较基准是**任务级** tick（从不按场景复位），
     进入第 15/16 个场景时它早已 ≈300 ⇒ 夹具总是**第一个执行 tick**就动手，两个自愈场景
     **从未测过"中途"**；而且放弃时还**静默假装做过**。
   - ② 全局覆盖断言里的 `DIAGONAL` 是**靠 ① 的偶发绕行凑出来的** —— ① 修好后电池**如实变红**
     （`coverage=FAIL([DIAGONAL])`），补一个"真的会执行对角线"的场景才收口。
   - ③ **无头通道不是无噪声的：敌对生物会杀 bot / 推 bot**。实测一次
     `假人死亡: Alice was blown up by Creeper → 直接清除` ⇒ 电池**无判决**（`exit=3`）；
     同轮更早 bot 已被推离预期格（`feet=1,64,68`，起点 z=66）。**这是"通道抖动"目前最强的实证候选**。
2. **我原本的计划被证据否掉了**：本轮原打算做"`pathing` 子用例起点契约"（假设抖动来自起步位置）。
   实测**修复前后各 3 轮全量 + 3 轮隔离，路径场景逐字相同**，抖动**不复现** ⇒ 起点假设**不成立**，
   改为处理下面这些**代码级/环境级可证**的缺陷。

---

## 1. 先记"计划被否掉"的过程（避免下次重走）

| 观察 | 数据 | 结论 |
|---|---|---|
| 隔离跑 `pathing` 3 轮 | `PASS`，`ticks=496`，**51 行场景日志逐字相同**（`diff` 三份全 IDENTICAL） | 路径场景**本身是确定性的** |
| 全量 `core` 3 轮（修 (A) 之后） | `PASS` 3/3；场景行三轮逐字相同 | 抖动**不复现**（6/6 绿） |
| 隔离 vs 全量 | 只差 1 行：`lava_course` 的 `movements=12` vs `13`（两者都 `PASS/lava_contacts=0`） | 顺序耦合**存在但良性**；路径步骤**不是**抖动源 |
| 历史 3 红（`partial_search` / `+wall` / `+disturb`） | `/tmp` 里的证据日志**已随会话丢失**（实测 `/tmp` 已被清空，仅剩 dsh 自己的临时目录） | **不能**再引用那些日志下结论；改走可重跑的无头通道 |

> ⚠️ **纪律教训（新增）**：`/tmp` 不是长期证据位 —— 无头电池日志必须落到
> `/home/fb486/alice-evidence/<日期>-<主题>/`（本轮已这么存），否则跨会话即失效。

---

## 2. 缺陷①：夹具时机基准失效（代码 + 日志双证）

### 2.1 代码事实

`SceneCheck` 的 `wallTick` / `disturbTick` 在表里写 **30**：

```java
new SceneCheck("place_course+wall",    …, Kind.EXECUTE_COMPLETE, 30, 0, …)   // 注释："计划前方封路"
new SceneCheck("place_course+disturb", …, Kind.EXECUTE_COMPLETE, 0, 30, 0, 1, …)
```

但 `tickFixtures()` 用的是**任务级** `ticks`：

```java
if (scene.wallTick() > 0 && !walled && ticks >= scene.wallTick()) { … }
```

`ticks` 在 `tick()` 里 `++`，**只有总预算判据用它**，`prepare()`/`advance()` 都**不复位**它。
`git log -S` 证明它**从引入就是这样**（`176486c`，D-054，串联回归扩到 11 项），不是后来被改坏的：

```
git log --oneline -S "disturbTick" -- src/main/java/com/dddgn/alice/task/PathingRegressionTask.java
⇒ 176486c feat(kernel): 疾跑门控（D-053）+ 串联回归扩到 11 项（D-054）
```

而 `30` 这个**值**的来源，是**每次运行从 0 起算**的两个同族夹具：

| 出处 | 计数器 | `30` 的真实含义 |
|---|---|---|
| `PathingDisturberItem.DISTURB_TICK = 30` | —（注释："在执行**中途**把 bot 平移 1 格"） | 走了一半 |
| `PathSessionDiagnosticTask.ticks` | **任务内自增** | "本次寻路开始后第 30 tick" |
| `docs/AI_TEST_MATRIX.md:87` | — | "普通右键（**第 30 tick** 在计划前方封路）" |

⇒ **常量被搬过来了，基准没有**：同一份语义在两处实现里**分叉**。

### 2.2 日志事实（修复前）

```
[Regression] wall_placed scene=place_course+wall    at=2, 64, 66   tick=346     ← 声明 30
[Regression] disturbed   scene=place_course+disturb from=0, 64, 66 → 0, 64, 67  tick=422
[Regression] scene=place_course+wall    result=PASS … route=TRAVERSE,ASCEND,DESCEND,FALL,PLACE_STEP_AND_TRAVERSE
[Regression] scene=place_course+disturb result=PASS … route=TRAVERSE,DIAGONAL,FALL,PLACE_STEP_AND_TRAVERSE
```

`+disturb` 的实际过程（修前）：

```
segment_start index=0/8 type=TRAVERSE from=0,64,66 to=1,64,66 actualFoot=0,64,66
[Bot] teleported 0,64,66 → 0,64,67                    ← 扰动发生在"第一段刚开始、还没走"
failed … status=INVALID_PRECONDITION code=TRAVERSE_INVALID_PRECONDITION index=0 actualFoot=0,64,67
[PathRetry] replan attempt=1 … feet=0,64,67           ← 于是变成"从偏移格重新起步"
```

⇒ 场景注释声称的"**段内**重同步 / 重规划"与 `PathingDisturberItem` 注释声称的
"确定性地触发 `*_STALE_START`"**都没发生**；发生的是**起点重规划**。

### 2.3 独立对照：修复后的位置与 2026-09-09 客户端验证记录**对上了**

`docs/AI_DECISIONS.md:569`（**2026-09-09 客户端验证**，早于本次、与本次修改无关）：

> 状态：**已通过客户端验证**（2026-09-09：`wall_placed at=5,64,66` → `[PathRetry] replan replans=1 reason=INVALID_PRECONDITION` → `COMPLETED replans=1`）

| | 封路格 | 与客户端验证过的场景 |
|---|---|---|
| 修复前（电池） | `at=2,64,66` | **不是同一个**（夹具"动手太早"，墙落在起点前方第 2 格） |
| **修复后（电池）** | **`at=5,64,66 sceneTick=30 pathIndex=3/9`** | **一致** |

⇒ 修复把电池**拉回了它本来要复现的那个场景**。

---

## 3. 修法（`PathingRegressionTask`，夹具层）

1. **加场景局部基准** `private int sceneTicks`：runner 建好（=本场景开始执行）那一刻置 0，
   每个执行 tick `++`；`tickFixtures()` 一律用它。`ticks` 保留给总预算，并加注释禁止再拿它做场景内时机。
2. **声明了夹具就必须真的动手**：场景判据新增
   `wallTick>0 ⇒ walled`、`disturbTick>0 ⇒ disturbed`，否则该场景 `FAIL`，detail 带
   `/FIXTURE_NOT_FIRED=[wall]` 或 `[disturb(no_valid_cell)]`。
3. **删掉静默降级**：旧代码扰动找不到落点时 `disturbed = true;` —— **没传送却当成做过**，
   场景会退化成普通 `place_course` 却照样报 PASS。改为 `disturbGaveUp` + `warn`
   （`disturb_not_applicable`）并让本场景判 FAIL。
4. 日志可判读：`wall_placed … sceneTick=30 pathIndex=3/9`、`disturbed … sceneTick=32`、
   场景开始行补打 `wallTick=/disturbTick=`、结果行补 `/sceneTicks=`。

**修复后（隔离跑，2 轮逐字相同，`ticks=493`）**：

```
[Regression] wall_placed scene=place_course+wall    at=5, 64, 66 sceneTick=30 pathIndex=3/9
[Regression] scene=place_course+wall    result=PASS detail=COMPLETED/replans=1/minReplans=1/route=TRAVERSE,DIAGONAL,FALL,PLACE_STEP_AND_TRAVERSE/sceneTicks=73
[Regression] disturbed   scene=place_course+disturb from=4, 64, 66 → 4, 64, 67 sceneTick=32
[Regression] scene=place_course+disturb result=PASS detail=COMPLETED/route=TRAVERSE,FALL,PLACE_STEP_AND_TRAVERSE/sceneTicks=69
```

变化是真的：`+wall` 的路线**从含 `ASCEND` 变成不含**（`TRAVERSE,ASCEND,DESCEND,…` → `TRAVERSE,DIAGONAL,…`），
墙从"起点前方第 2 格"变成"**走到第 3/9 格时**在前方第 2 格"。

> **与历史红的关联：只是推测，不声称因果。** 历史红之一是 `+wall` 报 `ASCEND_NO_HEADROOM`，
> 而**修复前该场景路线恰好含 `ASCEND`**、修复后不含 ⇒ 这是"夹具把 bot 逼进一条它本不该走的 ASCEND 绕行"
> 的合理怀疑。但**没有**留下当时的日志（见 §1），**无法证明**，故不写成结论。

---

## 4. 缺陷②：`DIAGONAL` 覆盖是偶发凑出来的（修 ① 之后电池先变红）

修完 ① 后全量 `core` **2/2 FAIL**：

```
pathing=FAIL … coverage=FAIL([DIAGONAL])
executed=TRAVERSE,ASCEND,DESCEND,DOWNWARD,PILLAR,FALL,BREAK_AND_TRAVERSE,BREAK_AND_ENTER,PLACE_STEP_AND_TRAVERSE
(passed=29/30 skipped=0) ticks=3386 → FAIL
```

**根因（代码事实）**：`REQUIRED_COVERAGE` 要求 9 种可执行 Movement 各被执行过至少一次，但
**没有任何场景是"真的会执行对角线"**：

- `dip_course` 的地形注释**明写**标定路线是 `TRAVERSE + DIAGONAL + DIAGONAL + TRAVERSE`，
  可它注册成 `Kind.PLAN_FIRST_TRAVERSE`（**只规划、不执行**）⇒ 不进 `executedUnion`；
- `lava_course` 也是只规划（首步 `DIAGONAL`）；
- 于是 `DIAGONAL` **只**来自 `+disturb` 修前那条偶发绕行（`0,64,67 → 1,64,66` 的斜向回归）。

⇒ **覆盖断言此前是靠运气过的**；① 让那条绕行消失，断言就如实报缺。

**修法**：加一行复用同一份地形的执行场景（`+` 后缀在 `prepare()` 里取 `dip_course_terrain`，无需新数据包文件）：

```java
execute("dip_course+run", new BlockPos(0, 64, 66), new BlockPos(-1, 64, 63), false,
        MovementType.TRAVERSE, MovementType.DIAGONAL),
```

实测（隔离跑）：

```
[Regression] scene=dip_course+run result=PASS detail=COMPLETED/route=TRAVERSE,DIAGONAL/sceneTicks=20
[Regression] SUMMARY … dip_course=PASS dip_course+run=PASS … coverage=PASS
executed=TRAVERSE,DIAGONAL,ASCEND,DESCEND,DOWNWARD,PILLAR,FALL,BREAK_AND_TRAVERSE,BREAK_AND_ENTER,PLACE_STEP_AND_TRAVERSE
```

`dip_course`（只规划，断言 `first=TRAVERSE`/Q7 路线偏好）**保留不动** ⇒ 两个断言都在，且对角线变成**确定性覆盖**。

---

## 5. 缺陷③：无头通道并不"无噪声" —— 敌对生物会杀 bot / 推 bot（**本轮最重要的通道事实**）

修 ① 之后、确认 ② 的那一批 `core` 里，第 1 轮**不是断言失败，而是没有判决**：

```
[alice] [R4 Session] failed session=regression-place_course+wall-0 status=MOVEMENT_FAILED code=PLACE_NO_VALID_FACE index=1 actualFoot=1, 64, 66
[PathRetry] replan attempt=1 … feet=1, 64, 66
… session …-1 failed code=TRAVERSE_INVALID_PRECONDITION index=0 actualFoot=… (feet=1, 64, 68)
[Regression] wall_placed scene=place_course+wall at=3, 64, 68 sceneTick=30 pathIndex=0/8     ← 墙被放到 z=68（起点是 z=66）
[alice] 假人死亡: Alice was blown up by Creeper → 直接清除
[Headless] RESULT verdict=no_verdict exit=3 ticks=3259
```

**读法**：bot 先被**推离预期格**（`z=66 → 68`，于是 `PLACE_NO_VALID_FACE`、
`TRAVERSE_INVALID_PRECONDITION`、墙被放到 `z=68`），随后被**炸死** ⇒ 电池无判决。
**这不是断言失败，是环境噪声。**

### 5.1 事实

| 项 | 事实 | 出处 |
|---|---|---|
| 无头服务端难度 | `difficulty=easy`、`spawn-monsters=true`、`pvp=true` | `/home/fb486/alice-server/server.properties` |
| 夹具是否会生成敌对生物 | **不会** —— 场景函数只 `summon minecraft:marker`，代码只 `summon minecraft:item`（掉落物播种） | `grep -rn "summon" tools/test-scenes/ src/main/java` |
| 代码是否依赖难度 | **不依赖**（全仓零 `getDifficulty`/`Difficulty.` 命中） | `grep -rn "getDifficulty\|Difficulty\." src/main/java` |
| 死亡后果 | `假人死亡 → 直接清除` ⇒ 任务消失 ⇒ `verdict=no_verdict exit=3`（**不是 FAIL，是"没有判决"**） | 上面日志 |

⇒ 怪物来自**环境**（自然生成 / 存档母本自带），**不是**被测行为的一部分。

### 5.2 修法（**改环境，不改生产代码**）

`tools/headless-battery.sh` 在启动前把**无头服务端自己**的 `server.properties` 设成
`difficulty=peaceful` 并打印一行 `夹具洁净度：difficulty=…`。理由：
- 脚本本来就**拥有**这个服务端目录（它做 `--install`、拷世界、装数据包、清 `alice_*.dat`）⇒ 环境是它的职责；
- `peaceful` 会**连存档里已有的**敌对生物一起清掉（`spawn-monsters=false` 做不到这点）；
- **不碰客户端存档、不碰 `mods/` 之外的任何东西、零生产代码改动**；
- 反过来说：**这一行也让"隔离跑"和"全量跑"之间少了一个不可见变量**。

### 5.3 为什么它是"历史 3 红"目前**最强的**候选（但仍不声称已证）

`place_course+wall`（`ASCEND_NO_HEADROOM`）、`place_course+disturb`（`STALE_START`）、
`partial_search`（起点 `from.z` 差 2 格）—— **三个症状的共同点都是"bot 不在预期位置"**，
而本轮**直接拍到**了"怪物把 bot 推离预期格"这条机制（`feet=1,64,68` + 同轮死亡）。
但：那 3 红的日志已随 `/tmp` 丢失（§1），**无法复算** ⇒ 记为**强候选，未证**。


## 6. 遗留 / 待确认（**没有**顺手改）

1. **1 格横向漂移不触发漂移检测，被控制器"走回去"吸收**：修复后 `+disturb` 在 `sceneTick=32` 把 bot
   从 `4,64,66` 平移到 `4,64,67`（第 3 段进行中），结果是 `replans=0`、**无 `resync` 日志**、
   该段 `ticks=9`（同场景其它 TRAVERSE 段 `4~5`）后正常完成。⇒ 这是**正确的自愈结果**，但机制是
   "移动控制器把 bot 拉回目标格"，**不是** `PathSession` 的重同步、也不是重规划。
   与 skill 已登记的偏离（`alice-baritone-kernel-alignment`：漂移检测用 `低于最低脚位 >1 格 / 离两端 >3 格`
   **距离启发式**，标注"临时实现，待改为 Baritone 的 `getValidPositions().contains(feet)`"）是**同一处**：
   `>3 格` 阈值让 1~3 格横向漂移**绕过**漂移检测。本次结果正确 ⇒ 不改，但建议单独立项按 Baritone 对齐。
2. **历史 3 红**：现在有 3 个候选（起步位置 ✗ 已实测排除、夹具时机 ①、怪物 ③），
   **没有一个被证明**；台账按"**不复现 + 三个候选**"更新，**不**宣布修好。
3. **`PathSessionDiagnosticTask`（物品流）的同类静默降级 —— ✅ 已修（§9）**：找不到扰动落点时
   `disturbed = true` + info `disturb_skipped`（**不传送却算做过**）；封路始终不适用时**一行日志都没有**。
   现在两侧共用 `FixtureScript` 原语 + 终态判 `FIXTURE_NOT_FIRED` ⇒ 路径走完但夹具没生效 = **FAILED**。
   物品侧接线本轮只到 `COMPILES`（客户端入口）⇒ **下一次 R4 客户端轮次是它的验收点**。
4. **怪物只在无头通道被清零，客户端仍是 `easy` 有怪物**（有意不同构，见 §5.2）⇒
   **用户 2026-09-15 裁定：客户端先不动** —— 客户端本就是真人看着跑（能一眼看出"被怪打了"），
   夹具层清实体属新行为、要再占一轮客户端验证。**先观察**下一次客户端电池是否真被怪物干扰
   （判据：日志出现击退/位移 + 判决异常），**有证据再改**。届时候选 =
   `RegressionBatteryTask` 起始处 `kill @e[type=#minecraft:hostile, distance=…]`。
5. **食物/疾跑门控是否也构成"长跑 vs 短跑"的第二条不可见变量**：本轮**没有**证据 ——
   全量日志里 `sprint|food|hunger` 命中 **0 行** ⇒ 无法从日志判定，仅记为**待测猜测**，未动。

---

## 7. 验证结果（本轮的判据）

| 轮次 | 目标 | jar | 结果 |
|---|---|---|---|
| 改前 | `single:pathing` ×3 | `f478d9f…`（T3 轮 18 的旧 jar 之后重建） | PASS 3/3，`ticks=496`，场景行**逐字相同** |
| 改前 | `core` ×3 | 同上 | PASS 3/3（`30/30`） |
| 修① | `single:pathing` ×2 | 修① | PASS 2/2，`ticks=493`；`wall_placed sceneTick=30` / `disturbed sceneTick=32` |
| 修①② | `core` ×2 | 修①② | 第 1 轮 **`exit=3` 无判决（Creeper）**；第 2 轮 PASS `30/30` `coverage=PASS` |
| 修①②③ | `core` ×3 | 修①②③（peaceful） | **PASS 3/3**，`coverage=PASS`，`30/30`；`grep -ci 'creeper\|假人死亡'` = **0**；**三轮 pathing 场景行逐字相同** |
| 最终 | `core` ×2 + ×3 | 同上 + `sceneTicks` 归零重构（`build/libs/alice-1.0.0-1.20.1.jar` = `67114b1d…`） | **PASS 5/5**，`coverage=PASS`，`30/30`，怪物 0 |

**合计（修①②③ 之后）**：`core` **8 轮全绿**（`passed=30/30`、`coverage=PASS`、`grep -ci 'creeper\|假人死亡'` = 0），
且 **8 轮的 `[Regression] scene=` 行逐字相同**（同一份 `latest.log` 片段可 `diff`）。
对照：改前同一条无头通道是 **9 轮 3 红**。
> **诚实边界**：8/8 绿**不能**证明"抖动已被根除"（若真实红率 10%，8 连绿的概率仍有 ≈43%）。
> 能说的是：**三个具体缺陷被证伪/修掉**，且**修后没再出现**；历史 3 红的归因仍停在"候选"（§1）。

`post-peaceful-core-r{1,2,3}` 之间唯一差异：`写入类例外=56/43/51`（K4 的**写入类例外计数**，来自前面的
挖掘/伐木步）与总 `ticks=3394/3328/3371` —— **所有步骤判决逐字相同**（30/30 PASS）。
⇒ 判决层确定；只有"写了几次"这类计数不稳定（属预期，非本次范围）。

---

## 8. 复算命令（不依赖本次会话）

```bash
cd /home/fb486/projects/alice
./gradlew build --no-daemon                                   # 产出交付 jar
bash tools/headless-battery.sh single:pathing --no-build      # 秒级～1 分钟，路径串联回归
bash tools/headless-battery.sh core --no-build                # ~3.5 分钟，CORE 全量（判据含 coverage）
L=/home/fb486/alice-server/logs/latest.log
grep -aE 'wall_placed|disturbed|scene=place_course\+|dip_course\+run' "$L"   # 夹具时机 + 对角线覆盖
grep -aoE 'coverage=[A-Z]+.*executed=.*' "$L" | head -1                      # 9 种 Movement 覆盖
grep -aoE '\(passed=[0-9]+/[0-9]+ skipped=[0-9]+\).*→ (PASS|FAIL)' "$L" | tail -1
```

本轮证据留存：`/home/fb486/alice-evidence/2026-09-15-start-anchor/`（**含 `README.md` 逐文件说明**）。
⚠️ **不要再往 `/tmp` 放证据**：本轮开始时上一会话的 `/tmp` 证据已被清空，历史 3 红因此无法复算（§1）。

---

## 9. 追加（同日）：把"同一份夹具两处实现"合并，并补掉物品侧剩下的静默降级

§5 的教训不只是"时机算错了"，而是**同一份语义被写了两遍**（电池侧 `PathingRegressionTask` /
物品侧 `PathSessionDiagnosticTask`），分叉**没有任何编译期信号** —— 实测两处分叉：
时机基准（任务级 tick vs 本趟运行 tick）、封路目标格（`footCell` vs `bot.blockPosition()`）、
放弃策略（都静默）。所以这次**不是再打一个补丁，而是消掉这一类**：

**新增 `task/FixtureScript`（共用原语，只收"能共用的部分"）**
- `wallPlan(bot, projectedPath)` → `WallPlan(target, pathIndex, pathLength)`：**脚位一律 `MovementHelper.footCell`**
  （D-105：箱子 0.875 / 底半砖 0.5 这类非满高支撑下，用 `blockPosition()` 会取错格）；
  目标格非空气 ⇒ 返回 `null`（这次封路没意义），由调用方重试或如实上报。
- `notFired(missing)` → `/FIXTURE_NOT_FIRED=[…]`：**两侧共用同一串** ⇒ `grep FIXTURE_NOT_FIRED` 一处通吃。
- 仍然**各持**（靠注释钉住）：时机计数器（电池按场景 / 物品按本趟运行）与终态策略。
  按 D-220，两边都**必须响亮**，不许静默降级。

**物品侧（`PathSessionDiagnosticTask`）的三处"响亮"**
1. 扰动找不到落点：旧行为 `disturbed = true` + **info** `disturb_skipped`（**不传送却算做过**，整趟退化成
   普通路径测试却报 DONE）⇒ 现在 `disturbGaveUp` + **warn** `disturb_not_applicable`；
2. 封路始终没放下石头：旧行为**连一行日志都没有** ⇒ 现在终态判据把它变成 `FIXTURE_NOT_FIRED`；
3. 结果行带夹具字段：`[R4 Session] result … /FIXTURE_NOT_FIRED=[wall]`，且**路径走完但夹具没生效 ⇒
   `Status.FAILED`**（理由串 `FIXTURE_NOT_FIRED:[wall]`）—— 不再"干净地报 DONE"。
   若路径本身也失败，则保留路径失败为理由，夹具未生效另记 warn（两条都不吞）。

**验证（诚实分级）**
- **`SERVER_TESTED`（共用原语）**：`FixtureScript` 被电池侧调用 ⇒ 无头通道**逐字等价**验证了重构：
  `single:pathing` 场景行**与重构前逐字相同**（`diff` 全等），`wall_placed at=5,64,66 sceneTick=30
  pathIndex=3/9`、`disturbed … sceneTick=32`、`coverage=PASS` 9 种 Movement 全覆盖；
  `core` ×2 仍 PASS `30/30`。
- **`COMPILES`（物品侧接线）**：扰动放弃 / 未生效 ⇒ FAILED 这条**只有在客户端右键那两件物品才会执行**
  ⇒ 本轮**不冒充** `SERVER_TESTED`。**下一次客户端跑 R4 时必须看**：
  `[R4 Fixture] disturbed|wall_placed` 必须在（否则就是它如实报 FAILED，而不是 bug）。
