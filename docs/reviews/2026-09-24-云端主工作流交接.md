# 交接：云端转为主工作流，本地转为兜底副本（2026-09-24 用户裁定）

## 用户裁定（原话要点）
- **只迁移 `projects` 工作区内的会话**（不迁其它 slug、不迁全部会话）；
- **`settings.yaml` 不迁移**（云端保持它自己那份）；
- 迁完之后，**本地这份（我）成为兜底副本：不在用户授权下不参加工作**；
- 用户将**把云端当主工作流**；遇到问题再回来找本地。

## 已完成（都有实测证据）
| 项 | 状态 |
|---|---|
| 云端 **git 凭据** | ✅ `git config credential.helper store` + `~/.git-credentials`(600) ⇒ `pull` 成功、`push --dry-run` 通过（此前报 `could not read Username`） |
| 云端 **preset** | ✅ 补入 `alice-forge-assistant` + `whale-girl`（迁移会话头记录的 preset 就是它，缺了会导致 resume 失败） |
| 云端 **工作区登记** | ✅ `~/.dsh/storages/workspace.json` 加 `/home/fb486/projects`（重启后保留） |
| 云端 **路径与 slug** | ✅ `sudo mkdir /home/fb486/projects` + `alice → /workspaces/Alice-mcbot` 软链；服务以该 cwd 启动；slug 约定 `--path--`（启动脚本已修 `5916f0f`） |
| **试点会话** | ✅ 能列出、能恢复、能写：云端 rc.3 成功写入「你好」并进入工作态（用户手动截断）；22 个 zstd 帧全部可解、仍为 v3 世代 |
| **单向门** | ✅ 机械层面：无升代、本机可解码；本机原件未被改动 |

## 迁移范围
- **迁**：`~/.dsh/sessions/--home-fb486-projects--`（177 个会话 / 284 M）；
- **排除**：试点会话 `session-99966497-…`（保留云端那份含「你好」的版本，不被本地旧版覆盖）；
- **不迁**：`--home-fb486--`、`--home-fb486-projects-alice--`、`--home-fb486-projects-Mod-on-Forge-1.20.1--` 等其它 slug；`settings.yaml`；`storages/`（云端自己那份）。

## ⚠️ 已知风险与纪律
1. **单写者**：迁移过去的会话**不要两边同时写**（会分叉且无法合并）。本会话（`--home-fb486-projects--` 里的活跃会话）
   在迁移瞬间正在被本地写入 ⇒ 副本的**末帧可能被截断**；若云端加载失败，让本地重取一次该会话即可。
2. **模型差异**：云端 `settings.yaml` 与本地不同 ⇒ 迁过去的会话可能需要切模型（实测切到
   `deepseek-official/deepseek-flash` 正常）。
3. **本地待命规则**：本地侧的 AI **未获授权不参加工作** —— 不改代码、不跑门禁、不提交、不主动推进；
   仅在被明确要求时行动（本文件即为该规则的书面依据）。本地仍保留**全部原件**（迁移是复制不是移动）。

## 云端上手指南（给云端 agent / 用户）
- 云端仓库：`/workspaces/Alice-mcbot`（`/home/fb486/projects/alice` 软链指向它）；
- 云端开发+编译+无头测试怎么跑：`docs/CLOUD_MIGRATION.md` §14（含 `ALICE_CLIENT_SAVE` / `ALICE_CLIENT_MODS` 两个环境变量与 CORE PASS 记录）；
- 世界母本权威：`docs/CLOUD_MIGRATION.md` §15（权威 = 存档派生版；两端哈希一致）；
- 客户端测试数据通道（Windows 管家 agent + 文件信箱）：`docs/CLIENT_AGENT_CHANNEL.md` 与
  `docs/CLIENT_AGENT_NEW_DEVICE_TEST.md`；
- **排期唯一来源**：`docs/OPEN_ITEMS_LEDGER.md` §11（未完成项在队首）。
