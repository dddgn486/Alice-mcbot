# 2026-09-25 真机第四轮（机制 A jar）：「被自己搭的方块拦住」

> 用户报告：**「一个没有修复的问题，bot 被自己搭的方块拦住了，请看截图」**（截图 `2026-09-25_23.17.44.png`）。
> 本文只放事实 + 可复算的读数 + 候选修法；**未改任何行为代码**。

## 0 取证条件

| 项 | 值 |
|---|---|
| jar | `mods/alice-1.0.0-1.20.1.jar` sha256 `7a236881a4bae91c56aae7810fa0176dce5898fd4ef3ac093a2215d9d5c0ba0c`（= 机制 A 的工件） |
| 日志 | `logs/latest.log`，**2506 行**，`23:14:44` → `23:17:49`（整个文件就是这一轮） |
| 作业 | `23:16:08` `dir=east main=20 spurSpacing=3 spurLen=32 side=BOTH start=-68,54,172 units=404` |
| 结局 | `SUMMARY dir=E main=3/20 spurs=10/12 abandoned=2 mined=37 skipped=3 ores=24/25 searchNodes=484 searchLimit=0 return=no worldChangesInside=61 outside=0 ticks=1693 → FAIL` |

## 1 ✅ 机制 A 生效（先记好消息）

| 读数 | 本轮 | 上一轮（片 A jar） |
|---|---|---|
| `[R4 Session] segment_stall` | **0 次** | 2 次（各 145 tick） |
| 「停在洞沿上」 | 未出现 | 2 处 |

⇒ 「向下挖却在边缘没掉下去」这一条**本轮没有复现**。（验证等级：`WINDOWS_CLIENT` 由用户下一轮确认。）

## 2 事故链（每一环都有日志锚点）：`(-65, 54, 181)` 的一生

这一格是**主巷单元 3 的 south1 支巷、第 9 格（unit 44 cell 1/2 = south9）的脚位格** —— 也就是
**bot 自己刚挖通的通道格**。

| 时刻 | 事件（逐字） |
|---|---|
| `23:17:20.506` | `[Fishbone] cell unit=44/404 cell=1/2 target=-65, 54, 181 spur=south9` |
| `23:17:20.605` | `[WRITE] break -65, 54, 181 minecraft:stone by=fishbone:BULK_EDIT` ⇒ **变成空气（通道的一部分）** |
| `23:17:21.704` | `[WRITE] break -65, 55, 181`（头位格）⇒ 通道净高 2 挖通 |
| `23:17:22.653` | `CHASE_APPROACH 走进刚挖完的那一格 -65, 54, 181` ⇒ **bot 走进去站在里面**（脚位 -65,54,181） |
| `23:17:23–29` | 追簇往上挖：`-65,56,181` / `-65,57,181` / `-66,56,181` … ⇒ 在**自己通道的正上方掏出一根烟囱** |
| `23:17:29.5 / 31.0` | 收集器为够一个落在 `-66,56,181` / `-67,57,181` 的掉落物，规划 `PILLAR + ASCEND` 从通道往上爬 |
| ⚠️ `23:17:31.257` | **`[WRITE] place -65, 54, 181 minecraft:cobblestone by=collect-drops:attempt0:STEP_PLACEMENT`** ⇒ **bot 把自己刚挖通的通道格又填回了圆石**（`[Pillar] placed pos=-65,54,181 from=-65,54,181 to=-65,55,181 feetY=55.166`：踩着它爬上去） |
| `23:17:31.257` | `[Ledger] skip -65, 54, 181 place=…（区外：D-398 R1/R2 不记账、不恢复；累计 15 次）` ⇒ **这块圆石永远不会被回收** |
| `23:17:31.310` | `[Events] PICKUP_DETOUR … 为拾取掉落物**改造地形**` |
| `23:17:32.006` | `goal_excluded cell=-66, 56, 181`（模型说够得着、执行期 `inPickupRange` 判否）⇒ `no_standable_approach` ⇒ **`retire` 该掉落物** ⇒ **白爬一趟，3 件掉落物留在地上** |
| `23:17:36.056` | 下一个支巷格 `-65, 54, 182` ⇒ `no_valid_standing_point`（`footPassable=false headPassable=false belowSolid=false`：脚下两格都空了）⇒ `spur_abandoned` **放弃 23 个单元** |
| ⚠️ `23:17:36.155` | `SPUR_RETURN 原路退回主巷 feet=-66, 56, 181 → junction=-65, 54, 172` ⇒ **`UNREACHABLE`**（`descend_precondition=11`、`nodes=3`、`searchLimit=0`）⇒ `spur_return_failed:PLAN_UNREACHABLE` |
| `23:17:36.204` | `RETURN 开始 feet=-66, 56, 181 → start=-68, 54, 172` ⇒ **也 `UNREACHABLE`**（`descend_precondition=11`、`nodes=3`）⇒ **`return_failed`** ⇒ 作业 `FAILED`，**bot 被留在原地** |

**与截图一致**：bot（tango）停在 `(-66,56,181)` 附近 —— 它自己掏出来的那根烟囱口上，
旁边就是它自己填进去的那块圆石所在的通道列。

## 3 根因（分成三层，都是**事实层**，不是推测）

### 3.1 直接原因：**放置落进了"作业自己的通道必需格"**

`collect-drops` 的 `PILLAR/STEP_PLACEMENT` 把方块放进了**它自己刚挖出来、而且自己刚走过**的通道脚位格
（`(-65,54,181)` = unit 44 的脚位格）。放置前该格是空气、且是**主巷往返的必经格**（回程 `SPUR_RETURN` 的
目标就是沿这条支巷退回 `(-65,54,172)`）。
⇒ 用户看到的现象「**bot 被自己搭的方块拦住**」在日志里就是这个动作。

### 3.2 系统原因：那块圆石**按裁定永远不会被回收**

`WorldModLedger.recordPlacement` 第一条：
```java
// ⭐ D-398 R1/R2/R4：区外不记账（= 不承担恢复责任 = 不产生回收义务）。
if (ProtectionZones.isWild(level, pos)) { … skip … }        // isWild == !isProtected
```
矿洞这一带**没有保护区**（`isWild==true`）⇒ **不记账、不恢复** ⇒ 没有任何流程会拆掉它。
⚠️ 这**不是遗漏**：`WorldModLedger:165` 逐字写着「这是"区外一定不恢复"的直接推论，不是遗漏；
**要改就得改裁定本身**」（`D-398` R1/R2）。⇒ 想靠"记账 + 回收"来修，得先动这条裁定。

### 3.3 未定：**"下不去"到底是谁拒的**

`descend_precondition` 计数 = `!MovementHelper.canDescend(level, from, to)`（`SurfaceMovementProvider:102`），
**它是一个聚合计数，日志里不打印是哪一条子条件拒的**（`canDescend` 内部有多条）。
另有两条**读数上的疑点**必须留着：
- `nodes=3` —— A\* 只展开了 3 个节点就判 `UNREACHABLE`（`searchLimit=0` ⇒ 不是限流）；「起点脱困」
  那条受限首跳路径（`SurfaceMovementProvider` 的 `S-1 / D-133`）也会产生这种"只探几步"的形状，
  但**日志不足以判定**。
- 目标只是"沿自己挖的通道往回走 9 格、下降 2 格"，直觉上不该无路可走。

⇒ **这一层必须靠一次探针取证**（把 `canDescend` 拒的那一条子条件打出来），不许凭代码猜。

### 3.4 顺带：本轮另一条弃巷仍是**旧账**

`23:17:00.554 no_valid_standing_point target=-65, 54, 163 faceStandable=1/6 **footPassable=true**
headPassable=false belowSolid=false` ⇒ `spur_abandoned`（放弃 24 个单元）。
`footPassable=true` = **目标格自己已经是空气** ⇒ 正是 `D-444`/`1.4j①` 那条 **`isAlreadyPassable` 用两格谓词
问单格** 的签名（3/3 同形）。⇒ **这一条仍未修**（等用户裁定是否提到片 B 开头）。

## 4 候选修法（**未实施**，请裁定）

| # | 修法 | 代价 / 边界 |
|---|---|---|
| **A** ⭐ | **通道保护 `I5`（`1.4j②`，`D-443` 裁定 8 已定）**：把"作业形状驱动的通道必需格集合"接进**放置准入** ⇒ 谁都不许把方块放进作业自己的通道格（行走层 + 净空 + 支撑格）。**本截图就是这条裁定的第一份真机证据** | 判据现成（作业形状已知）；实现要接进放置那条链路，属片 C 范围 |
| **B** | **"只拆自己放的"自清理**：作业在**单元/支巷收尾**时，把自己放的 STEP/PILLAR 方块拆掉（不依赖 ledger 的区外裁定） | 与 `D-398` R1/R2 的"区外不恢复"**不冲突**（这是作业自己的清理，不是 ledger 的回收义务）；但要新增"作业自己的放置记录"这一份状态 |
| **C** | **别爬**：当"够那个掉落物"的唯一路径需要**把方块放进自己的通道格**时，直接不爬（如实放弃那件掉落物） | 最小改动、代价是掉一件；与 3.1 同源，但只治"爬"这一处 |
| **D** | **探针（先做，不改行为）**：把 `canDescend` 的拒因分类打出来 + 打印 `SPUR_RETURN` 失败时的 3 邻域格状态 ⇒ 把 3.3 从"未定"变成事实 | 一个临时诊断轮，之后删掉 |

**我的建议**：**先做 D**（一轮就能把 3.3 定死，否则修 B/C 都是在猜），**同时**把 A 排进片 C
（它本来就是已裁定的条目，本轮给了它真机证据）。

## 5 需要用户回答的现场问题

1. 截图里**挡住 bot 的那块圆石**，是不是就在它**身下/身侧那一格**（`(-65,54,181)`）？还是另有别处（比如画面上那片大片圆石平台是什么）？
2. bot 当时是**完全不动**（卡死），还是在原地小幅抖动/尝试？
3. 它上方那根**竖井（烟囱）**是你看到的吗？bot 是站在井口/井里吗？
4. 复现频率：这一轮是**偶发**还是每次都这样？
