# 客户端一次跑 · 测试单（2026-09-17）

> 目的：把当前所有**只有客户端能回答**的项一次跑完。**中途不管是否符合预期都不要停**，
> 最后把"你看到了什么"一次回给我即可（日志我会自己去读 `1.20.1-Forge_47.4.10/logs/latest.log`）。
>
> **回我三样就够**：① 做了哪几项（编号）② 每项你**实际看到**什么（一句话，能不能对上"期望"）
> ③ 任何意外（卡顿/报错/奇怪画面/复现不了）。**不需要你贴日志**，除非我随后追问。

## 0. 起跑前（30 秒）

- [ ] **jar 是这版**：客户端 `mods/alice-1.0.0-1.20.1.jar` 内容摘要应为
      `fe2b99e09febd5bdf76bd49ab6610caf94740ead22dfbdbb76ff58625afe1c74`（761 条目，时间 09-17 18:49）
      ⇒ 判定命令：`./tools/jar-content-hash.sh --compare build/libs/alice-1.0.0-1.20.1.jar "<客户端>/mods/alice-1.0.0-1.20.1.jar"`
- [ ] **重启客户端**（jar 是 18:49 同步的；之前就开着的话不重启不会加载）
- [ ] 进世界 `新的世界`，确保世界里有一个 bot：`/alice spawn Alice`（已存在则跳过）
- [ ] `mods/` 里的 `.bak.*` 文件不用管（不是 `.jar` 结尾，Forge 不加载）

---

## 1. `alice:job_launcher` → 看 `[Job] launch`（约 2 分钟）

| 项 | 内容 |
|---|---|
| 前置 | bot 在场 |
| 入口 | `/give @s alice:job_launcher` → **右键** |
| 期望 | 日志出现 `[Job] launch bot=Alice <job 描述>`，随后是正常的伐木/任务流与 `task_execution_terminal` |
| 请回填 | 见到 `[Job] launch` 了吗？（有/没有）· bot 实际有没有去干活 · 有没有报错 |

> 意义：矩阵注记 ② 查明"电池从不经过 `JobLauncher`" ⇒ **只有这条物品路径能证明统一 Job 入口还活着**。
> 若日志里**一条都没有**，请告诉我"右键有没有反应"（可能被拒：`[Job] launch 拒绝 …`）。

---

## 2. 死亡目视（约 2 分钟）

| 项 | 内容 |
|---|---|
| 前置 | bot 在场，站在安全地面 |
| 入口 | `/kill Alice`（或让它烧死/淹死） |
| 期望（画面） | bot **干净消失**：**无掉落物**、无死亡动画残留、任务高亮被清除、实体列表不残留 |
| 期望（日志） | `假人死亡: <死因> → **倒下态**（存档保留：pos=… tick=…）` |
| 期望（重启后） | **不会多出一个 bot**；日志现 `存档假人处于**倒下态**（数据保留，等待复活机制）：health=0.0 cause=… pos=… tick=…` |
| 请回填 | 画面是否干净（有无掉落/残留/幽灵实体）· 重启后有没有多出一个 bot · 若用**着火**路径，任务有没有被中断（那是**正确行为**，不算 bug） |

> 数据侧我已无头端到端验过（落盘 + 重启读回）；这条只补"看得见的行为"。
> **若重启后多出一个 bot，立刻告诉我** —— 那是 D-276 的漏洞。

---

## 3. `alice:permission_demo` → 请示界面（约 3 分钟）

| 项 | 内容 |
|---|---|
| 前置 | bot 在场 |
| 入口 | `/give @s alice:permission_demo` → **右键** |
| 期望 | 聊天栏出现**可点击的选项按钮**；点"批准"（或按提示 Y/N）后 bot 继续执行 |
| 期望（日志） | `[Perm] demo decision … → terminalReason=…`、`[Perm] demo SUMMARY reason=… ticks=… → DONE`、`[Perm] answer id=… option=… by=<你的名字>` |
| 请回填 | 按钮**点得动**吗 · 点了之后 bot 有没有真的继续 · 聊天栏有没有报错 |

> 意义：矩阵注记 ① —— 这是**唯一从未客户端验过**的决策层交互面
> （D-141 一格面板 → D-142 改成聊天按钮 + Y/N 之后仍待客户端；以前翻车点正是"游戏内鼠标被锁定 ⇒ HUD 按钮点不到"）。

---

## 4. `/alice auto-mine`（约 1 分钟）

| 项 | 内容 |
|---|---|
| 前置 | 附近有铁矿石（没有的话先 `/setblock` 放几个也行） |
| 入口 | `/alice auto-mine minecraft:iron_ore 8` |
| 期望 | bot 自己找到并挖够 8 个，正常收工；`task_execution_terminal kind=… terminalReason=…` |
| 请回填 | 挖到几个 · 是否正常收工 · `terminalReason` 里 `driver=` 是 `system` 还是 `in_game_player` |

> ⚠️ **已知缺口（不是 bug）**：`driver=system`（未归因）是**预期**的 —— 玩家侧归因尚未标注（台账 F1-残）。
> 你说"现在补"我就补。

---

## 5. `alice:restore_check`（约 1 分钟）

| 项 | 内容 |
|---|---|
| 前置 | 先制造点"痕迹"：在附近 `/setblock ~2 ~ ~2 minecraft:stone` 放两个方块（否则 `N=0`） |
| 入口 | `/give @s alice:restore_check` → **右键** |
| 期望 | `[RestoreCheck] 待恢复 N 条；bot 传送到 <坐标> 旁的可站格 <坐标>` + 恢复任务终态 |
| 请回填 | `N` 是几（与实际痕迹数对得上吗）· bot 落点是不是**可站格**（没卡墙里/没悬空） |

---

## 6. `/alice progress <ticks>` → 推送频率观测（约 3 分钟）

| 项 | 内容 |
|---|---|
| 前置 | 本轮 jar 新加的**游戏内开关**（此前只有夹具能开 ⇒ 这条一直没法测） |
| 入口 A | `/alice progress` → 应回 `进度事件间隔 = 0 tick（0 = 关）…` |
| 入口 B | `/alice progress 200` → 应回 `进度事件间隔 = 200 tick（有任务时每 200 tick 报一次 PROGRESS）` |
| 入口 C | 给 bot 派点活（如 `/alice auto-mine minecraft:iron_ore 8`），观察 10 秒 |
| 期望 | 日志/聊天出现 `[Events] PROGRESS … 进度：任务=… 进度=…`（每隔 10 秒一次）；不派任务时**不报** |
| 入口 D | `/alice progress 0` 关掉 ⇒ 不再报 |
| 请回填 | 开关查询/设置的回话对不对 · 有没有看到 `PROGRESS` 行、频率是否"够用" · 从你下命令到 bot 动起来的**体感延迟**（RTT） |

> 意义：`survey/16 §1.3/§12.1` 的"推送频率够不够 / 桌面 AI RTT"至今**零数据**；本项就是它的观测手段。

---

## 7. V-4 Baritone 对照 · FALL（约 8 分钟，需**另开一个实例**）

> ⚠️ **先读这一条（2026-09-17 实测）**：**不要同时开两个实例**。固定客户端与 `Bariton_contrast` 疑似**共用同一个 `config/`**
> （`Bariton_contrast` 没有自己的 `config/`）⇒ 两个实例同时跑，第二个会在世界加载时报
> **「数据包错误」**，但真因是 `config/sophisticatedcore-common.toml` 被另一个进程锁住（见 D-279）。
> **推荐做法**：Alice 侧跑完、**退出客户端**，再开 Baritone 侧跑；或确认对照实例用**独立游戏目录**（PCL 版本隔离）。

**两边并排**：Alice 侧 = 固定客户端；Baritone 侧 = `D:\JAVA_projects\worldedit-test\versions\Bariton_contrast`
（Forge `47.4.23` + `baritone-standalone-forge-1.10.5.jar`，世界 `新的世界` **已带数据包**，首次进服先 `/reload`）。

| 步骤 | 操作 |
|---|---|
| B-1 | Baritone 侧：`/function alice_test:contrast_fall` ⇒ 它生成场景、把你传到起点 `(22,64,68)`、在目标 `(23,61,68)` 放 marker、提示命令 |
| B-2 | **立刻**输入 `#goto 23 61 68`（手动输入有 0~N tick 延迟 ⇒ **跑 1~3 遍取最小**） |
| B-3 | 到达后看聊天 `[对照计时] … N tick` |
| A-1 | Alice 侧：`/function alice_test:fall_course` → 右键 `alice:pathing_fall` |
| A-2 | 读日志 `[Fall] SUMMARY …`（`[R4 Session] result … ticks=N` 若有） |

**⚠️ 读数前必须扣掉（否则结论会错）**
- **Alice 在 FALL 场景有 Baritone 没有的两条守卫**：`no_deep_fall`（深落差距绝）+ `fall_recover_guard`（落差恢复守卫）
  ⇒ **Baritone 直接跳下而 Alice 先判定/绕行，不是"Alice 更慢"，是策略不同**；只有**两边都走同一条下落路线**的那段 tick 才可比。
- **Baritone 会疾跑**，Alice core 从不疾跑 ⇒ 平地更快是**预期**。
- **不要**去开 `DESCEND_OVERSHOOT_GUARD`（默认关 = Baritone 原样）。

| 场景 | Alice ticks | Baritone ticks | 路线是否一致 | 备注（扣了哪些因素） |
|---|---|---|---|---|
| fall（3 格落差） |  |  |  |  |

---

## 8. V-4 Baritone 对照 · PILLAR（约 5 分钟）

| 步骤 | 操作 |
|---|---|
| B-1 | Baritone 侧：`/function alice_test:contrast_pillar` ⇒ 起点 `(24,64,44)`、目标 `(25,67,44)`（上升 3 格） |
| B-2 | **立刻** `#goto 25 67 44`（跑 1~3 遍取最小） |
| A-1 | Alice 侧：`/function alice_test:pillar_course` → 右键 `alice:pathing_pillar` |
| A-2 | 读日志 `[Pillar] SUMMARY …`（含 `pillar_plan`/`resource_guard`/`pillar_execute`） |

- 本场景**无**落差守卫 ⇒ 主要扣"疾跑"。这是**放置类**场景：注意两边"垫高"策略是否一致。

| 场景 | Alice ticks | Baritone ticks | 路线是否一致 | 备注 |
|---|---|---|---|---|
| pillar（上升 3 格） |  |  |  |  |

---

## 9.（可选，需你指定场景）性能数字

`survey/10 §25/§32/§38/§39` + `survey/11` 的 mobcap / 单机掉帧等目前仍是**推算**。
若你要测：告诉我场景（多 bot？大量实体？连续挖矿？）+ 时长。
**不想测也直接说"不测"** —— 我会在台账里关掉这几条并注明「用户放弃测量」，不让它悬着。

---

## 10. 意外观察（不用管编号，想到什么写什么）

- 有没有卡顿/掉帧/卡死？
- 有没有报错弹窗或聊天栏错误？
- 有没有紫黑贴图、橡皮筋、实体闪烁、任务高亮残留？
- 有没有"看起来正常但你就是觉得不对"的地方？
