# `survey/29` + `survey/30` 拉取与核对（2026-09-23）

> **本轮输入**：`survey/29-讨论记录-20260923.md`（473 行，原始素材）· `survey/30-诊断与下一步表-20260923.md`（277 行，结论/建议）。
> 两份都**只增不改**、**不构成实现授权**（`30` 头部逐字："本文不是待办清单，不是排期，不是审查任务，不是实现授权"）。
> **拉取**：`git fetch` ⇒ 远端 `98bd354..2f10f66`（`d6db852`=29 · `2f10f66`=30）⇒ `git pull --ff-only`，本地 Z1/CORE 提交**未受影响**。
> ⭐ 两份报告的基线 commit `d6db852` 的**父提交 = `98bd354`（我的 `Z1` 真机确认提交）** ⇒ 报告**已包含** `Z1`/CORE 瘦身，
> 其统计里 `D-408`、台账 2,152 行都对得上 ⇒ **不是过期视角**。

---

## 一、我逐条复算/复核的结果（`2f10f66` 工作树）

### 1.1 `survey/30 §1` 的量化表 —— **8/8 全部复算一致** ✅

| 指标 | 报告值 | 我复算 | |
|---|---|---|---|
| Java 文件 / 行数 | 496 / 103,526 | **496 / 103,526** | ✅ |
| `task/` 行数 | 51,556（49.8%） | **51,556** | ✅ |
| `task/` 夹具文件 | 84 / 181 | **84** | ✅ |
| `pathing/` 行数 | 8,397 | **8,397** | ✅ |
| `docs/` 行数 | 46,357 | **46,357** | ✅ |
| 决策条数（`### D-`） | 349 | **349** | ✅ |
| 台账行数 | 2,152 | **2,152** | ✅ |

⇒ **勘测侧的量化纪律成立**（可复算、命令写明来源）。§1.1 的三条读数（重心错位 / 台账自我生成 / 分母太大）
我**同意其算术**，评价见 §三。

### 1.2 `survey/30 §4.1` A1 —— **"`LumberJob` 缺终态闩锁"：代码事实成立** ✅

| 类 | 事实（我读到的） |
|---|---|
| `task/MineTask.java` | `:87 private Status terminalStatus;` · `:328-329 if (terminalStatus != null) return terminalStatus; // D-175：终态幂等` · `:333` 赋值 ⇒ **有闩锁** |
| `job/lumber/LumberJob.java` | `terminalStatus` **命中 0 次**；只有 `:209 private String terminalReason` + `:279 public String terminalReason()` ⇒ **无状态闩锁** |

⚠️ **但"这就是 `idempotent=false` 的根因"仍是推断**（报告 §6-2 自己声明没跑过）⇒ 判据应为
`single:lumber_job` 期望 `idempotent=true`（修前 false）**先红后绿**。

### 1.3 `survey/29 §2.1` —— **"击杀产物落 `FOREIGN`"链条：代码事实成立** ✅（含一次反证检查）

| 环节 | 我核到的原文 |
|---|---|
| `perception/ScopeBuffer.java:319-324` | `source = matchBreakSource(pos,tick)` → `provenance = null` → `matchIndirectOrigin(pos,tick)` |
| `:398`（`matchBreakSource`） | `for (BreakRecord record : recentBreaks)` ⇒ **只看破坏记录** |
| `:279`（`matchIndirectOrigin`） | **同样** `for (BreakRecord record : recentBreaks)` |
| `decision/DropPolicy.java` | `FOREIGN`（`:31`）⇒ `:95` 逐字"想捡 `FOREIGN` 只能**显式派活**" |
| ⭐ **反证检查（我加的）** | 全 `src/` 里**没有任何** `LivingDropsEvent` / `LivingDeathEvent` 消费者（只有夹具读 `ItemEntity`）⇒ **不存在补偿路径** |

⇒ 缺口"读代码这一侧"成立；**"杀一头牛 ⇒ 产物真落 FOREIGN 且静默"仍须夹具复现**（报告 §4-1 已如此声明）。

---

## 二、报告提出的候选（**勘测侧声明为候选、非指令**）

- `survey/30 §4`：三个杠杆 —— ① CORE 反馈 分钟→秒（A：修 `lumber_job`；B：**逐步缓存**，需先验步间独立性）
  ② 修假绿夹具（假绿比红贵）③ 把已有结论武器化（底线不进配置面 / 必选-可选守卫分界）。
- `survey/30 §5`：A1 终态闩锁 · A2 逐步缓存可行性验证 · A3 击杀归属归属 · A4 假绿夹具几何重做 +"能红"断言武器化 ·
  B1「下一步表」最小版（五列 + 一条门禁 + `tools/next-step.sh`）· B2 底线门禁 · B3 能力清单生成 + 防漂移门禁。
- `survey/30 §5.1` 明确**不建议现在做**：猎杀功能本体 · 怪物威胁线（除反射层以外）· 队列本体（`GoalRecord`）· 记忆库 · **任何新能力**。
- `survey/29 §5`：**`Q-1…Q-22` 待裁定**（其中 `Q-20` 队列闸改预算制 / `Q-21` 规范化哈希循环检测 / `Q-22` 能力清单生成
  ⇒ **用户已同意**；其余 19 条 ⏳）。**不在此处抄**——指针 = `survey/29 §5`。

### 2.1 我的评价（**不是裁定**）

| 候选 | 我的看法 | 与现队列的关系 |
|---|---|---|
| ⭐ **A1 终态闩锁** | **同意先做**：无前置、判据现成（`single:lumber_job`）、且直接决定 CORE 能否转绿 ⇒ 决定 `D-352` 缓存能否生效 | 与 `§11` 的 `Z2/Z3` **不冲突**（不同 scope：`job/lumber` vs `ledger/restore`） |
| ⭐ **A2 逐步缓存** | **同意先"验证"再"改机制"**：报告自陈"若步间不独立 ⇒ 必须放弃"。本项目已有现成判据 = `D-201` 附注一的逐步 diff。⚠️ 与 A1 **scope 不相交**，可并行 | 新增项，进 `§11` |
| **A3 击杀归属** | **同意"归属先行"**：先只做登记（不写攻击），判据 = 新夹具断言 `provenance != FOREIGN`；这是真缺口且不依赖任何设计 | 新增项（能力线 P3 之前置） |
| **A4 假绿几何** | **已在 `§11`**（`Z1` 尾项）⇒ 与报告一致，无需新增 | 已有 |
| **B1 下一步表** | ⚠️ **我持保留**：它要引入**第五个真相源**（台账 / 电池步名 / 门禁名 / 契约表之外）。报告自己写了"最小版 + 一条门禁，否则变成第 350 条决策"——**同意这个克制，但建议排在 A1/A2 之后**（先让方法成本真的降下来） | 待用户拍板 |
| **B2 底线门禁 / B3 能力清单** | 都便宜、都能红 ⇒ 可按报告顺序 | 新增项 |

---

## 三、我必须点出的两处（诚实边界）

1. `survey/30` 的**诊断结论（"方法太贵"）我认同**，但它的**处方（换"下一步表"）不解决成本本身**——
   成本来自"每次改动要同步 6 处 + 一次电池"；`§4.1 A1/A2` 才是**真的降成本**（把 CORE 从分钟级打到秒级），
   `B1` 只是**让"该做什么"变得可审计**。⇒ 二者**不是同一层的东西**，别把 `B1` 当成本解法。
2. 报告的 **`§4.2`** 与**我今天刚做的事**部分重叠：`Z1` 片已经修了 3 个夹具前提
   （`scaffold` / `craft_station` / `restore_underfoot_safety`），并新增了注入即红的判据
   （`ledger_zone_scope` 四臂 + `CleanupWrappedTask` 注入证明）⇒ 报告的"假绿"清单**比我今天的状态旧**，
   它点名的 `restore_underfoot_safety` 仍是唯一**待重做几何**的那一个（记在 `§11` `Z1` 尾项）。

---

## 四、落点（本文件之外我只改了三处）

| 文件 | 改动 |
|---|---|
| `docs/OPEN_ITEMS_LEDGER.md §11` | 新增 **§G 2026-09-23 拉取登记**（`survey/29`+`30` 逐条指针 + A1/A2/A3 候选 + B1 待拍板） |
| `docs/HANDOVER.md` | 顶部块加一行"新输入"指针（不抄内容） |
| 本文件 | 上面的复算/复核结果（**事实与推断分开**，照 `survey/29 §6.3` 的归档纪律） |

**未做**：没有实现 `A1/A2/A3`，没有改 `CURATION`，没有动 `survey/`（只增不改）。
