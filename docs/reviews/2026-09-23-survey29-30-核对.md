# `survey/29` + `survey/30` 拉取与核对（2026-09-23）

> **本轮输入**：`survey/29-讨论记录-20260923.md`（473 行，原始素材）· `survey/30-诊断与下一步表-20260923.md`（277 行，结论/建议）。
> 两份都**只增不改**、**不构成实现授权**（`30` 头部逐字："本文不是待办清单，不是排期，不是审查任务，不是实现授权"）。
> **拉取**：`git fetch` ⇒ 远端 `98bd354..2f10f66`（`d6db852`=29 · `2f10f66`=30）⇒ `git pull --ff-only`，本地 Z1/CORE 提交**未受影响**。
> ⭐ 两份报告的基线 commit `d6db852` 的**父提交 = `98bd354`（我的 `Z1` 真机确认提交）** ⇒ 报告**已包含** `Z1`/CORE 瘦身，
> 其统计里 `D-408`、台账 2,152 行都对得上 ⇒ **不是过期视角**。

---

## 一、我逐条复算/复核的结果（`2f10f66` 工作树）

### 1.1 `survey/30 §1` 的量化表 —— **8/8 全部复算一致** ✅

| 指标 | 报告值 | 我复算 | |
|---|---|---|---|
| Java 文件 / 行数 | 496 / 103,526 | **496 / 103,526** | ✅ |
| `task/` 行数 | 51,556（49.8%） | **51,556** | ✅ |
| `task/` 夹具文件 | 84 / 181 | **84** | ✅ |
| `pathing/` 行数 | 8,397 | **8,397** | ✅ |
| `docs/` 行数 | 46,357 | **46,357** | ✅ |
| 决策条数（`### D-`） | 349 | **349** | ✅ |
| 台账行数 | 2,152 | **2,152** | ✅ |

⇒ **勘测侧的量化纪律成立**（可复算、命令写明来源）。§1.1 的三条读数（重心错位 / 台账自我生成 / 分母太大）
我**同意其算术**，评价见 §三。

### 1.2 `survey/30 §4.1` A1 —— **"`LumberJob` 缺终态闩锁"：代码事实成立** ✅

| 类 | 事实（我读到的） |
|---|---|
| `task/MineTask.java` | `:87 private Status terminalStatus;` · `:328-329 if (terminalStatus != null) return terminalStatus; // D-175：终态幂等` · `:333` 赋值 ⇒ **有闩锁** |
| `job/lumber/LumberJob.java` | `terminalStatus` **命中 0 次**；只有 `:209 private String terminalReason` + `:279 public String terminalReason()` ⇒ **无状态闩锁** |

⚠️ **但"这就是 `idempotent=false` 的根因"仍是推断**（报告 §6-2 自己声明没跑过）⇒ 判据应为
`single:lumber_job` 期望 `idempotent=true`（修前 false）**先红后绿**。

### 1.3 `survey/29 §2.1` —— **"击杀产物落 `FOREIGN`"链条：代码事实成立** ✅（含一次反证检查）

| 环节 | 我核到的原文 |
|---|---|
| `perception/ScopeBuffer.java:319-324` | `source = matchBreakSource(pos,tick)` → `provenance = null` → `matchIndirectOrigin(pos,tick)` |
| `:398`（`matchBreakSource`） | `for (BreakRecord record : recentBreaks)` ⇒ **只看破坏记录** |
| `:279`（`matchIndirectOrigin`） | **同样** `for (BreakRecord record : recentBreaks)` |
| `decision/DropPolicy.java` | `FOREIGN`（`:31`）⇒ `:95` 逐字"想捡 `FOREIGN` 只能**显式派活**" |
| ⭐ **反证检查（我加的）** | 全 `src/` 里**没有任何** `LivingDropsEvent` / `LivingDeathEvent` 消费者（只有夹具读 `ItemEntity`）⇒ **不存在补偿路径** |

⇒ 缺口"读代码这一侧"成立；**"杀一头牛 ⇒ 产物真落 FOREIGN 且静默"仍须夹具复现**（报告 §4-1 已如此声明）。

---

## 二、报告提出的候选（**勘测侧声明为候选、非指令**）

- `survey/30 §4`：三个杠杆 —— ① CORE 反馈 分钟→秒（A：修 `lumber_job`；B：**逐步缓存**，需先验步间独立性）
  ② 修假绿夹具（假绿比红贵）③ 把已有结论武器化（底线不进配置面 / 必选-可选守卫分界）。
- `survey/30 §5`：A1 终态闩锁 · A2 逐步缓存可行性验证 · A3 击杀归属归属 · A4 假绿夹具几何重做 +"能红"断言武器化 ·
  B1「下一步表」最小版（五列 + 一条门禁 + `tools/next-step.sh`）· B2 底线门禁 · B3 能力清单生成 + 防漂移门禁。
- `survey/30 §5.1` 明确**不建议现在做**：猎杀功能本体 · 怪物威胁线（除反射层以外）· 队列本体（`GoalRecord`）· 记忆库 · **任何新能力**。
- `survey/29 §5`：**`Q-1…Q-22` 待裁定**（其中 `Q-20` 队列闸改预算制 / `Q-21` 规范化哈希循环检测 / `Q-22` 能力清单生成
  ⇒ **用户已同意**；其余 19 条 ⏳）。**不在此处抄**——指针 = `survey/29 §5`。

### 2.1 我的评价（**不是裁定**）

| 候选 | 我的看法 | 与现队列的关系 |
|---|---|---|
| ⭐ **A1 终态闩锁** | **同意先做**：无前置、判据现成（`single:lumber_job`）、且直接决定 CORE 能否转绿 ⇒ 决定 `D-352` 缓存能否生效 | 与 `§11` 的 `Z2/Z3` **不冲突**（不同 scope：`job/lumber` vs `ledger/restore`） |
| ⭐ **A2 逐步缓存** | **同意先"验证"再"改机制"**：报告自陈"若步间不独立 ⇒ 必须放弃"。本项目已有现成判据 = `D-201` 附注一的逐步 diff。⚠️ 与 A1 **scope 不相交**，可并行 | 新增项，进 `§11` |
| **A3 击杀归属** | **同意"归属先行"**：先只做登记（不写攻击），判据 = 新夹具断言 `provenance != FOREIGN`；这是真缺口且不依赖任何设计 | 新增项（能力线 P3 之前置） |
| **A4 假绿几何** | **已在 `§11`**（`Z1` 尾项）⇒ 与报告一致，无需新增 | 已有 |
| **B1 下一步表** | ⚠️ **我持保留**：它要引入**第五个真相源**（台账 / 电池步名 / 门禁名 / 契约表之外）。报告自己写了"最小版 + 一条门禁，否则变成第 350 条决策"——**同意这个克制，但建议排在 A1/A2 之后**（先让方法成本真的降下来） | 待用户拍板 |
| **B2 底线门禁 / B3 能力清单** | 都便宜、都能红 ⇒ 可按报告顺序 | 新增项 |

---

## 三、我必须点出的两处（诚实边界）

1. `survey/30` 的**诊断结论（"方法太贵"）我认同**，但它的**处方（换"下一步表"）不解决成本本身**——
   成本来自"每次改动要同步 6 处 + 一次电池"；`§4.1 A1/A2` 才是**真的降成本**（把 CORE 从分钟级打到秒级），
   `B1` 只是**让"该做什么"变得可审计**。⇒ 二者**不是同一层的东西**，别把 `B1` 当成本解法。
2. 报告的 **`§4.2`** 与**我今天刚做的事**部分重叠：`Z1` 片已经修了 3 个夹具前提
   （`scaffold` / `craft_station` / `restore_underfoot_safety`），并新增了注入即红的判据
   （`ledger_zone_scope` 四臂 + `CleanupWrappedTask` 注入证明）⇒ 报告的"假绿"清单**比我今天的状态旧**，
   它点名的 `restore_underfoot_safety` 仍是唯一**待重做几何**的那一个（记在 `§11` `Z1` 尾项）。

---

## 四、落点（本文件之外我只改了三处）

| 文件 | 改动 |
|---|---|
| `docs/OPEN_ITEMS_LEDGER.md §11` | 新增 **§G 2026-09-23 拉取登记**（`survey/29`+`30` 逐条指针 + A1/A2/A3 候选 + B1 待拍板） |
| `docs/HANDOVER.md` | 顶部块加一行"新输入"指针（不抄内容） |
| 本文件 | 上面的复算/复核结果（**事实与推断分开**，照 `survey/29 §6.3` 的归档纪律） |

**未做**：没有实现 `A1/A2/A3`，没有改 `CURATION`，没有动 `survey/`（只增不改）。

---

## 五、补遗（2026-09-23 二次核对：**读了承重前提，一条候选的结论要更正**）

> 触发：用户"继续读报告" ⇒ 我不只读结论，把两份报告的**承重前提到场复算**了一遍（本项目纪律：
> 任何"数字/状态"都必须指向一次当场执行的复算）。结果 = **两个前提都成立，但它对 A1 的暗示不成立**，
> 且发现一条**两份报告都没有的新线索**（§5.6）。

### 5.1 ✅ 承重前提①：`D-352` CORE 缓存**只写 PASS** —— 代码事实成立

| 位置 | 原文事实 |
|---|---|
| `tools/headless-battery.sh:195` | `[ "$CACHED_VERDICT" = "PASS" ]` 是复用条件之一 ⇒ **判决必须逐字是 `PASS`** |
| `tools/headless-battery.sh:377-390` | 写缓存整段在 PASS 分支内（`:378` 逐字"只缓存 PASS：红/降级/无判决一律不写"） |

⇒ ⭐ **"只要 CORE 不是全绿就永不命中"成立** ⇒ 唯一红项 `lumber_job` 使每轮 CORE 真跑（≈4–5 min）。
`survey/30 §4.1` 对现状的描述**准确**。

### 5.2 ✅ 承重前提②：电池幂等断言确实是**集中执行点** —— 成立

`task/RegressionBatteryTask.java:733-746`：每步终态后再 `tick()` **两次**，`again1/again2 != status` ⇒ `idempotent=false`；
`:755-757`：`skipped` 需要 `idempotent`；`PASS` 需要 `status == DONE && idempotent`。
⇒ ⭐ 该断言对**所有**终态任务生效 ⇒ §5.5 的扫查**不需要新增判据**。

### 5.3 ⭐ 更正 C1：`LumberJob` **有**重入守卫，但守卫**硬编码状态**（≠"没有闩锁"）

```java
// src/main/java/com/dddgn/alice/job/lumber/LumberJob.java:304-306
if (terminated) {
    return Task.Status.DONE;      // ← 无条件 DONE
}
```

`finish(status)`（`:743-767`）以 `FAILED` 返回并置 `terminated=true` ⇒ **下一次 tick 却返回 `DONE`**（违反 `D-178`）。
**日志逐字自证**（`run/headless-logs/20260922-225609-core.log:3363`）：
`idempotent=false（再 tick 返回 DONE/DONE，期望 FAILED）`。
⇒ 勘测侧"只有 `String terminalReason`、无状态闩锁"的**措辞不准确**（守卫存在），但**缺陷判断正确**（守卫不记状态）。

### 5.4 ⭐⭐ 更正 C2：**补闩锁不足以让 CORE 转绿** ⇒ 该杠杆归 A2，不归 A1

`survey/30 §5 A1` 的评价列写"是 CORE 转绿的必需项之一"，易被读成"修完就绿"。**不成立**：

| 事实 | 出处 |
|---|---|
| 该步 PASS 判据 = `status == DONE && idempotent` | `RegressionBatteryTask.java:757` |
| 该步跑**真 `LumberJob`**（quota 16），**无** `skipWhen` / `withDoneWhen` | `task/check/modules/LumberModule.java:83-87` |
| 实测 `FAIL ticks=995 reason=no_reachable_candidate` ⇒ **`status` 从来不是 `DONE`** | `20260922-225609-core.log:3363` |

⇒ **补闩锁只把 `idempotent` 变 `true`，`lumber_job` 仍是 FAIL** ⇒ ⭐ **`D-352` 缓存不会命中** ⇒
"把 CORE 反馈时间从分钟级打到秒级"这条**乘法级杠杆只属于 A2（逐步缓存）**。
A1′ 仍值得做（`D-178` 契约、便宜），但其价值是**诚实**，不是**缓存命中**。

### 5.5 ⭐ 更正 C3（新事实）：同一形状**全仓 6 处**，`MineJob` 也在其中

`if (terminated) return Task.Status.DONE;` 的出现点（我扫的）：

| 类 | 位置 | 能否终态 `FAILED` |
|---|---|---|
| `job/lumber/LumberJob.java` | `:304-305` | **能**（4 条出口）⇒ 本轮实测红 |
| `job/mine/MineJob.java` | `:338-339` | **能**（`:344/370/590/693`）⇒ **潜在同类隐患**（当前 CORE 步恰好都 DONE 才没暴露） |
| `job/collect/CollectJob.java` | `:120-121` | 待核（另有 `innerFailed` 逻辑，`:212/236`） |
| `task/RestoreScopeTask.java` | `:142-143` | **能**（`:157/305/333`） |
| `task/LumberFailureCheckTask.java` | `:103-104` | 能（但夹具本身红了也红） |
| `task/ClearGuardCheckTask.java` | `:89-90` | 能（同上） |

⇒ 唯一用**正解**（存 `terminalStatus` 并回放）的是 `task/MineTask.java:87/328-329`（`D-175` 注释）。
⇒ **修法 = 把 `MineTask` 那个形状搬过去（6 处一遍）**。

### 5.6 ⭐⭐ 新线索 C4（两份报告都没有）：`lumber_job` 的**真正阻塞点**

日志取证（`run/headless-logs/20260922-225609-core.log`，22:52:44 → 22:53:36）：

| 层 | 证据 |
|---|---|
| 场景 | `scene=alice_test:lumber_course_terrain rc=855` / `…lumber_course_trees rc=186` ⇒ **树真种上了**（不是冷区块假红） |
| 选择 | `[Job] select job=lumber picked=tree@20,64,208 candidates=5 rejected=[tree@22,64,218:trunk_too_tall, …×4 not_nearest]` |
| 候选期主因（历史 32 次） | `trunk_too_tall(unreachable=4,hard=2)` —— `LumberCandidateSource.java:107-115`：4 格原木**无站位**，其中 2 格**硬遮挡且无清障方案** |
| 执行期 | `[WRITE-REFUSED] break pos=20,64,208 by=BlockBreakSession reason=world_unchanged（destroyBlock=false 方块仍是 Oak Log）` + `block_break_failed … code=REFUSED ticks=8 progress=1.07` |
| 放大机制 | 失败一次即进 `attempted` ⇒ `LumberJob.java:646` 记为 `already_attempted` ⇒ **该树在本次 job 内被永久排除** ⇒ 5 棵耗尽 ⇒ `no_reachable_candidate`（`ticks=995`，预算 1500 未用尽即放弃） |

⇒ **两条独立症状**：① 站位规划给不出（同窗口 `MiningPlanner` 反复 `planning failed … reason=no_reachable_standing_point`）；
② `destroyBlock=false` ⇒ **本地进度已满（`progress=1.07`）但世界没变**（`action/BlockBreakSession.java:104-120`，即 `D-323` 那一类）。
⚠️ **②的具体来源未定**（`ServerPlayerGameMode.destroyBlock` 的哪一条前置）——**不猜**，需探针或反编译源。
⭐ **价值**：若 ① 与 `§11 P5`/`D-391`（被包住的目标给不出站位）**同族**，则 `lumber_job` 就是**那条缺陷类最便宜的可红复现**
（`single:lumber_job`，确定性地形，~1000 tick）。

### 5.7 归属澄清：**不是 `Z1` 的连带**

`grep 'lumber_job=FAIL' run/headless-logs/*.log` 最早出现在 **`20260920-234751-core.log`（2026-09-20 23:47）**
⇒ **早于 `Z1`（09-22）两天** ⇒ 与 `Z1`/CORE 瘦身**无关**（与 `HANDOVER` 记的"既有红项"一致）。

### 5.8 补遗对候选表的净影响

| 候选 | 补遗后的状态 |
|---|---|
| **A1** | **改为 A1′**：形状 = "守卫硬编码状态"，修法照 `MineTask`，**6 处一遍**；⚠️**不解决 CORE 转绿** |
| **A2** | **升级为本轮唯一"乘法级"杠杆**（缓存命中只取决于它，不取决于 A1） |
| **A3** | 不变（代码链成立 + 无补偿路径） |
| **新增 C4** | `lumber_job` 真正阻塞点（站位 + `destroyBlock=false`），**可能与 `D-391`/`P5` 同族** ⇒ 待查 |
