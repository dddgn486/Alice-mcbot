# 勘测分诊与任务队列（2026-09-17）

> **性质**：主线对 `survey/` 六份**从未被分诊**的报告（10/11/12/14/15/16）的**逐条分诊结论** + **当前任务队列**。
> **口径**：勘测报告是**建议**，没有决策效力（`survey/README.md` 自述）；本文里凡标 `已核实` 的都附 `文件:行`，
> 凡与代码冲突的以代码为准并列入「勘测侧更正」。**本文不改 `survey/` 任何文件**（该目录由勘测员维护，只增不改既有结论）。
> **分诊方法**：4 个只读子代理逐条取证（每条给 `文件:行`），主线对每份的关键结论抽验；共分诊 ~90 条。

## 一、分诊总表（按"现在能不能做"分档）

| 档 | 条数 | 含义 |
|---|---|---|
| **A 现在可做（已采纳，含已完成的）** | 12 | 有可红判据 / 纯静态核实 / 零代码登记，**不依赖用户拍板与客户端** |
| **B 需先拍板** | 19 | 产品口径 / 范围红线 / 优先级取舍 —— 已归并为 **6 个裁定**（见第四节） |
| **C 需客户端验证** | 9 | 视觉/物理/真机行为（含"动协议"的代价） |
| **D 仅登记未来** | 26 | 策划性、无可红判据；或报告自述"现在不做" |
| **E 勘测侧更正（事实错误/过期前提）** | 12 | 与代码不符，按代码为准，列在第三节 |

## 二、A 档：现在可做（本日已做 / 已入队）

| 项 | 出处 | 判据（怎么知道做对了） | 状态 |
|---|---|---|---|
| **J-3 补全**：4 个生产 Job（lumber/region_lumber/mine/collect）覆写 `taskName()` 返回 `NAME` | 客户端日志实测暴露（`kind=LumberJob`） | 门禁 `check-exec-record.sh` 新规则 **R4**（声明 `NAME` 必须覆写且返回它）；注入"返回类名"⇒ 门禁红 ✓ | ✅ **本日完成** |
| **survey 10 #37**：`PathRetryRunner` 落一行搜索规模/耗时日志（`status/cost/nodes/moved/ms/partial`） | `survey/10 §2.3` + 悬崖1/3 | `single:pathing` 日志 `[PathRetry] plan …` 逐字可采集；**已采到真实数据**（见 §2.1） | ✅ **本日完成** |
| **survey 12 §3 纪律落地**：`RecipeDump.STATION_BY_TYPE`（**11** 条）与 `tools/recipe-graph.py:30`（**9** 条）**同一份数据的第二副本** ⇒ 加双向覆盖断言挂 `check-all.sh` | `survey/12 §3`（"单一出处"）、`survey/14 §11-1` | 两组站点键集合必须相等；改一处不改另一处 ⇒ 门禁红 | ⏳ 入队（下一个） |
| **survey 10 #28**：`CandidateMenu` 每次 `new MineCandidateSource` **11 次**冗余扫描 ⇒ 合并为一次 | `survey/10` | 判据现成：电池步 `mine_menu` 的 `[MineGoal] candidate_menu mine=11` 与 `[MineMenu] SUMMARY checks=11 failures=0` **逐字不变**；反向对照（改回 11 次）⇒ 扫描计数回升 | ⏳ 入队 |
| **survey 16 §2（收窄后）**：`MineJob.subTasks()` 在 `miner != null`（新尝试上场）时只报 `cleared=`，**上一轮失败事实消失** | `survey/16 §2`（原话部分过期：M4b/D-231/D-234 已落地） | 需**运行中**观测（结束态走 `finishedMinerNode` 分支）⇒ 判据待设计：`Step` 目前无 tick 钩子，**先改代码再补判据**（诚实标注） | ⏳ 入队（带判据缺口） |
| **survey 16 §1**：把 `progressSummary()` 接成低频事件（复用既有事件环，先不新增 S2C） | `survey/16 §1` | 事件环里出现周期性 `progress` 事件；电池步不回归 | ⏳ 入队 |
| **survey 15 #24**：JEI 催化剂 ↔ `RecipeDump.STATION_BY_TYPE` **同源性**静态核实 | `survey/15 §10-1` | 纯离线核对（JEI jar 在本机）⇒ 结论写回本文档 | ⏳ 入队（离线） |
| **survey 15 #7/#28**：诚实失败比例（`UNREACHABLE`/`NO_REACHABLE_CANDIDATE`/`HOLD_NO_EXIT`）落成可复算指标 | `survey/15 §4.4` | 服务端终态计数可复算（全仓 22 处命中） | ⏳ 入队 |
| **零代码登记（已核实为真）** | `survey/12 §4.5`、`§5.3`、`survey/14 §5.2` | 见第三节"已核实" | ✅ 本文档即登记 |

### 2.1 本日新采到的**真实数据**（首次，勘测报告此前全部是推算）

`ALICE_HEADLESS=1 tools/headless-battery.sh single:pathing` ⇒ `[PathRetry] plan …`：

| attempt | status | cost | nodes | moved | ms | partial |
|---|---|---|---|---|---|---|
| 0 | REACHED | 16.70 | **54** | 408 | **1** | false |
| 1 | REACHED | 7.36 | **10** | 69 | **1** | false |

**完整 CORE 轮（38 步，含 ④ 有界预算已生效）**：最大 `nodes=975 / moved=5287 / ms=5`，
**`SEARCH_LIMIT` 命中 0 次** ⇒ 保守初值（20_000 节点 / 3_000 ms）**没有误伤任何真实行走**。

⇒ 结论（对裁定 ④ 有用）：**当前电池场景的搜索规模在 10¹~10³ 节点、毫秒级**，离 `CorePathPlanner` 的
`20_000 节点 / 3_000 ms` 天花板还差 1~2 个量级；勘测报告推算的"数十~数百 ms"**在这些场景下未出现**。
⚠️ 边界：本数据只覆盖电池场景（无远距离跨区目标），**不能**外推到"远距离/多段/大落差"。

## 三、E 档：勘测侧更正（12 处，按代码为准）

| # | 报告原话 | 事实（`文件:行`） |
|---|---|---|
| 1 | `survey/10 §6.1 A1`「逃生请求需要给有界预算」 | **已有界**：`PathRequest.java:34-35 ESCAPE_MAX_NODES=4_000 / ESCAPE_MAX_MILLIS=100L`（构造器覆盖 `UNLIMITED`）；`SurvivalExitTask` 走 `survivalEscape` |
| 2 | `survey/11 §2.3`末「真正无界的是逃生动作」 | 同上；**仍然无界的是行走类** `PathRequest.of`（`:30` 默认 `UNLIMITED`，`:65/78/91/110/129` 全传它） |
| 3 | `survey/12 §0.1`「`mermaid` 0 命中 ⇒ 没画图」 | `docs/authz/flow.svg`（14,686 B）已存在；三份图**已接门禁**：`check-all.sh:127/128/129` |
| 4 | `survey/14 §11-1`「`STATION_BY_TYPE` 有 **18 条**」+ 路径 `task/craft/RecipeDump.java` | 实为 **11 条**，路径是 `decision/RecipeDump.java:58-69`（该路径在 HEAD 与基线均不存在） |
| 5 | `survey/15 §3.6` 同两处（18 条 / `task/craft/` 路径） | 同上；另有 `tools/recipe-graph.py:30` 的**第二份副本（9 条）且无门禁** |
| 6 | `survey/15 §5.3`「L1/L2 今天可做（有 `PARAM_SET`/`QUEUE_REORDER`）」 | 两符号**只出现在 survey 05/15 的提案表**，`src/` **0 命中** ⇒ 是**待建**，不是可做 |
| 7 | `survey/15 §5.2`「七条已拍板口径」 | `DECLARE_DONE`/`QUEUE_COMMIT`/`RESOURCE_SELF_FETCH` 在 `AI_DECISIONS.md` **0 命中** ⇒ **未落档**（要落档才有约束力） |
| 8 | `survey/15 §7.2` 引 `FurnaceStation.java:128` | 实际相关行是 `:29`（自述不采信、一律 `mayPlace`）与探针 `:463`；`GridDiscovery` 610 行 ✓ |
| 9 | `survey/15 §5.1`「LLM 职责边界在代码中没有任何结构承载」 | 过强：`PermissionGate`（capability+options，D-140）、`MiningProfile`（D-111 能力信封，51 处）、`WriteEnvelopes`（D-241，14 处）**都已存在**；准确说法是「**LLM 侧的 Grant 集合**未实现」 |
| 10 | `survey/12 §4.5` 可回收性读者计数 41/35 | 现测 `PATH_REVERSIBLE`=**44**、`LOCAL_STEP`=**36**（漂移，结论不变：空插槽的两级仍无产出者） |
| 11 | `survey/14 §10.1`「`recipe-graph.py` 已在跑」 | 实测 `python3 tools/recipe-graph.py --selftest` **失败**（缺原版数据目录，依赖 `/tmp/rcp` 或 `$ALICE_VANILLA_DATA`）；`grep recipe-graph tools/*.sh` **0 命中** ⇒ **没挂任何门禁** |
| 12 | `survey/12 附录` / `survey/14 附录` 用 `cd /workspace/Alice-mcbot` | 本仓是 `/home/fb486/projects/alice`（抄了会失败） |

**已核实为真、无需更正的（抽样）**：`survey/15 §4.1` 三处结构数字（376 file / 67,549 行 / task 27,645(102) 等**逐字复算一致**）；
`survey/15 §5.4-2` 「`GoalAction` 恰好 7 个动作」（`GoalAction.java:115-121` 六 case + `:68 Refused`）；
`survey/12 §4.3` `RiskSwitches.KNOWN` **只有 1 项且默认 false**、消费者恰 2 处；
`survey/14 §5.2` 槽位语义已由 `FurnaceStation:29` 的 `mayPlace` 解决。

## 四、B 档：需你拍板的 **6 个裁定**（已归并去重）

| 裁定 | 涵盖的勘测条目 | 需要你回答的是 |
|---|---|---|
| **① 产品定位** | 14 §0.1/结论1；15 §1.1–1.2；14 §2；15 §2.1–2.2 | 是否正式把定位写成「**AE 的外挂（互补≠替代）**」并落 `AI_DECISIONS`？是否裁定「**AI 自主设计产线永不进蓝图**」？ |
| **② 外部驱动者/陪伴线边界** | 16 §3/§4/§5.3/§5.4/§6 | 是否引入"外部驱动者"概念？桌面 AI **是否继承玩家身份**（还是只继承信封）？是否开**对话线**（输出永不进 `GoalAction.parse`）？`propose` 是否成为**一等动作**（须同时改 `GoalDirector.VOCABULARY` 与 `GoalAction` 白名单）？ |
| **③ 工作站形态** | 14 §3/§3.3/§3.4/§11-2；15 §3/§10-2/§10-5；15 补遗 | 能力声明用什么载体（数据文件/存档/命令）？是否接受「**默认只看外部**、读内部=预设授予」与「**只对单方块机器**调配置」两条边界？ |
| **④ 预算与并行** | 10 §2.3/悬崖1/3；12 §4.4；ledger G4 | **行走类**要不要有界预算（会改行为：撞 `SEARCH_LIMIT`）？多 bot 并行/「每 tick 只让 K 个做重活」是否立项，**K 与"昂贵工作"的分类标准**是什么？（本日已给你 `nodes/ms` 真实数据，见 §2.1） |
| **⑤ `NO_PROGRESS` 语义** | 16 §7 | 它该检测「**站着不动**」还是「**白忙一场**」（脚位在动但目标没进展）？ |
| **⑥ AE 阶段与准入** | 14 §6/§9/§10.4/§11-3；15 §7 | 是否新增「**AE 深度集成**」阶段？若要，**是否先在本机客户端装 AE2**（今天 `mods/` 里没有 AE2 ⇒ 该阶段无实测载体）？是否把"适配数据化（改 JSON 不改 Java）"定为准入前提、格雷单列？ |

> 未被归入上面 6 条的**小项**（不用你逐个回答，我按纪律处理）：`survey/12 §3/§4.2` 的"图维护判据"只有第①类可执行（生成器+门禁），
> 第②③类报告自己承认"挂不上门禁"⇒ 按 `AGENTS.md` 准入尺子**不得进主线**（只在 `survey/` 与本文档留指针）。

## 五、C 档：需要客户端轮次（等你有空一次跑完）

| 项 | 入口 | 看什么 |
|---|---|---|
| `survey/16 §1.3/§12.1` 推送频率阈值与桌面 AI 延迟预算 | 现有 `alice:goal_director` + 实际交互 | `progress` 事件频率是否够用；RTT（报告称"零数据"） |
| `survey/16 §10/§11` AI 主动说话 / 非游戏内答复请示 | 需先拍板 ② | 是否复用既有 S2C（决定动不动 `PROTOCOL_VERSION`） |
| ~~`survey/14 §10.3`/`§11-3` 第一个「可操作工作站」（`mekanism:enriching`）~~ | ~~`machine_station` 步~~ | **✅ 已由无头 FULL 回答（2026-09-17，FULL 52/52 PASS）** ⇒ **不需要客户端轮次**；只剩「画面里机器站点对不对」这类目视项（可选）。⚠️ 该步 `single:` 单跑会 FAIL（缺 FULL 隐含前置） |
| `survey/10 §25/§32/§38/§39`；`survey/11` 五项 | 见各报告 | mobcap / 单机掉帧 / 其余"待确认"数字 |
| **台账 105/107**（唯一真验证债） | Baritone `#goto` 对照 | 必须同时标注「Alice 侧多开两个守卫」 |

## 六、D 档：仅登记未来（26 条，不列全；要点）

`survey/15 §8/§9/§11`（档3+预设、AE 功能集、11 条显式留白）、`survey/16 §8/§9/§12.2–12.4`（决策缝替换点、触发集合扩张受 `maxRequestsPerMinute=6` 挤压、多驱动者并发）、
`survey/10 §3/§4/§8/§13/§19/§21–§23/§31/§33/§36/§40`、`survey/12 §6.1`（第 4 个生成器：开关清单，只有 1 个开关 ⇒ 收益低）。
**共性**：它们要么**没有可红判据**，要么**报告自述现在不做**，要么**依赖上面 6 个裁定**。本轮一律不排期。

## 六·五、survey/17（同日新推）初步分诊

`survey/17-以手段换约束-20260917.md`（477 行，三大设想：**视觉只读标注器 / bot 视角渲染 / 带成本的回收道具**）。
报告自述三条**今天都不做**；下面是主线对"现在就动"部分的初判（逐条分诊见同目录后续补充）：

| 项 | 性质 | 备注 |
|---|---|---|
| §1.5 硬边界「**视觉结果只进汇报/对话，不进执行路径**」 | **需先拍板**（红线候选） | 报告给的证伪判据很准：`GoalAction` 动作集里是否出现由视觉驱动的新动作 |
| §1.6 视觉天然落在 `survey/16` 的**对话线** | **依赖 ②（D-267 地基 F4）** | 说话通道（不碰世界）正是 D-267 的 F4 地基项 ⇒ **地基先做**的顺序与用户裁定一致 |
| §2.3 借用玩家客户端渲一帧（**用户已选形态**） | **需先拍板 + 需客户端验证** | 代价明确：需**新 C2S 包** ⇒ 动 `PROTOCOL_VERSION`（协议不一致拒连）；§2.4 前提：**共享一双眼睛**（否则 N 倍成本、撞 survey/10 的多 bot 灾难） |
| §3.3 「死亡前传送」→ 改「**死亡后在锚点复活**」 | **需先拍板** | 报告的更正成立：多数死因**没有"死亡前"窗口**；而死亡事件**一定存在**，且项目**已预留**（`BotManager.onLivingDeath` 注释原话「暂时策略，**后续可改为重生**」）⇒ 痛点「死了进度归零」有现成挂点 |
| **§4.2(c)「忙碌 ≠ 进展」** | ⭐ **佐证 D-268⑤ + 新发现** | 外部 141 小时实测的"种土豆"失效模式 = 用户刚裁定的「白忙一场」同源。⚠️ **但它暴露我实现里的一个细节**：`currentTaskProgressSummary()`（含 `failed=N`）整串进了指纹 ⇒ **`failed` 在涨会被当成"有进度"而反武装**。正确口径 = **产出**（`mined x/y`、背包）算进度，**失败计数不算** ⇒ 已入队（见 §7 第 7 项） |

**逐条分诊结果（37 条）**：现在可做 **8** / 需先拍板 **10** / 需客户端 **5** / 仅登记未来 **14**。
**架构冲突 6 条（关键事实）**：
① §1.5 假设的"只进汇报/对话的出口"**今天不存在**（`GoalDirector.tell` 为 `private`、外部调用者 0）⇒ 与 D-267 F4 同结论；
② 真正的通路是 **prompt**（`GoalDirector:296-300`）⇒ 截图描述若进 prompt 就等价于进了决策线；
③ 像素/文本**无类型隔离**（`GoalAction.parse:351`）⇒ "只读"今天无法构建成红；
④ 权限门「只有玩家能批准」**成立**（`PermissionGate.answer` 的 4 个调用者：客户端包/键位/命令/夹具）；
⑤ ⚠️ 勘测高估协议成本：**加包不必跳 `PROTOCOL_VERSION`**；
⑥ 视觉与桌面 AI 都缺"谁驱动的"这一维 ⇒ 应与 **D-267 F1** 同批。

**勘测员这一份的价值**：它给已有裁定提供了**外部实测佐证**（§4.2 三条分别印证了可回收性底座、维生系统独立于决策层、以及刚做完的 NO_PROGRESS 语义）——
这正是 `survey/` 目录存在的意义：**不是给新任务，而是校准已有判断**。

## 七、当前任务队列（主线口径）

**进行中 / 已完成（本日）**：142 客户端验证 ✅、R2-残 收口 ✅、J-3 补全（+门禁 R4）✅、survey10 #37 数据采集 ✅、
**裁定 ④ 行走有界预算（+门禁 W-P1）✅**、**裁定 ⑤ `NO_PROGRESS` 白忙一场（+可红判据）✅**、裁定 ①⑥ 与 ② 的地基清单已落 `AI_DECISIONS`（D-266/D-267）✅。

**下一步（按本队列顺序做，不需你介入）**：
1. ~~`RecipeDump` ↔ `recipe-graph.py` 站点表**单一出处断言**~~ ✅ **2026-09-17 完成**：新门禁 **S-P1**
   （`tools/station-mapping.py`，挂 `check-all.sh`）—— 结论：两张表服务**不同输入域**（Java=运行时**类型 id**，
   Python=数据包**序列化器 id**）⇒ **不该合并，应锁死**：Python 表必须是 Java 表的子集（键与站点名都要同）。
   实测 Java=11 / Python=9，仅 Java 有的是 `minecraft:crafting`、`minecraft:smithing`（运行时类型 id，符合预期）；
   反向对照：改 Python 侧站点名 ⇒ 红 ✓
2. ~~`CandidateMenu` 11 次扫描合并~~ ✅ **2026-09-17 完成**：新增 `MineCandidateSource.candidatesForTargets`
   （**一遍扫描 · 结果分发**，候选集逐字不变）+ 底层**真值读取计数**；实测 `block_reads=15625 = 25³`
   （原先是 11 × 15625 ≈ **17 万次**/建菜单 ⇒ **降 ~11 倍**）。判据：夹具新 check「矿扫描一遍完成（读取 ≈ 单遍，
   不是 目标数×单遍）」；**反向对照**（真的改回逐目标扫 + 真值计数）⇒ `single:mine_menu` **FAIL** ✓。
   ⚠️ 顺带修掉一个**数字不诚实**：`[MineMenu] SUMMARY checks=11` 是**写死的字面量** ⇒ 新增 check 不计入；
   已改为真实计数（现 `checks=12`）。
3. ~~`MineJob.subTasks()` 新尝试上场时保留上一轮失败事实~~ ✅ **2026-09-17 完成**：
   修复 = 活动分支带上 `lastAttemptFailure()`（原来传空串）；新增**夹具只读** `hasActiveAttempt()`/`attemptFailureCount()`；
   新建夹具 `MineFailureVisibilityCheckTask` + 电池步 **`mine_failure_visible`**（MAIN 档，`ore_course` 场景）：
   **自己持有并手动 tick 一个 `MineJob`**（`MineJob.tick()` 已核实自足：自开作用域、自驱动内部 `MineTask`），
   用 `identityCheck` 覆盖器"**前 2 个候选判为 target_replaced、之后放行**"构造出
   **「活动尝试 + 已有失败」**的组合（⚠️ 不能用 `pos -> false`：那会让 `miner` 永远为 null，活动分支到不了 ⇒ 假绿）。
   实测：`active=true attemptFailures=2 node.failure=56, 62, 136:target_replaced`；**反向对照**（failure 改回空串）⇒ 该步 **FAIL** ✓。
   ⚠️ 顺带确认"空转即失败"：夹具在未观测到该组合时**显式判失败**（no_progress 假绿的教训）。
   **缺口**（`survey/16 §2` 收窄后）：`MineJob.subTasks()` 在 `miner != null`（新尝试上场）时只报
   `cleared=N`、**failure 传空串** ⇒ 上一轮失败事实从快照里消失（`miner == null` 分支反而有 `finishedMinerNode`）。
   **修法**（极小）：活动分支也把"上一轮尝试的失败"带进 `TaskNode.leaf(..., lastFailure)`。
   **判据设计（关键：必须在"尝试进行中"采样，结束时走的是另一分支）**：
   - 新建夹具 `MineFailureVisibilityCheckTask`：**自己持有并驱动一个 `MineJob`**（复用 `mine_stale` 的
     `pos -> false` 身份复检竞态 ⇒ 必然产生 `target_replaced` 尝试），在其 `tick()` 里**手动 tick 那个 Job**，
     轮询直到"存在活动尝试（`miner != null`）且已有一轮失败" ⇒ 断言 `job.subTasks()` 里 **MineTask 节点的
     failure 字段非空**（且含失败码）。
   - **反向对照**：把活动分支的 failure 参数改回 `""` ⇒ 该 check 必红。
   - 挂到电池新步（`mine_failure_visible`，MAIN 档）+ `docs/AI_TEST_MATRIX.md` 一行。
   ⚠️ 未做原因：需要"夹具手动驱动 Job"这一新形态（现有夹具没有先例）——先确认 `MineJob.tick()` 能在
   未被会话接管的条件下正确工作，再落代码。
4. ~~`survey/16 §1`：`progressSummary()` 低频事件~~ ✅ **2026-09-17 完成**：`EventThresholds` 新增
   `PROGRESS_EVENT_INTERVAL_TICKS`（**生产默认 0 = 关**）+ `setProgressEventInterval`（夹具缝）+ `progressEmits`；
   有任务在跑时按间隔把「任务 + 进度摘要」送进**既有事件环** `DecisionEvents`（环 + 日志 + 通知决策层），
   **不新增任何 S2C/协议面**。判据挂在夹具 `MineFailureVisibilityCheckTask`（它自带运行中的任务）：
   ①前提 check「生产默认关」②「70 tick 内至少 2 次（间隔 20）」③收尾必须复位为 0。
   **反向对照**（把上报关死）⇒ 该步 **FAIL** ✓。
5. `survey/15 #24/#7/#28` 三条离线核实/指标（JEI 同源性、诚实失败比例）。
6. 台账里 **S-6 / S-9 / S-10** 三项（你已裁定 S-6 容器与 S-9 伤害事件；S-10 待裁定）。
7. ~~**报告⇄代码静态核对门禁**~~ ✅ **2026-09-17 完成**：新门禁 **R-P1**（`tools/ref-integrity.py`，挂 `check-all.sh`）——
   文档里的 `` `文件:行` `` 引用，**指向存在的文件时行号不得超出该文件长度**（= 必然过期）。实测 **校验 299 处、0 超界**；
   **注入**（把某条引用的行号改成一个远超文件长度的值）⇒ 红 ✓。设计要点：① "文件不存在"**只提示不失败**（历史记录/提案会合法引用
   已删文档与未实现文件，硬红会锁死主线）；② 裸文件名按**基名唯一**解析，**歧义即跳过**；③ ⚠️ 基名索引**必须含
   Baritone 参照仓** —— 否则 `MovementHelper.java` 会被误认成本仓文件、行号立刻"超界"（第一版实测的假阳性）。
   这一条直接命中本轮的痛点：同一天人工抓到 **16 处**勘测件的路径/行号/数字错误。
8. **survey/17 §4.2(c) 的指纹修正**：`NO_PROGRESS` 指纹里把"失败计数"从进度里剔除
   （`failed` 涨 ≠ 进度：`mined` 不动而 `failed` 在涨正是"种土豆"）—— 由用户的 ⑤ 裁定 + 新勘测共同要求。

**等你**：第四节 6 个裁定（其中 ①②③⑥ 决定队列的**大方向**，④⑤ 决定**行为口径**）+ 第五节客户端轮次。

## 八·二、survey/17 完整分诊（2026-09-17 第二次拉取）

**背景**：`survey/17-以手段换约束-20260917.md`（**477 行，已写完**）此前在 `§6.5` 只当**占位**处理；
现按用户指示「本工作段结束后再拉一份最新报告」**重新分诊** → 本节取代 `§6.5` 的占位内容（`§6.5` 保留为历史）。

**报告结论（§0）**：三条设想（视觉语义标注 / bot 视角渲染 / 带成本回收道具）**都是「给 bot 的礼物」，
都带成本或约束，都不主张现在做** ⇒ 与用户裁定完全一致 ✓。

### 8.2.1 已裁定且已落地（无待办）

| survey/17 | 裁定 / 落地 |
|---|---|
| 视觉本体（§1、§2） | 用户裁定「**先做 F4，视觉本体缓**」⇒ **F4 已落地**（D-273）；视觉按需、**评价不留存**；新增 C2S **不跳** `PROTOCOL_VERSION` |
| 回收道具（§3） | 用户采纳 **死亡后复活 / 召回符 / 一次性保命**（其余酌情）⇒ 见 `DEATH_AND_REVIVAL_DESIGN_DRAFT` |
| §3.5 设计原则 | **已升格为 D-278**（外挂只降**代价**、不降**概率**） |

### 8.2.2 新事实（现在就能用）

- **一次性保命的触发前提已具备**：`§3.4 建议 3` 要「低血量或受致命伤时**自动触发**一次短距脱离」，
  而"挨打"在 **D-271（事件观测）+ D-277（窗口查询 + 快照 `damage` 节点）**之后**可观测** ⇒
  等复活线**第 2 步之后**接它时**不必再补观测**。
  ⚠️ 它必须挂在**维生系统**那条独立路径上（D-271 实测：着火 ⇒ 维生中断 ⇒ 任务被掐掉 ⇒ 任务层检查靠不住）。

### 8.2.3 待用户裁定（登记，未开工）

| # | 项 | 出处 |
|---|---|---|
| a | **任务保险**：失败时消耗资源**自动重跑一次**（而非把终态交给 LLM 重新决策） | `§3.4 建议 4` |
| b | **临时权限凭证**：花资源买**一次性**危险动作授权（如"本任务允许挖 32 格通道"） | `§3.4 建议 5`（受 D-278 约束） |
| c | 视觉三个未解问题（何时看 / 谁出钱 / 看到的东西算不算"世界事实"） | `§1.7` |
| d | bot 视角渲染：**一双眼睛还是 N 双眼睛** | `§2.4` |

### 8.2.4 仅作将来实验对照（不是工作项）

`§1.8`（视觉证伪点）、`§2.6`（渲染证伪点：借客户端渲一帧 / 离屏帧缓冲 / 画质）、`§3.6`（道具证伪点）。

## 八、文档落点（为什么这么写）

- **不新增散文规则**：`AGENTS.md` + `AI_DEVELOPMENT_PLAYBOOK.md` + `AI_PROJECT_STATE.md` 合计 **1472/1476 行**（余量 4）⇒ 本日所有结论落
  **本文档**（新文件，不受冻结）+ `docs/AI_DECISIONS.md`（决策，采纳后）+ `docs/OPEN_ITEMS_LEDGER.md`（待办与债）+ `docs/AI_TEST_MATRIX.md`（可验项）。
- **不改 `survey/`**：该目录由勘测员维护（`survey/README.md` 自述"只增不改、零改动既有文件"）⇒ 勘测侧的 12 处更正只在本文件登记，由勘测员自己回写。
