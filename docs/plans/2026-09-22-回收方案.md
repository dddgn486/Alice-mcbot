# 回收方案（现状 / 缺口 / 冗余 / 以后怎么回收）

- 日期：2026-09-22 · 触发：用户 2026-09-22「我需要一套**完整的回收方案**，应对各种回收状况，
  指清楚**现在能怎么回收**，要设计**以后怎么回收**，有没有**冗余设计**」
- 口径基线：`D-398`（责任边界，决定性断言）+ `D-397`（三件套作用域不一致）+ `D-399`/`D-400`（守卫与判据）
- 纪律：本文每条现状都带 `文件:行`；**未核实**的进 §6，不当结论用。

---

## §0 一页结论

1. **今天能回收的只有一种东西**：**我方放置的、被判为 `TEMP` 的方块**（脚手架/垫脚/支撑/工作站），
   且必须满足"作用域还没关、区块加载着、bot 到得了、现场还是我方那块、拆了不会把 bot 摔下去"。
2. **今天根本不做、也不该做的**：我方**挖掉的地形**（账本**只记放置**，破坏**不进账本** ⇒ 无 `previous` 可还原）、
   容器里拿走的物品、流体/光照等副作用。
3. **最大的结构缺口**：回收义务**没有唯一真相** —— 义务由 `WritePolicyMatrix` 的行决定、
   记录在 `WorldModLedger`、额度记在 `WriteBudget`、触发散在 5 处（任务/作业/夹具包装/电池/启动读），
   而**"作用域关闭"这件事本身不强制回收**（电池 `endStep` 只关账、只 warn）。
4. **冗余确实存在 6 处**（§3），其中 3 处应当合并或降级为遥测。
5. **以后怎么回收**（§4）：把"义务"收敛到**一个模型**（三分类 × 五个触发器 × 四条不变量），
   并把 `D-398` 的保护区区外无义务作为**第一刀**（= 队列 `Z1/Z2/Z3`）。

---

## §1 今天的回收能力（实测清单）

### 1.1 义务从哪来 —— **集中策略表**（不在各处 `if` 里）

| 事实 | 出处 |
|---|---|
| 义务 = `Policy{TEMP, KEEP}`，由**行**解析：`Row(id, Zone{EXTERNAL\|WORKSPACE}, Task{…}, Obligation{TEMP\|KEEP})` | `action/WritePolicyMatrix.java:197-199`、`:296`(`P-01`)、`:322`(伐木)、`:327`(合成) |
| 理由自身也带默认：`WriteReason.temporary()`；两者**不矛盾**（防"显式 KEEP 把该回收的放过"） | `WritePolicyMatrix.java:38/53` |
| 例：`P-01` = `EXTERNAL × TRAVERSAL ⇒ TEMP`（"走进某处不该留永久痕迹"）；伐木"补种=KEEP / 脚手架=TEMP"；合成"工作站用完即拆 TEMP，熔炼要写容器" | 同上 |
| `STEP_PLACEMENT`（脚手架）与"支撑"**刻意区分**，前者 `TEMP` **必须配对拆除** | `WriteReason.java:54` |

### 1.2 记录在哪 —— `ledger/WorldModLedger`

| 性质 | 事实 |
|---|---|
| **只记放置** | `recordPlacement(level, owner, grant, pos, previous, placed)` 仅两处调用（`action/BlockInteraction.java:371/594`）⇒ ⭐ **破坏不进账本** |
| 键与持久化 | `Entry(pos, placed, previous, reason, policy, scopeId, owner, tick)`；存档 `alice_world_mod_ledger` |
| 作用域 | `openScope`/`closeScope`（**一次任务一个**，键 = `owner#seq:label`）；`scopeOf` **无 scope 时回退 `implicit`**（"不静默丢弃记录"：`WorldModLedger.java:107-110`） |
| 义务查询 | `pendingTemporary(server, scopeId)`；`pendingForOwner`；`at(pos)` |
| 退休（销账）三条路 | `dropStale(level)`（现场已不是我方 ⇒ 批量销、**带日志**）、`forget(level,pos)`（单条销）、`RestoreScopeTask` 内的 `not_ours` 判定（**绝不拆不是自己放的**） |

### 1.3 谁来执行 —— `task/RestoreScopeTask`

流程（已实测）：`dropStale` → 逐条 `at(pos)` → **复检 `nowId.equals(entry.placed())`**（不符 ⇒ `forget`+`skipped`+`not_ours` 归因）
→ `Stage{APPROACH, DESCEND, SIDE_BREAK}`（"站上去 → 向下拆"）→ `D-399` 守卫（脚下不安全 ⇒ 延后重试一次 ⇒ 仍不安全 ⇒ `skipped`+`underfoot_unsafe`）
→ 收尾收集掉落物 + 终态 `restore_done`/`restore_partial`。

### 1.4 谁触发（**散在 5 处** ⇒ 见 §3 冗余 4）

| 触发者 | 行为 |
|---|---|
| `task/MineTask.java:452` | 挖矿任务自己回收 |
| `job/lumber/LumberJob.java:269/551` | 伐木作业把 `RestoreScopeTask` 当**子任务**（"仍在架上"自上而下拆） |
| `task/CleanupWrappedTask.java` | **夹具包装器**：跑完必收尾（因为电池 `endStep` **只关账不拆**） |
| `task/RegressionBatteryTask.java:857-863` | 电池收尾：`closeScope` + `dropStale`，**不拆**（只 warn "建拆同权未闭合"） |
| `bot/BotManager.java:714` | 读 `pendingTemporary(server, **null**)`（= `implicit` 桶）⇒ 只在读、不在收 |

### 1.5 今天**做不到**的（≠ 缺陷，多数是设计边界）

1. **挖掉的地形**：账本无 `previous` ⇒ 不可还原（且按 `D-398` 区外本来也无义务）；
2. **容器物品**：`WriteBudget` 有 `containerWrites` **计数**，但**没有逐 item 账** ⇒ 还不了原；
3. **流体 / 光照 / 方块实体副作用**：完全未记录；
4. **未加载区块**：`pendingTemporary` 里若有，回收走到那格时无法读世界 ⇒ 只能跳过/延后（今天**没有**延后机制）；
5. **重启后**：条目在存档里活着，但**没有"启动补收"**（只有 `BotManager` 读一眼）；
6. **死亡后**：义务是否随 `death_persistence`（`D-276`）延续 —— **未核实**（§6）。

---

## §2 回收状况矩阵（状况 × 今天 → 缺口）

| # | 状况 | 今天 | 缺口/风险 |
|---|---|---|---|
| S1 | 我方放 `TEMP`（脚手架/垫脚/支撑/工作站），任务正常收尾 | ✅ 能收（`MineTask`/`LumberJob` 触发） | 只在**这两个**任务里接了；其它任务靠包装器 |
| S2 | 同 S1，但**任务失败/超时** | ⚠️ 视触发点而定（作业层子任务可能没跑到） | 无"失败路径必收"的统一保证 |
| S3 | 同 S1，**作用域已关**（任务结束） | ⚠️ 条目留在账本；`pendingForOwner` 只增不减 | 作用域关闭**不强制**回收（`D-081` 只在电池里 warn） |
| S4 | 条目在现场**已不是我方方块**（玩家拆了/别人改了） | ✅ `dropStale`/`not_ours` 销账 + 日志 | 无（这条是好的） |
| S5 | **跨任务/无任务**时放的方块（`implicit` 桶） | ⚠️ 记录进去了，但**没有任何任务会去收** | 需要"孤儿条目"策略（`D-398` 之后区外可不管，**保护区内必须管**） |
| S6 | **区块未加载** | ❌ 无机制 | 需要延后/补收 |
| S7 | **服务器重启** | ❌ 无启动补收 | 需要启动补收（幂等） |
| S8 | **bot 死亡** | ⚠️ 数据保留（`D-276`），义务未定义 | 需裁定 |
| S9 | **拆了会让 bot 摔下去** | ✅ `D-399` 守卫：延后重试 ⇒ `underfoot_unsafe` 归因 | 强判据不可红（`D-400` 已记录） |
| S10 | **保护区外**的一切 | ⭐ `D-398`：**无义务** | 实现还没跟上（= 队列 `Z1`） |
| S11 | 容器写入 | ⚠️ 只有计数 | 若要"还物"，需逐 item 账（**建议不做**，见 §4.1） |

---

## §3 冗余设计盘点（6 处，逐条给处置建议）

| # | 冗余 | 现状 | 建议 |
|---|---|---|---|
| 1 | **`WriteBudget`（计数） vs `WorldModLedger`（逐块）** | 同一件事（"我方改了多少世界"）两份账 | **账本为主真相**；`WriteBudget` **降级为遥测/闸门**（只做"要不要拦"，不再做"改了多少"）—— 呼应 `D-387 P1-a`（两份不同源曾静默失效） |
| 2 | **`countThrowaway()`（背包里的一次性方块数） vs 账本位置** | 两种"知道该收什么"的方式 | 保留背包计数**仅用于收集阶段**（掉落物/材料）；**回收义务只认账本位置** |
| 3 | **销账三条路**（`dropStale` / `forget` / `not_ours`） | 三种语义相近的退休 | **合并到一处函数**（`retire(reason)`），保证都带日志与归因码 |
| 4 | **触发者五处**（MineTask / LumberJob / CleanupWrappedTask / 电池 / BotManager 读） | 每个新任务都要记得接 | **收敛为两条**：① 任务/作业收尾（统一在作业层）② **作用域关闭钩子**（关账即登记"待收"）；夹具包装器降为"兜底" |
| 5 | **`implicit` 桶** | 无任务时的兜底 scope | `D-398` 之后：**区外不再需要它**；保护区内**不该**出现 implicit（出现即 bug，应可断言） |
| 6 | **"建拆同权"（`D-081`）在电池里只是 warn** | 不变量靠人记得 | 做成**门禁**（关账时该 scope 的 `TEMP` 条目必须为空，否则红） = 队列 `Z2` |

---

## §4 以后的回收方案（目标模型）

### 4.1 义务三分类（**禁止**与**必须**同等重要）

| 类 | 内容 | 必须 / 禁止 | 判据 |
|---|---|---|---|
| **A. MUST_RECLAIM** | 我方放置且 `TEMP`（脚手架/垫脚/支撑/工作站） | **必须**回收；失败必须**带理由** | 账本 `pendingTemporary` 回 0 或每条都有归因码 |
| **B. NEVER_RECLAIM** | `KEEP`（道路/补种）、**我方挖掉的地形**、区外（`D-398`）的一切 | **禁止**回收（"恢复无主区域没有意义"） | 区外 `pending` 不进账本；KEEP 条目不出现在待收队列 |
| **C. CANNOT_RECLAIM** | 容器物品、流体/光照/方块实体副作用 | **明确不做**，但必须**如实记账**（可查，不许假装可逆） | 有计数/日志；文档与 UI 不承诺"可逆" |

### 4.2 触发器集（5 → 2 条主路 + 2 条补收）

| 触发器 | 说明 |
|---|---|
| **T1 任务/作业收尾** | 统一在作业层：成功、失败、超时、取消**四条路径都要走**（今天只有部分） |
| **T2 作用域关闭钩子** | `closeScope` 时把该 scope 的 `TEMP` 条目登记为"待收"（**关账 ≠ 已收**，但**不许静默**） |
| **T3 启动补收** | 服务器启动读存档账本 ⇒ 保护区内、已加载的待收条目补做（幂等；未加载的排队） |
| **T4 进入范围补收** | 目标区块加载后（玩家走近/bot 路过）补做；**不许**为回收强制加载区块（红线：写入不得由寻路/回收隐式触发超范围加载） |
| **T5 手动** | 一条命令/物品触发（测试与善后） |

**幂等 + 断点续做**：条目在存档里 ⇒ 每次只处理"还没到位"的；`dropStale`/`not_ours` 保证重复执行安全。

### 4.3 四条不变量（全部要能"注入即变红"）

- **I-1 安全**：任何移除前，移除后 bot 必须有落脚面（`canWalkOn`）——`D-399` 已落地（判据为其弱形态 + 台账，`D-400`）。
- **I-2 归因不静默**：没拆成的每一条都必须带码（`not_ours` / `underfoot_unsafe` / `side_break_failed` / `chunk_unloaded`…）——`C2` 夹具已在钉。
- **I-3 只拆自己的**：现场 ≠ 账本 ⇒ `forget` + `not_ours`（已有 ✓，保留）。
- **I-4 区外无义务**（`D-398`）：区外不记账、不回收、无限修改；**保护区内一定记账**。

### 4.4 需要用户裁定的两个点

1. **重启补收**（T3）要不要做？代价：启动时可能有世界写入（与"启动干净"冲突）。
2. **未加载区块**（T4）：只允许"被动补收"（不强制加载），还是要主动加载收尾？（我建议**被动**，与红线一致。）

---

## §5 落地切片（映射到队列 `OPEN_ITEMS_LEDGER §11`）

| 切片 | 内容 | 对应队列 |
|---|---|---|
| **Z1** | 账本/回收**收窄到保护区内**（`D-398` R1–R4）；`implicit` 在保护区内出现即可断言为 bug | 已有 |
| **Z2** | **"建拆同权"门禁化**（关账时保护区内 `TEMP` 条目必须为空）+ 冗余 3/4 的合并 | 已有 |
| **Z3** | 区外取消格数额度；保护区内"不许静默降级" | 已有 |
| **RC1** | 触发器收敛：作业层统一收尾（含失败/超时/取消）+ 作用域关闭登记待收 | 新增 |
| **RC2** | 补收（T3 启动 / T4 进入范围），**幂等**，且不许隐式加载区块 | 新增（需 §4.4 裁定） |
| **RC3** | `CANNOT_RECLAIM` 的**如实记账**（容器逐 item 不做 ⇒ 但要有可查计数/日志） | 新增 |
| **RC4** | 冗余 1 处置：`WriteBudget` 降级为遥测/闸门，账本为唯一真相 | 新增 |

---

## §6 未核实的边界（诚实）

1. **死亡后义务是否延续**：`death_persistence` 只保证"数据不删"，未见回收义务的延续逻辑（未读全）。
2. **除 `MineTask`/`LumberJob` 外**，其它作业（craft/machine/container）是否也接了回收 —— 未逐个核。
3. **`BotManager:714` 读 `implicit` 桶**之后拿它做什么（只读？警告？UI？）—— 未读全。
4. **电池 `endStep`** 在真实（非测试）路径上**没有对应物**：生产里"作用域关闭"是否一定有回收跟进 —— 未核。
