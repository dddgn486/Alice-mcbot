# Alice 会话交接包 - 2026-09-07

> 本文档用于将当前会话的完整上下文迁移到新会话。  
> 包含所有关键决策、未提交改动和隐性知识。

## 📍 当前位置

- **仓库**：`/home/fb486/projects/alice`
- **分支**：`master`
- **HEAD**：`3dbe84c` (diag(phys): add disableable M3 M5 packet observer)
- **阶段**：Alice Pathing Core R2-B 已完成客户端验收

## ✅ 最新完成的工作（R2-B Traverse）

### 已实现
1. **新增源码**：
   - `src/main/java/com/dddgn/alice/task/TraverseDiagnosticTask.java` (120 行)
   - `src/main/java/com/dddgn/alice/pathing/core/TraverseExecution.java` (140 行)
   - `src/main/java/com/dddgn/alice/pathing/core/TraverseExecutionFactory.java` (79 行)
   - 整个 `src/main/java/com/dddgn/alice/pathing/core/` 包（R2-A 契约）

2. **修改源码**：
   - `BotManager.java`：新增 `assignTraverseDiagnostic(BotPlayer, BlockPos)` 方法
   - `BotCommand.java`：新增 `/alice pathing traverse <north|south|east|west>` 命令
   - `AliceItems.java`：移除旧测试物品 `pathfinding_tester`、`traverse_core_diagnostic_tester`、`movement_*_tester`

3. **构建工件**：
   - SHA-256: `fc0ad051208422882b5d8c1060bb4afe822552a1ef753ac5e0964b8e09e0fbbe`
   - 已同步到固定客户端：`/mnt/d/JAVA_projects/worldedit-test/versions/1.20.1-Forge_47.4.10/mods/alice-1.0.0-1.20.1.jar`

4. **验收状态**：`USER_ACCEPTED`
   - 用户确认测试通过
   - 证据已归档：`.alice-supervision/client-tests/pathing-core-r2b-traverse-20260907/evidence/`
     - `evidence-report.md`: 完整测试报告
     - `test-case.md`: 测试用例描述
     - `latest.log` / `debug.log`: Windows 客户端日志
     - `r2b-traverse-logs.txt`: 提取的 `[R2-B Traverse]` 专项日志

### 核心设计决策
- **单一入口**：命令 + 方向参数，自动从 Bot 当前位置派生目标
- **严格几何**：只接受同高度四向相邻目标（north/south/east/west）
- **超时保护**：100 tick 执行预算
- **日志前缀**：`[R2-B Traverse]` 便于筛选
- **终态清理**：成功/失败/超时均清理 `BotController` 输入
- **不接入生产**：独立验证链，不接入 MineTask

## 📦 未提交改动（574 个文件）

### 核心代码改动（需保留）
```bash
# 查看核心改动
git diff -- src/main/java/com/dddgn/alice/bot/BotManager.java
git diff -- src/main/java/com/dddgn/alice/command/BotCommand.java
git diff -- src/main/java/com/dddgn/alice/item/AliceItems.java

# 新增文件
git status --short | grep "??" | grep "src/main/java/com/dddgn/alice/pathing/core"
git status --short | grep "??" | grep "src/main/java/com/dddgn/alice/task/Traverse"
```

**重要**：R2-B 相关改动尚未提交到 Git。

### 文档改动（需保留）
- `docs/AI_PROJECT_STATE.md`：更新为 R2-B 已完成
- `docs/AI_DECISIONS.md`：D-022 更新为 `USER_ACCEPTED`
- `docs/AI_TEST_MATRIX.md`：新增 R2-B Traverse 行
- `docs/START_HERE.md`：翻新为稳定的协作规范
- `docs/ALICE_PATHING_CORE_R1_CONTRACT.md`、`docs/ALICE_PATHING_CORE_R2_MOVEMENTS.md`：R1/R2 契约文档

### 预设配置改动（需保留）
- `~/.dsh/.agent-presets/alice-forge-assistant/agent.cordis.yml`：整合了 START_HERE 规范

### 可忽略的改动
- 574 个文件中大部分是：
  - 删除旧测试证据（`D .alice-supervision/client-tests/...`）
  - 新增证据（`?? .alice-supervision/client-tests/minetask-scene-*-20260905/`）
  - 这些不影响代码功能，可以单独提交或暂时保留

## 🧠 隐性知识（仅在记忆中）

### 1. R2-B 失败历史
- **第一次失败**：使用测试物品 `alice:traverse_core_diagnostic_tester`
  - 问题：工件混乱、多个测试物品残留、目标派生不确定
  - 日志未出现 `[R2-B Traverse]`，实际加载了旧 JAR
- **解决方案**：移除所有测试物品，改用命令 + 方向参数

### 2. BotManager 的预存在尾随空格
- `git diff --check` 报告 BotManager.java 有尾随空格
- 这是**预存在的**，不是本轮引入的
- 不要在修复时顺手清理，避免扩大改动范围

### 3. Windows 工具链使用方式
- **Windows 是源码镜像**，不是 Git 管理端
- **WSL 是唯一 Git 端**，所有提交、推送在 WSL 完成
- **镜像命令**：`./tools/mirror-windows-workspace.sh`（排除 `.git`、`build`、`run`）
- **工件同步**：`./tools/sync-windows-artifact.sh [jar] [repo] [runtime-mods]`
  - 第三个参数（runtime-mods）显式给出才同步到客户端
  - 自动验证 SHA-256 一致性

### 4. 固定客户端测试环境
- 路径：`D:\JAVA_projects\worldedit-test\versions\1.20.1-Forge_47.4.10`
- 日志位置：`logs/latest.log`、`logs/debug.log`
- **关键习惯**：用户说"测试通过"后，AI **必须主动读取日志**
  - 筛选关键前缀（如 `[R2-B Traverse]`、`[MineTask]`）
  - 确认 started/completed/failed 终态
  - 验证实际脚位、支撑、onGround、controller 状态

### 5. 验收等级的严格区分
- `IMPLEMENTED`：代码已实现
- `COMPILES`：编译通过
- `SERVER_TESTED`：服务端日志确认行为
- `WINDOWS_CLIENT`：客户端可见行为确认
- `USER_ACCEPTED`：用户明确认可

**构建成功 ≠ 客户端可用**，必须有真人客户端反馈。

### 6. 当前架构边界（不可悄悄改变）
- MineTask/DropCollectionTask 保持 `HARD_PATH`
- `SOFT_SURFACE` 只能通过独立实验入口推进
- 旧 `PathExecutor` 继续作为兼容后端，不删除
- Movement 实验 1-6、M0/M1/M2 已验证但**未接入生产**
- R2-B 是独立验证链，不接入 MineTask

### 7. Skills 使用习惯
- **必读场景**：
  - FakePlayer → `forge-fakeplayer-lifecycle`
  - 实体同步/回弹 → `forge-entity-sync-broadcast`
  - 移动/碰撞/物理 → `forge-entity-physics-collision`
  - GUI/Menu → `forge-container-menu-protocol`
  - 连续失败 → `debugging-root-cause-analysis`
- 这些 skill 代表 60-94 小时的项目知识

## 🎯 Alice Pathing Core 完整路线图

### 为什么停止 M0/M1/M2 实验

**历史背景**（D-019，已废弃）：
- 原计划：先建立 `MovementPlan` 数据契约 → 独立编译/测试入口（M1）→ 适配 BotMiner（M2）→ 接入 MineTask
- **问题**：
  1. `MovementPlanCompiler` 是**事后编译器**，不是 Movement-aware 搜索内核
  2. 它基于旧 `BlockPos` 路径编译，而不是从搜索阶段就考虑 Movement 能力
  3. 通用 `MiningPlanner` 输出与 M1 编译契约不一致
  4. 直接推进 MineTask 会重复造轮子并破坏成熟 Legacy 行为

**决策**（D-020）：
- **停止修补**：不继续 M0/M1/M2 → MineTask 的接入路线
- **重新设计**：从零构建 Baritone-like Movement-aware Pathing Core
- **保留实验**：M0/M1/M2 作为独立实验和迁移证据保留，不删除

### 新寻路框架目标

**总目标**：构建 **Alice 版 Baritone Pathing Core**

```text
Baritone 级路径计算与 Movement 执行内核
+ Alice Bot 可回收性策略
+ Alice 生存系统兜底接口
+ Alice 任务层失败交接
+ Alice 决策层/LLM 重试与接管
+ 本地行为包与权限策略适配
```

**不以性能或 Movement 数量超过 Baritone 为目标**；目标是比直接依赖 Baritone 更好地表达 Alice 的 Bot 生命周期、任务包、行为包、可回收性和失败事实。

### 借鉴 Baritone 的设计

**核心架构**（参考 Baritone [MineProcess](https://github.com/cabaletta/baritone/blob/e962c395067db099c46f6e9a0f67cef48b4a02fb/src/main/java/baritone/process/MineProcess.java)、[Movement 类型](https://deepwiki.com/cabaletta/baritone/4.2-movement-types)、[PathExecutor](https://github.com/cabaletta/baritone/blob/d3c170afaec71d7101c7c5a5a7b4ac374830b66/src/main/java/baritone/pathing/path/PathExecutor.java)）：

```text
Decision / LLM
  选择目标、预算、策略与是否授权特殊能力
        ↓
Domain Task (MineTask / PlaceTask / RoadBuildTask)
  拥有领域目标与任务生命周期
        ↓ PathRequest
Pathing Core (Movement-aware 搜索)
  搜索 Movement 路线、成本、策略约束、路径有效性
        ↓ PathPlan / PathSession
Movement Executor
  执行单个 Movement、重验前置/后置、清理输入、报告事实
        ↓
Server Bot / World (权威服务端)
```

**关键分离**：
1. **领域动作 vs 通行 Movement**：
   - `MineTargetAction`（挖目标方块）≠ `BreakAndTraverseMovement`（为通过而破坏障碍）
   - `PlaceBlockAction`（在领域目标位置放置）≠ `PlaceStepAndTraverseMovement`（临时台阶）
   - 所有世界修改必须携带明确意图：`PATH_ACCESS` / `TARGET_ACCESS` / `DOMAIN_ACTION` / `TEMPORARY_SUPPORT` / `RECOVERY_CLEANUP`

2. **GoalSpec 与 HeuristicModel 解耦**（D-R1-03）：
   - `GoalSpec` 只负责目标满足判断和语义
   - 成本估算统一由 `HeuristicModel` 基于 `CostModel` 计算
   - Goal 不再拥有独立 heuristic 语义

3. **MovementPolicy 与 WorldMutationPolicy 分离**（D-R1-04）：
   - `MovementPolicy`：是否允许使用某种 Movement
   - `WorldMutationPolicy`：该 Movement 具体能否破坏/放置哪些方块
   - 前者不能替代后者

### Alice 特有策略（超越 Baritone）

**三层安全模型**：

1. **生存系统兜底**（运行期最后防线）：
   - 伤害、生命值、受击状态
   - 熔岩、火焰、窒息、溺水
   - 敌对实体和紧急环境风险
   - 中断当前任务、紧急撤离或交给生存任务

2. **寻路提前避险**（`RecoverabilityPolicy`）：
   - 可回收性采用等级枚举（D-R1-01）：
     - `LOCAL_STEP`：当前 Movement 完成后至少能回到上一个脚位
     - `PATH_REVERSIBLE`：当前路径具备可验证的反向回收路线
     - `SAFE_EXIT_REQUIRED`：当前路线完成后仍能到达指定安全出口
     - `EMERGENCY_EXIT_REQUIRED`：必须保留满足生存兜底的紧急撤离能力
   - 自然下降默认最多一格
   - 默认不挖当前支撑方块
   - 默认不进入无法返回的单向地形

3. **禁区与保护区分离**：
   - `ForbiddenZonePolicy`：限制 Bot 进入或经过（熔岩、危险流体、任务禁区）
   - `ProtectedAreaPolicy`：限制世界修改（建筑、箱子、红石结构、用户标记区域）
   - **允许经过保护区 ≠ 允许修改保护区**

### 核心数据契约

**PathRequest**（显式请求，不是含义不清的 `BlockPos target`）：
```text
PathRequest
├─ botId
├─ startFoot (起点脚位快照)
├─ goal / GoalSpec (目标规范)
├─ allowedMovementTypes
├─ forbiddenMovementTypes
├─ safetyPolicy
├─ recoverabilityPolicy
├─ worldModificationPolicy
├─ costModel
├─ heuristicModel
├─ searchBudget
├─ cancellationToken
└─ requester/task metadata
```

**MovementSpec vs MovementExecution**（D-R1-02）：
- `MovementSpec`：纯数据，可计算、可序列化、不可直接执行
- `MovementExecution`：绑定 Bot/实时世界、只能由 PathSession 执行
- 规划输出保存 `MovementSpec`，执行阶段在主线程根据实时世界创建 `MovementExecution`

**单 Bot 单 active PathSession**（D-R1-05）：
- 一个 Bot 同时最多拥有一个活动 `PathSession`
- 新会话必须显式取消、等待终止或拒绝旧会话
- 只有带有当前 `botId` 和 active `sessionId` 的会话可以写入 Controller

### 实施阶段

**R1 - 契约冻结**：
- 状态：设计草案，不授权修改生产代码
- 内容：定义 PathRequest、GoalSpec、策略、WorldView、MovementSpec、PathPlan、PathSession、执行交接边界
- 文档：`docs/ALICE_PATHING_CORE_R1_CONTRACT.md`
- 已确认决策：D-R1-01 到 D-R1-05（可回收性等级、纯数据 MovementSpec、Goal 解耦、策略分离、单 PathSession）

**R2-A - 纯数据契约实施**（已完成）：
- 状态：已实施并编译
- 内容：新增 `pathing.core` 包
  - `MovementType`、`MovementCapabilities`、`RecoverabilityLevel`、`IntrinsicReversibility`
  - `MovementSpec`、`PlanningDependency`
  - `WorldView`、`LiveExecutionContext`
  - `MovementExecution` 接口、`MovementExecutionFactory` 接口
- 保留边界：不修改 MineTask、Legacy PathExecutor、旧 Movement

**R2-B - Traverse 独立验证**（已完成 USER_ACCEPTED）：
- 状态：已实施、编译、客户端验收通过
- 内容：
  - `TraverseExecutionFactory`、`TraverseExecution`（同高度四向相邻）
  - `TraverseDiagnosticTask`（最小任务包装）
  - `/alice pathing traverse <north|south|east|west>` 命令
- 设计决策：
  - 严格几何：只接受同高度四向相邻（north/south/east/west）
  - 100 tick 超时保护
  - 明确的 `[R2-B Traverse]` 日志前缀
  - 终态清理 BotController 输入
  - 不接入生产 MineTask
- 工件：SHA-256 `fc0ad051208422882b5d8c1060bb4afe822552a1ef753ac5e0964b8e09e0fbbe`

**R2-C - 其他基础 Movement**（待用户决策）：
- Diagonal（同高度对角）
- Ascend（一级上升，允许水平位移）
- Descend（一级下降，允许水平位移）
- 仍保持独立验证链，不接入 MineTask

**R2-D - 世界修改 Movement**（未启动）：
- `BreakAndTraverse`（为通过而破坏障碍）
- `PlaceStepAndTraverse`（为通过而放置台阶）
- 需要明确 `WorldMutationPolicy` 和资源/工具验证

**R3 - PathSession 与多段链接**（未启动）：
- PathSession 生命周期管理
- 多段 Movement 链接执行
- 动态 stale 检查和重规划
- 失败交接到 Task 层

**R4 - Movement-aware 搜索器**（未启动）：
- 替换当前零启发式 Dijkstra（D-013）
- Movement Provider 候选生成
- 策略约束的搜索剪枝
- 可接受 heuristic 设计

**R5 - 任务层适配**（未启动）：
- 重新评估 MineTask 适配
- PlaceTask、RoadBuildTask 等任务的 PathRequest 生成
- Domain Action 与 Transit Movement 的协调
- 失败重试、LLM 接管和策略调整

### 完成后做什么

**短期目标**（R2 阶段）：
1. 验证基础 Movement 类型（Traverse/Diagonal/Ascend/Descend）的物理链和执行器
2. 建立稳定的独立验证链和证据标准
3. 不接入生产 MineTask，保持 Legacy 系统稳定

**中期目标**（R3-R4 阶段）：
1. 实现 PathSession 和多段链接
2. 实现 Movement-aware 搜索器
3. 完成策略约束的候选生成和剪枝

**长期目标**（R5 阶段）：
1. 让 MineTask 等任务使用新 Pathing Core
2. 逐步退役 Legacy PathExecutor（当新系统完成等价客户端证据）
3. 支持本地行为包和权限策略适配
4. 与决策层/LLM 的重试和接管集成

**最终愿景**：
- Alice 拥有 Baritone 级别的 Movement-aware Pathing Core
- 加上 Alice 特有的可回收性、生存兜底和任务交接
- 支持本地行为包、权限策略和多 Bot 协作
- 保持服务端权威和显式失败语义
- 让 LLM 专注于目标级决策，而不是逐 tick 控制

---

**当前位置**：R2-B Traverse 已完成验收，等待用户决定是否继续 R2-C 或暂停。



用户待决定：

1. **继续 R2-C**：实现 Diagonal、Ascend、Descend 的独立验证链
2. **暂停 R2**：等待完整 PathSession/Planner 设计评审
3. **归档 R2-B 证据**：用户提供日志/截图后归档到 `.alice-supervision/client-tests/pathing-core-r2b-traverse-20260907/`
4. **其他任务**：回到 MineTask 恢复契约整理或其他优先事项

## 📚 关键文档索引

**必读文档（新会话启动时）**：
1. `docs/AI_PROJECT_STATE.md` ← 当前目标和已验证状态
2. `docs/AI_DECISIONS.md` ← 不可改变的架构决策
3. `docs/AI_TEST_MATRIX.md` ← 测试入口和验收等级
4. `docs/START_HERE.md` ← 协作规范（已翻新）
5. `docs/AI_DEVELOPMENT_PLAYBOOK.md` ← 详细开发流程

**设计文档**：
- `docs/ALICE_PATHING_CORE_ARCHITECTURE.md` ← 总体架构
- `docs/ALICE_PATHING_CORE_R1_CONTRACT.md` ← R1 契约草案
- `docs/ALICE_PATHING_CORE_R2_MOVEMENTS.md` ← R2 Movement 设计
- `docs/MINETASK_RECOVERY_CONTRACT.md` ← MineTask 恢复层级

**Skills 知识库**：
- `.alice-supervision/skills/*.skill.md` ← 60-94 小时积累的技术知识

**预设配置**：
- `~/.dsh/.agent-presets/alice-forge-assistant/agent.cordis.yml` ← 已整合 START_HERE 规范

## 🚀 新会话启动清单

新会话接手时，执行以下步骤：

```bash
# 1. 确认环境
cd /home/fb486/projects/alice
pwd
git status --short | wc -l
git branch --show-current
git log -1 --oneline

# 2. 读取核心文档
# 按优先级读取上述"必读文档"

# 3. 理解当前状态
# - R2-B Traverse 已完成验收但未提交 Git
# - 574 个未提交文件（核心改动 + 证据清理）
# - 固定工件 SHA-256: fc0ad051208422882b5d8c1060bb4afe822552a1ef753ac5e0964b8e09e0fbbe
# - 用户待决定下一步方向

# 4. 向用户确认
# 复述当前位置、下一步选项、不会改变的边界
```

## 📞 交接完成标志

新会话可以回答以下问题，说明交接成功：

1. R2-B Traverse 的验收状态是什么？（`USER_ACCEPTED`）
2. 为什么不用测试物品而用命令？（工件混乱历史）
3. 用户说"测试通过"后 AI 应该做什么？（主动读取 Windows 日志）
4. MineTask 能接入 SOFT_SURFACE 吗？（不能，只能独立实验）
5. 当前有多少未提交改动？（574 个文件）
6. 下一步有哪些选项？（R2-C / 暂停 / 归档证据 / 其他任务）

---

**交接时间**：2026-09-07  
**交接人**：鲸鱼娘 (DeepSeek R1 会话)  
**下一步**：等待用户决策 🐋
