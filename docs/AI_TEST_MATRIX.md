# Alice 当前测试矩阵

> 轻量记录关键场景，不要求每个功能制作复杂报告。客户端测试在 Windows，由真人完成。
>
> 恢复层级字段见 `docs/archive/legacy-2026-08/MINETASK_RECOVERY_CONTRACT.md`；A/B/C 已用事件序列契约复核，不改变其行为边界。

## 证据等级

```text
CODE_REVIEW -> COMPILES -> SERVER_LOG -> WINDOWS_CLIENT -> USER_ACCEPTED
```

## 测试入口原则

优先使用可 `/give` 获得的游戏内测试物品，通过手持右键/Shift+右键操作。复杂坐标命令只用于精确诊断，不作为普通测试主入口。

## 当前架构阶段说明

> **2026-09-13 更新**：下面这段"R1/R2 入口尚未定义"的说法**已过期** —— 新 Pathing Core 已建立，
> 且有一整套零参数入口。**当前可用入口与判据的唯一权威清单是 [`TESTING_GUIDE.md`](TESTING_GUIDE.md)**；
> 本文件只保留"能力 × 等级"的历史验收记录。

当前测试矩阵分为两类：

- Legacy 正式任务：MineTask/A/B/C、不可达安全失败，继续作为稳定基线；
- Movement 独立实验：M1/M2 证据保留，但不代表新 Pathing Core 已建立，也不授权 MineTask 接入。

### 当前主线入口与等级（2026-09-13）

| 能力 | 零参数入口 | 关键判据 | 等级 |
|---|---|---|---|
| **串联回归电池**（CORE **30** / FULL **40**，★首选） | `alice:regression_battery` 右键（CORE）或 `/alice battery full` | `SUMMARY … PROFILE=core … (30/30) → PASS` + `K4=OK(…)`；SKIP 会写明（例如未装模组时的 `craft_probe_upgradetab=SKIP` / `machine_*=SKIP`） | `WINDOWS_CLIENT`（第五轮 `(28/28) ticks=2765 → PASS`；**第十二轮 `(30/30) ticks=3370 → PASS`，`latest.log:3899`**，含新步 `craft_machine`；**第十三轮（D-218 复跑）`(30/30) ticks=3414 → PASS`，`latest.log:3955`**，`K4=OK(… 写入类例外=51)`）；**第十九轮（2026-09-15，jar `462b10b5…` = T3 七步 + D-220）`(30/30) ticks=3337 → PASS`，且 pathing 步 54 行场景日志与无头 `core` **逐字相同**（跨通道等价，比对前须 `tr -d '\r'`：Windows 日志是 CRLF，直接 diff 会整段假差异）** |
| 挖掘回归（11 用例） | 电池 `mine_regression` / `alice:mine_regression` | `SUMMARY free=PASS … scope_reopen_keeps_drops=PASS`；`dropsLeft` 只数本用例新增（D-168） | `WINDOWS_CLIENT`（11/11） |
| **K-4 谓词一致性** | 电池 SUMMARY 的 `K4=` 段；`alice:bot_report` 的"目标准入（K-4 累计）"行 | 真异常 0；写入类例外仅计数 | `WINDOWS_CLIENT`（收口：不引硬拒） |
| K-1 部分计划 | 电池 `partial_search` / `alice:partial_search_check` | `partial_with_prefix=PASS … verdict=PASS` | `WINDOWS_CLIENT`（电池内 PASS） |
| 基-8 能力闸门 | 电池 `capability_gate` / `alice:capability_gate_check` | 用例含 `safe_cancel_wiring`、`session_status_no_dead_value`（K-5：状态枚举不得有死值） | `WINDOWS_CLIENT`（电池内 PASS；K-5 新用例待本轮电池） |
| K-2 legacy 双内核 | 无独立入口（纯删除，回归靠电池） | 电池 SUMMARY 全 PASS | `COMPILES`（删除依据 = 按路径分析的零活引用） |
| L2 菜单/容器 | 电池 `transfer` / `alice:transfer_check` | `fixture=PASS end_to_end=PASS … verdict=PASS` | `WINDOWS_CLIENT`（电池内 PASS） |
| 决策层 6 步 | 电池内 `decision_contract` / `decision_trace` / `permission_gate` / `pickup_gate` / `collect_job` / `recipes_dump` | 各自 `SUMMARY` | `WINDOWS_CLIENT`（电池内 PASS） |
| **挖矿候选菜单契约（M1 / G1）** | 电池步 `mine_menu`（场景 `alice_test:ore_course_terrain`） | 一次右键跑电池（CORE） | `[Goal] candidate_menu mine=N`（矿石场景 N≥1）；`[MineMenu] SUMMARY checks=11 failures=0 … → PASS`。**判据**：缺 target / 不在菜单里的 target ⇒ 必须 `Refused`（`mine` 不许猜位置）；命中菜单 ⇒ `productTag` **逐字等于**条目的 `block=`；`radius` 必须覆盖目标距离；LLM 自写冲突的 `productTag` ⇒ 以菜单为准且有 clamp；空/未知 `productTag` 由 `JobLauncher.refusalReason` **如实拒绝且不抛异常** | `SERVER_TESTED`（2026-09-15 无头 `core`：`(31/31) ticks=3404 → PASS`，`mine=6`、`checks=11 failures=0`）
| **长作业周期复评（M2 / G2）** | 电池步 `no_progress`（场景 `alice_test:ore_course_terrain`） | 一次右键跑电池（CORE） | `[NoProgress] SUMMARY emits=2 failures=0 → PASS`。**判据**：窗口关着时**不许**报；开窗（40 tick）+ 有任务在跑 + 无可观测进度 ⇒ 报**恰好一次**（同 episode 不重复）；出现进度（脚位变化）⇒ **重新武装**、再停滞 ⇒ 报第二次（滞回是活的）；自检窗口内 `GoalDirector.isSuspended=true`（**只记录不通知**决策层）；**收尾把窗口复位回 0** | `SERVER_TESTED`（2026-09-15 无头 `core`）
| **专有终态理由（M3 / G3 归因）** | 电池步 `mine_no_tool`（场景 `alice_test:ore_course_terrain`，**不发镐**） | 一次右键跑电池（CORE） | `mine_no_tool=PASS`。**判据**：`MineJob.terminalReason()` 必须是 **`tool_missing`**（而不是总括码 `no_reachable_candidate`/`partial_quota`）；归因基于**逐码精确比较**（`AttemptFailure(pos, code)`），**不是** `LumberJob` 那种在拼接串上 `contains()`。Job 自身 FAILED 是**预期**（该步验归因，不验挖到）；归因不对 ⇒ `doneWhen` 永不成立 ⇒ 预算耗尽记 `TIMEOUT` 判红 | `SERVER_TESTED`（2026-09-15 无头 `core`）
| 阶段 3-A A1/A2/A3 合成阶梯 | `alice:craft_check` / `alice:craft_action_check` / `alice:craft_table_check`（先 `/function alice_test:craft_table_course`） | 各自 7/7 `verdict=PASS`；A3 含 `no_world_write`（零写入硬断言，夹具自带传送） | `WINDOWS_CLIENT`（三项均实测 7/7，2026-09-13） |
| **阶段 3-A A3b 自放工作站** | `alice:craft_station_check`（先 `/function alice_test:craft_station_course`）；电池步 `craft_station` | `start_premise no_station_premise holding_station_item station_placed write_accounted placed_table_craft teardown_clean verdict` 全 PASS；`teardown_clean` = **方块回空气 + 账本 pending=0** | `IMPLEMENTED` + `COMPILES`（**待客户端**，D-190） |
| **阶段 3-A C 模组站点真合成** | `alice:craft_station_craft_check`（先 `/function alice_test:craft_tab_course`）；电池步 `craft_station_craft` | `[CraftStationCraft] SUMMARY … materials_consumed=true product_produced=true deprovision_verified=true verdict=PASS`（实测：消耗 8 圆石 / 产物 1；**产物进容器**；不自动补料） | `WINDOWS_CLIENT`（2026-09-13 PASS） |
| **阶段 3-A L2 工作站装配** | `alice:craft_station_provision_check` 右键（先 `/function alice_test:craft_tab_course`） | `[ProvisionCheck] SUMMARY … provision_verified=true deprovision_verified=true item_returned=true verdict=PASS` | `IMPLEMENTED` + `COMPILES`（**待客户端**，D-194） |
| **阶段 3-A S1 合成工作站可切换 + 网格探针** | `/alice craft station [<id\|auto>]`；`alice:craft_grid_probe` 右键（先 `/function alice_test:craft_tab_course`） | `[CraftGridProbe] SUMMARY … discover=OK grid=NxM … grid_addressable_without_tab=… read_only=true verdict=PASS` | `IMPLEMENTED` + `COMPILES`（**待客户端**，D-192） |
| 阶段 3-B S1 机器配方只读 | 电池步 `machine_route`（无需场景；旧临时入口 `alice:machine_probe` 已按 S5 回收） | `[MachineProbe] SUMMARY … verdict=PASS`；模组不在 ⇒ SKIP；零写入；**含 Thermal 的新绿基线**：`machine_rows=59 with_site=49 no_site=10 declared_menu=2 executable=1` + `unmapped=[]` + **`row_block_missing=[]`**；**第十五轮起采样命名空间按表推导**（台账⑭）⇒ 期望 `namespaces=[mekanism, thermal] types=56 type_recipes=1823` 且 **`with_site_confirmed=46` / `with_site_unobserved=[mekanism:smelting, thermal:brewer, thermal:hive_extractor]`**（分桶 `46+3+10+0=59`。⚠️ 上面这组 `with_site=49 no_site=10` / `with_site_confirmed=46` 是 **T3 之前的历史基线，保留不动**；**【无头基线（T3 第一步后，2026-09-14）】**现在应为 `machine_rows=59 with_site=55 no_site=4 shared_site=6`、`with_site_confirmed=52`、`no_site=[mekanism:energy_conversion, mekanism:evaporating, mekanism:gas_conversion, mekanism:infusion_conversion]`、分桶 `52+3+4+0=59`、`unmapped=[]`、`row_block_missing=[]`；来源 = **一条命令** `bash tools/headless-battery.sh core`（`verdict=PASS`）。**证据等级 = 「无头基线」，不是 `WINDOWS_CLIENT`**（这些是表自身读数，探针与生产共用同一读取器；渲染/物理/GUI 仍须真人）。**【无头基线（T3 第二步 B3a 后，2026-09-14）】**：读取器改「先原版语义、再模组名族」+ 逐字段出处；`machine_rows=59 with_site=55 no_site=4 shared_site=6` 与 `with_site_confirmed=52` **不变**（判据未动）；读数 `samples=108 unreadable_via_vanilla=88 upstream_readable=47 machine_output_not_item=61 input_readable=65 chance_declared=27 vanilla_input=0 vanilla_output=20 divergent=0 read_notes=0 query_probed=3 query_reachable=3`（逐命名空间：`mekanism 31/20/31/30/0`、`thermal 57/26/31/34/27`）。**⚠️ 两条口径纪律**：①`upstream_readable`/`input_readable`/`chance_declared` 的**旧数值全部作废**（它们取自**未排序**抽样，而 `getRecipes()` 迭代序跨轮不稳定 ⇒ **同一 jar 都会变**，实测 `input_readable` 65/64、`query_machine_route` 0/2/1；现两处遍历与自检选样已排序，**同 jar 三轮 SUMMARY 逐字相同**）；②`query_machine_route` **不许当判据**（机器产出常同时有原版合成路线，查询层按设计先走 `craftable` ⇒ 仅改采样口径它就 0/1/2 → 3）；真正有意义的是 **`query_reachable`**（读取器读出 X ⇒ 查询层不得对 X 报 `NO_RECIPE`），**本轮只计数未断言**。**`recipe_order_hash` 不是断言**，只用于区分「枚举序变了」与「读取器变了」。证据 `docs/reviews/2026-09-14-T3-B3a-读取器vanilla优先与探针确定性.md`。**【无头基线（T3 步骤 A：探针可见性，2026-09-14）】**：枚举来源由**表**改为**配方注册表** ⇒ `unmapped` 的遍历域从「已登记命名空间」扩到**全部非原版类型**（旧实现里未登记模组**连枚举都进不去**）。新读数 `unmapped_total=19` + `unregistered_ns=[create=15(rows=0,recipes=506), extendedcrafting=4(rows=0,recipes=25)]`；**已登记部分读数逐字未变**（`types=56 type_recipes=1823 samples=108 upstream_readable=47 input_readable=65 with_site_confirmed=52 …`），唯一差异 = `unmapped=[]` → `unmapped=[19 项]`（**旧 `unmapped=[]` 不是「没有未登记类型」，而是「看不见」**）。`unmapped` / `unregistered_ns` 仍是 **warn-only、不许升级为判红**（模组集可变，「我们 0 行」是**待办**不是错误）；**采样范围仍只限已登记命名空间**（`samples=108` 不变）。两轮同 jar SUMMARY 逐字相同。证据 `docs/reviews/2026-09-14-T3-步骤A-探针可见性.md`。**【无头基线（T3 步骤 A2+C，2026-09-14）】**：未登记命名空间也做**纯只读**形状定点采样（同一份读取器，**只加新键、不动现有 8 个桶**）⇒ `unregistered_sampled=37`、`create out_readable=29/29`、`extendedcrafting out_readable=8/8`、`unregistered_notes=0`；**承重读数** `unregistered_vanilla_only_in=35/37`、`unregistered_vanilla_only_out=37/37` ⇒ 模组名族对 Create/EC **读不出东西**，是 B3a 的原版路径在读 ⇒ **B3a 对接第 3 个模组是承重的**（改前这批会判 `MACHINE_RECIPE_UNSUPPORTED`）；M-4 答案：`unregistered_query_no_recipe=0`、`query_reachable=6/6`、`verdicts={MACHINE_ROUTE=2, MISSING_INGREDIENTS=4}` ⇒ **查询层对新模组物品是诚实的，没有把"做得出来"报成 `NO_RECIPE`**。⚠️ 两轮同 jar：轮 1 `partial_search=FAIL`、轮 2 `PASS` ⇒ **该步是非确定性的**（起步时 bot 未落地：`on_ground=false`、`from.z=404` vs `406`），**与本改动无关但属通道缺陷**，已登记台账。，"未观测"回到"真·零配方"本义）；另有**逐命名空间一行**覆盖计数可单独 grep | `WINDOWS_CLIENT`（第五轮 `(28/28) → PASS` 内含；**第十四轮 `(30/30) ticks=3279 → PASS`，`latest.log:2908`/`:2989`**）⇒ **Thermal 32 行的方块 id 全部经客户端注册表复核通过**：`row_block_missing=[]`、`unmapped=[]`、`machine_station` 仍精确找到 2 台、`craft_machine` 逐字不变（`fallback_used=false` + `route_recipe=mekanism:enriching/clay_ball` + `product_after=4`）。**✅ 第十五轮（多命名空间探针）已复核通过**（`latest.log:3007`/`:3177`/`:3178`）：`命名空间=[mekanism, thermal] 类型=56 条数=1823`、**`with_site_confirmed=46`**、`with_site_unobserved=[mekanism:smelting, thermal:brewer, thermal:hive_extractor]`（分桶 `46+3+10+0=59`）、`row_block_missing=[]`、`unmapped=[]`、同轮 `(30/30) ticks=3353 → PASS`（`machine_cycle` / `machine_station` / `craft_machine` 逐字未变）；逐命名空间行 `namespace=mekanism types=26 type_recipes=1171` + `namespace=thermal types=30 type_recipes=652` ⇒ **"Thermal 32 个类型里哪 30 个真有配方"第一次有了自动化证据**（不再只靠手工作业）。**同轮新发现（台账⑮）**：`namespace=thermal … upstream_readable=0 input_readable=0`（57/57 抽样全读不出）——Thermal 机器配方**不是读不出**，而是**访问器名字不同**（`getInputItems/getOutputItems/getOutputItemChances` vs Mekanism 的 `getInput/getOutputDefinition`）。**✅ 第十六轮（台账⑮ 修法）已复核**：`MachineRecipeFacts` 扩名族 + 探针与生产**共用唯一读取器** ⇒ 实测 `namespace=thermal upstream_readable` **0 → 26**、`input_readable` **0 → 34**（57 条抽样；流体输出的 `refinery`/`crucible` **仍读不出**，是如实结果），`namespace=mekanism` 四个计数 **31/20/31/30 逐字未变**、`row_block_missing=[]`、同轮 `(30/30) ticks=3351 → PASS`、`craft_check` 负例物品仍是 `minecraft:barrier`。**⚠️ 同轮自纠**：我第一版把概率判据写成 `chance<1.0`（`probabilistic_output=23/57`），而这个数**与静态 JSON 算不出来**（期望 ~5）⇒ **那是在猜语义**，已撤，改为**只声明"上游给了概率信息"、不解释数值**，字段名 **`chance_declared=`**（语义未取证 ⇒ 台账⑯）；该字段**下一轮读一次即可**，无预测基线。**✅ 2026-09-15 第十九轮客户端复核（jar `462b10b5…`，T3 七步全部落地后）**：探针 42 个字段中 **41 个与无头 `core` 逐字相同**，唯一差异 = `recipe_order_hash`（**已登记为非确定值**：`RecipeManager.getRecipes()` 迭代序）⇒ **T3 探针读数升 `WINDOWS_CLIENT`**；同轮电池 `(30/30) → PASS`。证据 `.alice-supervision/client-tests/d220-t3-20260915/evidence/` |
| **阶段 3-B S2+S3 机器站点只读（按 `MachineMap` 认机器）** | `/function alice_test:machine_course` → 电池步 `machine_station`（旧临时入口 `alice:machine_station_probe` 已回收） | `m1_binding=true m2_binding=true`（**"点对了哪台"只有它认得出** —— `menuClass` 两台机器相同、槽位表逐项相同）、`m2_menu_class_matches=true`、`m{i}_slot_roles`、表行完整划分守恒 `with_site_confirmed + with_site_unobserved + no_site + row_block_missing`；机器不在 ⇒ SKIP；零写入 | `WINDOWS_CLIENT`（第四轮 `(28/28) ticks=2789 → PASS`） |
| **阶段 3-B (c) 增量 2：机器路线的生产路径**（`CraftJob` 真的驱动一台机器） | **电池步 `craft_machine`（唯一入口）** —— 复用场景 `alice_test:machine_course`；夹具自带传送与复位、挑"只能靠机器做出来"的目标物、按前提备料 | `[CraftMachine] SUMMARY … verdict=PASS`：`target`（候选清单用**生产查询层**现场复核，判不出就红的 `no_machine_only_target`）+ **`fallback_used=false`**（D-218 后查询层按执行准入优先排 ⇒ 首选 `minecraft:clay_ball` 应直接可用）+ `route_station`（必须映射到 `EXECUTABLE` 的机器行）+ `job_terminal=DONE` + `m_product_landed=true` + `m_machine_emptied=true` + **`m_walk_state=DONE`**（生产**自己走到机器旁**；出现 `unexpected_walk_skipped` 即起点没生效）+ `product_after = product_before+1`；`[CraftJob] machine SUMMARY … verdict=PASS` 是同一条链上的旁证 | **`WINDOWS_CLIENT`（第十二轮，2026-09-14，`latest.log:3185`；整轮 `:3899`）**：`craft_machine=PASS ticks=255` + `[CraftMachine] SUMMARY … verdict=PASS`。实测细节：**目标物由后备逻辑选中** —— `candidates_tried=minecraft:clay_ball=MACHINE_ROUTE` / `minecraft:clay_ball=no_executable_adapter:mekanism:chemical_injection_chamber` / `minecraft:soul_soil=MACHINE_ROUTE` ⇒ 首选 clay_ball 的路线落在**尚无执行准入**的机器上（如实拒绝），于是换到 `target=minecraft:soul_soil`、`route_station=mekanism:enrichment_chamber`、`route_recipe=mekanism:enriching/conversion/soul_sand_to_soul_soil`；`input=minecraft:soul_sand x1 provision=given=1`（测试侧备料）+ `job_terminal=DONE job_reason=crafted:minecraft:soul_soil x1`；**`m_walk_state=DONE m_walk_ticks=41 m_machine_reach=1.5`**（生产**自己走过去**，与夹具逐字同值）+ `m_energy_source=present（…）`（**不是 `api_precharge`**）+ `m_product_landed=true m_machine_emptied=true m_input_consumed=true m_container_writes=2` + `product_after=1 product_landed_inventory=true`；旁证 `[CraftJob] machine SUMMARY … machine_cleaned_before=skipped（生产路径不动机器里的存量）… verdict=PASS`（`:3178`）；该步 `WriteBudget … scope=…#1596:Regression:craft_machine breaks=0 places=0 containers=2/32 refusedContainers=0`（`:3187`）。**✅ 第十三轮复跑（D-218 生效，2026-09-14，`latest.log:3240`；整轮 `:3955`）**：`craft_machine=PASS ticks=255` + **`fallback_used=false`** + **`target=minecraft:clay_ball`**（首选候选**直接可用**，不再落到后备的 `soul_soil`）+ `route_recipe=mekanism:enriching/clay_ball` + `input=minecraft:clay x1` + `job_reason=crafted:minecraft:clay_ball x4` + `product_after=4 product_landed_inventory=true` + `m_walk_state=DONE m_walk_ticks=41 m_machine_reach=1.5` + `m_energy_source=present（…）`。**"前/后"判决器干净**：同一入口、同一断言，仅排序改变 ⇒ `true`+`soul_soil`（第十二轮）→ `false`+`clay_ball`（第十三轮） |
| **阶段 3-B S4 单机最小闭环（3-B 第一次容器写入）** | **电池步 `machine_cycle`（唯一入口）** —— 临时物品 `alice:machine_cycle_check` 已按 S5 回收（D-215）；要单跑请先 `/function alice_test:machine_course` 再跑 CORE 档 | `[MachineCycle] SUMMARY … verdict=PASS` + **`energy_source=cube（场景电源，未补电）`** + `energy_at_open>0`（**这两条=场景电源自证**；退化成 `api_precharge` 就是场景坏了，别放宽断言）+ `input_consumed=true product_landed=true machine_emptied=true container_writes=2 reset=true` | `WINDOWS_CLIENT`（**第七轮 `latest.log:213`** 单跑：`energy_at_open=20000.0` + `cube` + `verdict=PASS`，D-213 复核通过；**第九轮电池内**：`machine_cycle=PASS ticks=210`（`:3130`）+ `(29/29) ticks=3108 → PASS`（`:3842`）；**第十轮 S5 回收后复核**：`(29/29) ticks=3083 → PASS`（`:3849`）+ Forge 报 `Unidentified mapping … alice:machine_cycle_check` ⇒ 物品已不在注册表）；**第十一轮 S4 v2（D-216）已实测"自走到机器旁"**：`latest.log:3209` `machine_distance_at_locate=8.5` +
`walk_state=DONE walk_ticks=41 foot_after_walk=66, 64, 305 machine_reach=1.5`（**无 `walk_skipped`**）+ 内核自述
`[R4 Session] completed session=machine-walk-0 segments=8 ticks=40 finalFoot=66, 64, 305` +
`(29/29) ticks=3116 → PASS`（`:3924`）⇒ 单机闭环在**够不着**的起点也能自己到站）；**第十二轮（夹具换薄壳后）无回归**：`machine_cycle=PASS ticks=251` 与第十一轮**逐字相同** |
| **K-3 安全点停止** | `alice:k3_stop_check` 右键（DEFER）/ **Shift+右键**（FORCED） | `deferred=1` / `forcedUnsafe=1`；terminal `cancelled:k3_defer:safe_point` / `cancelled:k3_forced:forced_unsafe` | `WINDOWS_CLIENT`（两种模式均实测通过） |
| 道具资源完整性 | `./tools/check-item-models.sh`（构建前） | `RESULT PASS`（空模型/坏 JSON/死贴图） | 脚本自测通过（D-170） |

## 当前矩阵

> **2026-09-16 全表复核注记（D-262）**：**顶部「当前架构阶段说明」表才是权威等级**，下方历史行的「待测」
> 字样**不可信**。本轮把剩下的 **22 行「待测」逐条核证**：16 行有证据 ⇒ 已改判；5 行**仍然待测**
> （105/107 Baritone 对照 = 全表唯一真验证债 V-4、142 `[Job] launch` 从未出现、207 `/alice auto-mine`、
> 217 `alice:restore_check`）；1 行机制已删（211）。
>
> 复核中发现 4 处**自相矛盾**（已按证据改判，留此备查）：
> ① `:138`（D-141 请示卡片）与顶部表「决策层 6 步 = `WINDOWS_CLIENT`」冲突 —— 请示**界面**从未客户端验，
>    D-142 改成聊天按钮+Y/N 之后**仍待客户端**；
> ② `:142`（`[Job] launch`）与顶部表「电池 `(30/30)` PASS」冲突 —— 电池 `lumber_job` 直接 `new LumberJob`
>    （`RegressionBatteryTask.java:312`），**从不经 `JobLauncher`** ⇒ `[Job] launch` 至今 0 条；
> ③ `:211`/`:215` 曾期望 `/alice ledger → pending=0`，但 `:209` 实测 `pending=5`、且 D-103 已删「收尾自动追加恢复」
>    ⇒ 该期望**不可能成立**（已改判为「机制已删」/「由显式恢复路径达成」）；
> ④ `:136/137/139/141` 的「电池内 PASS」**只在 FULL 成立** —— `permission_gate`/`pickup_gate`/`collect_job`/
>    `recipes_dump` 在 `docs/BATTERY_CURATION.md:73` 记为 **EXTRA**，CORE 30 步不跑它们。

> 说明：下方 Movement 实验 1-6、M1、M2 行是历史验收记录，不再代表当前注册的测试物品或可用入口；新 Pathing Core 重新设计后会建立新的最小入口。

| 能力 | 推荐入口 | 关键操作 | 关键观察 | 当前状态 |
|---|---|---|---|---|
| BotController | Bot 控制器测试物品/现有控制入口 | 手持后右键、Shift+右键，或少量命令辅助 | bot 是否移动、跳跃、停止、是否回弹/不可见 | 以最近 Windows 证据为准 |
| 目标选择器 | `alice:target_selector` | 右键目标方块 | 目标高亮、bot 接收任务、日志阶段 | 已有实现，等级需核对 |
| C1 扫描 | `alice:interface_scanner` | 右键方块 | GUI/聊天中的只读接口事实，无副作用 | Windows 已验收窄范围 |
| 道路规划 | `alice:road_planner` | 两点右键，Shift+右键重置 | 蓝图预览、障碍/流体处理 | 独立能力，按实际记录 |
| 转移选择 | `alice:transfer_endpoint_selector` | Shift+右键 source，右键 destination | 选择反馈、原版箱子 GUI 不被破坏 | 有历史证据，需按当前工件核对 |
| GUI/同步 | 对应测试物品 | 打开、点击、关闭重开 | 服务端改动是否实时显示，客户端是否崩溃 | 每项单独记录 |
| 实体同步 | 对应 bot/控制物品 | 生成并观察 bot | 生成、可见、位置、装备、回弹 | 每次物理改动必须重测 |
| MineTask 场景 A | `alice:mining_scene_tester` + `alice_test:scene_a` | 普通右键启动；重置后重复测试 | 目标/起点高亮、规划站位、挖掘、Bot 复用后位置同步、终态 | `USER_ACCEPTED`；三轮连续完成，证据见 `.alice-supervision/client-tests/minetask-scene-a-20260905/evidence/` |
| MineTask 场景 B | `alice:mining_scene_b_tester` + `alice_test:scene_b` | 生成场景 B 后使用 B 启动器普通右键 | 固定遮挡、有限目标访问清障、无隧道/软移动回退 | `USER_ACCEPTED`；证据见 `.alice-supervision/client-tests/minetask-scene-b-20260905/evidence/` |
| MineTask 场景 C | `alice:mining_scene_c_tester` + `alice_test:scene_c` | 普通右键自动标记中央障碍并启动；Shift+右键清除 | 长直路径动态阻挡、BotMiner 内部恢复、最终终态 | `USER_ACCEPTED`；截图和日志证据见 `.alice-supervision/client-tests/minetask-scene-c-20260905/evidence/` |
| MineTask 不可达安全失败 | 用户布置的不可达目标 + `alice:target_selector` | 选择无法到达的目标 | 初始 `UNREACHABLE` 直接安全失败，不执行 BotMiner、不重规划、不挖隧道；失败事实可见 | `USER_ACCEPTED`；证据见 `.alice-supervision/client-tests/minetask-unreachable-20260906/evidence/` |
| Pathing Core R2-B 单段 Traverse | `/alice pathing traverse <north|south|east|west>` | 选中/生成 Bot 后执行一次方向命令 | 目标仅为当前脚位同高度四向相邻一格；服务端 `[R2-B Traverse]` 记录 started 与 completed/failed，Bot 实际脚位、支撑、onGround 与输入清理可观察 | `USER_ACCEPTED`；固定工件 SHA-256 `fc0ad051208422882b5d8c1060bb4afe822552a1ef753ac5e0964b8e09e0fbbe`；证据见 `.alice-supervision/client-tests/pathing-core-r2b-traverse-20260907/evidence/` |
| Pathing Core R2-C Diagonal | `/alice pathing diagonal <northeast|northwest|southeast|southwest>` | 选中/生成 Bot 后执行一次对角方向命令 | 目标仅为同高度对角一格；两侧方块通行性检查防对角穿墙；`[R2-C Diagonal]` 记录 started/completed 与 actualFoot/support/onGround/controllerActive | `USER_ACCEPTED`；4/4 方向 8 ticks 完成，actualFoot 全部一致；验收工件 SHA-256 `804c5eb897a5337a07ff11286332805b1f2a4227d41bf1cdd4e5622982ed98cf`；证据见 `.alice-supervision/client-tests/pathing-core-r2c-movements-20260907/evidence/` |
| Pathing Core R2-C Ascend | `/alice pathing ascend <north|south|east|west>` | 选中/生成 Bot 后在一级台阶旁执行命令 | dy=+1 相邻一格；利用原版自动踩台阶（无跳跃）；头部空间不足时 `ASCEND_INVALID_PRECONDITION` 拒绝；SETTLING 验证落地 | `USER_ACCEPTED`；移除跳跃后完成良好，偶发上层水平位置偏差致 settling 超时（已知，暂不处理）；同一验收工件；证据同上 |
| Pathing Core R2-C Descend | `/alice pathing descend <north|south|east|west>` | 选中/生成 Bot 后在一格落差边缘执行命令 | dy=-1 相邻一格；`blockPosition().getY()` 检测真实下降（修复 getY() 恒真 bug）；SETTLING 验证落地；回收等级 LOCAL_STEP | `USER_ACCEPTED`；下降动作 4/4 成立，1/4 精确落点 completed，3/4 因自动踩台阶过冲报 settling 超时（用户裁定暂不处理）；同一验收工件；根因探针日志已归档并回收 |
| Movement 实验 1 平地物理 | `alice:bot_remote_control` | Bot 平地前进后停止 | `aiStep` 后位移、停止稳定、无明显双倍推进 | `USER_ACCEPTED`；证据见 `.alice-supervision/client-tests/movement-physics-experiment-1-20260906/evidence/` |
| Movement 实验 2 单个 WalkMovement | `alice:movement_walk_tester` | 右键无遮挡平地方块 | 单个 Movement 完成、实际脚位一致、输入清理 | `USER_ACCEPTED`；证据见 `.alice-supervision/client-tests/movement-physics-experiment-2-20260906/evidence/` |
| Movement 实验 3 连续 WalkMovement | `alice:movement_sequence_walk_tester` | 右键距离 Bot 至少 6 格的无遮挡平地方块 | 三段按序完成、段间无异常、实际脚位一致、最终停止 | `USER_ACCEPTED`；修复字段遮蔽后 Windows 日志确认连续三次 `COMPLETED`，实际脚位一致且无新异常；历史证据见 `.alice-supervision/client-tests/movement-physics-experiment-3-20260906/evidence/`；高差/跳跃按边界拒绝 |
| Movement 实验 4 台阶/下降 | `alice:movement_vertical_walk_tester` | 普通右键测试上一级；Shift+右键测试下降一级 | 记录目标/实际脚位、Y、支撑方块、onGround；区分自然通过、阻挡和安全拒绝 | `USER_ACCEPTED`；一级上升/下降均通过，证据见 `.alice-supervision/client-tests/movement-physics-experiment-4-20260906/evidence/`；下降方向标签有诊断瑕疵 |
| **串联回归（常规入口）** | `alice:pathing_regression` + `/function alice_test:pathing_course` | 普通右键一次 | **14 项**依次自动建地形并断言（执行 8 / 拒绝 2 / 岩浆路线安全 1 / 路线偏好 1 / 重规划 1 / 重同步 1）；日志 `[Regression] SUMMARY ...=PASS/FAIL coverage=PASS executed=...`（D-061 覆盖断言） | `WINDOWS_CLIENT`（2026-09-09：12 项中 11 项 PASS，`lava_course` 按 D-057 改断言后 PASS，12/12）；D-058 新增 `fall_course` 后待复测 |
| Pathing Core R3 自检电池 | `alice:pathing_battery` + `/function alice_test:pathing_course` | 生成场景后普通右键一次跑完 8 项 | `SUMMARY` 全 PASS（含 `plan_budget=SEARCH_LIMIT`） | `USER_ACCEPTED`；8/8 PASS；证据见 `.alice-supervision/client-tests/pathing-r3-battery-20260908/` |
| Pathing Core R4 会话 | `alice:pathing_session` | 普通右键 | `plan→session` 多段执行、COLUMN→EXACT 容差、结构化终态 | `USER_ACCEPTED`；`status=COMPLETED segments=2/2`；证据见 `.alice-supervision/client-tests/pathing-r4-session-20260908/` |
| R4 自愈（扰动） | `alice:pathing_disturber` + `/function alice_test:place_course` | 普通右键（第 30 tick 起把 bot 平移 1 格） | bot 被位移后自行恢复、任务有终态、不空跳；**声明了夹具就必须看到 `[R4 Fixture] disturbed`**，否则结果行带 `/FIXTURE_NOT_FIRED=[disturb…]` 且任务 `FAILED`（D-220：不静默降级） | `WINDOWS_CLIENT`（用户 2026-09-09 通过）；修复前曾空跳 17 次不终止（D-035）。**✅ 2026-09-15 负例分支也已实测（jar `35fa4580…`）**：`r4_negative_disturb` 场景 ⇒ `not_fired missing=[disturb]` + `FAILED code=failed:FIXTURE_NOT_FIRED:[disturb]`、`disturbed` 计数 0。**2026-09-15 补测（第十九轮，jar `462b10b5…`）**：`[R4 Fixture] disturbed from=4,64,66 to=4,64,67 tick=33` + `result … COMPLETED segments=8/8 ticks=69 finalFoot=8,62,66 replans=0`、`FIXTURE_NOT_FIRED` 命中 0 ⇒ 物品侧新接线（夹具必动手）走的是**正常分支**，升 `WINDOWS_CLIENT`。**⚠️ 用户观察"看不出来什么问题"是预期**：1 格横向位移被控制器走回目标格吸收（无 resync/replan）⇒ 见台账 §5.6 与评审 §6.1 的 D-035 对齐议题。**负例分支（夹具未生效 ⇒ FAILED）仍未跑**（需"注定放不下"的场景） |
| R5 破坏通行 | `alice:pathing_breaker` + `/function alice_test:break_course` | 普通右键 | 破坏进度/工具选择/直连 2 格语义、终态 | `USER_ACCEPTED`；证据见 `.alice-supervision/client-tests/pathing-r5-break-20260909/` |
| R5 放置台阶 | `alice:pathing_placer` + `/function alice_test:place_course` | 普通右键 | 缺支撑时放置台阶后通行、终态 | `USER_ACCEPTED`；证据见 `.alice-supervision/client-tests/pathing-r5-place-20260909/` |
| Baritone 对照（连续行动轨迹） | `/function alice_test:contrast_trace_pathing` + `#goto 0 62 44` | 一键准备并开始记录；立刻输入 goto | `latest.log` 每 tick 一行 `[TRACE]`（位置/速度/落地/偏航）；看水平速度是否在方块边界归零 | 后续准备，暂不测试 |
| 任务层重规划（D-043） | `alice:pathing_waller` + `/function alice_test:place_course` | 普通右键（第 30 tick 在计划前方封路） | 日志 `[R4 Fixture] wall_placed` → `[PathRetry] replan` → 会话 `COMPLETED replans=1`；任务终态 COMPLETED。**必须看到 `wall_placed`**，否则结果行带 `/FIXTURE_NOT_FIRED=[wall]` 且任务 `FAILED`（D-220） | `WINDOWS_CLIENT`（用户 2026-09-09 通过）；修复前曾空跳 17 次不终止（D-035）。**✅ 2026-09-15 补测（第十九轮，jar `462b10b5…`）**：`[R4 Fixture] wall_placed at=5,64,66 tick=30 pathIndex=3/9` + `[PathRetry] replan reason=BLOCKED code=SEGMENT_FUTURE_BLOCKED` → 新计划首段 `BREAK_AND_TRAVERSE`（**用户观察"挖掉石头"与日志逐字对上**）→ `result … COMPLETED segments=3/3 replans=1`；`FIXTURE_NOT_FIRED` 命中 0 ⇒ 物品侧新接线走**正常分支**，升 `WINDOWS_CLIENT`。目标格算法已与电池侧**共用** `FixtureScript`（`footCell` + 前方第 2 格）。**⚠️ 负例分支（夹具未生效 ⇒ FAILED）仍未跑**（需"注定放不下"的场景） |
| 自愈场景（**电池侧**：封路 / 平移） | `alice:regression_battery`（CORE 的 `pathing` 步内场景 `place_course+wall` / `place_course+disturb`） | 一次右键跑全量电池 | `wall_placed … sceneTick=30 pathIndex=3/9` + `replans=1`（封路）；`disturbed … sceneTick=32`（平移）；**声明了夹具就必须真的动手**，否则该场景 `FIXTURE_NOT_FIRED` | `WINDOWS_CLIENT`（2026-09-15 第十九轮，jar `462b10b5…`：客户端右键一次 **`(30/30) ticks=3337 → PASS`**，`wall_placed at=5,64,66 sceneTick=30 pathIndex=3/9`、`disturbed sceneTick=32`、`coverage=PASS`、`FIXTURE_NOT_FIRED` 命中 **0**；**pathing 步 54 行场景日志与无头逐字相同**；无头侧 `core` 累计 **10/10** 全绿。见 `reviews/2026-09-15-夹具时机基准与DIAGONAL覆盖.md`。⚠️ 修前这两个场景的 `30` **从未生效**，实际是"第一个执行 tick"就动手） |
| 垂直上升 PILLAR（D-055） | `alice:pathing_pillar` + `/function alice_test:pillar_course` | 普通右键 | `[Pillar] SUMMARY pillar_plan=PASS resource_guard=PASS pillar_execute=PASS detail=REACHED/first=PILLAR/pillars=3`（3 格深 1×1 基岩竖井；规划器选 2×PILLAR+1×ASCEND cost=11.68；清空快捷栏方块后应不可规划） | `WINDOWS_CLIENT`（2026-09-09：三项全 PASS，两次真实放置） |
| COLUMN 完成判定（D-056） | 任意执行场景（PILLAR/ASCEND/DESCEND/DOWNWARD/PLACE/BREAK 全量） | 一次串联回归 | 所有 `segment_done` 都必须发生在 bot **落地后**（日志 `ticks=` 不再出现 4 tick 假成功；PILLAR 必先出现 `[Pillar] placed`） | `WINDOWS_CLIENT`（2026-09-09：12 项回归除 lava 外全 PASS） |
| 风险开关（D-059） | `/alice risk` / `/alice risk descend_overshoot on|off` | 一条命令 | 默认 `descend_overshoot=false`（Baritone 原样，不检查过冲列）；打开后 DESCEND 恢复过冲列校验，日志 `[Risk] switch ... all=...` | `WINDOWS_CLIENT`（2026-09-09：默认关闭下回归 13/13；开关未切换） |
| 回归覆盖断言（D-061） | `alice:pathing_regression` | 普通右键一次 | 每个执行场景必须执行到其声明的 Movement（detail 带 `/route=`），且全局 9 种可执行 Movement 至少各执行一次；缺失即 FAIL | **`WINDOWS_CLIENT`**（2026-09-16 复核：本文件 `:91` 已有 `coverage=PASS`（9 种 Movement 全到）⇒ 原「待测」作废） |
| 挖掘动作/收集重写（D-071/D-072/D-073） | `/function alice_test:scene_a` + 右键 `alice:mining_scene_tester`；伐木物品应提示已禁用 | 右键 | `[MineRunner] walk_start/break_start/done`；挖完后 `[CollectDrops] SUMMARY collected=1/1 ... mismatch=0`（来源由破坏事件配对，日志 `作用域捕捉掉落物: .. source=4,64,4`；D-076 起 `collected` 为**物品个数**、收集为**簇级** + 守恒交叉校验 → 需复测确认）；`alice:auto_lumberer`/`alice:lumber_planner` 只输出"已禁用" | `WINDOWS_CLIENT`（2026-09-09 21:19 复测：`collected=1/1 entities=1/1 clusters=1 mismatch=0 ticks=14`，`MineTask` COMPLETED 37 tick，簇级收集口径） |
| 挖掘站位两模式（D-070） | `alice:mine_course_runner` + `/function alice_test:mine_course` | 普通右键 | `[MineCourse] SUMMARY free=PASS wall=PASS blocked=PASS headroom=PASS buried=PASS`（free/wall/headroom=模式 A；blocked=模式 TUNNEL；buried=found_but_unminable 或 TUNNEL） | `WINDOWS_CLIENT`（2026-09-09：5/5 PASS，TUNNEL 与 found_but_unminable 均按预期） |
| 挖掘站位选优批次 2（D-069） | `/function alice_test:scene_a` + 右键 `alice:mining_scene_tester`；`/alice mining estimate lower_bound\|dijkstra` | 右键 + 命令 | 日志 `[MiningPlanner] target=.. candidates=N estimate=.. nodes=.. ms=.. planned=.. chosen=.. cost=.. pathSize=..`；两种估算方案可切换且都能挖到；不再出现 legacy `SurfacePathfinder` 日志 | `WINDOWS_CLIENT`（2026-09-09：两种估算均 REACHED；回归 14/14 + coverage PASS） |
| Mine 迁移批次 1（D-064） | `alice:mining_scene_tester` + `/function alice_test:scene_a`（或 `alice:target_selector` 右键方块） | 右键 | `[MiningPlanner探针] planned ... pathStatus=REACHED` → `[BotMiner探针] 站位移动启动` → `挖掘完成: target=4, 64, 4`；无 legacy `PathExecutor`/`SurfacePathfinder` 日志；**站位不得是 `target.above()`、face 不得为 down**（D-065） | 待复测（D-064 + D-065） |
| Place 迁移（D-063） | `alice:target_selector`（Shift+右键方块侧面）+ `/function alice_test:place_target_course` | Shift+右键 | 日志 `[PlaceTask] walk_to_stand ...` → `[PlaceTask] completed target=.. stand=.. feet=..`；目标格出现方块；无库存/无支撑面时 `place_resource_unavailable` / `place_no_valid_face` | `WINDOWS_CLIENT`（2026-09-09：4/4 COMPLETED） |
| Follow 迁移（D-062） | `alice:follow_runner` + `/function alice_test:follow_course` | 右键启动 → 原地跳 → 走过墙 → 再右键结束 | 无 `follow_*` 失败码；日志 `[Follow] replan reason=...`、跳跃期间 `[Follow] target_airborne hold_goal=...`、结束时 `[Follow] SUMMARY stopped ticks=.. replans=.. minDist=..`（缺陷回归点：原地跳不得触发 `follow_target_not_on_safe_surface`/`follow_no_path`） | `WINDOWS_CLIENT`（2026-09-09：跟随正常、跳跃无失败） |
| WalkTo 迁移（D-060） | `alice:walk_to_runner` + `/function alice_test:walk_course` | 普通右键 | `[WalkTo] SUMMARY walk_flat=PASS walk_over_wall=PASS walk_unreachable=PASS walk_unsafe=PASS`（新内核规划+执行；失败码保持 legacy 语义，SEARCH_LIMIT ≠ UNREACHABLE） | `WINDOWS_CLIENT`（2026-09-09：4/4 PASS） |
| BREAK_AND_ENTER（D-068） | `alice:pathing_break_enter` + `/function alice_test:break_enter_course` | 普通右键 | `[BreakEnter] SUMMARY plan_a=PASS plan_b=PASS execute_a=PASS`（目标列 2 格高/1 格高两种；首步必须是 BREAK_AND_ENTER；实跑后脚位落在柱内） | `WINDOWS_CLIENT`（2026-09-09：三项全 PASS；回归待复跑确认 coverage） |
| 落差 FALL（D-058） | `alice:pathing_fall` + `/function alice_test:fall_course` | 普通右键 | `[Fall] SUMMARY fall_plan_2=PASS fall_plan_3=PASS no_deep_fall=PASS fall_recover_guard=PASS fall_execute=PASS`（2/3 格落差可规划且首步 FALL；4 格落差不得生成 FALL 且路线无 ≥4 格单段下落；不可回收落点不得生成 FALL，可回收落点必须能生成） | `WINDOWS_CLIENT`（2026-09-09：五项全 PASS；回归 13/13） |
| Baritone 对照（FALL）**⚠️ 半收口（2026-09-17，D-280）**：Alice 侧已**无头可取**（`single:fall_execute` ⇒ `exec_ticks=17`）；Baritone 侧需用**新停表**重跑后填表（旧数字含打字时间，作废）；读数口径见 `BARITONE_CONTRAST_TESTING.md` §6-6 | `/function alice_test:contrast_fall`（或 `contrast_trace_fall`） + `#goto 23 61 68` | 一键准备后立刻输入 goto |
| 岩浆路线安全（D-057） | `alice:pathing_lava_guard` + `/function alice_test:lava_course` | 普通右键 | `[LavaGuard] status=... lava_contacts=0 result=PASS`（允许 REACHED：抬高一格 + 岩浆上方搭桥 + 落回平台 B，全程不进入岩浆格） | `WINDOWS_CLIENT`（2026-09-09：`lava_contacts=0 result=PASS`，回归 12/12） |
| Baritone 对照（PILLAR）**⚠️ 半收口（2026-09-17，D-280）**：Alice 侧 `single:pillar_execute` ⇒ `exec_ticks=36`；Baritone 侧待用新停表重跑 | `/function alice_test:contrast_pillar`（或 `contrast_trace_pillar`） + `#goto 25 67 44` | 一键准备后立刻输入 goto |
| 垂直下落（D-048/D-050） | `alice:pathing_downward` + `/function alice_test:vertical_course` | 普通右键 | `[Vertical] SUMMARY downward_execute=PASS shaft_plan=REACHED`（Baritone 原样语义：破坏脚下掉 1 格；竖井可规划） | **`WINDOWS_CLIENT`**（`R4_BARITONE_ALIGNMENT_AUDIT.md:252`：`downward_execute=PASS shaft_plan=REACHED`） |
| 顿挫对比轨迹（D-049） | `/alice trace`（开关）+ `alice:pathing_session` | 开→跑场景→关 | `latest.log` 每 tick 一行 `[TRACE]`；已实测（D-051：Alice 每格停顿 vs Baritone 全程 0.28 连续） | `WINDOWS_CLIENT` |
| 站立判定（A+B 对齐 D-041） | `alice:pathing_fence_guard` + `/function alice_test:fence_course` | 普通右键 | 聊天/日志 `status=UNREACHABLE`（栅栏不可站、不可穿）；`REACHED` 即缺陷 | `WINDOWS_CLIENT`：`status=UNREACHABLE` → PASS（2026-09-09） |
| 路线偏好（Q7 验收 D-040） | `alice:pathing_dip_route` + `/function alice_test:dip_course` | 普通右键 | 基线（标定前，客户端实证）`first=DESCEND movements=3 cost=4.41`（≈34 tick）；标定后应为 `first=TRAVERSE movements=4 cost=4.66`（≈28 tick） | `WINDOWS_CLIENT`：标定后 `first=TRAVERSE movements=4 cost=4.66` → PASS（2026-09-09） |
| 流体屏障（内核对齐 D-037） | `alice:pathing_fluid_guard` + `/function alice_test:fluid_course`（充水墙）或 `alice_test:lava_course`（封闭岩浆池） | 普通右键 | 聊天/日志 `status=UNREACHABLE`（拒绝把含流体方块当可挖阻挡物）；`REACHED` 即缺陷；岩浆场景另看 bot 是否朝岩浆移动 | **`WINDOWS_CLIENT`**（`R4_BARITONE_ALIGNMENT_AUDIT.md:243`：`fluid_course`+`lava_course` 通过） |
| Movement 实验 5 场景 D 动态阻挡 | `/function alice_test:movement_d` + `alice:movement_dynamic_block_tester` | 普通右键启动；行走中途自动在 `(5,64,20)` 放置封闭横向石墙；Shift+右键清理 | 验证动态障碍、实际停留位置、失败语义和输入清理；不自动重规划 | `USER_ACCEPTED`：Bot 停在墙前，`FAILED/movement_timeout_WalkMovement`，超时分支记录 `controller_stop_movement`；独立 `BLOCKED_DYNAMIC` 分类码尚未实现；证据见 `.alice-supervision/client-tests/movement-physics-experiment-5-20260906/evidence/` |
| Movement 实验 6A 动态阻挡分类 | `/function alice_test:movement_e` + `alice:movement_block_reason_tester` | 执行中在 E 场景 x=5 放置封闭横向方块屏障 | 仅有新方块碰撞体与 Bot 碰撞箱相交且无进展时报告 `BLOCKED_DYNAMIC`；记录障碍/阶段/noProgress 并清理输入 | `USER_ACCEPTED`：`FAILED/movement_blocked_dynamic_WalkMovement`，`failureCode=BLOCKED_DYNAMIC`，`failurePhase=EXECUTING_MOVEMENT`，实际脚位 `4,64,40`，`onGround=true`，证据见 `.alice-supervision/client-tests/movement-physics-experiment-6a-20260906/evidence/` |
| Movement 实验 6B 无动态障碍超时对照 | `alice:movement_timeout_diagnostic_tester` | 右键启动不修改世界的诊断 Movement，持续 RUNNING 至超时 | 无动态障碍证据时保持 `MOVEMENT_TIMEOUT`，结构化报告记录 `dynamicObstacleEvidence=NONE`，并清理输入 | `USER_ACCEPTED`：至少两次运行一致，`failureCode=MOVEMENT_TIMEOUT`，`noProgressTicks=101`，证据见 `.alice-supervision/client-tests/movement-physics-experiment-6b-20260906/evidence/` |
| Movement 实验 6C 启动前置条件 | `/function alice_test:movement_f` + `alice:movement_precondition_tester` | 普通右键在 Bot 头部放置测试方块后立即检查；Shift+右键清理 | 起点头部空间被占用时直接 `INVALID_PRECONDITION`，不进入 Movement 执行或超时；清理输入 | `USER_ACCEPTED`：多次约 1 tick 直接 `INVALID_PRECONDITION/PRECONDITION`，无 Movement 执行或超时；截图中 Bot 位于头部障碍处属于夹具可见现象，外层另有窒息中断记录；证据见 `.alice-supervision/client-tests/movement-physics-experiment-6c-20260906/evidence/`；工件 `759ffcaba21f99a987ad87494d512edce11742abab5b849891071427cf7f1cd2` |
| Movement 实验 6 结果契约对照 | 场景 E / 6B 诊断入口 / 场景 F | 对照动态方块阻挡、无明确障碍的普通超时、启动前非法前置条件 | 服务端按证据区分 `BLOCKED_DYNAMIC` / `MOVEMENT_TIMEOUT` / `INVALID_PRECONDITION`；三者均清理输入，不自动重规划 | `USER_ACCEPTED`；6A、6B、6C 均已验收 |

| Movement MVP M1 计划编译 | `alice:movement_plan_tester` | 右键无遮挡平面目标方块，编译 Bot 当前脚位到目标脚位的相邻路径 | `MovementPlanCompiler` 接受同高度四向相邻段，执行完成且实际脚位一致；非相邻/高差/障碍路径显式拒绝 | `USER_ACCEPTED`；修复构造器校验后 Windows 日志确认 3 次 `compiled`/`COMPLETED`，段数 8/20/24，实际脚位均与目标一致；证据为 Windows `latest.log` 2026-09-06 18:19:42-18:20:01 |
| Movement MVP M2 BotMiner 后端 | `alice:movement_plan_mining_tester` | 右键无遮挡目标方块；Bot 从当前脚位编译到合法挖掘站位 | MovementPlan 由 BotMiner 消费，MovementPathExecutor 完成后才开始挖掘；移动失败不提前挖掘、不回退旧 PathExecutor | `USER_ACCEPTED`；Windows 日志 21:09:40-21:09:42 确认 5 段 Movement 从 `-5,64,-5` 到 `-4,64,-1` 完成，实际脚位一致，随后开始并完成挖掘；任务终态 `COMPLETED`、durationTicks=35；工件 SHA-256 `deaf45944fcb9270eb62186c357bf88c312ec417addb2bd9a10a0a092faf946d` |

| 模组兼容：连锁挖掘掉落捕获（Ore Excavation 1.13.174） | `alice:chain_test_runner` + `/function alice_test:chain_course` | 普通右键 | `[ChainMine] SUMMARY chain_mod=present settings(..) mined=N broken=N drops=1 collected=N inventory_gain=N chain=PASS collect=PASS`（`drops` 应为 **1**：模组把整簇掉落物聚合生成在同一格；被取消的逐格生成不得登记，见 D-075 修正）（连锁真的发生：`mined>=2`；掉落物被捕获并收进背包：`collected>0` 或 `inventory_gain>0`；`broken` 应与 `mined` 相等——不等说明 `BlockEvent.BreakEvent` 未触发或作用域未生效） | `WINDOWS_CLIENT`（2026-09-09 21:06：`drops=1 collected=9/9 clusters=1 mismatch=0 ticks=8`，任务 COMPLETED 32 tick；修复前为 `drops=10 collected=1/10 ticks=220`；策略见 D-075） |

| 连锁挖掘生产开关（D-077） | `/function alice_test:chain_mine_course` + `alice:target_selector` | ① `/alice chain off` → 右键中间铁矿石；② `/alice chain auto` → 重跑场景 → 右键同一块 | off：**不出现** `[ChainMine] prod_*`，只挖 1 格（`collected=1/1`）；auto：`prod_armed → prod_trigger → prod_done`，整条 3x3 脉被连锁挖掉（`mined≈8 broken=9`），`[CollectDrops] SUMMARY collected=9/9 mismatch=0`，任务 `COMPLETED` | `WINDOWS_CLIENT`（2026-09-09 22:34：`off` 时无 `prod_*`，单格 `collected=1/1 mismatch=0 ticks=12`；`force` 已于 22:20 验证 `collected=9/9`；`auto` 未单独跑——与 `force` 同路径，仅多一道矿石/原木白名单） |

| 挖掘专项串联回归（批次 5，D-078） | `/function alice_test:mine_regression_course` + `alice:mine_regression` | 普通右键 | `[MineRegression] SUMMARY free=PASS wall=PASS blocked=PASS headroom=PASS buried=PASS floating_plan=PASS exec_direct=PASS exec_blocked=PASS exec_floating=PASS exec_chain=PASS`；执行项细节行含 `collected=n/期望 inventoryDelta=n dropsLeft=0` | `WINDOWS_CLIENT`（2026-09-09 23:13：**10/10 PASS**、`ticks=117`、任务 COMPLETED；`floating_plan support=23,64,190`、`exec_floating supportPlaced=true inventoryDelta=0`） |

| L3 伐木 Job（D-080，切片 J1，**真树夹具**） | `/function alice_test:lumber_course` + `alice:lumber_job` | 普通右键 | 夹具由玩家催熟的真树抓取（`tools/capture-scene.py`）；预期由 `tools/analyze-lumber-scene.py` 离线预测：橡树(20,64,208) logs=4/1 可见/2 掏空/**1 需清障**；云杉(28,64,208) logs=7/3/4/0；2x2 高大云杉(22,64,218) 77 原木含 6 硬遮挡→拒绝。期望 `[Job] select picked=tree@20,64,208`、`rejected` 含 `22,64,218:trunk_too_tall(...hard=6)` 与 `28,64,208:not_nearest`；砍伐顺序自下而上（第 2 格走侧面/清障，第 3 格起掏空仰挖）；终态 `result=DONE reason=quota_met`、背包原木增量 = 4、作用域无剩余掉落物 | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-10 20:35：`result=DONE reason=quota_met`、`logs 4/4 trees 1/1 cleared=3 inventoryDelta=4`、`writes breaks=7 unknown=0`、`ticks=354`）。实跑与离线预测一致（清障 3 格：`18,65,207`/`19,65,207`/`19,65,208`；原木 3、4 根零清障走"掏空仰望"）。多轮复跑通过（20:09 / 20:17 / 20:24 / 20:35） |

| 挖掘回归复验（R2b 授权闸门 + D-086 假失败修复后） | 右键 `alice:mine_regression` | 期望**首次即** 10/10 •无 `UNAUTHORIZED_MOVEMENT` •无 `cluster_budget` | `WINDOWS_CLIENT`（2026-09-10 20:34:52 **单次启动**即 10/10 PASS，`ticks=146`；`exec_floating=PASS`、日志无 `reanchor`/`cluster_budget`） |
| 寻路回归复验（R2b 后） | 右键 `alice:pathing_regression` | 14 场景全 PASS，含全部写入型 `BREAK_AND_TRAVERSE`/`DOWNWARD`/`PILLAR`/`PLACE_STEP`/`BREAK_AND_ENTER`；`UNAUTHORIZED_MOVEMENT` 必须为 0 | `WINDOWS_CLIENT`（2026-09-10 20:27:29 全 PASS，`terminal=COMPLETED`；越权检查命中 0） |
| 世界写入授权审计（D-082） | 任意任务跑一次，看 `[WRITE]` 行与终态 `writes … unknown=` | `by=<任务身份>:<REASON>`；`unknown=0`（计数为**会话累计**，跨任务不重置） | `WINDOWS_CLIENT`（2026-09-10：`by=MineRegressionTask:EXPECTED_TARGET` / `SUPPORT_PLACEMENT` / `lumber:LINE_OF_SIGHT`，全程 `unknown=0`） |

| T6 离线可规划性自检（D-125） | 本机 `tools/check-scene-connectivity.py --all`（+ `--selftest`） | 命令行（无需客户端） | 每个原木/矿石目标至少有一个**可达**的合法站位；`expect_gap` 场景必须仍不可达；依赖破坏/放置/加高的场景只报统计不判目标 | `本机自证`（2026-09-12）：`--selftest` 双向断言通过；`--all` 22 场景无硬伤（**聚合 bug 已修**：此前 `--all` 恒报"无"）；判据 = 有合法站位但**全部不可达**（`policy_only` 场景跳过）；盲区："本来就看不见、需清障"的目标只给软提示；对 `a5901f5` 旧版本文件复跑 ⇒ 9 目标里 **8 个判无站位**（含 `19,64,213`）——即在离线复现并拦下 D-117 |
| D-145 S5/P1 只读配方图逆推规划器 | `python3 tools/recipe-graph.py --selftest`（离线）；后续接游戏内导出 | 离线命令 | BOM 聚合展开（需求累加 + ceil + **余料回填** + 二次需求重新入队）、**标签→成员**（优先原始材料）、环有界；未知配方类型如实跳过计数 | **本机自证 `PASS`**（原版数据：木镐 ⇒ 5 木板 ⇒ **2 原木**；库存足够短路；铜锭↔铜粉环有界） |
| D-144 S3.5 收集授权（GRANTED_AREA） | 右键 `alice:pickup_gate_check`（三用例）＋ `alice:collect_grant` 选区 ＋ `/alice grant [always\|clear]` | 零参数右键 + 命令 | 授权 = 水平范围 + 竖直自适应，`ONCE/SESSION/ALWAYS`（ALWAYS 持久化且报告带 ★ 标记）；`DropPolicy.effectiveProvenance` 为唯一判定入口（登记在册 → 授权区 → FOREIGN）；`anyDrops` 退役，收集改为"扫世界 + 按策略过滤"，被拦下数量可观测 | **`WINDOWS_CLIENT` + `USER_ACCEPTED`**（`AI_DECISIONS.md:5062`：`SUMMARY a_picked=4 b_remaining=1 c_picked=4 c_remaining=0 → PASS`） |
| D-143 S3.5 被动拾取闸门 | 右键 `alice:pickup_gate_check`（零参数，含两用例） | 零参数右键 | 掉落物归属 `OURS_DIRECT`（直接配对）/`OURS_INDIRECT`（松窗 60tick/4格）/`FOREIGN`；被动拾取走 `EntityItemPickupEvent` 闸门（只对 bot，不动真人玩家）；策略不允许即拦截并记 `[Pickup] blocked` | **`WINDOWS_CLIENT` + `USER_ACCEPTED`**（`AI_DECISIONS.md:5007`：`SUMMARY a_picked=44 b_remaining=1 → PASS`） |
| D-141 S3b 请示卡片（只做一格界面） | 右键 `alice:permission_demo` ⇒ 屏幕右侧卡片 ⇒ 点击按钮 | 零参数右键 | 服务端推 `PermissionNoticePacket`（发起/撤下）⇒ 客户端一格面板（标题/能力/理由/倒计时/两个按钮）⇒ 点击发 `PermissionAnswerPacket` ⇒ 走同一个 `PermissionGate.answer`（与 `/alice ask` 等价，只有玩家能批准）；点击钩子 `InputEvent.MouseButton.Pre`（无界面也触发，已字节码确认） | **条目已废（2026-09-16 复核）**：D-142 移除了全部鼠标交互代码、改走聊天按钮+Y/N ⇒ 本行不再适用；**后继 D-142 的客户端验证仍待做**（`AI_DECISIONS.md:4959/4961`） |
| D-140 S3 请示层（服务端契约版） | 右键 `alice:permission_demo`（零参数）＋ `/alice ask`、`/alice policy` | 零参数右键 + 命令 | 能力分级 AUTO/NOTIFY/ASK/IGNORE；`ASK` 超时=拒绝并**自动返回**；结论按 bot+能力暂存供轮询取走；`always` 持久化；只有玩家/控制台能批准；`pendingRequests` 进快照与报告 | **`WINDOWS_CLIENT`**（`AI_DECISIONS.md:4894/4913`：S3 两条路径均通过） |
| D-139 S2 候选菜单 + 动作 target | 右键 `alice:goal_director`（零参数，手动触发=诊断模式） | 零参数右键 | 服务端生成有界候选菜单（`tree@…`/`drops@…`/`region:saved`）⇒ 快照带 `menu` ⇒ 动作 `target` **必须命中菜单**（未命中即 `Refused`，不猜坐标）；`lumber`/`collect` 必须带 target，quota 被菜单实际数量夹取 | **`WINDOWS_CLIENT`**（`OPEN_ITEMS_LEDGER.md:488`：S2 选择层已记 `WINDOWS_CLIENT`） |
| D-135 决策层循环（② 第 2 步） | 右键 `alice:goal_director`（零参数） | 零参数右键 | 权威快照 → 严格动作词汇表（start_job/stop_current/report_status/no_op，未知一律拒绝）→ 事件驱动 + 三闸节流 → 只走既定入口执行；LLM 未配置时如实登记并保持确定性策略 | **`WINDOWS_CLIENT`**（`AI_DECISIONS.md:4744`：三条路在游戏内全部通；台账 §6.47） |
| D-134 统一 Job 入口 + 终态契约（② 第 1 步） | 右键 `alice:job_launcher`（自带 `lumber_course` 复位+传送） | 零参数右键 | `JobRequest` → `JobLauncher`（唯一构造点 + 入口发料）→ `BotManager.assignJob`；终态记录新增 `botId` 与 `Job.terminalReason()`（区分"配额达成"与"提前收工"） | **`WINDOWS_CLIENT`（2026-09-17 10:35 用户右键实测）**：`[Job] launch bot=tango kind=LUMBER center=23,64,207 radius=16 quota=2 maxTicks=3600` ✓；终态 `task_execution_terminal … terminalReason=quota_met` ✓；另见 `[Job] 生产入口只搬运不发料（LUMBER）…`（= 台账 R2-残 要的那条）。⚠️ 本次客户端日志里 `kind=LumberJob` 属**旧口径**：`LumberJob` 当时没覆写 `taskName()`；该缺口已于同日补齐（4 个生产 Job 覆写并返回 `NAME`，新门禁 R4 防漂）⇒ 之后应显示 `kind=lumber` |
| S-1 维生出口（D-132 / P1-C；**2026-09-15 修正过一处排除格口径**，见 S-5 行） | 右键 `alice:survival_exit_check`（自动生成 `alice_test:survival_course`） | 零参数右键 | 维生否决后**必须有一次明确出口**：`SurvivalSystem.nearestSafeRefuge`（纯查询）→ `SurvivalExitTask`（复用已验收 WalkTo）；逃生任务豁免二次否决（防每 tick 自杀循环）；找不到落点则**如实登记"无出口"** | **`WINDOWS_CLIENT`**（2026-09-12 14:47/14:58，3 次复现：`[Survival] 逃生出口 refuge=66,64,103` → `[Search] start_escape` → `segment_done ticks=5 actualFoot=66,64,103` → `kind=SurvivalExitTask terminal=COMPLETED`；`hazard=SUFFOCATING` 整会话只出现 1 次=逃生后危险消失） |
| **队列第④项：`progressSummary()` 低频进度事件**（2026-09-17） | 夹具 `MineFailureVisibilityCheckTask` 的 3 条 check（电池步 `mine_failure_visible`，MAIN，无参数） | 期望 `checks=5 failures=0`；关键断言：默认间隔=0（生产关）、70 tick 内 ≥2 次上报、收尾复位为 0 | **`SERVER_TESTED`**：单步 PASS；**反向对照**（上报关死）⇒ FAIL ✓ |生产默认关 ⇒ CORE 行为不变 |
| **队列第③项：`MineJob` 新尝试上场保留上一轮失败事实**（2026-09-17） | 电池步 `mine_failure_visible`（MAIN，场景 `alice_test:ore_course_terrain`，无参数） | 期望 `[MineFailure] SUMMARY checks=2 failures=0`；关键日志 `active=true attemptFailures=2 node.failure=…:target_replaced` | **`SERVER_TESTED`**：电池单步 PASS；**反向对照**（活动分支 failure 传空串）⇒ FAIL ✓ | 只读访问器 `hasActiveAttempt()`/`attemptFailureCount()` |
| **D-264 S-8 收口：删掉零读者的 `policyVersion`**（2026-09-16，用户裁定「删字段」） | 新门禁 `check-kernel-predicates` 的 **S8-P1**（`policyVersion` 不得无声复活） | 无参数（离线门禁 + 编译） | 期望：`grep -rn policyVersion src/main/java` = 0；门禁 PASS | **`SERVER_TESTED`**：`compileJava` 通过、CORE PASS、**反向对照**（把字段加回来 ⇒ S8-P1 红）✓ | 另登记 **S-10**：`PlanningDependency` 分量零读者（未修）|
| **D-262 `AI_TEST_MATRIX.md` 22 行「待测」逐条核证**（2026-09-16） | 只读子代理逐行取证 + 父 agent 抽验 5 条关键证据；矩阵行就地改判 + 顶部写入「4 处自相矛盾」注记 | 无参数（文档级） | 期望：每行判决都能落到既有客户端/决策证据上 | **文档级（非运行验证）**：16 行改判 `WINDOWS_CLIENT`（6 行含 `USER_ACCEPTED`）、5 行仍待测（105/107/142/207/217）、2 行失效、1 行 `SERVER_TESTED`；`check-all.sh` 12 PASS + 1 预期 WARN |
| **D-261 `survival_exit` 火焰判据重建（天生易红 → 确定性）**（2026-09-16） | 火焰阶段：窗口 ≥3 个伤害节拍 + `hurtTime` 上升沿计命中 + 归因判据 | 无参数（既有 `survival_exit` 步） | 期望 `checks=124 failures=0`，`窗口内新命中≥2` | **`SERVER_TESTED`**：单步 PASS ×4（`新命中=4`）；**反向对照**：把窗口改回 21 tick ⇒ 前提判据 + 命中计数双红；CORE 全绿 | ⚠️ 暴露**生产侧盲区**：危险伤害按血量差记录 ⇒ 被回血抹平的伤害不可见（未修，已登记）|
| **D-260 G3/R1-残 收口：连锁破坏预算拒绝必须可上报**（2026-09-16） | 夹具新增用例 `exec_chain_budget_refused`（在既有 `mine_regression` 步内，不新增电池步）：`WriteBudget.setCaps` 把破坏上限压到 1 ⇒ 3×3 矿脉必然触发拒绝 | 无参数（离线） | 期望 `refused=true/terminalReason=chain_budget_refused/chainStopped=true/remainingBreaks=0` | **`SERVER_TESTED`**：`single:mine_regression` PASS；**反向对照**：拆掉 `terminalReason()` ⇒ `mine_regression=FAIL`；CORE 全绿（预算恢复无污染）|
| **D-259 R5-残 收口：菜单写入原语编译期强制**（2026-09-16） | 无新入口：`InventoryCraft.click`／`StationProvision.click` 每个重载强制 `WriteGrant` + `WriteReason.menuWrite()`；5 个调用点交出 `CRAFT_GRID` | 无参数（编译期 + 门禁） | 期望：`check-policy-matrix.sh` ⑨ PASS；CORE 全绿 | **`SERVER_TESTED`**：① 去掉 grant 实参 ⇒ 编译失败；② 重载去掉 `WriteGrant` ⇒ 门禁红；③ CORE `PASS`（craft_* 六步全绿 ⇒ 无误杀）|
| **D-258 审计表全表复核（§1/§3/§4/§5/§6，36 行）**（2026-09-16） | 无新入口：4 个只读子代理逐行核 `文件:行` 证据 + 父 agent 交叉复核；台账行就地改判 | 无参数 | 期望：每行判决都能落到代码符号/电池步上 | **`SERVER_TESTED`（离线文本级）**：14 行「未实现」作废、12 行部分失真、5 行真缺口保持、2 条新发现（菜单合成绕过容器写入预算／`chainRefusedByBudget` 死哨兵）；`check-all.sh` 11 PASS + 1 预期 WARN |
| **D-257 内核审计 §2 收口：K-4 谓词统一（K-3/K-5 是过期行）**（2026-09-16） | 新门禁 `tools/check-kernel-predicates.sh`（挂 `check-all.sh`）：K4-P1 四个执行工厂必须引用规划侧同一谓词；K5-P1 声明了的状态必须有生产者；行为证据 = CORE 仍 `(38/38)` | 无参数（离线门禁）| 期望：谓词漂移=0、死状态=0 | **`SERVER_TESTED`**：两类注入各自让门禁红（删 `canTraverse` 调用 / 删三条状态映射）；CORE `(38/38) ticks=4300 → PASS`（无需客户端测试）|
| **D-256 K-1 收口：best-so-far 前缀的两个「被当成完整计划」漏洞**（2026-09-16） | 既有 BASELINE 步 `partial_search`（零新增电池项）：前缀自洽不变式 + 裁剪助手单测 + 容差决策三分支 + 场景前提三条（`scene_chunks_loaded`/`scene_terrain_built`/`scene_floor_present`）；新场景 `partial_search_terrain`（夹具**自传送**到 (16,64,245)，不再依赖电池前序步骤）| 无参数（离线门禁 + 既有电池步）| 期望：前缀交出且自洽、冲突时裁到冲突之前、前缀尾段用 `COLUMN` | **`SERVER_TESTED`**：`single:partial_search` PASS + **四条反向对照实测能红**（分别翻转 ⇒ FAIL）；CORE `(38/38) ticks=4261 → PASS`（无需客户端测试）|
| **D-255 §5.9 C1：人工确认解除通道**（2026-09-16 用户裁定「甲」） | 既有 BASELINE 步 `transfer`：内存账本判 `resolveManual` 语义（阻塞⇒放开/终态拒绝/`abort` 仍保护）+ **端到端真实命令通道** `resolveChannelCommand`（世界账本 `NOT_MOVED` 探针 + 负向对照「没打 `confirm` 不许解除」）；结构规则 R3 | 运维命令 `/alice transfer-resolve <request> confirm`（**不需要客户端测试**：判据全在离线层）| 期望：阻塞条目 ⇒ `ABORTED`/`resolved_by_operator`、门禁放开、物品零移动 | **`SERVER_TESTED`**：`single:transfer` PASS + 反向对照两类实测能红（翻转 `resolveReleases` ⇒ FAIL；删 `confirm` ⇒ 端到端 FAIL + R3 红）；CORE 见 HANDOVER |
| **D-254 §5.9 收口：挂起传输的唯一时钟 + 门禁接线判据**（2026-09-16） | 既有 BASELINE 步 `transfer` 的 `ledgerPolicies` 夹具（零新增电池项）：用**生产时钟** `TransferLedgerData.clockNow` 判过期语义（新鲜不许误判过期 / 陈旧必须降级 `manual_takeover_required`）+ 门禁纯函数 `refusal(...)` 三条（干净 / 挡住 / 结清）；新门禁 `tools/check-transfer-clock.sh`（挂 `check-all.sh`）判 R1 时钟混用、R2 替换型派活过门禁 | 无参数（离线门禁 + 既有电池步）| 期望：R1/R2 无违例、`transfer` 步 PASS | **`SERVER_TESTED`**：`single:transfer` PASS + **反向对照两类都实测能红**（翻转 `gateRefuses`/`staleDowngraded` ⇒ FAIL；注入 `getTickCount()`/删门禁调用 ⇒ 门禁 FAIL）；CORE `(38/38) ticks=4260 → PASS`（无需客户端测试：判据全在离线层）|
| **D-251 水位切片 B 落地（+ 段间浮着 / 水里失败判定）**（2026-09-16） | 电池步 `pathing` 的 `deep_pond_course` 从**现状登记 `refused`** 改成**执行判据**（纯通行、零写入）；`survival_exit` 的灌水竖坑前提判据改成「计划自洽 + 从坑底起步」；无头 `single:pathing` / `single:survival_exit` / CORE | 无参数；客户端复核入口 `alice:survival_full_check`、`alice:pathing_regression`（各一次右键） | 期望：深水池 `COMPLETED`、灌水逃生 **DONE** 且水里零放置、`自写入冲突` 计数可见 | **`SERVER_TESTED`**：`single:pathing` PASS / `single:survival_exit` PASS / CORE `(38/38) ticks=4229 → PASS`；**`WINDOWS_CLIENT` 待用户复核**（AI 不自行宣布完成）；**`WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-16 20:01–20:03 真人复核**：`alice:survival_full_check` ⇒ `checks=122 failures=0 → PASS`、`alice:pathing_regression` ⇒ 全场景 0 FAIL 且 `terminal=COMPLETED`；用户确认「符合我的预期」）** |
| **D-250 计划自我写入自洽性（②′）**（2026-09-16） | 无新入口：挂在既有 `pathing` / `survival_exit` 步与 CORE 上；新增**电池硬自断言** `selfwrite_unresolved == 0`（摘要里带 `自写入冲突=N`） | 无参数 | 规划期告警行 `[SelfWrite] 计划不自洽…禁掉那条边重搜` / `[SelfWrite] 重搜 N 次后计划自洽`；`PathingStats` 累计 `selfwrite_conflict`、`selfwrite_unresolved` | **`SERVER_TESTED`**：`single:pathing` PASS、`single:survival_exit` PASS、CORE `(38/38)` PASS（`自写入冲突=0` ⇒ 对今天**零影响**）；切片 B 实验里守卫**按预期开火并修好**（D-250 表） ；**`WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-16 20:01–20:03 真人复核**：`alice:survival_full_check` ⇒ `checks=122 failures=0 → PASS`、`alice:pathing_regression` ⇒ 全场景 0 FAIL 且 `terminal=COMPLETED`；用户确认「符合我的预期」）** |
| **D-248 水位切片 B：勘查完成、规划那半回退**（2026-09-16） | 电池步 `pathing` 新增场景 `deep_pond_course`（3 格深水池，两侧到顶石墙 ⇒ 绕不过去）；无头 `single:pathing` / `single:survival_exit` / CORE | 无参数 | **现状登记**：`refused("deep_pond_course")`（岸上出发 ⇒ 纯通行过不去深水）、`refused("deep_pond_course+floor")`（池底出发 ⇒ `UNREACHABLE`）⇒ 谁把"游过去"做出来，这两条变红提醒改文档 | **`SERVER_TESTED`（回退态）**：`single:pathing` PASS / `single:survival_exit` PASS / CORE `(38/38)` PASS；⚠️ 水位例外那半在 CORE 里引出**未解释回归**（D-248 有探针证据与三个候选解释）⇒ 已回退 ；**`WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-16 20:01–20:03 真人复核**：`alice:survival_full_check` ⇒ `checks=122 failures=0 → PASS`、`alice:pathing_regression` ⇒ 全场景 0 FAIL 且 `terminal=COMPLETED`；用户确认「符合我的预期」）** |
| **D-247 水位切片 A：蹚水 + 水速成本**（2026-09-16） | 电池步 `pathing` 新增场景 `water_course`（执行）与 `water_course+cost`（只规划，复用同一地形）；无头 `single:pathing` | 无参数 | ① 水沟（1 格深、**横跨全场** ⇒ 绕不过去）纯通行 **COMPLETED** + 零写入（⇒ 蹚水可行、不靠搭桥）；② 计划成本必须把水里步子按 `WATER_TRAVERSE_MULTIPLIER=7.25` 计价（期望值**从计划自身推导**：陆地步×1.0 + 水步×独立来源 7.25，实际 ≥ 期望×0.9） | **`SERVER_TESTED`**（`single:pathing` PASS；实测 `cost=17.50 / expected=17.50`；**反向对照**：常量改 1.0 ⇒ 恰好 `water_course+cost` 红（`cost=5.00/expected=17.50`）而执行那条仍绿；CORE `(38/38)` PASS）；**`WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-16 20:01–20:03 真人复核**：`alice:survival_full_check` ⇒ `checks=122 failures=0 → PASS`、`alice:pathing_regression` ⇒ 全场景 0 FAIL 且 `terminal=COMPLETED`；用户确认「符合我的预期」）** |
| **D-245 逃生放置不自动回收（回收时机交玩家）**（2026-09-16 用户裁定） | 电池步 `survival_exit`（BASELINE）相位 `SHAFT_ESCAPE` 扩判据；无头 `single:survival_exit` | 无参数 | **负向门禁**：干燥竖坑逃生结束后① 垫脚方块**仍在世界里**（`SHAFT_PIT_BOTTOM` = `Cobblestone`）；② 账本里仍是 `TEMP` 待拆（`WorldModLedger.at`）。⇒ 谁把自动回收接进逃生路径，这两条立刻红 | **`SERVER_TESTED`**（`checks=120 failures=0 → PASS`；**反向对照已做**：两条断言取反 ⇒ 恰好这两条变红，失败信息带实测值）；**`WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-16 20:01–20:03 真人复核**：`alice:survival_full_check` ⇒ `checks=122 failures=0 → PASS`、`alice:pathing_regression` ⇒ 全场景 0 FAIL 且 `terminal=COMPLETED`；用户确认「符合我的预期」）** |
| **D-243 水里垂直移动（丙的第一个子集）**（2026-09-16） | 电池步 `survival_exit`（BASELINE）相位 `FLOODED_SHAFT`（tripwire 已翻成正断言）；CORE 覆盖陆地 PILLAR/ASCEND 回归 | 无参数 | 灌水竖坑：逃生任务到 `DONE` + bot 脚位 y ≥ 101 且 `inWater=false`（PILLAR 水里上浮 + 放置；ASCEND 纯输入） | **`SERVER_TESTED`**（`checks=112 failures=0 → PASS`；反向证据 = D-242 那一轮同判据 FAILED + `wastedJumpLandings`）；**`WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-16 20:01–20:03 真人复核**：`alice:survival_full_check` ⇒ `checks=122 failures=0 → PASS`、`alice:pathing_regression` ⇒ 全场景 0 FAIL 且 `terminal=COMPLETED`；用户确认「符合我的预期」）** |
| **D-244 水柱省料分支（起点/目的地都是水 ⇒ 上浮不放方块）**（2026-09-16） | 电池步 `survival_exit`（BASELINE）相位 `FLOODED_SHAFT` 扩判据；CORE 覆盖**干地** PILLAR/ASCEND 回归 | 无参数（`alice:survival_full_check` 一次右键跑到同一相位） | ① 前提自证三条：坑里 2 格都是水 / 水上那格不是水 / 计划第一段是水柱 `PILLAR`；② **主判据：脚位在水里时一次都没放方块**（逐 tick 取 `WriteBudget.places` 增量 + 按当时脚位是否在水里归因）；③ **干地反向对照**：2 格深干燥竖坑仍然靠放置上来（`放置≥1`）⇒ 省料分支没串到陆地；④ 端到端照旧 `DONE` + 脚位 y≥101 + `inWater=false` | **`SERVER_TESTED`**（`checks=118 failures=0 → PASS`；**反向证据 = D-242/D-243 那一轮**：水里那一段会 `[Pillar] placed`、该判据变红）。实测比预期更强：整段逃生**零放置**（`PILLAR` 水柱上浮 11 tick + `ASCEND` 从水里跳上干地板 14 tick）；**`WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-16 20:01–20:03 真人复核**：`alice:survival_full_check` ⇒ `checks=122 failures=0 → PASS`、`alice:pathing_regression` ⇒ 全场景 0 FAIL 且 `terminal=COMPLETED`；用户确认「符合我的预期」）** |
| **D-242 水里那档：规划得到、执行不了**（2026-09-16） | 电池步 `survival_exit`（BASELINE）新相位 `FLOODED_SHAFT`；无头 `single:survival_exit` | 无参数 | 灌水竖坑：纯通行 `exit=none`；带准备金**计划** `REACHED`；额度三连（含写动作 ≥1、≤8、移动集不含 DOWNWARD/FALL）；**tripwire**：水里执行 FAILED 且 bot 仍在原地水里（做出来就翻红提醒改文档） | **`SERVER_TESTED`**（`checks=112 failures=0 → PASS`；反向对照关掉准备金档 ⇒ 判据变红）；**`WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-16 20:01–20:03 真人复核**：`alice:survival_full_check` ⇒ `checks=122 failures=0 → PASS`、`alice:pathing_regression` ⇒ 全场景 0 FAIL 且 `terminal=COMPLETED`；用户确认「符合我的预期」）** |
| **D-241 逃生准备金（提案 B 第一步）**（2026-09-16） | 电池步 `survival_exit`（BASELINE）新相位 `SHAFT_ESCAPE`；无头 `single:survival_exit`；另有 CORE 步 `write_policy` 审策略表 | 无参数（`alice:survival_full_check` 一次右键跑到同一相位） | 2 格深竖坑：纯通行 `plannableRefuge(...,false)==null`；信封闸门三连；`plannableRefuge(...,true)!=null` + `escapeNeedsWrites`；端到端驱动 `SurvivalExitTask(...,true)` 到 DONE 且脚位 y ≥ 101 | **`SERVER_TESTED`**（`checks=99 failures=0 → PASS`；反向对照关掉准备金档 ⇒ 三条判据变红；CORE `write_policy` PASS）；**`WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-16 20:01–20:03 真人复核**：`alice:survival_full_check` ⇒ `checks=122 failures=0 → PASS`、`alice:pathing_regression` ⇒ 全场景 0 FAIL 且 `terminal=COMPLETED`；用户确认「符合我的预期」）** |
| **D-238 出口必须是"可规划"的**（2026-09-15） | 电池步 `survival_exit`（BASELINE）新相位 `UNREACHABLE_REFUGE`；无头 `single:survival_exit` | 无参数 | 封石盒里隔着石墙留一个几何落点（干、可站、距 4）⇒ `plannableRefuge == null`；软危险 ⇒ `HOLD_NO_EXIT`（不再为它杀任务）；硬危险仍 `INTERRUPT`；回平台正对照 `plannableRefuge != null` | **`SERVER_TESTED`**（`checks=87 failures=0 → PASS`；**反向对照**：`decide` 退回只看几何 ⇒ 行为判据变红）|
| **D-237 溺水先上浮自救（乙）**（2026-09-15） | 电池步 `survival_exit`（BASELINE）新相位 `OPEN_WATER`；无头 `single:survival_exit` | 无参数（`alice:survival_full_check` 一次右键跑到同一相位） | 露天水池（21×21、水 4 深、拆掉天花板）⇒ `canFloatUp=true` + 半径 8 无落点 ⇒ `decide(LOW_AIR,99)==FLOAT_UP`；**并端到端**驱动 `SurvivalFloatTask`：到 `DONE` + 眼睛离开水面 + 空气 ≥100 | **`SERVER_TESTED`**（`checks=78 failures=0 → PASS`，实测 `tick=44 air=102 y=102.81`；**反向对照**：`canFloatUp` 恒 false ⇒ 两条新判据变红）。接线只观测到一次（真判决会打死整轮电池）|
| **D-236 溺水不再静默（深水 + 无落点）**（2026-09-15） | 电池步 `survival_exit`（BASELINE ⇒ CORE 跑）新相位 `DEEP_WATER`；无头 `single:survival_exit` | 无参数（`alice:survival_full_check` 一次右键即可在客户端跑到同一相位） | 自建封闭水牢（17³ 石壳 + 内部水）⇒ 半径 8 无干燥落点（几何自证）；空气够 ⇒ `WATER_CONTACT`；空气点 0 一 tick ⇒ **`LOW_AIR`**（溺水排在涉水之前）；`decide(LOW_AIR,99) == ABANDON_NO_EXIT` + 判定码 | **`SERVER_TESTED`**（`checks=65 failures=0 → PASS` + **反向对照**：溺水改回 `HOLD_NO_EXIT` ⇒ 两条新判据变红）。⚠️ `BotManager` 的"放弃任务"接线只有纯判据 + 编译级（端到端会打死电池）|
| **M4b 收尾：四 Job 统一子阶段失败口径**（D-234，2026-09-15） | 电池步 `decision_contract`（MAIN ⇒ CORE 跑；无头 `single:decision_contract`） | 无参数 | `TaskNode.finished` 三条：成功 ⇒ `lastFailure` 为空；失败 ⇒ `code@phase`；长度 ≤ `ONE_LINE_MAX`。四 Job 共用该口径 ⇒ 一次钉住四个 | **`SERVER_TESTED`**（`checks=10` 正向 PASS + **反向对照**（口径短路）⇒ `FAIL`）。⚠️ lumber/collect/region 的**端到端**只到 `IMPLEMENTED`（现有六用例无"子任务结束且失败"型）|
| **M3b 两条归因映射首次被观测**（D-232，2026-09-15） | 电池步 `mine_stale` / `mine_budget`（MAIN ⇒ CORE 跑；无头 `single:mine_stale`、`single:mine_budget`） | 无参数 | `mine_stale`：身份复检恒假 ⇒ 每次尝试 `target_replaced` ⇒ 终态理由**必须**是 `stale_target`；`mine_budget`：本作用域预算压 0 ⇒ 每次尝试 `WRITE_BUDGET_EXHAUSTED` ⇒ 终态理由**必须**是 `write_budget_exhausted`。理由不对 ⇒ `doneWhen` 不成立 ⇒ 预算耗尽判红 | **`SERVER_TESTED`**（两步 `PASS` + **反向对照**：把 `deriveTopLevelReason` 短路 ⇒ **两步各判红**；CORE `(37/37)`）|
| **M4b 任务树带子阶段失败事实**（D-231，2026-09-15） | 电池步 `mine_no_tool`（MAIN ⇒ CORE 跑；无头 `single:mine_no_tool`） | 无参数 | 归因正确（`terminalReason=tool_missing`）**且** `job.subTasks()` 里出现非空 `lastFailure`（形如 `no_suitable_tool@EVALUATING`）；缺其一 ⇒ `doneWhen` 不成立 ⇒ 预算耗尽判红 | **`SERVER_TESTED`**（正向 `PASS` + **反向对照**短路"结束子节点"那支 ⇒ `FAIL`；CORE `(35/35)`）|
| **D-230 `vanillaTick` 实验开关（默认关）**（2026-09-15） | 无头 A/B：`ALICE_EXTRA_JVM_ARGS="-Dalice.bot.vanillaTick=true" tools/headless-battery.sh core`（新钩子） | 环境变量 | 开关 ON/OFF 两态都应 `single:survival_exit` 55/0 且 **CORE (35/35)**；且"拆雪后血量回满、饱和度是否被消耗"能区分两条路径（OFF：回满但不掉饱和；ON：额外掉饱和） | **`SERVER_TESTED`**（ON/OFF 两态 CORE 均 `(35/35) PASS`；A/B 数据见 D-230）。**结论：默认不启用**（收益仅饥饿/`updateIsUnderwater`，代价含"饿死"新失效模式） |
| **D-229 冻结（细雪）危险档**（2026-09-15） | 电池步 `survival_exit`（BASELINE，无头 `single:survival_exit` / CORE）；真人入口 `alice:survival_exit_check` **疾跑+右键** | 零参数 | ① 决策表：冻结第 1 tick 不否决 / 过宽限+有出口 ⇒ 否决 / 理由码 `survival_freezing`；② 细雪里 `ticksFrozen` 真的累积 ≥ 60；③ 到阈值判成 `FREEZING`；④ 无出口+过宽限 ⇒ `HOLD_NO_EXIT`；⑤ 全冻（140）后真的掉血（期间最低血量口径） | **`WINDOWS_CLIENT`**（2026-09-15 真人实测：`alice:survival_full_check` 右键 ⇒ 聊天 `SUMMARY checks=55 failures=0 → PASS`，日志 `hazard=FREEZING duration=1…121`、无出口 `exit=none decision=continue`、全冻后每 2 秒 `掉血 … hazard=FREEZING`）＋`SERVER_TESTED`（`single:survival_exit` `checks=55 failures=0 → PASS`；**反向对照**阈值改 2000 ⇒ ②③④ 精确变红；CORE `(35/35) ticks=3830 → PASS`）。⚠️ 待真人验：疾跑右键看 `hazard=FREEZING` + 全冻掉血 |
| **D-228 假人必须跑原版 baseTick（火焰/空气/共享标志）**（2026-09-15） | 电池步 `survival_exit`（BASELINE，无头 `single:survival_exit` / CORE）；真人侧看火焰与掉血 | 零参数 | ① 着火后 `sharedFlagOnFire()` 必须为真；② `remainingFireTicks` 必须递减；③ 相位期间最低血量必须低于点火前；④ 必须出现带 `hazard=ON_FIRE` 的掉血事件；⑤ 入水后 `airSupply` 必须真的被消耗（溺水产线可达） | **`WINDOWS_CLIENT`**（2026-09-15 真人实测：**火焰看得见**、着火每 20 tick 掉 1 血、走动/放置/挖掘照旧、无崩溃）＋`SERVER_TESTED`（正向 `PASS` 45/0；**反向对照**注掉 `baseTick()` ⇒ 这 5 条精确变红；CORE `(35/35) ticks=3701`）。⚠️ 待真人验：客户端**看得见火焰/掉血**（见 HANDOVER §6） |
| **§5.9 挂起传输不许堵死 `assign*` + 拒绝必须可见**（D-227，2026-09-15） | 电池步 `transfer`（BASELINE，无头 `single:transfer` / CORE）内的 `ledgerPolicies` 夹具；命令/物品侧另有可见文案 | 零参数 | ① `suspendUnfinished` 后 `location != BOT_INVENTORY` 的未完成请求必须 **`ABORTED`** + `code=aborted_no_bot_inventory` + `manualTakeover=false`（**不许**挂起阻塞）；② `BOT_INVENTORY` 的仍然 `SUSPENDED` + `manual_takeover_required`（既有断言不变）；③ 启动结清必须**出声**：`[Transfer] 启动结清：N 条…`；④ `assignWalkTo` 拒绝必须可向外看到（物品侧显示真实原因，不再静默） | **`SERVER_TESTED`**（`single:transfer` 正向 `PASS` + **反向对照 `FAIL`**；CORE `(35/35)`；自愈实测 11 条 → `ABORTED`）。⚠️ 未验：结清结果的落盘、`assignWalkTo` 返回 false 的 live 会话判据（见台账 §5.9 末尾） |
| **S-5 维生决策（软危险纳入否决 + 无出口不否决 + 掉血可见）**（D-226，2026-09-15） | 电池步 `survival_exit`（无头 `single:survival_exit` / 游戏内电池 CORE）**＋** 真人入口 `alice:survival_exit_check`（右键=窒息硬危险；**潜行右键=着火软危险**） | 零参数 | ① `decide` 决策表：硬危险无条件否决（理由码 `survival_lava_contact`/`survival_suffocating`）、涉水/无危险不否决、软危险宽限期内不抖动、**过宽限且有出口**才否决（`survival_low_air`/`survival_on_fire`）；② 封闭场景（`alice_test:survival_sealed_course`，17³ 实心体）四层前提自证后：软危险 ⇒ `HOLD_NO_EXIT`（**不否决**）、硬危险仍否决；③ **真实着火** ⇒ 真的不否决 + `exit=none decision=continue`；④ 脚位格口径（半砖上 `blockPosition`≠`footCell`，排除脚位格后无落点；**旧口径会把自己那格当出口**）；⑤ 出口真能走到（`SurvivalExitTask` `COMPLETED` + 脚位 == 落点）；⑥ 掉血 ⇒ 一条 `DANGER`（`delta=`）且冷却窗口内恰好 1 条 | **`WINDOWS_CLIENT`**（2026-09-15 真人实测：硬×2+软×1 全部 `否决 → 逃生出口 1 格 → SurvivalExitTask COMPLETED`；着火期间每 20 tick 一条 `掉血 … hazard=ON_FIRE` ⇒ 掉血可见；**火焰看得见**）；此前为 `SERVER_TESTED`（2026-09-15 无头：`single:survival_exit` = `checks=38 failures=0 → PASS`；CORE = `(35/35) ticks=3686 → PASS`）。⚠️ **那半不能离线验**（电池会话任务=电池本身，真否决会把整轮电池 `complete` 掉）⇒ 只能真人验；**2026-09-15 已真人验**：硬×2 + 软×1 = 否决 → 逃生出口 1 格 → `COMPLETED` ✅。⚠️ 注：**"无出口 ⇒ 不否决"** 那半仍是 `SERVER_TESTED`（真人场景里一直有出口）。 |
| S-2 未加载区块/边界准入（D-132 / P1-A + 审计 §3.A:181） | 右键 `alice:chunk_guard_check` | 零参数右键 | 目标区块未加载 ⇒ 新状态 `GOAL_NOT_LOADED`（≠UNREACHABLE≠SEARCH_LIMIT）且**不加载它**；跨区块的边只在已加载区块内扩展；世界边界外不扩展 | **`WINDOWS_CLIENT`**（2026-09-12 14:58，2 次复现：`SUMMARY far_goal=GOAL_NOT_LOADED no_sync_load=true PASS near_goal=REACHED PASS border_predicate=true not_reached=true local_space=true skipped_border=true PASS → PASS`） |
| S-4 挖掘前流体风险（D-132 / P0-C） | 右键 `alice:fluid_mine_check`（自动生成 `alice_test:fluid_mine_course`） | 零参数右键 | 目标正下方是岩浆 ⇒ `MiningPlanner` 硬拒 `fluid_risk_lava`，且 `MineTask` **不许再加高/清障**（`cleared=0 gained=0`）；普通目标必须挖完 | **`WINDOWS_CLIENT`**（2026-09-12 14:27：`SUMMARY plan_refuse=fluid_risk_lava run_refuse=FAILED_fluid_risk_lava no_clear_gain=true control=DONE → PASS`） |
| J8 Slice A 可持续伐木区（MAINTAIN）（D-129） | 右键 `alice:region_lumber`（夹具区域=伐木场景盒）；或跑 `alice:regression_battery` 第 10 步 `region_maintain` | 零参数右键 | 区域型 Job：巡查 → 按区域边界过滤候选 → 复用一次性 `LumberJob` 砍一棵 → 继续巡查；**常驻**（默认不收工，只由显式打断；`idle-stop=true` 才在无树无苗无欠时 `DONE idle_no_work`）；全树失败 ⇒ `FAILED no_reachable_candidate`+逐树理由；区域状态持久化（区域/我种的苗/选定树苗物品/巡查统计） | **`WINDOWS_CLIENT`**（`AI_DECISIONS.md:4051`：Slice A 实测 12:30–12:31；本行 `idle-stop=true` 亦记 `WINDOWS_CLIENT`） |
| J7 Step 4 失败语义收敛（D-128） | 右键 `alice:lumber_failure_check`（**6 例**）；另跑 `alice:lumber_job` + 串联电池 | 零参数右键 | 候选期码正名 `trunk_too_tall`；缺斧 ⇒ 前置 `FAILED tool_missing`（不徒手撞预算）；逐树理由区分 `climb_incomplete`（爬过没砍完）与 `partial_tree`；顶层归因只在全失败同因时上抛；建拆未闭合后缀改名 `scaffold_restore_incomplete` | `WINDOWS_CLIENT`（2026-09-12 12:15，两轮）：`lumber_failure_check` **6/6**（`inventory_full=PASS(DONE inventory_full)`、`tool_missing=PASS(FAILED tool_missing)`、`all_rejected=PASS(… trunk_too_tall)`）；`lumber_job` 不变（`trees 5/5 cleared=9`）。原（预期 `lumber_failure_check` **6/6**：`tool_missing=PASS(status=FAILED reason=tool_missing)`、`all_rejected=PASS(… trunk_too_tall)`；`lumber_job` 与电池行为不变）。未覆盖：`climb_incomplete` 的场景（要求「成功加高后仍有原木没砍完」） |
| J7 Step 3 崩溃兜底（D-127） | 右键 `alice:scaffold_check`（第二轮：故意不拆 → 判定 → 续做）；另跑 `alice:regression_battery`（`scaffold` 步） | 零参数右键 | 账本里我方未拆除的 `TEMP` 放置：就近（≤16 格）⇒ `RestoreScopeTask` 续做并把账本收尾；远 ⇒ 如实报告不自动走回去（§12.3 拆除须在架上） | `WINDOWS_CLIENT`（2026-09-12 10:58）：`recover_decide decision=ready pending=4` + `RECOVERY SUMMARY decision=ready ledger_before=4 ledger_after=0 residue=0 → PASS`；第一轮 SUMMARY 原样 PASS；电池 `scaffold=PASS ticks=321`、(9/9)。启动报告在真实残留（4 条）上触发。原（预期 `[Scaffold] recover_decide … decision=ready` + `RECOVERY SUMMARY decision=ready ledger_before=N ledger_after=0 residue=0 → PASS`）。未覆盖：`too_far` 分支（场景太小无法安全拉开 >16 格）与真实崩溃重启路径 |
| T7 清障预算按棵重置（跨阈值）（D-126） | `/function alice_test:lumber_course` → 右键 `alice:lumber_job`（需先 `/reload`） | 零参数右键 | 场景东扩 + 第 4 棵同型橡树（基底 `33,64,208`）⇒ 累计清障应 **≥9**，跨过 `MAX_CLEAR_PER_TREE(8)`；配额由 T5 自动升到 5 | `WINDOWS_CLIENT`（2026-09-12 10:35）：`场景可行树=5 ⇒ 配额=5`；`DONE quota_met trees 5/5 logs 26/26 cleared=9 inventoryDelta=23 scaffoldLeft=0 ticks=806`；逐树清障 20:3 / 19:2 / 29:1 / 云杉:0 / 新树 33,64,208:3 ⇒ **cleared=9 跨过阈值 8**，job 级计数器会在第 9 格失败 ⇒ 该回归从「压线」变成真断言 |
| T5 配额随场景推导（D-125） | 右键 `alice:lumber_job`（`lumber_course` 场景） | 零参数右键 | 配额不再写死 4，而由 `LumberCandidateSource` 数出的可行树数决定；日志 `[Job] lumber 场景可行树=N ⇒ 配额=N` | `WINDOWS_CLIENT`（2026-09-12 10:19）：`[Job] lumber 场景可行树=4 ⇒ 配额=4（T5：配额随场景推导）`，终态 `DONE quota_met trees 4/4 logs 19/19 cleared=8 inventoryDelta=19 scaffoldLeft=0 ticks=689`；场景 tellraw 已改为「配额=场景可行树数」 |
| T4 作用域重开继承掉落物（D-124） | 右键 `alice:mine_regression`（第 12 例 `scope_reopen_keeps_drops`）；另跑 `alice:regression_battery`（`scaffold` 步已去掉 D-108 手工收养） | 零参数右键 | 会话内**重开作用域**应继承「仍活着且在新区间内」的我方掉落物归属（跨任务由 `end()` 清空）；判据：挖出掉落物（未收集）→ 重开 → `liveDrops()` 仍 ≥1 | `WINDOWS_CLIENT`（2026-09-12 10:04–10:07：`mine_regression` **12/12** ×2、、`scope_reopen_keeps_drops=PASS … liveDropsAfterReopen=1`；电池仍 9/9）。`scope_reopen_keeps_drops=PASS … liveDropsBeforeReopen=1/liveDropsAfterReopen=1`；`作用域重开: … 继承掉落物=1` 在**三个历史病灶点**（自建用例 / scaffold 收尾 / lumber ②③ 之间）均出现。首测曾假失败：破坏当 tick 查账恒为 0（`pending` 在 tick END 才确认，见 D-124 附注） |
| T3 扫尾预算推导（D-123） | 右键 `alice:regression_battery`（覆盖 `lumber_job` 与 `scaffold` 两处扫尾） | 零参数右键 | ① 扫尾的信封与 tick 预算不再写死 `gain<=8`/`200`，改为**从树干高度与待收物数推导**；日志应出现推导过程（`sweep_up_start … trunkHeight=7 gain<=8 blockBudget=8 budgetTicks=380`） | `WINDOWS_CLIENT`（2026-09-12 09:40）：电池 **9/9 PASS**（2407 tick），且一轮内出现**两个不同树干高**、算术可核对：`trunkHeight=3 → gain<=4/blockBudget=4/budgetTicks=280`、`trunkHeight=7 → gain<=8/blockBudget=8/budgetTicks=380`、不加高 → `180`。未覆盖：截断分支（`trunkHeight+1>12`）与 ① 扫尾超时分支 |
| 串联回归电池（D-122） | 右键 `alice:regression_battery`（零参数；先 `/give @s alice:regression_battery`） | 零参数右键 | 一次跑完 9 项常用回归（clear_retry / write_budget / scaffold / clear_guard / lumber_failure / mine_regression / lumber_job / mine_job / pathing），**每步独立作用域**、失败不中断 | `WINDOWS_CLIENT`（2026-09-12 00:52–00:55）：`[Regression] SUMMARY clear_retry=PASS write_budget=PASS scaffold=PASS clear_guard=PASS lumber_failure=PASS mine_regression=PASS lumber_job=PASS mine_job=PASS pathing=PASS (9/9) ticks=2416 → PASS`；每步独立作用域已由 `[WriteBudget] SUMMARY scope=…:Regression:<step>` 各自计数证实；噪声逐条归类（`no_suitable_tool`×5 + `no_tool`×1 + `clear_skip`×4 全是期望输出，`tool_in_main_inventory`/`clear_exhausted`/`scaffold_left` 均 0）。口径差异：电池内 `lumber_job` 的 `inventoryDelta=21`（共用一个世界、会捡前面步骤的落地原木），干净增量请单独跑手工入口 |
| T2 限次清障换候选（D-121） | 右键 `alice:clear_retry_check`（自带场景，零参数） | 零参数右键 | 一个清障候选失败 ⇒ **换下一个**（不再一次失败就放弃整棵树）；只有候选都用过/预算用尽才闩锁。清障子任务信封 = 父信封子集（`clear=none`、加高 `min(父,1)`、无建拆同权） | `WINDOWS_CLIENT`（2026-09-12 00:32–00:33，**连续 3 轮 PASS**）：`[ClearRetry] SUMMARY retry=PASS attempts=4 failed=4 clear_then_mine=PASS cleared=1 targetIntact=true sub_profile=PASS → PASS`；日志逐条为 **4 个不同候选**（`5,65,64`→`5,64,64`→`6,65,63`→`6,64,63`）各自 `clear_skip`，全程 `exhausted=false`；相位②清障成功并挖掉目标；`clear_subtask_profile … gain<=1 … clear=none`。**未覆盖（登记）**：几何不可达导致的"首候选失败"仍缺专门场景；清障子任务加高的**行为**仍只有信封日志证据 |
| D-119 工具语义（生产 MineTask 不发工具） | 右键 `alice:mine_regression`（第 11 例负例）；另跑 `alice:lumber_job`+`lumber_course`、`alice:scaffold_check`、`alice:restore_check`、`alice:clear_guard_check`、`alice:write_budget_check` | 零参数右键 | 挖"必须正确工具才掉落"的方块（石头/圆石/矿石）时**没有正确工具**⇒ 如实失败 `no_suitable_tool`、不改世界、**不发工具**；不要求工具的方块照旧（徒手可挖，但最佳速度 ≤1 会打 `no_effective_tool` WARN）。工具一律由**入口**用 `FixtureToolKit` 准备 | **已 `WINDOWS_CLIENT` 两入口（2026-09-12 00:18）**：`alice:mine_regression` **11/11**（`no_tool_refuses=PASS status=FAILED/reason=no_suitable_tool/targetKept=true/toolNotGiven=true/noToolFabricated=true/ticks=1`，全程仅 1 条 `no_suitable_tool`）；`lumber_job` `DONE quota_met trees 4/4 logs 19/19 cleared=8 inventoryDelta=19 scaffoldLeft=0`，零 `no_suitable_tool`。其余入口（scaffold/restore/clear_guard/write_budget/lumber_failure/mine_job/pathing）按 `TESTING_GUIDE §1.7` 清单**下次串联一起测**。**同日修正**：原按"破坏速度 ≤ 1"打 `no_effective_tool` 在伐木里刷了 8 条噪声（全是清障树叶，而 bot 手上有镐）⇒ 改为只报可行动的 `tool_in_main_inventory`（工具落主背包选不到）与 `no_tool`（身上没工具） |
| D-116 收集复用加高（树冠掉落物） | `/function alice_test:lumber_course` → 右键 `alice:lumber_job`；另跑 `alice:mine_regression` | 零参数右键 | 掉落物停在**头顶树冠**够不到时，① 就地扫尾用"原地加高"搭上去拿（日志 `[CollectDrops] gain_start/gain_done`），其放置由随后的建拆同权收回；③ 落地扫尾不给加高 | `WINDOWS_CLIENT`（2026-09-11 23:08：① 生效 —— `sweep_up_start live_drops=1` → `goal_shift 27,70,207→27,69,206` → `gain_start steps=1/8、2/8` → `entity_gone → SUMMARY collected=1/1` → `sweep_up_end swept=1 live_drops=0`；高云杉 7/7 不再 `product_not_collected`，`inventoryDelta` 14→15，`trees 3/4`）。同轮暴露并已修两处（见 D-116 附注二）：**假 `scaffoldLeft=1`**（`finish()` 拿账本当 `remaining`，未按世界事实对账 ⇒ Job 误 FAILED）、**夹具补工具顶掉整栈圆石**（`recovered=-8` 反推证实）。**两处均已 `WINDOWS_CLIENT` 验证（23:55–23:56）**：② 走到对账分支 `[Ledger] 销掉 1 条已失效条目(28,66,208 cobblestone→air)` → `[Restore] SUMMARY restored=2 skipped=1 reconciled=1 remaining=0 recovered=3 reason=restore_done_by_other_action → DONE`（旧代码此处会报 `remaining=1 → FAILED` + `scaffoldLeft=1`），Job 终态 `scaffoldLeft=0`；`recovered` 为 1/3、再无负值。`待复测`：`scaffoldLeft=0`、`[Restore] SUMMARY … reconciled=1 remaining=0 → DONE`、`recovered≥0`、`writes places` 仍为真实放置数。**2026-09-11 23:34 复测**（F2/F3 修复后首轮）：① 再次生效（`sweep_up_start live_drops=1` → `collected=1/1` → `sweep_up_end swept=1 live_drops=0`，这轮掉落物**直接落到脚下**因而不需加高 —— 见下"两轮差异"）；② `[Restore] SUMMARY restored=1 skipped=0 reconciled=0 remaining=0 recovered=1 → DONE`；终态 `scaffoldLeft=0`、`reason=partial_quota`（**不再含 `scaffold_left`**）、`inventoryDelta=15`、`writes places=1 unknown=0`。**结论**：`scaffoldLeft=0` / `recovered≥0` / 终态不含 scaffold_left 已 `WINDOWS_CLIENT` 验证；但**上一轮的触发条件是随机的，本轮未复现**（`skipped=0`），故 `reconciled>0 → restore_done_by_other_action` 这条**对账分支仍未在客户端跑过** ⇒ 该分支 `待复现验证`。<br>**两轮差异的根因（世界事实，非客户端物理）**：`27,70,207` 上是否有**树叶**托住掉落物，取决于**树叶随机刻腐烂**的时机 —— 顶部原木破掉后若树叶还在，掉落物停在树冠（22:44/22:46/23:08，需要 ① 加高）；若树叶已腐烂（该轮日志确有 `作用域捕捉掉落物: spruce_sapling/stick … source=unpaired` 的腐烂掉落），掉落物顺柱落下（23:34） |

| D-115 切片 B1（清障下沉 L2）复跑 | `/function alice_test:lumber_course` → 右键 `alice:lumber_job`；右键 `alice:lumber_failure_check`；右键 `alice:mine_regression` | 零参数右键 | 伐木的清障（腾站位/通视线）现在由 L2 按 `MiningProfile.clearBudget` 执行；行为应与改造前一致（同一 planner/同一理由/每棵树 8 格预算），日志改为 `[MineTask] clear_start/clear_end` | **`WINDOWS_CLIENT`**（本文件 `:33` 挖掘回归 11/11；`:201` `gainedTrees=1…scaffoldLeft=0`） |

| D-114 收集寻路目标=可站格 | 右键 `alice:mine_regression`（重点 `exec_floating`）；回归 `alice:pathing_regression`、`/function alice_test:lumber_course` → `alice:lumber_job` | 零参数右键 | 掉落物停在"站不住的格"（刚拆掉的支撑块所在格 / 井口）时，收集应改走"够得着它的可站格"（日志 `goal_shift`），不再 `UNREACHABLE` → 退役 | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-11 21:36）：`exec_floating=PASS … dropsLeft=0`；**伐木路径**上真实触发 `[CollectDrops] goal_shift item=28,70,207 → goal=27,69,207`（掉落物在树冠里站不住的格 → 改走可站格后收回）；同轮 `inventoryDelta` 两次运行分别为 1/2 ⇒ 印证"精确净增量不是不变量" |
| ⭐⭐ **`D-377`+`D-380`：无任务的水下 bot 必须被维生接管，且**一进水就浮**（2026-09-21）** | 电池步 `survival_idle_drown`（EXTRA，**第二个假人**天然无任务 + 自建 1 格宽水井；`ALICE_HEADLESS=1 tools/headless-battery.sh single:survival_idle_drown`） | 零参数 | ① 探针假人整只没入水中（`isEyeInFluid`）且**没有任务** ⇒ 判决（过宽限后）必须是 `FLOAT_UP`；② **作用域**：`air ∈ (0,200]` 且眼在水里时**共享分类表仍是 `WATER_CONTACT`**（这一档只对无任务生效，不许改 `classify` —— 第一版改了 ⇒ 电池步 `survival_exit` 被判真溺水 ⇒ 整轮 `no_verdict`）；③ **`D-380` 口径**：**头第一次露出水面时空气必须还很满**（`firstEyeOutAir > DROWN_PRECURSOR_AIR`）—— 第九轮客户端实测老口径（`air ≤ 100`）要等约 10 秒才动手，用户看到的是「**没有浮出来**」；④ 头**露出过水面** + 空气回到 `AIR_SAFE` | **`SERVER_TESTED`**（`[IdleDrown] SUMMARY checks=9 failures=0 观察=15 tick → PASS`；读数：`verdict(afterGrace)=FLOAT_UP air=200 foot=3000,-63,4200` → `sawTask=true firstTaskAir=198` → `sawEyeOut=true maxAir=199`（**15 tick 内出水**，旧口径约 200 tick）；**红→绿**：阈值复活 ⇒ 恰好红在口径那条（`firstEyeOutAir=91，必须 > 100`）+ 门禁静态红；门禁 `[D-377·危险处理不挂任务]` 含**反向断言**「无任务档不许有空气余量阈值」；CORE **51/52（仅既有 `lumber_job`）**、`survival_exit=PASS`）。**`WINDOWS_CLIENT` + `USER_ACCEPTED`**（2026-09-21 第十一轮，用户原话：「刚才也测试完了，**bot 确实能够马上浮起**。**这是期望的实现**」）⇒ 口径落地确认 ✓。⚠️ 仍**未观察**：浮起来后是否**停在水面**（`SurvivalFloatTask` 成功后不再按跳跃 ⇒ 可能缓沉）—— 归入「水中逃生」计划的第 0 项观察 |
| ⭐⭐ **3-b / `D-381`：「到位却够不到」⇒ 把那一格记进本簇排除集、取次优（+ 探针与搜索共用同一份候选枚举）**（2026-09-21 第七/八轮真机的精度残差） | 电池步 `collect_offcenter_retry`（EXTRA，**动作层**：真世界里一个真的 `CollectDropsTask` 被夹具每 tick 驱动一 tick，世界修改授权=true；`ALICE_HEADLESS=1 tools/headless-battery.sh single:collect_offcenter_retry`） | 零参数（自建空中孤岛 + 夹具自己传送/复位） | 场景：物品格头顶一块石头（1 格高夹缝 ⇒ 站不住）、物品压在格的 **+x 侧**（格内偏移 0.9）⇒ 西邻格中心到物品逐轴 **1.4 ≤ 1.425**；bot **就站在**西邻格里但**离心 0.49** ⇒ 真实盒子 **1.89 > 1.425**。判据 ① `D2`：`goal_excluded ≥ 1` + `collected == 1` + 物品不再在地上 + `no_approach == 0` + **世界零改动**；② `D0`：`approachCandidates(itemCell,1).size() == 16`（8 格 × **2 层**）+ 探针读数必须有 `(1,-1,0)stand=0/reach=1`（= 真机里 bot 站着的那一层）+ 层计数自洽 | **`SERVER_TESTED`**（`[CollectOffcenter] SUMMARY checks=17 failures=0 collected=1 goal_excluded=1 → PASS`；读数：`itemBox=[3400.78..3401.03 …] botPos=3399.010/2600.500 模型够得着=true 真实够不到=true` → `goal_excluded cell=3399,101,2600` → `reanchor goal_foot=3400,101,2599`（次优=正北邻格）→ `collected=1/1 movements=2 cost=2.00`；**红→绿 A**（撤 `excludeFailedGoal()`）⇒ `failures=3 collected=0 goal_excluded=0`（= 旧行为：物品留在地上）、**红→绿 B**（探针改回只扫 `dy=0`）⇒ `failures=1` 且**恰好**红在 `D0` 那条（`D2` 仍绿 ⇒ 两判据互不遮蔽）；回归：`collect_slot_approach=PASS`。⚠️ 客户端不适用（不是物理/视觉现象） | ⏳ 真机待观察：`collected=N/N` 是否变成**逐簇全满**（第七轮 12/93 件留在世界的那一族） |
| ⭐⭐ **`D-379`：「破坏通行」破掉的中间列是 bot 要踩过去的一格 ⇒ 它必须立得住**（2026-09-21 第八轮真机） | 电池步 `break_traverse_footing`（EXTRA，规划级·边生成层 + 执行工厂准入，**3 用例一次跑完**；`ALICE_HEADLESS=1 tools/headless-battery.sh single:break_traverse_footing`） | 零参数 | ① 中间列**悬空 + 落点是水**（= 真机 `BREAK_AND_TRAVERSE from=632,64,95→to=632,64,93` 破掉 `632,64,94` 后 bot 掉进 `632,62,94` 的形状）⇒ **不许**生成该边、执行工厂必须以 `BREAK_AND_TRAVERSE_NO_MID_SUPPORT` 拒绝、计数记 `…_fluid`；② 中间列悬空但只有 **1 格浅坑** ⇒ **同样不许**（契约判据：中间列立不住 ⇒「走到 `to`」不成立）、计数记 `…_dry`；③ **反证**：中间列**立在地板上**（正常的「破墙过去」）⇒ 这条边**必须**生成且工厂必须接受 | **`SERVER_TESTED`**（`[BreakTraverseFooting] SUMMARY checks=30 failures=0 用例=3 → PASS`；读数：`MID_FLOOR_FLUID edge=false canWalkOn(mid)=false factoryValid=false code=…NO_MID_SUPPORT@…,mid.belowBlock=Air fluidDelta=1` / `MID_FLOOR_SHALLOW edge=false dryDelta=1` / `MID_FLOOR_SOLID edge=true mid.below=Grass Block factoryValid=true`；**红→绿 A**（撤规划侧闸门）⇒ `failures=4`（①② 都生成了边 + 计数为 0）、**红→绿 B**（撤执行侧闸门）⇒ `failures=3`（`factoryValid=true code=null`）；门禁 `[D-379·破通行要站得住]` 8 条 pin；CORE **51/52（仅既有 `lumber_job`）**、`edge_completeness=PASS`；⭐ **不是死代码**：CORE 里 `break_traverse_no_mid_support_dry=9`，全部来自 `fluid_course`（`expect=PLAN_REFUSED` 负例场景）且该场景预期不变）。⏳ **客户端第十一轮待做**：采掘/拾取时不许再出现「破掉中间格 → 从中间列掉下去 → 落水」 |
| ⭐⭐ **`D-378`：`survey/27 §3 #1` 收口 —— 「88 段绕远」是不是那一行谓词造成的**（2026-09-21） | 电池步 `head_blocked_route_closure`（EXTRA，规划级，**当场自建基岩隧道 + 同一请求跑三遍**；`ALICE_HEADLESS=1 tools/headless-battery.sh single:head_blocked_route_closure`） | 零参数 | ① 生产 provider（修复后）：5 格「脚位空 + 头位实」夹缝的直挖路**必须可规划**且段数 ≤ **7**，其中恰好 **5 段**属于「旧谓词会拒掉的那一类」（脚位可通行 + 头位被挡）；② **旧谓词边过滤**（夹具自带 provider 包装，**不碰生产代码**；等价性可算：修复后已要求 `!bodyPassable`，旧闸门要求 `!canWalkThrough` ⇒ 差额恰好是「脚位可通行 ∧ 头位被挡」）：同预算下仍能到，但段数 **≥ 40**（实测 47）且**不含**那类边；③ 旧谓词 + 紧预算（40 节点）：**到不了**且 `nodesExpanded` 打满 40（= 复现 `SEARCH_LIMIT` 的机制） | **`SERVER_TESTED`**（`[RouteClosure] SUMMARY checks=27 failures=0 arms=3/3 directMovements=7 detourMovements=47 tightReached=false tightNodes=40 verdict=PASS`；读数：POST `7 段 / breakEnter=6 / 该类=5 / nodes=18 / cost=24.00`、PRE_AMPLE `47 段 / 该类=0 / nodes=49 / cost=49.83`、PRE_TIGHT `PARTIAL nodes=40 diag=budget exhausted(why=nodes)`；**红→绿 A**：把生产门禁撤成 `canWalkThrough(to)` ⇒ `failures=8`（POST 7→**47 段**、该类 5→0），恢复后复绿；**红→绿 B**：把反臂过滤器改成空操作 ⇒ `failures=10`（两臂都 7 段 ⇒ 证明对比来自过滤器）；门禁 `[D-378·夹缝路线收口]` 9 条 pin；CORE **51/52（仅既有 `lumber_job`）**）。⚠️ **诚实边界**：证的是**机制**，不是「真机那次 88 段的具体读数」（需冻结副本）；`SEARCH_LIMIT` 的绝对量级未复现（自建场景太小） |
| ⭐ **`D-377`：危险处理不许挂在任务上 + 沉底提前自救（只对无任务）**（2026-09-21） | 电池步 `survival_idle_drown`（EXTRA，**第二个假人**天然无任务 + 自建 1 格宽水井；`ALICE_HEADLESS=1 tools/headless-battery.sh single:survival_idle_drown`） | 零参数 | ① 探针假人整只没入水中（`isEyeInFluid`）且**没有任务** ⇒ 判决（过宽限后）必须是 `FLOAT_UP`；② **作用域**：`air ∈ (0,100]` 且眼在水里时**共享分类表仍是 `WATER_CONTACT`**（沉底档只对无任务生效，不许改 `classify` —— 第一版改了 ⇒ 电池步 `survival_exit` 被判真溺水 ⇒ 整轮 `no_verdict`）；③ `B1`：观察窗口内**出现一次任务**且**自救在空气还够时就开始了**（`firstTaskAir > 0`）+ 头**露出过水面** + 空气回到 `AIR_SAFE` | **`SERVER_TESTED`**（`[IdleDrown] SUMMARY checks=9 failures=0 → PASS`；读数链：`watch=1 hazard=WATER_CONTACT` → 首个任务出现在 `air>0` 时 → `eyeOut=true` → 拆除时 `air=103`；门禁 `[D-377·危险处理不挂任务]` **10/10 注入红且各命中专属条目**；行为级红对照：撤 B1 ⇒ `failures=4`（`hasTask` 全程 false、`air 59→-1`）、撤沉底档 ⇒ `failures=1`；CORE **51/52（仅既有 `lumber_job`）**、`survival_exit=PASS`）。⏳ **客户端第十轮待做**：把 bot 传送进水里（**且没有任务在跑**）⇒ 应当**上浮呼吸**，不再沉底淹死 |
| ⭐ **`D-376`：高度变化必须查「过渡空间」（搭石斜下 `PLACE_STEP dy=-1`）**（2026-09-21） | 电池步 `place_step_descend_clearance`（EXTRA，规划级·边生成层 + 执行工厂准入，**2 用例一次跑完**；`ALICE_HEADLESS=1 tools/headless-battery.sh single:place_step_descend_clearance`） | 零参数 | ① 搭石斜下的目标：`to`/`to.above()` 空气、`to.below()` 缺支撑（正要放），但 **`to.above(2)` 实心** ⇒ **不许**生成这条边、执行工厂必须以 `PLACE_STEP_AND_TRAVERSE_NO_SWEEP` 拒绝；② 反证：去掉那块 ⇒ 这条边**必须**生成且工厂接受 | **`SERVER_TESTED`**（`[PlaceStepClear] SUMMARY checks=24 failures=0 → PASS`，读数：`TRANSITION_BLOCKED edge=false sweep=false factoryValid=false code=…NO_SWEEP` / `TRANSITION_CLEAR edge=true sweep=true factoryValid=true`；**红→绿**：撤掉规划侧扫掠检查 ⇒ 夹具红（`edge=true`）；门禁 `[D-376·高度变化查过渡空间]` **7/7 注入变红且各命中专属条目**）。⏳ **客户端第九轮待做**：搭石斜下不许再出现「顶着格边界原地走 11 秒 ⇒ 段超时 ×2」 |
| ⭐ **`D-375`：够得着的可站格（收集目标不许「挖进去」）+ 决策层信号**（2026-09-21） | 电池步 `collect_slot_approach`（EXTRA，自建空中场景，**4 案例一次跑完**；`module:pickup` 右键或 `ALICE_HEADLESS=1 tools/headless-battery.sh single:collect_slot_approach`） | 零参数 | ① 1 格高夹缝：物品所在格**站不住**、邻格可站且**真够得着**（中心距 1.3 ∈ (旧粗判 1.2, 真实 1.425]）⇒ 必须走过去捡到、**世界零改动**；② 物品格 + 8 邻格全是 1 格高 ⇒ 如实 `no_standable_approach` 退休、物品留在世界、**不许挖穿**；③ 反证：封死石室（世界修改已授权）⇒ 合法破墙 + 决策层收到 `PICKUP_DETOUR`；④ 反证：永久 `PickupDelay` ⇒ 决策层收到 `PICKUP_SLOW` | **`SERVER_TESTED`**（`[CollectSlot] SUMMARY checks=39 failures=0 → PASS`；**红→绿**：把四条修法改回修复前形态 ⇒ `failures=7`，含 `UNREACHABLE` 真的挖穿天花板把物品捡走（`collected=1 worldChanges=1 no_approach=0`）+ 「收集器挑的目标格」判据红；门禁 `[D-375·收集目标可站]` **8/8 注入变红**）。⏳ **客户端第七轮待做**：夹缝掉落物应表现为「走到旁边就捡到、不再挖天花板」（看 `collected=N/N`、`no_approach=`、`slow_events=`/`detour_events=`） |

| D-112 挖掘侧建拆同权 | 右键 `alice:mine_regression`（自带复位）；`alice:mine_job` 需**先** `/function alice_test:ore_course` | 零参数右键 | 用完自己放的临时方块（支撑/台阶）后，**仍在架上**自上而下拆掉；`[MineTask] restore_start/restore_end` 可判读；任务收尾不得再出现 `仍有 N 条我方临时放置未拆除` | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-11 21:36）：`mine_regression` **10/10 PASS**；`exec_floating=PASS … ledgerRestored=1/scaffoldLeft=0/dropsLeft=0`；收尾**残留告警 0 条**；`mine_job` `COMPLETED` |

| D-111 能力信封（切片 A）复跑 | 右键 `alice:lumber_failure_check` / `alice:mine_regression` / `alice:scaffold_check`（三者自带复位）；`alice:lumber_job` 需**先** `/function alice_test:lumber_course` | 四次零参数右键 | 加高能力已下沉 L2（`MiningProfile`），四个调用点行为应与改造前一致 | **`WINDOWS_CLIENT`**（本文件 `:203` `lumber_failure` 5/5；`:227` `scaffold_check`） |

| ⭐ **保护区里搭柱子（`L2` 任务区的 `STEP_PLACEMENT` 放行）**（`D-338` 附注十/十一） | 玩家在**被认领区块**里发起区域任务（`region_lumber` ×1） | 零参数（按 `D-338` 附注十一的第三轮现场：一次 `lumber` 被如实拒 ⇒ 决策层换 `region_lumber`） | ① `ZONE_PROTECTED*` 拒绝 **144 → 0**；② `[Ledger] place 28,64,208/65/66 minecraft:cobblestone [TEMP STEP_PLACEMENT scope=…#1960:Region…]` = **真的在保护区里垫了三格**；③ 云杉 `chopped 6/7 failed=1 → 7/7 failed=0`；④ 内层 Job `places=0 → 3` | **`WINDOWS_CLIENT` + `USER_ACCEPTED`**（2026-09-19 第三轮，`latest.log` 17:28–17:30；用户判定「修复后符合预期」）。根因 = `CapabilityGate` 的 `Facts.protectionReason` 走裸 `SafeZoneData`（保护区**第四处**消费点，`D-338` 附注十） |
| J7 Step 2 伐木攀爬兜底（D-109） | **先** `/function alice_test:lumber_course`（含复位+传送+发物品）→ 再右键 `alice:lumber_job` | 零参数右键 | 够不到的原木先**贴着树干搭柱子爬上去**再砍（`CLIMB` 阶段）；每棵树用完**仍在架上**拆除我方放置；`climbed=/climbBlocks=/scaffoldLeft=` 进终态 | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-11 21:36）：高树靠 L2 `[MineTask] gain_start/gain_done` 完成，`lumber SUMMARY gainedTrees=1 gainedBlocks=1 scaffoldLeft=0`；不再出现 `no_reachable_standing_point` |

| J4 终止路径复跑（D-109 后须确认未被新阶段破坏） | 右键 `alice:lumber_failure_check` | 零参数右键 | 五条终止路径各一个用例 | **已并入 `:203`（2026-09-16 复核）**：同一入口 `alice:lumber_failure_check`，`:203` 已记 `WINDOWS_CLIENT`+`USER_ACCEPTED`（5/5 PASS）⇒ 本行不再单独计 |

| L3 伐木 Job（D-080，切片 **J2**：循环 + 配额 + 终止） | `/function alice_test:lumber_course` + `alice:lumber_job` | 普通右键 | 配额 **2 棵**；场景 4 棵树 / 3 棵可行（橡树`20,64,208`、复制橡树`29,64,215`、云杉`28,64,208`）/ 高大云杉被拒 | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-10 20:43：`result=DONE reason=quota_met`、`trees 2/2`、`logs 14/15`、`cleared=6`、`inventoryDelta=14`、`unknown=0`、`ticks=1152`）。顺序 = 橡树(20,64,208) → 云杉(28,64,208) → 复制橡树(29,64,215)（距离 3.2/5.1/10.0）；云杉如实报 `该树未完成 28,64,208:partial_tree gained=6/7 failed=28,70,208:no_reachable_standing_point`（**已知高树触及上限**，4.46>4.1，属 J7 攀爬范围）；循环走 `phase=NEXT` ✓。**注**：累计清障仅 6 格（<8），故本场景只是 `clearedThisTree` 按棵重置的*潜在*回归，真要逼出该缺陷需跨树累计 >8 格 —— **已由切片 J3 的场景补上**（3 棵同型橡树 ⇒ 9 格） |

| L3 伐木 Job（切片 **J3**：策略可替换性） | `/function alice_test:lumber_course` → 右键 `alice:lumber_policy_check` | 普通右键（零参数） | **只规划不执行**；同候选集对比 `NearestPolicy` 与 `NearestExposedPolicy` | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-10 21:26：`SUMMARY nearest=tree@20,64,208 exposed=tree@28,64,208 differ=true explainable=true exposedHonest=true → PASS`；候选特征印证重定义：橡树 `visible=0 exposed=false`、云杉 `visible=3 exposed=true`，而 `open_sky` 对全部 4 棵树均为 false） |
| L3 伐木 Job（配额**由场景推导**，当前 5 棵 = 按棵预算真回归） | 同上场景 → 右键 `alice:lumber_job` | 普通右键 | 3 棵同型橡树各需清障 3 格 ⇒ 累计 **9 > MAX_CLEAR_PER_TREE(8)**；云杉顶格仍不可达（如实 partial） | `WINDOWS_CLIENT`（2026-09-10 21:26，**配额 3 棵**轮：`DONE quota_met trees 3/3 logs 12/12 ``cleared=8 inventoryDelta=12 unknown=0 ticks=474` —— **但 8 格正好压线，未触发按棵预算回归**，见 D-092 附注）。现配额提到 **4 棵**：`待复测`（预期 `trees 4/4`、`cleared≥9`；若预算非按棵重置，第 3~4 棵必 `clear_budget` 失败）。**2026-09-11 根因修正（D-117 + 附注）**：09-10 21:28 的 `a5901f5` 加第 4 棵橡树时，其树冠占住了 `x=22` 与 `x=23` **两列** —— 而 y=65 的 z=212..214 是 x=17..31 的树冠墙、可行走台地只有 x=18..30 ⇒ **x=22..26 是唯一通道**（工作基线 21:25 的成功路线逐段为 `…(22,205)→(23,205)→(23,206)…`，南下走的是 **x=23**）。封死后 19 自 21:28 起每轮 `no_valid_standing_point`（与代码改动无关）；23:47 复测只补 x=22 不够（原路线在 x=23）。**故撤回第 4 棵橡树**（几何 diff 证明已等于 `a5901f5^`，即验证过能砍 19 的那套），配额仍 4，累计清障 >8 改由 20(3)+19(3)+28 云杉(3)=9 凑够（第 3 棵即跨阈值）。客户端数据包已同步（需 `/reload`）。**`WINDOWS_CLIENT`（2026-09-11 23:55–23:56，两轮连续）**：`result=DONE reason=quota_met trees 4/4 logs 19/19 cleared=8 inventoryDelta=19 gainedTrees=1 gainedBlocks=1 scaffoldLeft=0 unknown=0`；卡死点 `19,65,215` 已被清掉（`[WRITE] break 19,65,215 … LINE_OF_SIGHT`）；逐树清障 20:3 / 19:2 / 29:3 / 云杉:0，云杉靠 `gain_start steps=1/3` + ① 扫尾 7/7。**残留缺口（如实登记）**：累计清障正好 **8**（原期望 ≥9）⇒ 本行要检验的「清障预算按棵重置」**仍未真正被检验**（8 格压线，job 级计数器也能过）；功能面已全绿，属夹具覆盖度问题。 |

| L3 伐木 Job（切片 **J4**：五条终止路径 + 身份复检安全缺口） | `/function alice_test:lumber_course` → 右键 `alice:lumber_failure_check` | 普通右键（零参数，约 30 秒） | 五条终止路径**各有真实 Job 场景**；用例前重放场景保证独立 | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-10 23:05 **5/5 PASS**）：`no_candidates=PASS(no_reachable_candidate)`、`all_rejected=PASS(+too_large)`、`inventory_full=PASS(DONE)`、`goal_timeout=PASS(ticks=41 ≤120 不空转)`、`log_replaced=PASS(DONE quota_met, replacedPos=20,64,208 仍为圆石=true)` —— 末条证明 **bot 不挖玩家换上的非原木方块** |
| 安全缺口：队列原木身份复检（§6.2c⑤） | 同上 `log_replaced` 用例 | 夹具注入（Job 选完树后替换队列首格） | 断言"注入格仍是替换后的方块"而非字符串匹配 | `WINDOWS_CLIENT`（同上，随 J4 一并验证） |

| L3 挖掘 Job（切片 **J5**：L3 不只服务伐木 + 孤岛拆除） | `/function alice_test:ore_course` → 右键 `alice:mine_job` | 普通右键（零参数） | 场景为孤立长方体区域 + 石体顶面 6 处裸露铁矿；配额 4 | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-10 23:18：`DONE quota_met mined 4/4 inventoryDelta=4 writes breaks=4 unknown=0 ticks=240`）。证据：`candidates` 6→5→4→3（`attempted` 过滤生效、拒绝带 `:not_nearest` 理由码）；每次 `select` 按当前位置重算最近（d=4.0/4.0/8.0/4.0）；**日志格式与伐木逐字同构** ⇒ 同一套 Job/Trace 复用成立 |
| 通用挖掘入口（孤岛替代） | `/alice auto-mine <tag\|block> [count]` | 一行命令（无坐标） | 原 `AutoMineDecision` 已删除，改经 `assignMineJob` → `MineJob` | **仍待测（可选）**：`/alice auto-mine` 全仓无实测记录（`TESTING_GUIDE.md` 无该条目）⇒ 除非要用它当入口，否则不必专门验 |

| J6-a 世界修改账本（只记放置 + 动作层记录 + 只读查看） | 右键 `alice:pathing_regression`（`place_course`/`pillar_course` 会真的放置方块）→ `/alice ledger` | 零参数右键 + 一行只读命令 | 账本条目 `(pos, placed, previous, reason, policy, scopeId, owner, tick)`；策略由 `WriteReason.temporary()` 派生 | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-10 23:47）：7 条 `[Ledger] place …`，全部 `cobblestone←air [TEMP STEP_PLACEMENT scope=…:PathingRegressionTask]` —— **抓到的是内核 `PILLAR`/`PLACE_STEP_AND_TRAVERSE` 在寻路途中放的方块**（"Job 未必看得见"那一类）⇒ 动作层记录点选对了；`world_mod_ledger_close … 仍有 5 条未清除的放置（建拆同权未闭合）`；`/alice ledger → pending=5 TEMP=5 KEEP=0 openScopes=0`（7 条记录→5 条：两处被重放，一格一条覆盖 ✓）。同轮 `pathing_regression` 仍 14/14 PASS ⇒ 账本钩子与 D-095 谓词均无回归 |

| J6-b1 作用域恢复（建拆同权由会话强制） | 右键 `alice:pathing_regression`（其 5 处放置成为待恢复项）→ `/alice ledger` | 零参数右键 + 一行只读命令 | 恢复严格自上而下、只拆自己放的、不许挖地形 | **机制已删（2026-09-16 复核）**：D-103 删除了「收尾自动追加恢复任务」（`AI_DECISIONS.md:2502`）⇒ 本行期望的「自动追加」不再成立；恢复能力改由显式路径覆盖（见本表 `:212`/`:215`） |
| J6-b1 命令兜底 | `/alice restore` | 一行只读式命令（无坐标） | 处理崩溃/重启/升级前的历史遗留 | **`WINDOWS_CLIENT`**（`AI_DECISIONS.md:3939/3944`：续做走的就是同一条 `RestoreScopeTask`；**字面命令未单独敲**，如实标注） |
| 寻路回归复验（D-099 夹具修复后） | 右键 `alice:pathing_regression` | 14 场景 + 10 种 Movement 覆盖 | 一次性方块必须进快捷栏，否则 `PILLAR`/`PLACE_STEP`/`FALL` 生成不出来 | `WINDOWS_CLIENT`（2026-09-11 00:4x **14/14 PASS + coverage=PASS**；`[FixtureTool]` 覆盖策略生效） |

| J6-b1b 恢复的物质闭环 | 右键 `alice:pathing_regression` → 自动追加恢复任务 → 看 `[Restore] SUMMARY` | 零参数右键 | 恢复期间重开作用域让掉落物被登记，收尾调一次收集（`worldMod=false`） | **`WINDOWS_CLIENT` + `USER_ACCEPTED`**（本文件 `:184`：`restored=2 skipped=1 reconciled=1 remaining=0 recovered=3 → DONE`）；⚠️ 前提已变：触发它的「自动追加」机制已删（见 `:211`），现由显式恢复路径达成 |

| J6-b1b 恢复自检（夹具负责传送） | `/give @s alice:restore_check` → 右键 | 物品右键（零参数） | 场景相互孤立，恢复任务走不过去 → 夹具把 bot 送到待恢复方块旁的可站格 | **仍待测（可选，2026-09-16 复核）**：`alice:restore_check` 夹具自检**从未单独跑**（本文件 `:183` 记「其余入口下次串联一起测」）；恢复的物质闭环已由 `:184`/`:215` 覆盖⇒ 除非要单验该夹具，否则不必专门跑 |

| J6-b2 容器绕行自检（D-095 断言） | `/function alice_test:clear_guard_course` → 右键 `alice:clear_guard_check` | 物品右键（零参数） | 目标在墙后、唯一通道（缺口开在 y=65..66，脚位踩不到）被两个箱子堵住；跑真实 `MineTask` 后断言箱子完好 | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-11 18:31）：`predicate_refuses=true chest_intact=true target_removed=true → PASS`（91 tick）——合法绕行未触碰容器 |

| 运行期脚位格规则（D-105，无头断言） | 右键 `alice:pathing_regression`（末尾自动跑 `PathingRegression.run`） | 零参数右键 | `footCell` 给出的格必须 = 规划层 `canWalkOn` 认可的格：箱子 0.875 / 底半砖 0.5 / 灵魂沙 0.875 上移一格，地毯 0.0625 与整格不上移 | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-11 18:31）：`PATHING_REGRESSION PASS foot_cell_rule chest=true bottomSlab=true soulSand=true thinFace=true fullBlock=true` |

| 非满高支撑的可执行回归（D-105） | 右键 `alice:pathing_regression`（自动建地形并执行） | 零参数右键 | `chest_step_course`：起点 (1,64,126) → 目标 (2,65,126) 必须**真的执行 ASCEND 并完成**（旧行为：箱顶原地弹跳 → `SEGMENT_TIMEOUT` 161 tick）；`slab_step_course`：(4,64,145) → (8,64,145) 封闭走廊，中途一块底半砖必须走过去（旧行为：TRAVERSE 完成契约死锁） | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-11 18:31）：16 场景 + `foot_cell_rule` + `coverage` 全 PASS；`chest_step` 的 ASCEND 10 tick（旧 161 tick 弹跳）；全程无 `SEGMENT_TIMEOUT` |

| 执行期写入预算（D-106）：任务级上限 | `/give @s alice:write_budget_check` → 右键（零参数） | 物品右键 | 复用 `break_course`，本次任务破坏上限压到 1 格；断言"只拆 1 格 → 用满即停 → 如实失败 → 绝不继续拆"，并用**世界事实**复核（墙区空气格 ≤1） | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-11 19:15）：`CHECK breaks=1/1 places=0/0 refusedBreaks=1 refusedPlaces=0 exhausted=true wall_broken=1 passed_wall=false status=MOVEMENT_FAILED → PASS`；相比 Slice A：`refusedPlaces` 2→0、`replans` 2→1、耗时 51→24 tick，且不再出现 `[WRITE-REFUSED] place` |

| J7 Step 1 脚手架生命周期（D-107） | `/give @s alice:scaffold_check` → 右键（零参数） | 物品右键 | 搭柱爬到柱顶（5 次 `PILLAR`）→ 采高处够不到的目标 → **仍在柱顶**自上而下拆 → 落地；断言建 N 拆 N、账本 scope 清空、柱列无残留（世界事实复核） | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-11 19:46：`pillar=4/12`（计划用 4×`PILLAR`+1×`ASCEND` 上壁顶）、`torn=4 remaining=0 residue=0 target=gone on_top_at_teardown=true grounded=true → PASS`）；用户复测反馈"掉落物没收" → 补收尾收集阶段与 `drops_left` 断言后**第二次复测仍 FAIL**：`collected=0 drops_left=1`（世界事实断言生效）；根因是作用域重开丢失掉落物归属（D-108），第二次复测**又暴露时刻问题**：产物先落在作业点平台（`itemPos=39,69,46`），`pickupDelay` 内 bot 已沿柱下降，拆完平台就够不到了（`retire reason=MOVEMENT_FAILED`）。**已按用户裁定改为"两处扫尾"**：① 作业点就地（仍在架上）+ ③ 落地后；SUMMARY 增加 `sweep_up/sweep_ground/stranded=<坐标>`，**第四次复测待做**（预期 `stranded=- drops_left=0 → PASS`） |

| Slice B 计划期剪枝不误剪（D-106） | 右键 `alice:pathing_regression`（16 场景含 `break_course`/`place_course`/`pillar_course`） | 零参数右键 | 计划期闸门只能"拦注定执行不完的写边"，**不能**动合法写边；`[PathRetry] planned … writes>=b/p` 可见每个计划的下界 | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-11 19:15）：16 场景 + `foot_cell_rule` + `coverage` 全 PASS；预算实耗与 Slice A 完全一致（`breaks=5/64 places=7/32 refused=0`）；`plan_write_budget_insufficient` 0 次 ⇒ 零误剪 |

| 预算不误伤合法路径（D-106 反例守卫） | 右键 `alice:pathing_regression`（16 场景） | 零参数右键 | 正常任务的破坏/放置应远低于 64/32；出现 `[WriteBudget] exhausted` 即说明上限需要按数据调整 | `WINDOWS_CLIENT` + `USER_ACCEPTED`（2026-09-11 19:03）：16 场景 + `foot_cell_rule` + `coverage` 全 PASS；预算实耗 **breaks=5/64 places=7/32 refused=0** ⇒ 零打扰，64/32 有实测余量 |

| R2 零进展快速失败（D-105） | 任一卡死场景（如旧版 `clear_guard` 的箱顶弹跳） | 观察日志 | 连续 3 次"起跳后落回同一脚位格"即 `[R4 Session] no_progress … → TIMEOUT/SEGMENT_NO_PROGRESS`，约 40 tick 内退出而不是 161 tick | **`SERVER_TESTED`（D-242 已实测触发）**：`AI_DECISIONS.md:9965` `no_progress … wastedJumpLandings=3 ⇒ SEGMENT_NO_PROGRESS ⇒ TIMEOUT`（水里 PILLAR 构造场景）⇒ 原「待测」作废 |

### 任务区 / 权限阶梯（`D-338` 附注六/八/十/十三，2026-09-19）

| 判据 | 证据等级 | 证据 |
|---|---|---|
| 任务区**几何 + 锁定 + 覆盖规则 + 生命周期**（工作区域方块级 ⇒ 区块最小覆盖；随 `scopeId` 生灭） | `SERVER_TESTED` | `single:task_zone`（**89 判据 / 0 失败**）、`module:protection` 3/3、`ALICE_HEADLESS=1 check-all` 17 PASS（CORE 51/51） |
| **权限阶梯**（`L0` 只读 / `L1` 脚手架 / `L2` 工作面 / `L3` 全权）接进**四处**消费（候选扫描 / 破坏 / 放置 / **能力闸门**） | `SERVER_TESTED` + `WINDOWS_CLIENT` | 客户端：`[TaskZone] level=L2 chunks=6`、`ZONE_PROTECTED` **144→0**、内层 Job `places=0→3`、云杉 `chopped 6/7→7/7`、`[Ledger] place … [TEMP STEP_PLACEMENT]`；**三次先红后绿**反向对照（拆候选消费 ⇒ 恰 2 红；破坏闸恒放行 ⇒ 恰 3 红；恒拒 ⇒ 恰 3 红） |
| 无任务区 ⇒ **逐字** `protected_area`（既有码/行为不变） | `SERVER_TESTED` + `WINDOWS_CLIENT` | 判据"无任务区逐字回归"；客户端用例②（1 tick FAILED） |
| **终止路径进事件环**（显式停止/顶替/启动前拒绝）+ **被丢弃触发的计数进快照** | `SERVER_TESTED` + `WINDOWS_CLIENT` | 源码规则 `rule_stop_event_ring`（删调用 ⇒ 红，已做反向对照）+ **`llm_contract`**（⚠️ 不是 `decision_contract`）的 `dropped_triggers_visible`；客户端：`bot_report` 快照 `recentEvents` 里 `tick=1787 type=STOP`、`droppedTriggers=3`；`D-338` 附注十五。⭐ **`D-338` 附注十六（2026-09-20）补三条派活/受理被拒 + 门禁改成按站点结构断言**：`replaceTaskIfRunning`（未结清传输 / ⭐在飞传输**此前完全静默**）+ `assignJob`（kind 契约不全）进环 ⇒ **7 个站点**逐个断言；**反向对照六处注入全红**，而旧版"全局计数 ≥2"在同一批注入下**六次全部假绿**（剩余 `REFUSED` 调用仍有 4–5 条）。⚠️ 控制流逐字未变（只多写环记录）|
| **保护区里 LLM 自起任务封顶 `L1`**（玩家显式照旧 `L2`） | `SERVER_TESTED` + `WINDOWS_CLIENT`（**机制已确认**） | `task_zone` **89 判据 / 0 失败**（封顶组 7 条）+ 反向对照拆封顶 ⇒ **恰 3 红**；客户端 2026-09-19 19:06：LLM 自起那轮 5 棵树全 `zone_break_not_allowed`（该码只可能来自 `L1`）；`D-338` 附注十四 |
| ⭐ **`D-339`：夹具终态不交给决策层**（阻断"夹具失败 ⇒ LLM 自起无权任务 ⇒ 空转"整条链） | `SERVER_TESTED` + `WINDOWS_CLIENT` + `USER_ACCEPTED` | 夹具 `llm_contract` 新增 **`fixture_terminal_silent`**（夹具拦下 / 非夹具照旧交 / 控制组 `suspend(1)` 不联网）；**反向对照**注入 `if (false)` ⇒ **恰 1 红**（`handed=true`）、恢复后 9 判据全绿；`module:llm` PASS + CORE **51/51**（`ticks=4791`）。⭐ **客户端**（19:28，新包）：同场景同一失败 ⇒ 日志**只**多一行 `trigger_dropped reason=fixture_driver trigger=terminal:lumber(no_reachable_candidate)`，**全会话 `[Goal]` 仅此 1 行**（零 `decision_request`/`decision_action`/零 `决策层：` 聊天）；对照上一轮同场景 = 自起 `region_lumber` 空转 20 分钟 |
| ⭐ **反例：玩家显式发起的一次性作业在自己认领区里仍被拒**（**设计行为，不开豁免**） | `WINDOWS_CLIENT` + `USER_ACCEPTED` | 用户 2026-09-19 明示"**就是预期**…**是一个反例**"：`:260` `[Job] select job=lumber … candidates=0 rejected=[…:protected_area…]` ⇒ `:263` 1 tick `FAILED no_reachable_candidate`。⇒ 阶梯豁免只对**任务区**生效；**不要**为一次性作业造口子（`D-339` 同轮口径①） |
| ⭐ **`D-340`：夹具驱动的**事件**也不交给决策层**（补 `D-339` 没拦住的那条通道） | `SERVER_TESTED` + `WINDOWS_CLIENT` + `USER_ACCEPTED` | `llm_contract` 新增 **`fixture_event_silent`**；**反向对照**注入 `if (false)` ⇒ **恰 1 红**（`fixture_event_silent`，`fixture_terminal_silent` 仍绿 ⇒ 两闸门判据独立）；恢复 ⇒ `module:llm` PASS。⚠️ 判据**直接驱动 `onEvent`**（夹具自身 `isSelfCheck=true` ⇒ `notifyIfAllowed` 会先短路，用 `DecisionEvents.emit` 会假红）。⭐ **客户端**（20:17 新包）：`trigger_dropped reason=fixture_driver（夹具驱动的事件不交给决策层）` **×23**，而**全会话唯一**一次 LLM 调用是 `trigger=operator mode=directed`（= 用户自己的 `/alice instruct`，按设计绕过闸门）|
| ⭐ **`D-341`：「无权」≠「没有」** —— 区域作业在"树全被**永久授权拒绝**"时**如实失败**（不再空转） | `SERVER_TESTED` + `WINDOWS_CLIENT` + `USER_ACCEPTED` | 分类唯一出处 `ZoneAuthority.permanentDenial`（永久码齐、**不收** `trunk_too_tall` 类搜索性理由）+ `RegionLumberJob.permissionBlock` + `patrol()` 接线 ⇒ `no_permitted_candidate`。`task_zone` ⑫ 组 4 条（真扫描 + 手搭真树 + 已认领区；**89 → 93 判据 / 0 失败**）。内核规则 `rule_no_permitted_candidate`（**结构断言**）。**反向对照两条**：① 接线注入 `if (… && false)` ⇒ 内核红（第一版弱规则**放过了它**，已改结构断言后重放 ⇒ 红）；② `permanentDenial` 注入恒 `false` ⇒ `checks=93 failures=2`（恰是两条依赖"分类为真"的判据）。`module:lumber` + `module:protection` + CORE **51/51**（`ticks=4747`）PASS。⭐ **客户端端到端**（`/alice instruct` 起区域作业 ⇒ `driver=llm` ⇒ 封顶 `L1`）：`task_terminal_reason kind=region_lumber driver=llm terminalReason=no_permitted_candidate` + `[Job] maintain no_permitted_candidate zone_break_not_allowed tree@20,64,208:…` ⇒ **立刻如实失败**，对照上一轮同场景 = 空转 20 分钟 |
| ⭐ **`D-342`：最小跨任务循环检测（受理闸）** —— 同一 `(kind\|目标)` 窗口内失败 ≥2 次 ⇒ **拒再起** + 如实回读 | `SERVER_TESTED`（客户端待复验） | `GoalDirector`：`loopAttempts` 记账 + `loopRefusal` 受理闸（`assignJob` **之前**）+ 玩家显式**豁免** + 成功/过期复位 + `REFUSED` 进事件环；⚠️ 身份靠「受理记 key、终态按 key 记账」，不做字符串匹配；⚠️ 记账点在 **`complete` 里返程兜底 `return` 之前**（否则漏掉「失败触发返程」那次）。`llm_contract` **10 条判据全绿**（`loop_admission_control` 五断言：前两次放行/第 3 次拒+码+身份+次数/玩家豁免/成功复位/别的目标不受牵连）· 内核规则 `rule_loop_admission`（**顺序·结构断言**）· **反向对照两条**：① 拒绝分支注入 `&& false` ⇒ 内核红；② `loopRefusal` 恒 `null` ⇒ **恰 1 红** · `module:llm` + CORE **51/51**（`ticks=4770`）PASS || ⭐ **`D-342`：最小跨任务循环检测（受理闸）** —— 同一 `(kind\|目标)` 窗口内失败 ≥2 次 ⇒ **拒再起**（🔴 客户端首测**没生效**，见右） | `SERVER_TESTED`（🔴 **客户端首测 = 反例**：三次 `instruct` 全放行、`REFUSED` 零条） | **根因**：`kind` 生产两写法（受理 `JobRequest.Kind.name()` 大写 / 终态 `Task.taskName()` 小写）⇒ `startsWith` 恒假 ⇒ **静默不记账**、计数恒 0。**修**：`attemptKey` 归一大小写 + 夹具新增 **跨写法断言**（`attemptIdentity` 相等 + 跨写法记账计数为 1）。**⭐ 反向对照（重现现场）**：去掉归一 ⇒ 夹具**恰 1 红**（另五条仍 true）＋ 内核规则红。`llm_contract` **10 判据全绿** · 内核 `rule_loop_admission`（顺序·结构 + 归一化断言）· `module:llm` PASS。 另加 `task_zone` **第 ⑭ 条**（终态侧拼写**从生产对象取**：`RegionLumberJob.taskName()` ⇒ 改名即红；反向对照注入 `NAME=RegionLumber` ⇒ `checks=94 failures=1`）。⭐ **修订（同日，用户裁定「这类验证本该无头」）**：身份**不再跨边界做字符串匹配** —— 在飞身份只由 LLM 受理侧写、`beginTask` 在非 LLM 派活时 `clearAttempt`；`noteTerminalOutcome` 去掉 `kind` 参数；删掉 `task_zone` 那条已废弃的跨写法断言（94 → **93**）。夹具新增 `foreign_assignment_not_accounted`（7 断言全 true）；内核规则扩到锁整条链（受理闸在 `assignJob` 前 / 同一表达式 / `Driver=LLM` 在 `execute` 前 / `beginTask` 调 `clearAttempt` / `complete` 调记账且**在返程兜底前** / 禁止再出现 `startsWith(kind`）；**反向对照三条**（三类注入各 ⇒ 内核红）。`module:llm`+`module:lumber`+CORE **51/51（`ticks=4777`）** PASS。⭐ **本条线不需要客户端轮次**（见 `TESTING_GUIDE.md` 客户端准入尺子）|
| 客户端反向测试（保护区）三结论 | `WINDOWS_CLIENT` | ① 保护区里**玩家自己**发起照旧干活：`chopped=0→5` + `ALLOW … STEP_PLACEMENT`（**真垫了方块**）+ `sweep_up/restore` 闭环 + `scaffoldLeft=0`；② 封顶生效（见上行）；③ 事件环可取证（见上行） |
| ⭐ **`D-350`：区域任务显式收集自己的掉落物**（`deficit=0` 也收；收集授权窗口 = 扫描时长） | `SERVER_TESTED` + `WINDOWS_CLIENT` + `USER_ACCEPTED` | 纯函数 `RegionSweepCheckTask` **23 判据 / 0 失败** + 反向对照（入口退回旧行为）**恰 3 红**；`module:lumber` 6 步 PASS。⭐ **客户端 2026-09-20**（`region_lumber` 零参数入口，`x17..37 z203..231`）：`:2760` `扫描判定 HAS_SAPLINGS → ENTER（deficit=0 手里苗=3 区内可捡=13）` ⇒ `:2762` `sweep 开始 目标=13` ⇒ `:3021` `sweep 结束 status=DONE … 实际入包=16 区内剩余=2` ⇒ `:3092` `ENTER → NO_DEFICIT（区内可捡=0）` ⇒ `:3094` 起纯退避 `40→80→160→320→600`；`:3112` 新落物 `NO_DEFICIT → ENTER` ⇒ `:3183` `入包=1 剩余=0`。**对照轮**（`2026-09-19-3.log.gz`）同一区域：`sweep`/`实际入包`/`撤销授权` **各 0 行**，`deficit=0` 后 `interval=600` 原地待机到轮次结束。全程 `sweep_no_progress=0`、`failed=0`、正常关服；用户「符合预期…有新的掉落物也会去捡」。⚠️ `[Pickup] blocked provenance=FOREIGN` 行数两轮同为 26（**设计如此**：授权窗口内收走 19 件、窗口外照常拦）——见 `D-350` 附注一 |

## 电池自身的"起步前提"（2026-09-14 起）

| 前提 | 现状 | 读数 |
|---|---|---|
| 每步起步时 bot **已落地** | ✅ 已由 `RegressionBatteryTask.awaitGrounding` **有界等待**（≤40 tick）保证 | step 1（出生后）**每轮都等 4 tick** ⇒ `已落地（等了 4 tick）`；中段步骤若出现等待 ⇒ **warn** `起步时未落地`（= 上一步把 bot 留在了半空，值得单独查） |
| 每步起步时**没有别人的容器菜单** | ✅ 已有（`ownMenu` ⇒ 自动 `closeContainer()`） | `premise step=… own_menu=true` |
| 每步起步**位置固定** | ❌ **没有**（`partial_search`/`transfer` 起点参数为 `null`）⇒ 已知会引起非确定性 | 见台账 §9 末两条 |

⚠️ **注意**：等待本身**不能**当"起作用了"的证据 —— 必须看**等待之后该步是否 PASS**；反过来，
若某轮出现 `起步时未落地` 的 **warn**（非 step 1），说明**某一步真的把 bot 留在了半空**，
那是**被测系统的 bug**，不要因为它不再变红就忽略。

## 性能验证

| 能力 | 测试入口 | 关键观察 | 当前状态 |
|---|---|---|---|
| 性能回归验证 | 与 Movement/MineTask 测试相同的客户端实例和场景 | 本轮未复现“平均 FPS 很高但 low 帧很低/体感卡顿”；未发现需要继续定位的问题 | `USER_ACCEPTED`；本轮到此收口，不做进一步性能诊断 |

## Windows 反馈询问

测试构建交给用户后主动询问：

1. 使用了哪个物品和哪个按键？
2. 游戏中实际看到什么？
3. 预期和实际差异是什么？
4. 必现、偶发，还是执行一段后发生？
5. 是否出现紫黑材质、回弹、卡死、崩溃或聊天异常？
6. 能否提供 Windows runtime 的 `latest.log`/`debug.log` 片段、截图或短视频？

## 失败记录

发生服务端日志与客户端现象不一致时，标记 `EVIDENCE_CONFLICT`，先核对 Windows 实际 JAR 和日志，不直接宣布修复成功。
